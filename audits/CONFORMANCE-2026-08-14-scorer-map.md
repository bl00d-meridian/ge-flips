# Conformance map — the BINDING section against the universe scorer

2026-08-14 · the one-time mapping ordered by the conformance-gate ruling.

> **RATIFIED 2026-08-14 (consolidated rulings), with amendments applied in place:**
> - **Row 12's widening is law**, with the user's addition: scan 2 **re-reports** staged
>   stores every audit until consumed — a stalled stage cannot leave a store staged
>   indefinitely and invisibly. CLAUDE.md's rule and scan text carry it.
> - **Row 15 is pre-ruled for 1d: STAMP, DON'T EXCLUDE** (amended in the table below).
> - **Row 23 added** (amended form, below): the blacklist canary.
> - The partition register is ratified **including the deliberate non-answer on the ids
>   stock**; the pinned-era-fact move is ratified as standing practice (CLAUDE.md
>   DOCTRINE, staging practice, fourth rule).
> - Stages from 1d check **deltas** against this table; stanza in every report.

Verdicts: **APPLIES** (with the checking mechanism), **N/A** (with the reason — the
property absent, not just the old surface), **NEEDS WIDENING** (ratified above).

## §1 The mapping

| # | BINDING rule | Verdict | Check / reason |
|---|---|---|---|
| 1 | The tool proposes and prefills; it never acts | **APPLIES** | The scorer measures; it admits, funds and caps nothing (`DB.scorerT2` comment states it; no scorer function writes watch/positions/budgets — scan 2 connectivity at each stage). Becomes load-bearing at the **cutover stage**, which has its own gate |
| 2 | A flag in a record means one thing; a second concern gets a second field | **APPLIES** | Schema fields carry one meaning each (`capped`, `control`, `v`, `pv`, `fundedNoChart`); `[R73.11]` guards the decision log; schema review per stage |
| 3 | Every automated decision states its reason inline | **N/A this stage** | The scorer makes no decisions — it measures. Re-enters at 1e if proposals cite cells; the warn notes already state their reasons |
| 4 | Never pool across populations answering different questions | **APPLIES — the instrument's central obligation** | `configHash` (`[R76.2]`), `SCORER_V`, 24 hour-bands (`[R76.5]`), the chart-coverage stamp (`[R76.9]`, flag 1), `pv` on T0 buckets (`[R75.5]`); scan 8 at every stage boundary |
| 5 | Every statistic opens to / ships with its rows | **APPLIES, matures at 1e** | Interim: `ids` carried per cell, truncation declared (`capped`, `[R76.4]`); per-cycle rows are deliberately not stored — **the archive is the ledger** and T0 replay re-derives them. 1e's drill must open cells to ids + the replay path; scan 5 at 1e |
| 6 | No claim about unobserved periods; denominators observed-only | **APPLIES** | `cycles` counts only buckets scored (`[R76.3]`); `t0Coverage` returns the pair (`[R75.3]`); at 1e any per-cell rate must render its coverage — scan 7 then |
| 7 | A component reports nothing where it should report it HAS nothing | **APPLIES** | (a) absent cell ≠ zero cell — cells exist only once fed, by construction; (b) stalled generator — **freshness streams shipped this pass** (archive + scorer, could-not-check states); (c) unreachable threshold — b=100 deferral stated in the grid comment; (d) silent failure — `t0FailNote`/`scorerFailNote` (`[R75.4]`/`[R76.7]`) |
| 8 | An ordered chain reports position in the ordering | **APPLIES, satisfied structurally** | Full fail sets from birth; funded = zero fails; no first-fail figure exists in the schema to mislabel (`[R76.2]` row text; scan 16 semantics on anything chain-shaped per stage) |
| 9 | Correct parts do not compose into a correct product | **APPLIES** | Both consumers of the 5m arrival crossed by stubbed-fetch walks: `loadHour → t0Accrue` (`[R75.5]`), `loadHour → scorerCycle` (`[R76.8]`, closed this pass); scan 10 enumeration per stage |
| 10 | A long-lived client detects and reports its own staleness | **APPLIES** | The two freshness streams added this pass; ages name the BUCKET last processed, not the attempt; session-scoped stamps read "not yet" after reload — could-not-check, never up-to-date |
| 11 | A simulation uses only information that existed at the modelled moment | **APPLIES at 1d** | Hour attribution to the bucket already holds (`[R76.5]`); 1d's fill sim inherits the causality rules; scan 11 runs at the 1d boundary |
| 12 | Data nothing reads, and surfaces nothing feeds, are defects | **NEEDS WIDENING** | As written, scan 2 must flag `scorerT2`/T0 as write-only today — their readers are RULED FUTURE STAGES (1d/1e), sanctioned by the staging practice. Proposed property wording below |
| 13 | Restraint may auto-arm; deployment never | **N/A until cutover** | Nothing arms, lifts, or widens what the allocator may fund. The pool switch is the deployment-class change and carries the cutover gate |
| 14 | The file is the press, one direction only | **N/A, with one note** | The scorerT2 import is a data carry, not a consent channel — it drops nothing and arms nothing. The carry re-derives hashes (`[R76.6]`), which is the file-as-press *spirit*: a file cannot smuggle a population identity |
| 15 | A manipulation defense never relaxes on the manipulator's evidence | **N/A — and the flagged question is PRE-RULED for 1d: STAMP, DON'T EXCLUDE** (user ruling, Aug 14 2026) | Pump flags are the machine's own defensive state — evidence-based, expiring, machine-armed — and excluding flagged items would make the measured population diverge from the market on the machine's say-so. Opposite treatment from the blacklist because the properties differ: the blacklist is the user's constitutional veto; pump exposure is a data-quality fact. 1d design: trips on pump-flagged items carry the stamp at birth, cell aggregates decompose by it, and any proposal citing a materially pump-exposed cell carries the caveat inline — the calibration-tripwire pattern, third instance |
| 16 | Metric honesty — copy claims exactly what it computes | **APPLIES** | This stage's copy sweep derived every rendered constant (`GATE.taxMult`, `effRoiFloorPct()`); warn notes name gaps as unobserved, not quiet (`[R76.7]`); scan 7 per stage |
| 17 | Membership bookkeeping applies itself | **N/A** | No membership machinery; the property (mechanical bookkeeping vs capital decision) has no scorer instance |
| 18 | Concur-recommended proposals skip the ceremony | **N/A until 1e** | No proposals cite cells yet |
| 19 | Walk-up attention budget ≤ 7 | **APPLIES trivially** | Pull-surface class by construction, zero rulings; the standing probe bound already asserts the budget |
| 20 | Every user-visible term ships with its glossary entry, same commit | **APPLIES at 1e; convention-noted this pass** | The two freshness rows follow the panel's existing unglossed-stream convention (13 prior streams; the `gov-freshness` entry covers the class). First scorer-specific TERMS arrive at 1e and gloss then |
| 21 | Every new probe assertion proven by seeding | **APPLIES, running record** | 1a: 6 seeds · 1b: 6 · 1c: 9 (two cascades recorded as propagation) · this pass: 3, all discriminating |
| 22 | Assert the term under test; name the pinning input | **APPLIES** | `SCORER_ID_CAP` is the one clamp on an asserted subject; fixtures sit far under it (non-binding, named); `scorerNoChart` extracted precisely because its with-chart limb cannot yet be reached through the product (`[R76.9]`); scan 9 enumeration per stage |

**Row 12's widening — RATIFIED as worded, with the re-report addition (now in CLAUDE.md's
rule and scan text):**

> *Data nothing reads is a defect — **unless its reader is a ruled future stage, named on
> record with the store**. A store accruing ahead of its consumer carries its reader's
> stage in the schema register; scan 2 reports it as **staged**, not orphaned; a staged
> store whose named stage ships without reading it becomes an ordinary orphan finding at
> that stage's boundary; and scan 2 **re-reports every staged store on every audit until
> it is consumed**, so a stalled stage cannot leave a store staged indefinitely and
> invisibly.*

**Row 23 — the blacklist canary (added by ruling, amended form, Aug 14 2026):**

| # | BINDING/DOCTRINE source | Verdict | Check / reason |
|---|---|---|---|
| 23 | *"The blacklist is the user's alone"* (DOCTRINE, commitments) | **APPLIES, narrowed to its actual property** | The precedent forbids ONE shape: the machine accruing **counterfactual profit evidence against the veto**. It does not forbid the instrument seeing the blacklist — known-bad items are defense-calibration indicia, and total exclusion would silence a canary. (a) **T0 archives everything**, blacklist included — tape history is defense-training substrate, no trading simulation involved. (b) **Verdicts**: blacklisted items ARE scored at the gate level, at the cycle layer, stamped at the cycle (partition-at-birth); EXCLUDED from funded counts, cell nets, rankings; COUNTED in the per-cell caution pair `blFunded`/`blIds` — *this cell would fund N blacklisted items*, a red-flag line at 1e, because a loose config admitting known pump vehicles is evidence against the config. (c) **Trips and fill sim never run on them** — no trips, no nets, no per-item cumulative rows, ever; the precedent's surviving core, costing the training use nothing. (d) **Honest caveat on record**: blacklist n is tiny; the pump-flag population is the real training corpus, the blacklist supplies permanent anchors. Checks: `[R76.10]`/`[R76.10b]` — the discriminating pair (leak-into-funding vs vanish-from-sight), both directions seeded; `[R76.10c]` the carry; the no-trip guarantee seeds at 1d when trips exist |

## §2 The partition register (conformance item 3 — every store this build created)

| Store / row type | Regime that writes it | Field recording it | On regime change |
|---|---|---|---|
| T0 `m5`/`h1` buckets | `t0Pack`'s shape | **`pv`** (`T0_PACK_V`, stamped from the first real bucket — `[R75.5]`) | bump the constant; readers partition by `pv`; no unversioned population exists |
| `scorerT2` cell | the config | key = `cfgHash`, **re-derived on import** (`[R76.6]`) | old population closes at its `lastAt`; nothing deletes it; the new hash accrues fresh. **This answers the control-cell case**: a user moving a live constant closes the control's population silently and correctly — the closure IS the partition working, and 1e renders both eras with their date ranges |
| `scorerT2` cell | gate arithmetic version | `v` (`SCORER_V`) | bump on any arithmetic change; readers partition by `v` |
| `scorerT2` counters | chart-input coverage | **`fundedNoChart`** vs `fundedItemCycles` (`[R76.9]`, flag 1) | 1d's wiring flips items to charted; the split decomposes the transition; the pinned era fact (`marketStatsFor().tr === null`) goes red when the wiring lands, forcing the stanza to account for it |
| `scorerT2.ids` stock | none — stock across regimes, **deliberately** | — | era-pure stock re-derives from T0 replay (the archive is the ledger); recorded here so nobody "fixes" it into a second split. **Ratified as recorded** |
| `scorerT2` caution pair (`blFunded`/`blIds`) | blacklist membership **at the cycle** | membership is evaluated per cycle, never retroactively | un-blacklisting moves an item into the main figures from that cycle forward; the two eras never mix, and no re-stamping pass exists to get wrong. Stock is uncapped at blacklist scale (a handful), noted rather than guarded |
| `rdiff` rows (IDB store, DB v2) | the scorer cycle + the plan stamp | `ch` (control config hash) + `v` (`SCORER_V`) per row; plan side three-state (`plan: null` + `planWhy` when unobserved) | a moved live constant changes `ch` — diff rows before and after partition by it; the plan side's unobserved state is recorded per row, never inferred; 90-day retention because the ledger's value is its length |
| **`t1` closed-trip rows** (IDB store, DB v3 — stage 1d) | the fill sim's crediting rules | **`fv`** (`SCORER_FILL_V` — the trip layer's OWN version, deliberately distinct from the paper book's `FILL_MODEL_V`: parameterized capture/participation is a different measurement) + `v` (`SCORER_V`) + `sz` (sizing convention, `"lim1"`) + **`hzH` (the RULED horizon set {6, 9.5} — ratified Aug 14 2026; one rolling cycle per item × participation × horizon, horizon in the trip key)** per trip | bump `fv` on any change to what a bucket credits; readers partition by it; a changed sizing convention changes `sz`, a changed horizon set is a new `hzH` value and therefore a new econ key — populations never pool. 30-day retention, rolled into econ BEFORE pruning (roll-then-prune); econ outlives the rows and 1e declares the openable-rows difference where it renders. **Econ gained `gross`/`top` per the ruled concentration measurement (top mover's share of gross \|net\| movement, ceiling 0.5, rank-disqualifier only); the ungraded-capture era fact is pinned (`[R78.17]`) and the rulings are decision-logged (`by: "user"`)** |
| **`t1open` carry** (IDB store, DB v3 — stage 1d) | the current session's open trips, whole map as one record | the trips inside carry their own stamps (`fv`, `v`, `sz`, `hzH`, `part`) — the carry is a container, not a population | a resumed trip keeps its birth stamps; a version bump mid-flight partitions at roll time by the trip's own `fv`. A carry that cannot be read warns immediately: the loss is UNRECORDED holes, named as such |
| **`scorerT2` econ buckets** (localStorage, inside each cell — stage 1d) | trip rolls | the KEY carries all four stamps: `h<horizon>\|p<participation>\|c<capture>\|fv<fill-model>` | a new horizon, participation level, capture point or fill-model version is a NEW key — pooling is structurally impossible, and the import carry drops any key missing a stamp. Exclusions (`insuf`/`unobs`/`vbl`) are counted beside `n`, never inside it |
| **trip `cells` membership** (on each t1 row — stage 1d) | the funding cells at the OPEN cycle | `cells` (config hashes at birth) | a cell that starts funding an item mid-trip joins at the next reopen (its own simulated placement could only exist from its funding cycle — the information-horizon rule applied to membership); a cell replaced by an import rolls nothing, and the trip row remains as the closed population's evidence |
| **trip `pump` / `noChart` stamps** (stage 1d) | pump-flag state / chart-input coverage at the OPEN cycle | `pump`, `noChart` per trip, decomposed per econ bucket (`pumpN`/`pumpNet`, `ncN`) | membership evaluated at birth, never retroactively — a flag raised mid-trip does not restamp (the bl mid-trip case is different and VOIDS, because the veto is constitutional, not evidentiary); when 1d-or-later chart wiring lands, `noChart` decomposes the transition exactly as `fundedNoChart` does at the cell layer |

## §2b Staged-store status at stage 1e (scan 2's re-report list, updated Aug 14 2026)

| Store | Status at 1e |
|---|---|
| `scorerT2` cells + econ | **CONSUMED** — read by the Scorer surface (grid, econ panels, drills) and the `analysis-scorer` export; off the re-report list |
| `t1` closed-trip rows | **CONSUMED** — read by the surface's trip drills and the export (bounded window, truncation declared); off the re-report list |
| `t1open` carry | consumed since 1d (boot resume) |
| `rdiff` rows | **STILL STAGED for the cutover gate** (its ruled reader). The 1e verdict line reads its day count and row total — an informational read that does not discharge the staging; scan 2 keeps re-reporting it until the cutover gate consumes it at verdict level |

## §3 The four flags — corrected per the consolidated ruling

**The user's flags 1–2 matched the reconstruction below; 3–4 did not.** The real flag 3
is the **reconciliation diff** (built: `rdiff`, `[R77.x]`, seeded, accruing from the next
app-open) and the real flag 4 is the **horizon-variant proposal riding 1d** (6h default,
the 9.5h overnight question, proposed alongside participation for one ruling). The
reconstruction's substitutes — the wiring seam `[R76.8]` and the control-cell closure
semantics — **stand as ratified bonus work**. Original reconstruction kept below for the
record.

Flag 1 was named in the ruling; **2–4 were my reconstruction from the 1c record.**

1. **The coverage stamp** (named) — **FIXED before further accrual**: `fundedNoChart` +
   the extracted `scorerNoChart` term + the pinned era fact, `[R76.9]`/`[R76.9b]`,
   seeded. The same class closed on T0 with `pv` (`[R75.5]` extended, seeded).
2. **The scorer's invisibility until 1e** — a generator rendering no state is the
   silent-state rule's shape (b). **FIXED minimally**: two scheduled streams in the
   freshness panel, ages naming the bucket last processed, "not yet" after reload. The
   full state line remains 1e's.
3. **The uncrossed scorer wiring seam** — §76 called `scorerCycle` directly; nothing
   proved `loadHour` calls it (the exact gap R75.5 closed for accrual, on the second
   consumer). **FIXED**: `[R76.8]`, seeded by deleting the wiring line.
4. **Control-cell population closure semantics** — what happens when the user moves a
   live constant was correct but unrecorded. **RECORDED** in the partition register
   (§2, row 2), which is what item 3 of the gate demands: the answer on record, not a
   code change.

## §4b Session-close stanza (row 23 · flag 3 · the deployment check)

> **RATIFIED 2026-08-14 (user ruling):** the seeding log below is accepted with seed B
> standing on the crashed run's own 09:06 report — **a seed's bite is proven by any
> artifact showing it bit, provided the code under test did not change between the
> artifact and close**, which the suite-matches-tree line covers. Cheaper than re-running
> and equally probative. M154's fix shape — catch-up where both inputs are settled, a
> guarded-out cycle does not consume its bucket, `[R76.11]` seeded — **approved as
> built**.

- **BINDING touched:** never-pool → the canary pair partitions bl from funded
  (`[R76.10]`/`[R76.10b]` — the discriminating pair, both directions seeded: A leaked bl
  into funding → 10 red/10b green; B dropped bl silently → 10b red/10 green); the diff's
  three-state plan side (`[R77.2]`, seeded); composition → the boot race (`[R76.11]`,
  seeded by hoisting the bucket-consume above the input guard; fix = the doRefresh
  catch-up; **M154**); interrogability → diff rows carry full fail sets (`[R77.1]`,
  seeded two ways: fail sets emptied, and the accrual call deleted — the second redded as
  a block-level crash on the absent row, recorded as propagation; the detail formatter
  was hardened so a live failure reports as a clean R77.1 red).
- **Seeding log, complete, no unknown-status entries:**
  | seed | target | result |
  |---|---|---|
  | A — bl counted into funded | R76.10 | RED, 10b green (discriminates) |
  | B — bl dropped silently | R76.10b | **RED on the crashed run's own report (Aug 14 09:06, PROBE-FAIL 1, `{"blFunded":0,"blIds":[]}`), 10 green — stands as proven on that evidence** |
  | C — diff fail sets emptied | R77.1 | RED, others green |
  | D — unobserved plan as empty comparison | R77.2 | RED, others green |
  | E — rdiff accrual call deleted | R77.1 | RED via block crash on absent row (propagation; formatter hardened) |
  | guard-order swap | R76.11 | RED, others green |
- **The deployment check (item 4, run twice):** first run — real network, fresh profile —
  returned `latestAt: live` + `t0LastBucket: set` + **`scoredBucket: 0`** at 111s, which
  is M154 caught before any real session shipped it. After the fix: **DEPLOY-OK — first
  bucket archived, scored and diffed within 2 seconds of boot**; rdiff row `extraN=3`,
  `plan: null — never built this session` (the correct third state on a fresh profile);
  16 cells, control `funded 3, noChart 3, blFunded 0`. The check ran in its own headless
  profile: the USER's browser profile verifies itself via the three freshness streams on
  next open, noted in HANDOFF.
- **Scans at the boundary:** pooling (8) — bl/funded and noChart splits ship with their
  decompositions; silent-state (2) — the diff's plan side has three states, the canary's
  absent-vs-zero handled by first-touch init; claims-vs-computation (7) — warn notes and
  freshness copy name gaps as unobserved; scan 14 over the new labels — `"NO funded
  figure"` (universal: exercised against every figure the schema has — count, stock,
  split — honest), `"never an empty comparison"` (universal over the row's states, all
  three exercised); scan 16 — no new chain.
- **DOCTRINE by inspection:** staging practice — the diff ships ahead of its cutover
  reader with its clock stated; zero-based complexity — one freshness row, no new
  surface.

## §4 This pass's conformance stanza (first instance of the standing format)

- **BINDING touched:** never-pool → `[R76.9]` seed RED on inverted term, `[R76.9b]`
  carry; partition-at-birth → `[R75.5]` seed RED on dropped `pv`; composition →
  `[R76.8]` seed RED on deleted wiring; silent-state (b) + staleness → freshness
  streams, verified by inspection against the panel's existing never/stale machinery;
  metric honesty → stream notes name gaps as unobserved.
- **Detectors same commit:** every surface this pass added (streams, stamp, wiring)
  landed with its assertion in the same tree.
- **Seeds:** 3 new, 3 bite, all discriminating; no cascades.
- **Scans at the boundary:** pooling (8) — the new counter ships WITH its decomposition,
  nothing new pools; silent-state (2) — the scorer now renders state, cells remain
  absent-vs-zero clean; claims-vs-computation (7) — stream copy checked against what the
  stamps compute (bucket age, not attempt age); scan 14 over the three new labels —
  `"every funded item-cycle"` (universal: exercised as the full-population equality in
  the only era that exists, honest), `"loadHour itself"` ×2 (source claims: the subject
  IS the wiring, honest); scan 16 — no new chain.
- **DOCTRINE by inspection:** zero-based complexity — no new surface, two rows in an
  existing panel; staging practice — no accrual clock in this pass's work.
