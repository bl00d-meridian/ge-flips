# The repair ledger — one row per repair, with the property named twice

**Ruled Aug 19 2026.** Every repair that goes through the staged cold-review pass lands a row here.
The proposal is `audits/PROPOSAL-2026-08-19-cold-repair-review.md`; the mechanics are
`tools/stage/*.sh` and the `PROBE_SRC` / `PROBE_SNIPPET` / `PROBE_REQ` overrides in
`tools/probe/run.sh`.

## Why the property is recorded twice

A repair made under the pressure of a just-delivered finding gets scoped to that finding's
**spelling**. Three instances in one week, each the repair for the one before it (MISTAKES M170).
The separation that works is not time — the same reader an hour later still has the finding in
front of it — but a **cold** reader, shown the diff and not the finding, and asked what property
the repair is about rather than whether it fixes the reported case.

**The user's addition to the proposal, and it is what makes this file worth keeping:** record the
cold reviewer's answer beside the repairer's own. If a later pass finds the same property somewhere
else, the two answers say which of two different failures happened —

- the **property was named too narrowly** (both answers are narrow, and the search was faithful to
  a bad definition), or
- the **search was run too narrowly** (the answers are right and the grep did not reach every site).

Those need different fixes, and with only one answer on record they are indistinguishable.

**The columns are deliberately not merged.** If the reviewer's wording is just a paraphrase, say so;
if it is genuinely a different property, that is the interesting case and it goes in as written.

## Ledger

| # | date | repair | property, as the REPAIRER named it | property, as the COLD REVIEWER named it | sites the reviewer's property returned | sites the finding named | verdict |
|---|---|---|---|---|---|---|---|
| 1 | 2026-08-19 | every path that creates a leg stamps its horizon (`hzH`) | *Every code path that creates a leg — a position or a standing quote — records the fill horizon in force when that leg was PLACED; a path that splits or re-tracks an existing leg carries the ORIGINAL's stamp rather than re-deriving one from the current schedule.* | *Any record that is later AGED against a horizon carries the horizon in force when the underlying ORDER was placed; a record derived from an existing one — split, remainder, undo, re-list — inherits rather than re-derives; and a record whose real placement moment is unknown gets no fabricated stamp unless the record already declares that same approximation in another field.* | **8 creation sites across 4 record types** — 4 `DB.positions.push`, 1 `w.quotePlaced`, 2 paper-book trips, 1 scorer trip — plus 5 carry-by-identity paths (3 undo splices, the `{...qp}` snapshots, `validateImport`'s conditional carries). All 8 stamped in the staged file; **nothing untouched and defective.** Wider than the repairer's, which stopped at *a position or a standing quote*; the 2 extra record types were already correct. **One divergence, and theirs is better:** my phrasing would have left `trackExistingOffer` ABSENT; their argument — its own `placedAt: Date.now()` already declares *ages from now*, and the fallback is a second silent approximation — wins, and I withdraw mine. | 2 | PASS |
| 2 | 2026-08-19 | operator state has one owner in each direction | *The six operator-state fields (`tBuy`, `tSell`, `tAt`, `qty`, `tierOv`, `t2Grad`) have exactly one owner in each direction: `opsOf`/`opsFor` for every read, `opsWrite` for every write. No site reads or writes any of them off a watch row directly.* | *A field whose authoritative LOCATION can move under a flag has exactly one term resolving reads and one resolving writes; no site touches either location directly, and a caller already holding the row passes it rather than re-finding it by id.* | **21 live accesses** — 6 writes (all through `opsWrite`), 15 reads (all through `opsOf`/`opsFor`). Returned and NOT defective: `opsSet` called directly by the two pool controls (reachable only in the armed regime, where `opsWrite` routes there anyway — a layered pair, not a second owner); `itemOpsMigrate` / `itemOpsReconcile` / `validateImport` (they read the row AS the row). Returned and out of scope BY DESIGN: `invTarget` and `scoutTier`, which the file's own overlay comment lists beside the six — one owner each (the row) in both regimes, so they satisfy the property rather than breaching it. | 4 | PASS |
| 3 | 2026-08-19 | the three cutover flags stop being independent | *Wherever two or more deployment-class flags can be ruled separately, the combinations they produce are enumerated, and any combination that WIDENS what the allocator may fund is refused outright and says which flag is missing.* | *A const naming a RULED intent and a term naming what is IN FORCE are different values. Every consumer that acts on, records or describes the state reads the in-force term; the const is read only where the question is literally "what was ruled".* | **7** — 6 in-force consumers all re-pointed, plus 1 correct const read (`cutoverFaultWarn`'s *was it armed?*). I also enumerated the combinations in BOTH directions rather than taking the widening one on trust: of 8, only `pool && !vol5` widens; `!pool && vol5` was checked and only ever SHRINKS a size, so refusing it would be restraint refusing restraint. | 1 (the vol5 pairing) | PASS |
| 4 | 2026-08-19 | seasoning distinguishes absent from failed | *A gate verdict may reset a streak only for an item that was actually EVALUATED. An item not evaluated this cycle neither breaks a streak nor credits one, and absence is never filed as failure.* | *A counter driven by per-cycle verdicts distinguishes three states — passed, failed, not evaluated — and acts only on the two that are verdicts. Where the population reaching the verdict loop is a SUBSET of the population the counter governs, the complement is not-evaluated and needs its own record.* | **3 per-cycle counters** — `DB.qual` (fixed here), `S.vol5Low` (already three-stated: `counted ? n : null`), `DB.poolSeen` (already a pass/observation pair with a named `not-scored` state). I also checked the CADENCE seam the property implies: the fail set is written in the same scorer pass as the pass set and the plan consumes it faster — over-consumption is idempotent, under-consumption cannot occur. Nothing untouched. | 1 | PASS |
| 5 | 2026-08-19 | NEXT UP carries the separation | *Any rendered ordering of candidates that mixes populations whose scores are not comparable shows the separation — and never claims an ordering the funding path does not have.* | *A rendered list must not imply a grouping or ordering the mechanism lacks — and must not hide one the mechanism has. Both directions, because the second is how a real split gets rendered as a flat queue.* | **4 rendered candidate lists** — funded picks (grouped), NEXT UP (grouped, disclaimer withdrawn because it stopped being true), Qualifying (deliberately ungrouped, reason recorded at the site), Benched (carries no ordering claim, so there is nothing to show). Paraphrase of the repairer's, widened to the second direction. | 1 | PASS |
| 6 | 2026-08-19 | the `qualV1` ruling recorded at its site | *A restraint may not be lifted by a file: a restored file's rows season from scratch.* | *No value a file carries may LIFT a restraint the importing browser would otherwise apply — and a one-shot migration flag IS such a value, because its carried value decides whether a restraint-applying migration runs.* | See row 9 — same property, and row 9 is where the wider search lands. **Repair 6's own two sites are complete.** | 1 | PASS |
| 7 | 2026-08-19 | A1 — the funding walk splits the two populations (ruled, deployment-class) | *Wherever the plan orders candidates, one comparator owns the order, populations whose scores are not comparable never interleave, and an explicit operator override crosses every boundary.* | *Exactly one comparator owns the order of a candidate set; every render of that set reads it; populations whose sort keys are not comparable never interleave; and an explicit operator override crosses every boundary.* | **8** — 6 fixed, 2 returned and named in the source (`applyFamilyRule`'s `x.score > cur.score`, and the raw `sort(b.score - a.score)` pre-sort feeding it). **The interaction is worth stating and is not in PASS.md:** family-winner selection runs on raw score BEFORE `planOrder`, so A1's guarantee is not yet whole — after the cutover a pool item can still BENCH a pin by winning its family, which is displacement arriving one stage earlier than the funding sort. Seen and queued, not missed. | 1 | PASS |
| 8 | 2026-08-19 | the boot merge gets the cap rule from one table | *"What is a legal value for a capacity setting" has exactly one owner, and every path that writes `DB` reads that owner.* | *A value's legality rule has exactly one owner — and MEMBERSHIP of that rule is decided by what the value DOES (does it bound what may be funded, sized or admitted), never by which object literal, which previous helper, or which earlier version of the rule it happened to share.* | **12, not 11.** The nine in the table, plus **`t1Budget`, `t2Budget`, `sleeveBudget`** — untouched. Each bounds what may be funded more directly than `slots` does (`pools = { 1: Math.min(t1Budget, deployable) }`); each resolves a present-but-unreadable value to its LOOSE default (60m / 30m / 60m) where the table's own rule says tight; and none is reached by `clampCapKeysAtLoad`, so the boot path still has for budgets the exact hole this repair closed for caps. The classification is also internally inconsistent: `sleeveMaxPos` (the sleeve's COUNT cap) is in the table while `sleeveBudget` (the same subsystem's MONEY cap) is not. **This is the repair's own recorded failure mode one generation later** — its comment says v1 *missed `slots` and `watchCap` because it was drawn from the 23 keys that happen to share one object literal*; v2 was drawn from the 9 keys that happened to share `impCap`. | 2 paths | **SENT BACK** |
| 9 | 2026-08-19 | `DB.qual` is not carried (the user's correction to their own ruling) | *A restraint may not be lifted by a file, by any route — and a completed seasoning streak lifts one whether it arrives by re-running the grandfather migration or verbatim in the file.* | *Same as row 6 — no value a file carries may LIFT a restraint, by any route, including a carried migration flag.* | **Property named RIGHT, search run NARROW** — which is the distinction this column exists to record. The repairer's wording is already the wide property; the grep was scoped to `qual`. Two instances the wider search returns, both untouched: (a) **`shadowExceptions` carries `status: "waived"`**, a standing per-item gate waiver, on the one-line rationale *these are RULINGS* — the identical argument the user overruled for `DB.qual`; (b) **`slotsSRA1`/`slotsSRA2` carry as `d.x ? 1 : 0`**, so an older backup lacking them writes an explicit **0** over the importing browser's 1 and re-runs the pair on the next boot — with a stored `slots: 6`, SRA1 no-ops and SRA2 raises it to **7**. Both sit inside the queued restore track, so this is PASS with the carry-forward named rather than SENT BACK. | 1 | PASS |
| 10 | 2026-08-21 | the sell leg ages on its own clock (ruled batch, pass-8 pair 1.2+1.3) | *A record aged against a horizon carries the stamp of the placement that created its CURRENT leg — each leg-creation moment stamps the gap in force then — and a stampless record freezes once at a fixed value rather than reading a live setting, because a live-read fallback retro-applies every settings change to every open leg at once.* | *A leg's aging horizon is a fact frozen onto the record at the moment of the placement it measures — the sell's own listing moment for a sell leg, not the buy's — and no live setting may reach an open leg's aging clock through a missing-stamp fallback; an invented stamp is frozen once at a stated moment, marked as invented, and the mark is disclosed wherever the horizon drives an instruction.* | **All aging readers and all stamp writers** (the reviewer's inventory: 10 reader surfaces incl. paper's eight `legHorizonH` sites, 9 writer paths). Their DISCLOSURE clause was wider than mine and caught two real omissions, both fixed pre-land: the quote-leg marker was write-only (`quoteLegAge` now returns `froze`, the quote line discloses), and `inheritHzH` dropped the marker at a split. Deliberately scoped out, recorded: the paper book's fallback (quarantined, stamped-at-open population; restores forbidden); the walk-up checklist rung list and home ladder inline still lack the note — **carry-forward**. Semantic note on record: `p.hzH` now means *horizon of the most recent placement event*. | 3 | PASS |
| 11 | 2026-08-21 | seasoning resets on a lapse; the promise is achievable (ruled batch, pass-8 pair 4.2+4.3) | *A streak that evidences RECENT consistency dies when its recency does — beyond a bounded gap the next pass restarts the evidence — and every reported count, verdict and promised time derives from the post-lapse reading and from the schedule the gate enforces.* | *A qualification streak whose last observation is older than a stated window is void — discarded, not paused — and the next pass starts a new streak; every promise a surface makes derives from the same schedule terms the gate enforces, reachable by construction, carrying its day.* | **The reviewer's naming was WIDER than the repairer's and the width was the finding:** their "a streak … is void" reached completed tenure, and they measured the consequence — one weekend away re-seasoned the whole funded book for ~27h, an expiry the standing retention rule says needs its own ruling. Settled by EXEMPTING completed streaks (`qualDone`/`qualLapsed`, one owner — which also closed their two-owner finding), so the reset reaches exactly the ruling's named case. Their reachability finding (the lapse copy could never render, because the streak loop resets the row before any read) produced the recorded lapse (`lapsedN`/`lapsedAt`). Also fixed pre-land: the no-schedule ETA could promise the past; the empty-payload warn over-promised "held". Carry-forward: the watch badge does not read `stale` (boot-edge state only). **The 48h window value is PROPOSED, not ratified.** **Extending the reset to tenure is available as its own ruling.** | 2 | PASS |
| 12 | 2026-08-21 | every unfunded reason, not the first (ruled batch, pass-8 pair 7.1+7.2) | *Where a mechanism can refuse for several reasons at once, the record carries the FULL reason set from the one site that decides it, every consumer derives from it, a positional first-match survives only where a partition needs one attribution, and a row class with no key must never inherit another class's default reason.* | *When several independent conditions can simultaneously block one outcome, the record carries the complete set in one owned term and every consumer derives from it rather than re-deciding; and no row may fall through to a default that positively asserts a state ("plan is full") that can be false for the row that reaches it.* | The reviewer verified the set cannot be empty, the old ternary's priorities are reproduced exactly, and the gate chain is correctly out of scope (its own ruled labelling regime). Fixed pre-land on their findings: the picker's unknown-key fallback re-encoded the confident "plan is full" default (now the third state, "reason not recorded"); the picker header recited "also binding" twice; `NU_SHORT.softfill` was copy nothing could read. Recorded, not fixed: the funnel's first-match disclosure says "gate" and now also covers allocator rows (copy item); the "budget" key is a catch-all (per-slot sizing and upstream clamps land there — pre-existing); the two label tables' key sets are comment-synchronized (registers genuinely differ). | 3 | PASS |
| 13 | 2026-08-21 | cohort provenance at birth (F0, batch 2026-08-21·B) | *A record that will later be grouped by population carries its population stamp from the moment it is created, written by the one site that creates it from the source object's own provenance; the reader groups on the stamp; absence stays absence through every carry, because a pre-stamp row must never be defaulted into either population.* | *A paper trip records, at the moment it is created, which candidate population produced it — one shared term (`paperSrcOk`) deciding what counts as a valid stamp at every writer, absent meaning "predates the stamp", never a default to either population; downstream, every aggregate over the trip store either splits by that provenance or explicitly refuses the population it must not read.* | **The reviewer's second clause was wider than the repairer's and both round-1 return grounds lived in it:** `shadowByGate` admitted pool trips into the per-gate stream that moves gate constants (fixed — excluded, stated in place); the two writers had no shared validity term (`paperSrcOk` extracted). Round 2b: the exception lane got its one population predicate (`excCohortOk`, `[R114.3]`) and the opportunity-cost label reads the one cohort owner (`[R114.4]`). Correct omissions each with the reason recorded: reconstruction (creates no rows), scanner/slice/gap prior stamps, `regimeSepRows`/`paperCapacity`/`paperSellFailures`. **Carried forward: `shadowByGate`'s inline exclusion and `excCohortOk` are two spellings of one population predicate, each branch-asserted — fold when next touched.** Seeds SB1–SB3, SB9–SB10, SB17–SB18, each red alone, staged tree hash-restored between. | 2 | PASS |
| 14 | 2026-08-21 | one coverage owner; the chart bench states its true cause (fix 2c + trace 1 / M180, batch 2026-08-21·B) | *One question — "which chart-coverage era is it" — has one owning term reading the gates' own snapshot, and every surface that renders or branches on the era reads that term; a bench reason states the GOVERNING cause, not a mechanism's internal value: below coverage the cause is the era, at coverage with an absent item the cause is no-trades-observed, and only a series fed by the item's own fetch may render a points count.* | *The question "which chart-coverage era is the pool in" has exactly one owner: `poolEraInfo`, reading the SAME snapshot the chart gates bench off (`S.chartCache.state`, produced by `chartWireState` — the only place observed hours become days); every surface that renders or branches on the era reads that term, so no two surfaces can disagree at the 7-day boundary; era-specific copy branches on the owner's state, so a sentence cannot survive into an era it is false of.* | Round-1 return grounds, both inside the property: the scorer analysis export header still derived observed/24 from the second snapshot (an artefact — fixed via `chartGateDaysText()`, the one artefact rendering, `[R115.3]`); the no-trades sentence claimed the 8-day coverage window while emptiness is read over `CHART_PTS_CAP`'s 7 (re-pinned, `[R115.1]`). Also: the era branch splits waiting (deliberate) from could-not-check/reading (unread, never absent); `scorerVerdictInline`'s waiting sentence cannot render at maturity; the ready figure renders REAL observed days, never the threshold. Verified deliberate non-consumer: `S.scorerSurf` appears in no era read. Correct omission: `cutoverDecisionLogOnce` (provably empty disagreement region; fold queued). Era-stale tooltips 16483/15493 listed for the copy sweep. Seeds SB4–SB7, SB11–SB13, SB15, SB20–SB21, each red alone. | 6 | PASS |
| 15 | 2026-08-21 | a bench remedy names a control its population can reach (F1, batch 2026-08-21·B) | *Copy that names a remedy names one the reading population can actually reach: a sentence naming a control renders only for records that have that control, and each population's sentence names its own real route.* | *(a) A candidate's population provenance enters the gate chain as an argument at the call (`candidateFor(w, src)`, both `planCandidates` sites, landing on every return path), so the chain's own copy can branch on population instead of receiving a post-stamp it cannot see. (b) A bench remedy must name a control the benched row's population can actually reach: a sentence directing the operator to a watch row may render only on rows that have one, and a pool row's remedy is the hand-pin route.* | The reviewer's search found the property at two sites the finding did not name: the sizing gate's no-buy-limit limb told a pool item to type a qty on a watch row it does not have (fixed, branched, `[R115.4]`), and the proven-loser re-test remedy (round 2b, `[R115.6]`). The ITEM_OPS schedule promise re-attributed as a ruling, not a mechanism. Correct omissions with reasons: margin-test copy (log form takes any item), promote picker (population-neutral), THE POOL rows (statements, not remedies), `gateTag`'s provenance-less tooltip (recorded disagreement site). Seeds SB8, SB14, SB16, SB19, each red alone. | 1 | PASS |
| 16 | 2026-08-21 | one owner for free gp (F5, batch 2026-08-21·C) | *Each capital figure has exactly one owning term, every render and every sizing path reads the owner, and the shadow reserve is never spendable in any figure anywhere — a second spelling of the same figure is where two numbers on one tab come to differ by exactly the reserve.* | *Free and working capital each have exactly one owning term — `workingStack()` = stack minus the shadow reserve, `freeGp()` = working stack minus committed — and every figure that renders or spends free/working capital reads the owner, so the shadow reserve is subtracted once, at the owner, and is never spendable in any figure anywhere.* | **Two rounds, two reviewers, each round finding a narrower instance of the same property:** round 1 (first reviewer) returned `workingCapital()` — a `DB.bank`-spelled respelling the `stack()`-spelled sweep missed (the repair was scoped to the finding's spelling; fixed — reads `workingStack() − DB.reserve`, `[R116.2]` discriminating on a post-stamp flip). Round 2 (second reviewer) returned the `met-capfeas` GLOSSARY entry still describing the OLD arithmetic — the panel and its own popover giving two definitions of one number (fixed). Recorded limits: `[R116.1]` pins values and the deleted form's absence, not spellings; the relabelled feasibility line is inspection-verified. Correct omissions: `workingCapital`'s four display clamps of the reserve amount (different question), the committed renders. `available()` deleted with the reason at its old site; two inline working-stack respellings folded. Seeds SC1, SC2, SC11, each red alone. | 1 | PASS |
| 17 | 2026-08-21 | one comparator owns the funding order, display, print, and the family seat (2f + A1 landing + F3, batch 2026-08-21·C) | *The question "which candidate comes first" has one owning term, total over the whole candidate set, and every site that orders, prints, counts positions in, or breaks a tie over candidates reads it — and the value key never compares across populations, because a weighted score from operator history and an unweighted core from none are different evidence answering one sort.* | *Every ordering or pairwise preference over plan candidates — the funding walk, the on-screen groups, NEXT UP, the printed plan, the queue-position claims a picker cites, and the family-seat decision — is decided by the single total comparator `planCmp` (promotion, then population, then tier, then the value key the item's evidence supports); no second sort and no raw-score comparison over plan candidates exists anywhere.* | **Lands the A1 ruling (row 7 — ruled Aug 19, cold-review-passed, never landed) fresh against the current tree, plus the family-winner ruling.** One deliberate delta from the Aug 19 form, stated: the population group moved INSIDE the comparator, because a pairwise seat decision needs a total order — the value key still never compares across populations (the group key settles first, `[R117.3]`). The reviews' catches, all fixed: a third copy of the stale "render grouping only" claim; the R92.3 row's false "within-group sorts unchanged" clause; the promoted header naming a control that does not exist (now claims the real lifts — daily reset + press-time undo); the queue blame promising "a demotion frees a slot for the first of them, not for this" where the walk actually funds the first row ahead WHOSE OWN OTHER BINDS CLEAR (copy and R117.6 row re-claimed); the checklist inline print's deliberate directness recorded at the site. Machinery verdict round 2: "clean and well-asserted" — all ~100 sorts in the file classified, no second owner. Seeds SC3–SC8 (SC8 re-anchored and re-run), each red on target with predicted-only propagation. | 3 | PASS |
| 18 | 2026-08-21 | the pool surfaces state the whole pool inventory from one owner (2g + the Clockwork trace, batch 2026-08-21·C) | *A surface announcing a population states that population's full inventory during the era it exists for — a member in a state the surface's count cannot express is invisible from the surface that announced it, and no member is claimed to be in a state it is not in.* | *During the waiting era, every rendered count of the pool population reads one owner — `poolInventory(P)`, which files every pool member of one plan build into exactly one of funded / waiting-on-the-chart-gate / other — so no surface derives "waiting" by different arithmetic, and no member is claimed waiting while it is funded or benched for another reason.* | **The reviewer's naming was structurally stronger than the repairer's and round 1 proved it:** the first form counted funded+waiting from two different populations on two surfaces (header: benched pool rows; tile: whole cell minus funded), and the disagreement region — a sparked pool item in NEXT UP or mid-seasoning — is exactly the item the repair is about, on an ordinary day. Fixed with `poolInventory` (partition by first-binding gate NAME via `gateName`, remainder bucket, both surfaces render its numbers and no others). Round 2 catches, fixed: two stale §113 rows (spec asserting the form the suite forbids — invisible to the pairing check); the other-states clause enumerating a closed list over a remainder; the tile's degrade got its fires-when-it-should drive (`[R118.2]` poisoned-build limb, the restraint asymmetry); the tile also got the renderHome degrade convention and one pool predicate. The scorer tile lead was found by the repairer's own search (the same defect on a second surface, `[R118.2]`); the tile's `buildPlan()` call verified idempotent by the reviewer. Seeds SC9–SC10, SC12–SC14 (SC9 re-anchored and re-run), each red on target with predicted-only propagation. | 1 | PASS |
| 19 | 2026-08-21 | the pins/pool naming sweep (queue 2h + F10, batch 2026-08-21·D) | *Rendered vocabulary names the population the mechanism actually reads. The legacy name survives only as partition values (sweep-proof, so history and exports stay joinable), era-branched copy that renders only in the era it is true of, and historical records — and where a legacy KEY must render, one mapper owns the rename so no surface invents its own spelling.* | *Rendered vocabulary names the population the mechanism reads: the operator-facing copy says pins (hand-curated rows) or the candidate set, and the old name watchlist survives only as a partition value, as era-branched copy that renders only while the pool is off, or inside historical records. Where a legacy key must render, exactly one mapper (`paperCohortLabel`) produces the label.* | **Round 1 SENT BACK on the same diagnosis as M170: "the repair followed the spelling of specific surfaces rather than enumerating renders of the population name."** The reviewer's enumeration returned ~9 classes of missed renders — several inside strings the sweep itself edited (the capacity line's `<b>Watchlist</b>` beside its own swept tail; two ternaries with a swept pool branch and unswept pin branch; the filter-opportunity line disagreeing with its own else-branch) — and **the mapper bypass**: `rateBlend` rendering raw cohort keys beside panels using the mapped label. All swept in the amendment, with six probe pins moved with their copy (`[R87.4]` `[R63.2]` `[R115.2]` `[R115.4]` `[R49.1]`×2 `[R49.2]`). Round 2: PASS on a fresh whole-index property scan. The **populations** glossary group (Pin, The pool) shipped same-commit; the paper cohort enumeration gained `pool`; F10's seat-economy checklist copy rewritten with a split-needle source assertion. Correct omissions upheld by the reviewer's own list: era-branched empties, partition values, recorded rulings, the observation-sense "unwatched". Seeds SD1–SD3, SD13, SD14 (SD2's three-reader propagation is the one-mapper design working). | ~50 | PASS |
| 20 | 2026-08-21 | the announced item stays findable; the sparked clear states its chart source (M181 + trace 2, batch 2026-08-21·D) | *An item announced on a surface is findable afterward from a durable record, membership-independent — a per-cycle population makes leaving a non-event, so the durable ledger must have a reader for the departed. And a verdict produced from an item's own fetch says so where it renders, from the chain's own resolution, never a re-resolve.* | *An item announced on a surface stays findable afterward from a durable record, independent of membership in the derived population it has since left; and a funded line whose chart evidence came from the item's own session fetch says so, reading the one resolution the gate chain itself stamped (`serSrc`), never a re-resolve.* | PASS in round 1. The reviewer verified: `poolPersistence` never returns not-scored for an id with a ledger row (so departed lines genuinely render); `DB.poolFirstClear` fully wired at all four points (writer, NOW, import carry, drill reader — not an orphan); `serSrc` one owner with exactly one consumer and no second spelling of the provenance sentence; and **no other derived population with a durable record lacks a departed-item reader** — the pool is the only per-cycle-derived population with one. One nit (the drill's empty copy named "on the plan" over a population that now includes the departed) fixed in the amendment. Seeds SD4, SD5, each red alone. | 2 | PASS |
| 21 | 2026-08-21 | the display batch (queue 2i — F2 F6 F7 F8 F9 F11 F12 F13 F14 F15 F16, batch 2026-08-21·D) | *Eleven registered instances of three properties, fixed at their sites: a question answered in two places gets one owner; a count or claim states exactly what the code computes; and a population's controls are reachable by that population.* | *Each question the product answers in more than one place gets one owning term with every other site reading it; each rendered count, label and claim matches what the code computes; and each control a population is constitutionally owed is present on that population's rows.* | **Round 1 SENT BACK — the sharpest catch was F11's THIRD press surface** (the review audit's §3, with its own hand-rolled evidence spelling ALREADY DRIFTED from the extracted `cluEvidenceLine` — the extraction's own property, unapplied at the site that drifted first; the absence assertion was scoped to the queue body and passed the audit surface unseen). Also: F2 had no assertion while its row claimed coverage; and two introduced defects — the F16 handlers left the step's own surfaces stale after the press, and Mark-as-current with an empty bank could silently DELETE today's measured dot. All closed in the amendment (`[R121.7]` audit limb · `[R121.9]` · `[R121.4]` dot-intact limb + re-renders + positive-figure guard), with four stale comments corrected and rows re-claimed to their probes. **One reviewer claim was pushed back and withdrawn on re-verification** (the pool row's 🚫✕ branch — the reviewer had conflated the group's row population with the header's chart-first count). Recorded limits: `[R121.2]` is a tripwire against future drift (the pre-repair spellings were identical); `[R121.3]`'s subject is the term. Seeds SD6–SD12, SD15, SD16, each red alone. | 17 | PASS |
| 22 | 2026-08-21 | a spark-fed reading states its age wherever it informs a verdict (trace-3 finding 2, ruled DISCLOSE; pass 2026-08-21·E) | *A reading taken from a session-scoped fetch states the fetch's age beside every verdict it informs — the age is a property of the fetch, so an archive-fed reading never carries the clause — and the age comes from the one resolution the chain itself stamped, never a re-resolve. Display only: no verdict moves.* | *A reading derived from a session-scoped fetch (a spark) states the age of that fetch beside itself wherever it informs a verdict the operator reads; the age comes from the one resolution stamped in `candidateFor`, never a re-resolve; an archive-fed reading carries no fetch clause because it has no fetch.* | **The repairer's search found the five bench sentences and the provenance note; the reviewer's found three more in-property sites** — the chart-still-loading bench's SPARK branch ("the strongest case of the property, sitting eleven lines above the comment that states the rule"), the trend/volTrend SHORT strings (the short-preferring surfaces rendered the frozen reading ageless — the caveat-placement rule), and the FALLING risk chip (which was also a pre-existing re-resolve; it now reads the chain's own stamp, honestly absent outside the build). All closed. The reviewer's residual — a relative age freezing into the durable `gapTxt` — closed post-verdict (`gapTxtOf` strips the clause at all three store sites: a durable record must not freeze a relative clock). The disclose ruling's re-open condition and its instrumentation are recorded in the traces file. Seeds SE1, SE2, SE7, SE8, each red alone. | 6 | PASS |
| 23 | 2026-08-21 | one badge term feeds both plan groups (the badge unification, points 1/2/4; pass 2026-08-21·E) | *One question — "does the instrument's own record fund this item, and how often" — has one renderer, read by every row that answers it, with population-specific WORDING derived inside the renderer; a no-history state renders as information in its own words, never as an empty slot; and realized figures sit beside simulated ones only in their own labelled column, never pooled.* | *One renderer (`poolDot`) owns the per-item persistence answer for every row population; population wording is derived inside the renderer, never passed by a caller; every no-history state renders as information in its own words; a pin's realized history renders beside, and never pools with, the simulated/gate columns.* | Round 1 SENT BACK on the same-commit glossary rule (the badge's own popover contradicted the glyph) **plus the reviewer's sharpest catch of the pass: the pin tooltip claimed "has never funded this pin" from a fact — a missing ledger row — that a retention-window or store-cap eviction can also produce.** The tooltip now claims exactly the computed fact ("no funded cycle on record", qualifier in place, "never funded" asserted absent); the `plan-persist` entry renamed "Persistence (pins and pool)" with a same-commit glossary limb; the settled fixture genuinely exercises the settled branch (the round-1 fixture read ACCRUING while its comment claimed settled). `poolSeenAccrue` verified population-complete for pins (no accrual change). Carried forward: the NON-pin NOT APPLICABLE wording carries the same eviction gap (pre-existing; next touch of the term). Byte-identity asserted between the row's dot and the renderer's output. Seeds SE3–SE5, SE9, each red alone (SE3's dormant-form propagation predicted). | 4 | PASS |
| 24 | 2026-08-21 | the paper circles retire to the dormancy lane (the badge unification, point 3; pass 2026-08-21·E) | *A retired feature leaves by the dormancy lane: one flag production reads gates its render sites AND its assertions; every surface that names it is swept or rewritten in the same change; and what it showed is stated to remain reachable where it actually lives, so nothing reads as lost.* | *One flag (`PAPER_DOT_RETIRED`), the same one production reads, gates every render site of the retired circles and every assertion about them; every surface naming them is swept or dormant-marked; what the circles showed is stated to remain reachable on a real surface.* | Round 1 SENT BACK (narrow) on **the gating enumeration being the spelling** — the repair's own comment listed "[R25.2]'s dot limb, [R25.6], [R25.7]" and the reviewer's property-scoped search returned two more circle assertions ([R27.1]'s row-dot limb, [R39.7]'s glossed-where-they-render limb); both gated/re-pointed. The family-generic properties (colour-within-identity, self-naming tooltips, fixed slots, the legend, primary-dot-leads) rewritten LIVE against the persistence badge with one deliberate delta stated: the badge never leaves its slot empty, because its no-history states are information. SE6 is the ERA DRIVE on record: flag flipped, exactly the era-pinned limbs red while all gated originals RAN AND PASSED — both eras drivable. `shadowCite` correctly untouched (the citations are not the dot). Reviewer's comment/legend corrections closed same round. Rider: the 48h `QUAL_GAP_RESET` ratification verified text-only. | 2 | PASS |

## The cold review of 2026-08-21 — one line

The reviewer was a fresh agent shown the CODE diff only (no findings, no PASS.md, no HANDOFF, no
audits) and asked for property, search, omissions, and the subsystem map. Full verbatim record and
per-finding responses: `staging/PASS.md` at landing time (durable copy: this file's rows 10–12).
Sixteen seeds total across the batch, each red alone, staged tree hash-restored between all of them.

## The cold review of 2026-08-19 — what the two columns actually said

**Eight PASS, one SENT BACK.** The one that failed is row 8, and it failed the same way its own
comment says its predecessor failed: **membership drawn from where the keys live rather than from
what the keys do.** v1 was drawn from the 23 keys sharing one object literal and missed `slots` and
`watchCap`; v2 was drawn from the 9 keys that had shared `impCap`, and missed the three budgets.
The fix is three rows in `CAP_KEYS`, not new machinery — which is exactly what makes it worth
sending back rather than waving through, because a one-line fix that nobody makes is still absent.

**Two rows carry a genuinely different property rather than a paraphrase**, and they are the ones
worth reading:

- **Row 8** — the reviewer's property makes MEMBERSHIP part of the rule; the repairer's makes
  ownership the whole rule and treats membership as an input to it. Only the first phrasing can
  fail.
- **Row 9** — the property was named RIGHT and the search was run NARROW. This is the first
  recorded instance of that half of the diagnostic, and it is the half the ledger was built to be
  able to see. The remedy is different from row 8's: nothing about the wording needs changing, the
  grep does.

**Rows 1 and 7 record a divergence that resolved in the repairer's favour**, which is also worth
keeping: on row 1 the reviewer's stricter *absence stays absence* reading would have made
`trackExistingOffer` worse, and the repairer's argument from the record's own `placedAt` is the
better one. A ledger that only ever recorded the reviewer catching the repairer would be a ledger
nobody trusted.

**One thing the mechanism itself got wrong, reported rather than fixed:** `staging/PASS.md` is on
the reviewer's READ list and contains, for repairs 1 and 2, a section headed *"The finding that
provoked it (for the record; the cold reviewer is NOT shown this)"*. The reviewer was shown it. The
searches for those two rows were run property-first regardless — repair 1's returned 8 sites where
the finding named 2, and repair 2's returned 21 where it named 4 — but the contradiction should be
closed by moving those blocks to a sibling file the READ list excludes, or by writing them only
after the review lands.

## Escape hatch, as scoped by the user

A **single-site fix with no generalisation available** may land in one session — the `flush()`/
`save()` swap is the example on record. **The moment a repair implies "and everywhere else like
it", it stages.** A repair that takes the escape hatch does not get a row here; if it later turns
out to have had a generalisation after all, that is a MISTAKES entry, not a retroactive row.

## Addendum after the adversarial pass — the reviewer's own search was narrow on row 4

**Recorded here rather than quietly corrected, because the ledger's value is that it records the
misses too.** The pass over these nine repairs
(`audits/ADVERSARIAL-2026-08-19h-pass8-staged-repairs.md`) found that **an offline boot, or one
`/latest` response with an empty payload, deletes the entire seasoning store** — `updateQualStreaks`
is the only accrual path with no live-data precondition, `renderAll()` runs from `doRefresh`'s
`finally`, and `candidateFor` returns `failed: "no live price in /latest"` for every row, which the
pass loop reads as a gate failure and resets on.

That is row 4's property exactly, in the reviewer's own words: *"acts only on the two that are
verdicts."* A failure caused by missing DATA is not a verdict. **The property was named right and
the search was run narrow** — it checked the three counters for how they handle *absence from the
candidate list* and never asked what else can set `x.failed`.

So the pair on row 4 now reads the same way row 9's does, and the diagnostic points at the search
discipline rather than at either wording. The concrete lesson for the next property-scoped search:
**when a property is about how a signal is CLASSIFIED, enumerate every producer of that signal, not
only the consumers of it.** Row 4's search enumerated consumers.

Row 1 also resolved against the reviewer, and the pass reversed that: the cold review withdrew its
*absence stays absence* clause for `trackExistingOffer` in the repairer's favour, and the pass showed
the withdrawal was wrong for a reason neither naming stated — `legHorizonH`'s fallback has a
one-hour floor and `placementHzH()` has none, so the new stamp is a strict regression in the
near-touch case. **Both namings were incomplete in the same direction**, which is the third cell of
the diagnostic and the one neither column can fill on its own.
