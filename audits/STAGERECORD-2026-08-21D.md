# STAGERECORD-2026-08-21D — durable copy of the batch-3 staged-pass record

Byte-level diff preserved at audits/DIFF-2026-08-21D-batch3.patch (the M182 practice).
Final staged reports: 1200 and 390 both 'PROBE-PASS [STAGED: staging/index.html]'.

═══════════ staging/PASS.md ═══════════

# Staged repair pass 2026-08-21·D — ruled-queue batch 3 of 3 (the naming sweep · the findable line · the display batch)

**Opened:** 2026-08-21 (restart session) · **Tree frozen at:** see `BASELINE.sha256`

All three repairs execute standing rulings from the restart directive's queue (items 2h, the
section-1 surface fix + trace-2 provenance obligation, and 2i).

---

## Repair 1 — the pins/pool naming sweep (queue item 2h + F10)

**The finding that provoked it:** (the surface audit's ~40-site naming list — cold reviewer is NOT
shown this)

**THE PROPERTY THIS REPAIR IS ABOUT** (the repairer's answer, written BEFORE the search):
> Rendered vocabulary names the population the mechanism actually reads. The legacy name survives
> only as partition values (sweep-proof, so history and exports stay joinable), era-branched copy
> that renders only in the era it is true of, and historical records — and where a legacy KEY must
> render, one mapper owns the rename so no surface invents its own spelling.

**The property-scoped search:** every occurrence of `watchlist` in the staged source, classified
one by one: rendered string / partition value / era-branched / historical / comment. ~50 rendered
sites swept across the audit's six families (chrome, plan/deploy copy, warn channel,
paper/prospect copy, scorer/rdiff wording, glossary/export notes, toasts); partition values left
with their sweep-proof notes (`paperCohortOf`'s key, `qual.src`, the cohort keys array, the
`(watchlist)` era branch in `planSubLine`, `cutoverPoolOn` branches); historical/recorded text
left (M156 glossary, decision-record strings).

**Sites touched:** the six families (h2 → Pins; the position-row "not on the watchlist" → "not
scored this cycle"; scout/cluster/mm/hours/paper/scanner/rdiff copy and glossary; both
removeWatch toasts → "Unpinned — stays scoreable in the pool"; the review cut button and the F10
seat-economy step rewritten to the pool economy; the scanner glossary's promote entry →
pin-with-overlays). `paperCohortLabel` added as the one label mapper; the **populations** glossary
group (Pin, The pool) ships in the same commit; the paper cohort enumeration gains `pool` (the
every-cohort-states rule now covers the F0 cohort). Two mid-sweep gluing defects (a dropped
trailing space joining words) caught and fixed in the same session by re-reading each multi-line
site.
**Sites returned but deliberately NOT touched (reasons in source where they live):** partition
values and import recognizers (sweep-proof — renaming a persisted key orphans stores);
era-branched copy (renders only when the cutover is off or the flag pair is refused);
historical/recorded strings (M156's own recounting, dated rationale records).

**Seeds:** SD1 (h2 reverted to Watchlist) → `[R119.1]` red alone; SD2 (`paperCohortLabel` made
identity) → `[R119.1]` label limb red, propagation to `[R39.7]` (the glossed cohort label reads
the mapper) and `[R119.2]` (the cohort ledger renders the raw key again) — one seeded term,
three readers, which is the one-mapper design working; SD3 (pool dropped from the cohort
enumeration) → `[R119.2]` red alone.

**FIRST COLD REVIEW (2026-08-21, fresh agent, diff + subsystem map only — the review was
interrupted once by an API session limit and resumed with its context intact):**
- Property (reviewer's naming, verbatim): *"Rendered vocabulary names the population the
  mechanism reads: the operator-facing copy says pins (hand-curated rows) or the candidate set,
  and the old name watchlist survives only as a partition value, as era-branched copy that
  renders only while the pool is off, or inside historical records. Where a legacy key must
  render, exactly one mapper (`paperCohortLabel`) produces the label."*
- Verdict (round 1): SENT BACK — the property-scoped search returned rendered sites the sweep
  missed, several inside the very strings it edited: the capacity line's `<b>Watchlist</b>`
  beside its own swept tail; the per-gate note; the qual-evict warn (whose probe pin had to move
  with it); the qual-provenance warn; the exception badge (`unwatched` beside the swept title);
  the filter-opportunity positive branch disagreeing with its own else-branch; two ternaries
  whose pool branch was swept and pin branch was not; **the mapper bypass** (`rateBlend`
  rendering raw cohort keys — two callers feed it `paperCohortOf` values); and a minor set
  (import confirm, NOW line, markout title, sibling/pin-cap tooltips, the cohort glossary's
  `what`). *"The repair followed the spelling of specific surfaces rather than enumerating
  renders of the population name."*

**AMENDMENT (repairer, same session):** all listed sites swept (the ternary pin branches now
read *"on its pin's row"*, with `[R115.2]`/`[R115.4]` re-pinned to the new sentences plus
old-form absence, and their requirement rows updated); `rateBlend` labels through the one
mapper (`paperCohortLabel` on the row label and the inline decomposition, the KEY kept on
`_cohort`), with the `blendFrag` scoping assertions re-pinned to the mapped label; the
qual-evict pin (`[R87.4]`) and the exception badge pin (`[R63.2]`) moved with their copy.
Era-branched empties, partition values and recorded rulings confirmed kept, per the reviewer's
own untouched-CORRECT list. Three further `rateBlend`-family pins surfaced by the run and
re-pointed to the mapped label (`[R49.1]` ×2, `[R49.2]` hours limb). **Amendment seeds SD13
(F10 copy reverted to the scout-backfill narration) → the `[R119.2]` F10 limb red alone; SD14
(the untiered pin branch reverted to the watch-row wording) → `[R115.2]` red alone — each
staged-tree hash-restored after.**

**COLD REVIEW (final)** — recorded by the reviewer after verifying every ground on the amended
diff:
- Reviewer's answer to *"name the property this repair is about"*: (verbatim, round 1, upheld)
  *"Rendered vocabulary names the population the mechanism reads: the operator-facing copy says
  pins (hand-curated rows) or the candidate set, and the old name watchlist survives only as a
  partition value, as era-branched copy that renders only while the pool is off, or inside
  historical records. Where a legacy key must render, exactly one mapper (`paperCohortLabel`)
  produces the label."*
- Confirmation: every ground closed; *"a fresh property scan of the whole staged index finds
  Watchlist/unwatched surviving only as decision-log records, era-branched empties, comments,
  and the observation-sense term."* Residual noted as cosmetic: nested parentheses in the
  blend's inline decomposition; R119.2's weak positive half, carried by its strong absence half.
- Verdict: PASS     <!-- final, round 2 -->

---

## Repair 2 — the announced item stays findable; the sparked clear states its chart source (M181 + trace 2)

**The finding that provoked it:** (M181; the trace-2 provenance obligation — cold reviewer is NOT
shown this)

**THE PROPERTY THIS REPAIR IS ABOUT** (the repairer's answer, written BEFORE the search):
> An item announced on a surface is findable afterward from a durable record,
> membership-independent — a per-cycle population makes leaving a non-event, so the durable ledger
> must have a reader for the departed. And a verdict produced from an item's own fetch says so
> where it renders, from the chain's own resolution, never a re-resolve.

**The property-scoped search:** every announcement record and durable pool ledger
(`DB.poolFirstClear`, `DB.poolSeen`, `poolSeenEvict`) checked for a membership-independent
reader; every surface rendering a sparked item's verdict checked for a source statement; every
consumer that would need the series source checked for re-resolution.

**Sites touched:** `poolDrill` — departed-item lines (reusing `poolPersistence`, which is already
membership-independent) with the last-passed clause, and the first-clear record's line in the
drill head; the previously-unused `seen` local becomes the reader. `candidateFor` stamps `serSrc`
(one owner for "which series answered"); the walk pushes the ruled provenance note on a sparked
pool clear below coverage, beside the waiver note.
**Sites returned but deliberately NOT touched:** the NOW line (already renders from the durable
record, day-gated by design); `poolSeenEvict` (already read in the drill head); THE POOL group
(counts current members by design — the drill is the durable reader).

**Seeds:** SD4 (departed loop removed) → `[R120.1]` red alone; SD5 (provenance note guard
inverted to pins) → `[R120.2]` red alone.

**COLD REVIEW (2026-08-21, fresh agent, diff + subsystem map only):**
- Reviewer's answer to *"name the property this repair is about"*: (verbatim) *"An item
  announced on a surface stays findable afterward from a durable record, independent of
  membership in the derived population it has since left; and a funded line whose chart
  evidence came from the item's own session fetch says so, reading the one resolution the gate
  chain itself stamped (`serSrc`), never a re-resolve."*
- Sites the reviewer's property-scoped search returned: `poolPersistence` verified correct for
  departed ids (never `not-scored` with a row); the `onPlan` exclusion verified complete;
  `DB.poolFirstClear` verified fully wired (writer, NOW, import carry, drill reader — not an
  orphan); `serSrc` verified one-owner with exactly one consumer and no second spelling of the
  provenance sentence; no OTHER derived population with a durable record and no departed-item
  reader exists. Nit fixed in the amendment round: the drill's empty copy named "on the plan"
  over a population that now includes departed items.
- Verdict: PASS     <!-- round 1 -->

---

## Repair 3 — the display batch (queue item 2i: F2 F6 F7 F8 F9 F11 F12 F13 F14 F15 F16)

**The finding that provoked it:** (the surface audit's drift-hazard register — cold reviewer is
NOT shown this)

**THE PROPERTY THIS REPAIR IS ABOUT** (the repairer's answer, written BEFORE the search):
> Eleven registered instances of three properties, fixed at their sites: a question answered in
> two places gets one owner (the sell-side net, the overnight verdict, the bank entry, the
> cluster press, the family-debug population); a count or claim states exactly what the code
> computes (the ⏳ count's full fail set, the first-reason partition label, the freshness line,
> the two hour clocks); and a population's controls are reachable by that population (the pool
> row's blacklist press, the tier map's missing sub-views).

**The property-scoped search:** per fix, the owning question's other spellings were searched
before editing — the sell-net formula (2 sites + the owner extracted), the overnight thresholds
(3 spellings in 2 functions), the bank writers (2 entries + the delegate branch), the cluster
presses (2 surfaces), the hour-clock renders (ledger LOCAL vs scorer UTC), `SURFACE_DEPTH` vs
the featTouch key space, the funnel's partition disclosures.

**Sites touched:** F2 `renderFamilyDebug` feeds pins ∪ pool; F6 the ⏳ count reads the full fail
set; F7 `posSellNet` extracted, both renders read it; F8 the pool row's 🚫 press (id-based
handler verified document-level); F9 the funnel disclosure names the NEXT-UP first-reason
partition; F11 the digest line gains members + `cluEvidenceLine`, the queue body keeps
coherence-drops and points at the digest; F12 the freshness T2 line names the Scorer sub-tab;
F13 `overnightSpreadVerdict` extracted, three threshold spellings folded; F14 the hours ledger
names LOCAL vs UTC; F15 the four sub-view keys; F16 one bank entry point (the dot stays a
measurement, records from the stamp; the data-snap input and its delegate branch deleted).
**Sites returned but deliberately NOT touched:** F17 (two fill simulators — accepted-by-design
per the audit, partitioned by stamps); the scorer grid's UTC label (already present in its drill
head); the coherence-drop queue (its own constitutional machinery, stays).

**Seeds:** SD6 (⏳ reverted to first-fail) → `[R121.1]` red alone; SD7 (positions table
re-spells the net inline, tax dropped) → `[R121.2]` red alone — the assertion is anchored at the
RENDER (the table's unrealized total vs the sell-mode lines' sum), because term-vs-term would be
true by construction; SD8 (the term's 1.1× buffer removed) → `[R121.3]` red alone — the term
carries the thresholds; the two consumers' wording-from-the-term is a read, recorded as the
limit in the row; SD9 (stamp stops recording the dot) → `[R121.4]` red alone; SD10 (pool row's
blacklist press removed) → `[R121.6]` red alone; SD11 (evidence clause dropped from the digest
line) → `[R121.7]` red alone.

**FIRST COLD REVIEW (2026-08-21, fresh agent, diff + subsystem map only):**
- Property (reviewer's naming, verbatim): *"Each question the product answers in more than one
  place gets one owning term with every other site reading it; each rendered count, label and
  claim matches what the code computes; and each control a population is constitutionally owed
  is present on that population's rows."*
- Verdict (round 1): SENT BACK — **F11's THIRD press surface**: the review audit's §3 still
  carried confirm/merge/dismiss for the same candidates with its own hand-rolled evidence
  spelling, already drifted from `cluEvidenceLine`'s wording ("over Nd" vs "over N overlapping
  days") — the extraction's own property, unapplied at the site that drifted first; `[R121.7]`'s
  absence check was scoped to the queue body only, so the audit surface passed unseen. Plus:
  F2's empty copy named the pass set over a membership-union feeder, and F2 had NO assertion
  (the row claimed coverage `[R121.3]` never tested); and three introduced defects — the F16
  handlers left the checklist/equity surfaces stale after the press, Mark-as-current with an
  empty bank could silently DELETE today's measured dot (`recordSnap` treats 0 as clear-today),
  and a quoted-anchor comment went stale. Verified clean: F6 (with `poolInventory`'s
  first-binding deliberately different), F7's single spelling, F13's owner, F8's id-based
  delegate, F9/F12/F14's claims, F15 complete against the writer. Noted limits: `[R121.2]`
  cannot discriminate the pre-repair form (the two spellings were textually identical — it is a
  tripwire against future drift); `[R121.3]`'s subject is the term.
- **One disagreement, recorded with the reason rather than adopted:** the reviewer called the
  pool row's `🚫✕` remove-branch unreachable ("a blacklisted item never appears in the waiting
  group"). The premise is wrong about membership: `cutoverPoolRows` excludes only pins, so a
  blacklisted cell item reaches `candidateFor`, benches on the blacklist gate with pool
  provenance, and renders in the group (an OTHER-states member) — the remove branch is
  reachable and stays.

**AMENDMENT (repairer, same session):** the audit §3 candidates block is a pointer (count +
staleness flag; presses and the second evidence spelling deleted), with a new `[R121.7]` limb
asserting it press-free at its own render; F2's empty copy names the union and F2 got its own
assertion (`[R121.9]`, pool-only book) and row; both F16 handlers re-render the checklist and
equity surfaces and guard `recordSnap` on a positive figure, with a new `[R121.4]` limb driving
the empty-bank press against an existing dot; the stale quoted-anchor comment, the F6
justification comment, the F11 pointer comment and the scorer-constants comment all corrected;
`[R120.1]`'s drill empty copy names the departed. The R121.3/R121.7/R121.4/R119.2 rows
re-claimed to exactly what their probes test. **Amendment seeds, each red alone with the staged
tree hash-restored after: SD12 (famDebug feeder reverted to pins-only) → `[R121.9]`; SD15 (the
audit §3 presses and hand-rolled evidence spelling restored) → the `[R121.7]` audit limb; SD16
(the zero-bank guard removed from Mark-as-current) → the `[R121.4]` dot-intact limb.**

**COLD REVIEW (final)** — recorded by the reviewer after verifying every ground on the amended
diff:
- Reviewer's answer to *"name the property this repair is about"*: (verbatim, round 1, upheld)
  *"Each question the product answers in more than one place gets one owning term with every
  other site reading it; each rendered count, label and claim matches what the code computes;
  and each control a population is constitutionally owed is present on that population's rows."*
- Confirmation: the audit §3 is a press-free pointer asserted at its own return; F2 has its
  discriminating assertion and honest empty copy; both bank handlers re-render and guard, with
  the empty-bank limb *"which the round-1 form would have deleted — the limb goes red on it"*;
  the four stale comments corrected; the rows claim exactly what their probes test. **On the
  🚫✕ pushback the reviewer withdrew its round-1 unreachability claim after re-verifying:**
  *"I conflated the two populations, which is exactly the header-versus-rows distinction the
  R118 work established"* — a blacklisted cell item reaches the group with pool provenance and
  the remove branch is reachable and rightly kept.
- Verdict: PASS     <!-- final, round 2 -->
