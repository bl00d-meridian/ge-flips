# REQUIREMENTS.md — thesis sleeve + analyst intelligence (consolidated build, Aug 10 2026)

Every requirement from the ruled spec, with a stable ID. The probe suite tags its
assertions with these IDs (`[R1.2]` in the assertion name); the probe report ends with a
`===REQS===` section listing per-ID PASS/FAIL derived from those tags — that section and
this file cross-reference each other. IDs marked **UI** are verified by inspection
(rendering/copy the headless suite exercises only incidentally); everything touching
money math, budgets, or ratification state carries probe tags. No silently dropped
gates: a row in this table without a probe tag says so out loud.

| ID | Requirement | Verified by |
|---|---|---|
| **§0 Recalibration** | | |
| R0.1 | T1 budget 100m→60m, T2 50m→30m; sleeve budget 60m carved from working capital; shadow reserve untouched; decision-log entry "resized to evidence…" | probe `[R0.1]` |
| **§1 Thesis sleeve** | | |
| R1.1 | Sleeve budget disjoint from tier pools and allocator; governs NEW entries only; migrated legacy holds excluded from budget and concurrency count | probe `[R1.1]` `[R1.1a]` |
| R1.1b | Crimson kisten / Elder maul holds migrate into the sleeve as legacy positions with their cost bases | probe `[R1.1a]` |
| R1.1c | Max 3 concurrent new positions (setting) | probe `[R1.1c]` |
| R1.2 | Mandatory fields — no save without item, qty, entry/cost basis, named catalyst + window, one-sentence thesis, exit ladder ≥2 rungs (trigger + fraction each), invalidation | probe `[R1.2]` |
| R1.2b | Ladder edits allowed but decision-logged with required reason | probe `[R1.2b]` |
| R1.3 | Monitoring: price vs entry and vs each rung, days held, catalyst countdown; mark-to-market de-emphasized, "judged at exit, not at the mark"; per-position catalyst-phase buttons, user-flagged only | UI (panel copy) + probe `[R1.4]` (phase→rung path) |
| R1.4 | Rung triggers: price within 5% (setting), date within 7d, or flagged phase → SELL card (qty from fraction, guidance = current instasell); logging the fill records it against the rung; realized P&L at exit vs cost, net of tax | probe `[R1.4]` `[R1.4b]` |
| R1.5 | Weekly SLEEVE section: manual thesis-health light (never inferred), realized exits, sleeve stats fully separate from flipping metrics; rides Export JSON | probe `[R1.5]` (separation + export) · UI (weekly section) |
| R1.6 | Guardrails: no sleeve position in current flipping inventory; full budget requires closing a position (no pool/reserve raids); update advisories informational on sleeve items | probe `[R1.6]` (mandate + budget refusal) · UI (Wednesday advisory) |
| **§2 Candidate engine** | | |
| R2.1 | Catalyst calendar (name, window, affected items/clusters); entry watch vs 90-day baseline; accumulation signature; DISCOUNTED vs RAMPING | probe `[R2.1]` |
| R2.2 | Weekly anomaly scan: >1m items, sustained price+volume rise 2–3 weeks, no linked catalyst, "reason unknown — no thesis, no position", max 5 flags/week by strength | probe `[R2.1]` (the scan's verdict math IS `slvSeriesStats`/accum, asserted there); the ≤5 cap and >1m pre-screen are code-fixed (`slice(0,5)`, `px > 1e6`) and network-bound, so not probe-asserted — stated here, not silently dropped · UI (copy) |
| R2.3 | Cluster crossover: catalyst-linked set moves, member lags → laggard flag ("same thesis, unpriced member") | probe `[R2.3]` |
| R2.4 | Ranking = discount depth × catalyst proximity × exit liquidity; warn when intended position > 15% of daily gp volume ("a thesis you can't exit isn't a trade") | probe `[R2.4]` |
| **§3 Analyst briefing (desk side — not probe-testable in the tracker)** | | |
| R3.1 | /briefing skill exists; BRIEFING.md defines sources (official news, wiki upcoming, r/2007scape, dev blogs), politeness, per-claim citation | inspection: BRIEFING.md, .claude/skills/briefing/SKILL.md |
| R3.2 | intelligence.json: typed records with sources, confidence tier, item ids, direction, VALID-UNTIL, contrary evidence, priced-in check; no source no record; dated BRIEF.md with covered range | inspection: BRIEFING.md contract · tracker enforces at import, probe `[R4.1]` |
| R3.3 | Standing rules: never launder rumor into fact; no proposals for flipping-inventory items; nothing requiring reserve/budget raids | inspection: BRIEFING.md · import annotation probe `[R4.1b]` |
| **§4 Intelligence layer** | | |
| R4.1 | Import reads intelligence.json to a ratification queue (thesis, sources, confidence, items); no-source and malformed records rejected; nothing activates unratified | probe `[R4.1]` |
| R4.1b | Flipping-inventory conflicts annotated at import; sleeve refuses those items as candidates | probe `[R4.1b]` |
| R4.1c | Ratify / edit-then-ratify / dismiss, all decision-logged; dismissals absorb same-type variants at Jaccard ≥0.6; resurface only on material change (confidence upgrade / direction flip) | probe `[R4.1c]` |
| R4.2a | Catalyst records create/update calendar entries in place — never duplicates | probe `[R4.2a]` |
| R4.2b | Cluster-membership proposals queue in the cluster ratification queue — membership never recomposes silently | probe `[R4.2b]` |
| R4.2c | Plan lines carry advisory ⚡ tags; never bench or resize by default; optional per-record 0.5× haircut toggle applied after all caps, "sized down: news risk (my ruling)" inline | probe `[R4.2c]` |
| R4.2d | Deflation flags: standing sell-on-drop tag; sleeve refuses those candidates with that reason | probe `[R4.2d]` |
| R4.2e | Wednesday advisory names what's shipping + watchlist/sleeve blast radius; generic line when no intel | UI (day-dependent; formula shared with probe-tested `wedClusterAdvisoryText` pattern) |
| R4.3 | Intel cannot touch: blacklist, proven-loser bench, realized metrics, gate constants, reserve; conflict copy "intel bullish; realized history … — history wins" | probe `[R4.3]` |
| R4.4 | Expiry archives with gradeable outcomes (right/wrong/unclear); BRIEFING SCORECARD hit rate by confidence tier in the weekly review | probe `[R4.4]` |
| **§5 Walk-up integration** | | |
| R5.1 | RULINGS PENDING block after execution items — one line per pending item across all queues with inline ratify/edit/dismiss/snooze 3d | probe `[R5.1]` |
| R5.2 | Cap 5 lines (setting), priority expiring intel → sleeve-actionable → cluster → audits, overflow "+N in queues" | probe `[R5.2]` |
| R5.3 | Staleness ladder: >14d default-action suggestion; >30d auto-dismiss logged "expired unruled — resurface only on material change" | probe `[R5.3]` |
| R5.4 | Header badge with pending count on every tab; no modals, nothing blocks execution | probe `[R5.4]` |
| R5.5 | Briefing reminder: last briefing >7d OR update Wednesday passed since it (Wednesdays assumed — no cheap endpoint); dismissible per occurrence, returns on next trigger; tracker cannot run the briefing itself | probe `[R5.5]` |
| **§7 Manipulation defenses (ruled Aug 10 2026)** | | |
| R7.1 | Source tiers T0–T3 stamped per record (best cited tier); T3 adversarial by default — never the sole basis of a catalyst/thesis; no catalyst record without a T0/T1 primary (enforced at emit AND at import); tiers/communities survive export | probe `[R7.1]` · BRIEFING.md (emit side, inspection) |
| R7.2 | Incentive heuristic: buy rec / price target / urgency framing = PROMOTION, emitted only as a `promotion-warning` record with direction=caution, never buy (tracker coerces regardless) | probe `[R7.2]` · BRIEFING.md (inspection) |
| R7.3 | Pump fingerprint: anomaly flag + promotion record within 14d (setting) → SUSPECTED PUMP; promoted + thin books (gp/day floor, setting) → same regardless of anomaly. Escalation: benched from sleeve candidacy with the reason, seed-style caution cap in the plan (half-size, one slot, wins never graduate it while flagged), posture-is-absence copy on every surface; dismissing the warning lifts it | probe `[R7.3]` |
| R7.4 | Scorecard grades by source tier and named community, not just confidence — "has T3 ever produced a correct, non-priced-in call" is measurable | probe `[R7.4]` |
| R7.5 | Human-layer rule, permanent in BRIEFING.md: never join, monitor, or fetch invite-gated trading discords or paid signal groups; public visibility is a minimum bar, not a guarantee | inspection: BRIEFING.md (a rule about the analyst's own conduct — nothing tracker-side to probe) |
| **§8 Gate-health vocabulary (ruled Aug 10 2026)** | | |
| R8.1 | "die-off detected" ledger tag: a volume-floor bench whose binding side is the confirmed 5m sample (≥5 units, 2 consecutive refreshes) against a 1h book that PASSES the floor writes an extra gate-health ledger row `die-off detected` (item·gate·day dedup, 30d retention, same track-record machinery as any gate); the bench copy names the tag inline | probe `[R8.1]` |
| R8.2 | Each first tag opens a 24h episode ({d,id,t,rec}); recovery = confirmed healthy 5m sample (≥5 units at/above the floor) — a quiet window is NOT recovery; continued fade never stacks episodes; weekly audit reports recovered-within-24h (median hours) vs still-faded-at-24h vs watching, numbers only — the twitchy-vs-trust verdict stays the user's | probe `[R8.2]` |
| R8.3 | Concur-recommended lane: proposals defending a standing rule against softer data batch under their own header and are excluded from S.depProposalCount (rulings-pending); the full ruling flow is reserved for change-proposals | probe `[R8.3]` |
| R8.4 | Proposal-engine discipline (prop-1 rejection): a constant-change proposal emits only with multi-day ledger persistence (item benched by the gate on 4+ of the last 7 days, cited inline); a 3+ near-miss pile without persistence renders as a concur-recommended observation, never a proposal; every change-proposal traces the next allocator stage for each admitted item (an item that relocates to another bench is not created capacity) | probe `[R8.4]` |
| **§9 Quote lines — inventory-mode buffer flow (ruled Aug 10 2026)** | | |
| R9.1 | Sizing transparency: a quote leg smaller than the walk-back (gap + sell-leg replacement) or the holding names the binding cap inline — participation cap / one-third rule / buy limit — in the plan-line reason-string convention; a gap needing multiple cycles says "buffer fills over ~N touches at current flow" | probe `[R9.1]` |
| R9.2 | Placed state: "placed" marks the quoted legs standing on the watch entry — timestamped, persisted (survives export→import validation), elapsed time + fill state shown at every walk-up until the cycle completes or the user presses cleared; stale-buy and sell-aging-ladder rules apply to placed quote legs on the position clocks and surface in the NOW bar | probe `[R9.2]` |
| R9.3 | Partial fills: fill dialog prefills the remaining standing qty (editable) plus price; buy fills add FIFO lots at the fill price, sell fills FIFO-consume at true cost; the line shows "x/y filled · z standing"; a fill completing both legs closes the cycle; the tool never places, cancels, or logs on its own | probe `[R9.3]` |
| R9.4 | Self-cross guard is UNIVERSAL (audit F5, ratified Aug 10 2026): every reprice/undercut/leg proposal checks the user's standing opposite-side orders in EVERY store — positions AND unfilled placed quote legs — both directions, with CANCEL-FIRST copy. Ledger note: the original spec predates this file and was never rowed; the one-directional build was the dropped half of an untracked requirement — this row closes the gap | probe `[R9.4]` (the previously-missing directions asserted explicitly) |
| **§10 Demand-driven briefing cadence (ruled Aug 10 2026)** | | |
| R10.1 | Catalyst-window tightening: any calendar catalyst window live or starting within catWinTightenD (setting, default 14) tightens the staleness threshold from 7d to briefTightStaleD (setting, default 3); reminder copy names the catalyst and the staleness ("catalyst window live (X) — analyst Nd stale"); reverts to weekly automatically when no window qualifies | probe `[R10.1]` |
| R10.2 | Tape-question trigger: a NEW anomaly flag or SUSPECTED PUMP escalation whose first-seen postdates the last briefing import elevates the reminder immediately regardless of staleness, naming the count and lead flag; clears when a briefing imports after the flag's timestamp OR every triggering flag is dismissed/expired (pump: warning-record dismissal/expiry); a flag arriving mid-window re-arms a dismissed occurrence | probe `[R10.2]` |
| R10.3 | One reminder line, never stacked; priority tape-question > catalyst-window > update-Wednesday > weekly staleness; a dismissed occurrence falls through to the next qualifying trigger | probe `[R10.3]` |
| R10.4 | Handshake: flags-pending.json export (every active flag with kind, name, firstSeen ISO, newSinceBrief, detail; exportedAt + lastBriefImportAt stamps); anomaly-flag dismissal silences the reminder only — it NEVER lifts a pump defense (warning-record dismissal remains the only lift path); dismissed state and both cadence settings survive export→import; BRIEFING.md standing rule 9 answers each exported flag by name | probe `[R10.4]` · BRIEFING.md rule 9 (inspection) |
| **§11 Scanner net-widening + discovery slice (ruled Aug 10 2026)** | | |
| R11.1 | T1 scanner defaults: max 8,000 gp, consumables-only OFF (toggle stays); T2 defaults: 5k–250k band, gp-flow floor 1.2m/hr; one-time migration moves stored old-default dials, custom values untouched; ALLOCATOR TIER BANDS UNCHANGED (T1 400–5k, T2 5k–100k) — discovery only; scout admits to the scanner ceiling and stamps the admitting tier | probe `[R11.1]` |
| R11.2 | Rotating discovery slice: 15 random items per scan cycle from the full mapping universe passing basic sanity only (tradeable, price in either tier band, nonzero volume), rendered collapsed ("beyond the net") with full stats; sample rotates with the price cycle (seeded — stable within a cycle); pure exploration — admits, funds, and feeds nothing | probe `[R11.2]` |
| **§12 Closed-loop feedback edges (ruled Aug 10 2026 — attention, never authority)** | | |
| R12.1 | New record types import: `long-catalyst` (requires T0/T1 primary like any catalyst; confidence COERCED to ≤ hinted until officially dated) and `watch-note` (direction always caution) | probe `[R12.1]` |
| R12.2 | Ratified long-catalyst joins the calendar flagged estimate-window; entry-watch screens it like any entry | probe `[R12.2]` |
| R12.3 | Story-resolution feedback: an import naming a flagged item stamps the flag answered (storyId) and archives it with its market signature (story type/confidence, peak-to-flag lag, retrace, nulls when the cache can't say); an import naming it nowhere increments the flag's no-story counter; the flags export carries `noStorySweeps` so the escalation rule (≥1 wider net, ≥2 watch-note) is machine-visible | probe `[R12.3]` |
| R12.4 | flags-pending.json carries `scorecardDigest` (graded outcomes by confidence/source tier/type) and `rulingsDigest` (ratify/dismiss/edit counts by type + recent logged reasons) — the analyst reads its own grades and the user's revealed preferences before sweeping; null until data exists | probe `[R12.4]` |
| R12.5 | Entry-watch DISCOUNTED→RAMPING transition on a calendar item writes a `catalyst-ramping` flag (per cat·item per 30d), exported and tape-question-elevated as a question about the CATALYST ("is the window moving up?") | probe `[R12.5]` |
| R12.6 | Lag profile: explained-flag archive yields median peak-to-flag lag (≥2 data points, else silent); displayed on unexplained flags as attention-only copy ("likely post-peak") — re-ranks nothing, blocks nothing | probe `[R12.6]` |
| **§13 Complexity governance (ruled Aug 10 2026)** | | |
| R13.1 | Walk-up attention budget: distinct-decision count (ruling lines shown + briefing reminder) instrumented per day, trend in the weekly review, and the count stays ≤ 7 under a seeded overload state (cap 5 rulings + 1 reminder = 6 max by construction) | probe `[R13.1]` |
| R13.2 | Feature-touch instrumentation (tabs, buttons, panels — first/last stamps) feeds a 90-day dormancy report in the weekly review; demotions are PROPOSED (collapse behind disclosure, never delete), ratified by the user | probe `[R13.2]` (instrumentation + report math) · UI (demotion is a proposal by construction — no demotion code path exists) |
| **§14 Clusters → catalyst baskets merge (ruled Aug 10 2026)** | | |
| R14.1 | Clusters tab removed; basket ledger, review queue, and coherence view live on the Sleeve tab under the calendar; all cluster machinery (caps, exposure, P&L, candidate queue, dismissal memory, Jaccard absorption) carries over unchanged | probe `[R14.1]` + existing cluster-cap probes now running against the merged layout |
| R14.2 | One object, one name: a basket attached to a calendar catalyst renders as that catalyst's basket (⚡ badge + per-catalyst "basket coherence…" button feeding the coherence panel); a catalyst-less basket renders "standing" (kept for caps + blast radius) | probe `[R14.2]` |
| **§15 Integration-audit fixes (F1–F4 ratified Aug 10 2026 — audits/AUDIT-2026-08-10.md)** | | |
| R15.1 | F1: estimate windows look like estimates everywhere they render — calendar card "est. window" badge, entry-watch runway "(estimate)", briefing reminder "(estimated window)" | probe `[R15.1]` |
| R15.2 | F2: lag profile reads the full signature — dominant story class (type [confidence]) and median retrace ride the note alongside the median lag | probe `[R15.2]` |
| R15.3 | F3: a flag with an active watch-note naming its item renders the link ("escalated to watch-note <id> — posture absence") instead of "escalation due" | probe `[R15.3]` |
| R15.4 | F4: one daily-series cache — `dailyFor` is a 31-day slicing read of the shared 90-day cache (no second fetch, no second invalidation story); chart overlays read it via `dailyView` | probe `[R15.4]` |
| **§16 Second-audit fixes (F6–F8 ratified Aug 10 2026 — audits/AUDIT-2026-08-10b.md; F5 is R9.4)** | | |
| R16.1 | F6: one item, one mandate inside flipping — inventory-mode items bench from the plan ("inventory mode owns this item…"); `committed()` counts unfilled standing quote buy legs at cost (gp standing in an offer is deployed gp); one-time "accounting corrected" decision-log entry when standing legs exist at migration | probe `[R16.1]` |
| R16.2 | F7: `S.depProposalCount` carries its reading's timestamp; the walk-up funnel line states "as of the last Plan refresh (N ago)" instead of posing as live | probe `[R16.2]` |
| R16.3 | F8: a negative funnel residual (stage double-count) renders as a red accounting-error row instead of silently disappearing — an error detector must fire, not hide | probe `[R16.3]` |
| **§17 Coherence-driven membership (ruled Aug 10 2026)** | | |
| R17.1 | ADD proposals: non-members with excess corr > 0.5 vs the basket composite (net of market factor, same construction as the detector) in ≥3 of the last 4 weekly readings — persistence, not snapshots; trend attached; class assigned: THESIS when a story source names the item (analyst record / rare wiki category shared with a member), STATISTICAL on numbers alone | probe `[R17.1]` |
| R17.2 | DROP proposals: members < 0.25 for 4 consecutive readings, decay trend attached; thesis members carry "story may still bind — decorrelation can be temporary"; statistical members drop on the numbers alone; a member's reading excludes itself from the composite it is judged against | probe `[R17.2]` |
| R17.3 | Circularity guard: statistical members ≤ 40% of any basket's membership (withheld candidates said out loud, never silently truncated); > 40% flags "thesis diluted — correlation cluster now, consider renaming or splitting"; the guard's reasoning is stated in the panel copy | probe `[R17.3]` |
| R17.4 | Proposals ride the ratification queue (walk-up ruling lines included) with per-basket:item dismissal memory (the singleton-set Jaccard case); confirm applies membership + class and decision-logs; statistical members count toward exposure caps but are EXCLUDED from entry-watch narratives and laggard logic (`catLinkedIds`); member class renders in ledger chips and the coherence view; `mclass`/`cohLog`/`cohProps` survive export→import | probe `[R17.4]` |
| R17.5 | Resurface rule, dismissed ADDS only (ruled Aug 11 2026 — material change quantified for a scalar signal): dismissal stamps the correlation level; the proposal resurfaces only when corr exceeds that level by ≥ 0.15 held in ≥3 of the last 4 readings, copy stating both numbers ("dismissed at 0.52 — now 0.68 for 3 readings, resurfacing on material change"); sub-threshold drift never re-nags; drops have NO resurface path; the shape generalizes to no other dismissal type without a ruling; `dismissedC`/`resurfaced` survive export→import | probe `[R17.5]` |
| **§18 Shadow book (ruled Aug 11 2026 — touches no capital, no real metric, no plan)** | | |
| R18.1 | Shadow positions per refresh: every single-gate benched near-miss (stamped with the gate and the gap) plus the funded picks under three regime books; entry at instasell + 1 tick (never the midpoint), allocator-sized (participation × horizon, one-third cap); dedup per family while open + 2×-horizon cooldown; open-count cap | probe `[R18.1]` |
| R18.2 | Honest fill simulation: a shadow buy fills only when the tape prints at-or-below its bid while OBSERVED (5m volume-weighted, capture-capped); sell side symmetric; observed-time recorded (unobserved windows are unknown, not unfilled — every verdict carries observed share); buy horizon from open, sell horizon from buy completion, unsold remainder force-exits at the then-current instasell; unfilled expiry recorded as would-not-have-filled | probe `[R18.2]` |
| R18.3 | Weekly per-gate counterfactual: "benched by X alone: shadow-traded N, filled M, net simulated P after tax, vs would-not-have-filled K" with the observed-share caveat; all shadow numbers quarantine-labeled simulated everywhere they render | probe `[R18.3]` |
| R18.4 | Three regimes on the ROI dial (current / one notch loose / one notch tight, other gates unchanged — stated), same fill sim, all three SIMULATED (never comparable to realized P&L, stated); daily divergence ledger (30d); regime curves side by side in the weekly review; persistent divergence (4 of 7 days) is the proposal-engine evidence — never auto-applied | probe `[R18.4]` |
| R18.5 | Fill-participation haircut (setting `shadowPartPct`, default 50%, ruled Aug 11 2026): a shadow leg claims fills up to capture-cap × participation of the tape volume that crossed its price — the conservative leg drives state and net; the 100%-participation reading accrues on the same record (one setting, no new collection) and the ledger reports both ("filled at N% participation / would also fill at 100%") in the cite, the per-gate line, the gate-health stream, and the expand view; exception proposals cite the conservative number; the knob clamps to [10,100]% | probe `[R18.5]` |
| **§19 Gate audit × shadow book unification (ruled Aug 11 2026)** | | |
| R19.1 | Gate-health renders BOTH evidence streams per gate side by side, natures labeled: realized ([real fills] — override stream is the clean evidence, latency stream labeled as latency-only per the definitions protocol) and shadow ([simulated — fill-model caveats]) | probe `[R19.1]` |
| R19.2 | Combined verdict: a too-tight case requires BOTH streams pointing the same direction — realized direction reads ONLY traded-while-benched; agreement → change-proposal citing both streams (4-of-7 persistence and traced consequences still required); disagreement → its own finding ("investigate the fill model or the attribution"), concur-lane, never counted as a change-proposal; incomplete pair → accruing note, no change; the rule applies to regime-divergence proposals identically | probe `[R19.2]` |
| **§20 Surface reorganization (user-directed, Aug 11 2026 — composition change, no new data or mechanisms)** | | |
| R20.1 | Four top-level tabs — Home / Trade / Sleeve / Review; Home is the boot surface (zero clicks) carrying, in order: NOW bar (global, above tabs), RULINGS PENDING digest, WHAT CHANGED delta block, vitals strip, walk-up checklist. Old Watchlist/Scanner/Flip Log fold into Trade sub-views; Routine's walk-up folds into Home, its weekly mode into Review; Shadow Fund panels join Review with their headline in the vitals strip. Nothing deleted — everything is one level down from a surface that earns its position | probe `[R20.1]` |
| R20.2 | Any pending ruling is actionable in 1 click: the digest (same lines, cap, and priority as R5.1–R5.3) renders on Home with its inline ratify/dismiss/snooze controls; the walk-up checklist points at the digest instead of duplicating it | probe `[R20.2]` |
| R20.3 | Vitals strip: funnel headline (pass · funded · % deployed), shadow book one-liner (open · closed · leading regime vs baseline, labeled simulated), sleeve one-liner (positions · nearest rung · nearest catalyst countdown), reserve/budget split — each tile a 1-click click-through to its full surface. The plan read behind the funnel tile keeps the deploy ledger and shadow book riding Home exactly as they rode the old default tab | probe `[R20.3]` |
| R20.4 | The walk-up attention budget (≤7, R13.1) binds on Home — the count and budget line render on the tier-1 surface | probe `[R20.4]` (+ R13.1 unchanged) |
| R20.5 | WHAT CHANGED delta block, since the last visit (2h-quiet-gap visit clock + end-of-visit snapshot): flags newly explained / escalated, shadow trips closed (count + net, simulated), sleeve rungs armed, regime-divergence movement, catalyst window transitions, briefing staleness transitions — one line per nonzero category only, truncated at 6 lines with "+N more" (mobile parity); first tracked visit says so instead of dumping history | probe `[R20.5]` |
| R20.6 | Legacy tab names (watch, scan, log, shadow, routine) alias into the new surfaces in setTab, so every historical data-goto, NOW-bar target, and close→prefill path still lands; the single checklist panel reparents between Home (walk-up) and Review (weekly) with mode following the tab | probe `[R20.6]` |
| R20.7 | Click-depth tier check: featTouch keys used inside 30 days whose surface sits >2 clicks deep render as a named contradiction in the attention report ("usage contradicts the tier assignment — propose a promotion"), clean state says so explicitly; the check ships as machinery because the usage ledger lives client-side | probe `[R20.7]` |
| **§21 Walk-up context awareness (user-directed, Aug 11 2026 — the checklist adapts to operating state)** | | |
| R21.1 | The walk-up checklist is dismissible in one tap for THIS visit (returns automatically next visit, keyed to the visit clock); NOW lines, rulings digest, WHAT CHANGED, and vitals are never part of the collapse — only the guided steps | probe `[R21.1]` |
| R21.2 | Standing minimize preference: one-line summary (title · steps done · reason) with a one-tap expand that is per-view; "pin open" clears the preference | probe `[R21.2]` |
| R21.3 | Auto-minimize without being asked when a visit has zero open positions (standing quote legs count as open book), zero armed rungs, and zero pending rulings — and the NOW bar never points into a walkthrough that isn't rendering | probe `[R21.3]` |
| R21.4 | Review replaces, never stacks: review-due renders as ONE line ("weekly review ready — N days of data", existing 3-day trigger); one tap (or opening Review while due) makes the review the visit's checklist, with the walk-up's execution core absorbed as step zero while a book is live; the NOW bar follows the engaged review via the same effective list; review interaction stamps the visit; no state renders two checklists (single panel by construction, probe-asserted) | probe `[R21.4]` |
| R21.5 | Flat-book posture: WHAT CHANGED and vitals lead the Home surface by construction while flat, and the delta block carries the standing line "book flat since \<date\> — shadow book accruing, N trips closed since" | probe `[R21.5]` |
| R21.6 | The review-ready line is a decision point and counts against the same ≤7 walk-up attention budget instead of hiding from it | probe `[R21.6]` |
| **§22 Interface fixes + tag consolidation (user-directed, Aug 11 2026)** | | |
| R22.1 | First-tap fix, cause stated: every render pass rebuilt identical checklist markup via innerHTML (60s poll + the boot data arrival ~1s after open), so a tap whose pointerdown landed on the just-replaced node never became a click. `setHTML` writes only on content change — unchanged renders keep their DOM nodes; probe asserts node identity across a background re-render and a responding first tap | probe `[R22.1]` |
| R22.2 | Scoping: rulings digest, WHAT CHANGED, and the walk-up checklist render on Home ONLY; the NOW bar and the vitals strip are GLOBAL (vitals moved above the tabs — amends the §20 Home-internal placement); the ⚖ badge stays on every tab as the pointer back to Home. **Regression + re-verification (Aug 11 2026):** an ID-specificity CSS rule (`#tab-home{display:flex}`) overrode the tab gate and rendered Home on every tab while existence-based probes passed — the probe now asserts ABSENCE (computed visibility off-Home on every non-Home tab), and the assertion was verified to fail against the original defect | probe `[R22.2]` (negative assertions) |
| R22.3 | Ruling path: every digest line's ratify/edit/dismiss actions are visible inline (no hover gating), and ruling from Home lands visibly — the line leaves the digest and the ⚖ badge updates in the same interaction (the intel handlers' missing re-render was the composition regression); audit deep-links anchor to the Review checklist panel | probe `[R22.3]` |
| R22.4 | No silent unfunded states: every watchlist row leads with an explicit plan-status badge — funded N / NEXT UP #k (exact reason) / qualifying n/3 / fails N gates (reasons) / benched (reason) / blacklisted / live position / mm cycle / gates pending — with the full reason in the title and the expand view | probe `[R22.4]` |
| R22.5 | Auto-promote: an all-gates-passing NEXT UP item funds automatically at the next recompute when slots or budget free — no press, `planPriority` untouched; the promote button only overrides ORDER (copy states it); allocator caps, budgets, and every gate unchanged | probe `[R22.5]` |
| R22.6 | Beyond-the-net gate tags: the full gate chain runs on every slice item each cycle, tagged in the same language as watchlist rows; missing chart data reads "gates pending chart", never nothing; one-tap +watch on every row | probe `[R22.6]` |
| R22.8 | Minimized checklist is ONE line total: the panel header, section title, progress bar, and attention line all fold in — only the summary row (title · steps done · reason · expand) renders, with the attention reading in its tooltip; active cue lines (elevated briefing reminder, review-ready) are cues, not chrome, and still render when genuinely firing | probe `[R22.8]` |
| R22.7 | Tag consolidation: at most four indicators per row — plan status / merged identity (tier+provenance+proof+·M) / aggregated ⚠ N risk chip (hard-bench flags red, advisory amber; full list in title; tap opens the expand view) / test-state dot; basket chips, intel tags, mm state, provenance detail move to the row's expand view; a filter row reaches every collapsed state; scanner rows adopt the same scheme | probe `[R22.7]` |
| **§23 Findability (user-directed, Aug 11 2026)** | | |
| R23.1 | The ⚖ badge ALWAYS renders on every tab: count > 0 → prominent count, click-through to Home's queue; count = 0 → a subtle inert ghost glyph — an empty state that renders proves the mechanism lives, and absence is never ambiguous between "nothing pending" and "badge broken". Diagnosis recorded: the reported missing badge was the hidden-at-zero design (never orphaned from the header path), probe-tested at nonzero only via direct call and never per-tab | probe `[R23.1]` (both states, every tab) |
| R23.2 | Standing "⭳ flags for analyst" export — reminder-independent: a line in the walk-up (expanded state) and an action in the Review scorecard area, exporting the same flags-pending.json regardless of reminder state; a quiet tape exports an honest empty docket (`note`: "zero flags, confirmed — a deliberate export, not an absent one"); the reminder-line placement stays as the escalation path | probe `[R23.2]` |
| R23.3 | Desk-side flags handoff (kills the Downloads hop): /briefing run-procedure step 0 resolves the Downloads folder via the known-folder registry (never hardcoded), moves the newest `flags-pending*.json` to `briefings/flags-pending.json`, deletes older numbered copies, and logs the outcome in the brief header; a >24h-old export proceeds but is labeled stale in the header and Flags Addressed section; a zero-flags `note` file is reported as a deliberate empty docket | inspection: BRIEFING.md step 0 + SKILL.md (desk side — not tracker-probeable) |
| **§24 Review-tab fixes (user-directed, Aug 11 2026)** | | |
| R24.1 | Friction-note lifecycle: only open (unresolved) notes render; copy-all stamps them `exported` (the FRICTION.md carry mark); per-note "✓ handled" and a one-tap "clear exported" sweep move notes into a collapsed "resolved (N)" disclosure; a clean Sunday reads "No open friction notes" | probe `[R24.1]` |
| R24.2 | Every deep link lands ON its target section — viewport-measured to the R22.2 standard (an anchor that "resolves" to a tab isn't tested until the probe confirms where the viewport ends up): all vitals tiles, NOW-bar gotos (now anchored: positions panel, log form, export chip, checklist), audit links (→ the review's audit step), and the ⚖ click-through (→ the digest, flashed) scroll the target's top into view and flash it (`.landed`); checklist steps carry `ckstep-<key>` anchors; wiring asserted separately from anchors because the original defect was a wrong goto value. **Sticky-chrome offset (amended same day):** `scroll-margin-top` set at scroll time from the measured height of pinned top chrome (header now; the vitals strip joins the measurement the moment it is ever sticky/fixed — it is in normal flow today, so its overlay height is honestly zero), +10px breathing room; the probe asserts the target's first VISIBLE title sits fully inside the viewport AND below the chrome's bottom edge — the prior "top in viewport" passed with the title hidden under the header, which is how the offset shipped green. Detector proven against the offset bug (14 landings fail with the pad removed); verified on desktop (1200×900, 58px chrome) and phone (390×844, 93px chrome) via `PROBE_WINDOW` | probe `[R24.2]` (17 destinations + wiring, both viewports) |
| **§25 Shadow ↔ row integration (user-directed, Aug 11 2026 — route existing evidence to the point of decision; no new collection)** | | |
| R25.1 | Toxic flow joins the ⚠ pool: realized markout breaching the toxicity threshold renders the advisory-amber chip entry ("avg adverse markout X vs margin Y"), on watch rows and on scanner rows for watched items; requires ≥3 marked-out fills — one bad markout is noise | probe `[R25.1]` |
| R25.2 | Shadow dot (dot-class, the four-BADGE rule holds): ≥3 closed shadow trips → solid green (shadow-profitable) / solid red (shadow-losing) / hollow at fill rate <40% ("the margin exists, the fills don't"); tooltip carries trips, fill rate, net, book, observed-share caveat, quarantine label | probe `[R25.2]` |
| R25.3 | The row expand view answers "what would have happened if I'd traded this" in one tap: trips, entry/exit, would-not-have-filled count, observed-time caveats, all labeled simulated | probe `[R25.3]` |
| R25.4 | NEXT-UP and bench lines cite the per-item shadow slice in the same sentence as the gate's verdict — "bench agrees" (net ≤0; "would not even have filled" at zero fills) vs "screening: probation candidate — a live half-size test decides, not the sim" (net >0; reworded from "evidence accruing" by the R27 reweight); the cite quotes both participation readings; rendered on plan bench rows, NEXT-UP lines, and the row-status badge reasons | probe `[R25.4]` |
| R25.5 | Discovery-slice single-gate near-misses shadow-trade (chart-dependent gates are unknowable for slice items and don't count toward the single-gate test), capped at 5 slice positions per cycle to bound the ledger; blacklist-gate items never shadow-trade — in the slice OR the watch near-miss path — because a ledger arguing against the blacklist is the machine building a case against a non-delegable call | probe `[R25.5]` |
| **§26 Shadow-backed exception lane (user-directed, Aug 11 2026 — per-item, evidence-gated, ruled)** | | |
| R26.1 | Evidence bar for a proposal: ≥5 closed shadow trips, fill rate ≥60%, net shadow-profitable, span ≥7 days, AND no red flags (not pump-suspected, not blacklisted, not toxic-flow); the one-tap proposal renders on the bench line and in the rulings digest; a waived/granted item never re-proposes | probe `[R26.1]` |
| R26.2 | Grant is a ruling (one tap, decision-logged): the item funds despite the named gate at probation terms — half size, a 2× tick-floor safety (its own stated bench reason when breached), one position — for 3 real round trips; probation is an explicit seasoning exemption (the ruling is the ratification) | probe `[R26.2]` |
| R26.3 | Adjudication by realized results on TWO named criteria (amended by R27.2): net P&L and adverse-markout %; 3 profitable probation trips with clean markouts → standing per-item waiver (logged, revocable in the row's expand view); losing → revoked, item returns to the bench, divergence recorded ("sim said yes, reality said no") as fill-model calibration data | probe `[R26.3]` `[R27.2]` |
| R26.4 | Cap 2 active exceptions — probes of the gate's edges, not a parallel permission system; at cap no proposal renders and the bench line states why; a waiver or revocation frees the slot. THE GATE ITSELF NEVER MOVES on shadow evidence alone — constants still require the two-stream agreement standard; two lanes, two evidence standards, labeled in every grant | probe `[R26.4]` (+ R19.2 unchanged) |
| **§27 Shadow reweight — screening, not evidence (outside review, ruled Aug 11 2026)** | | |
| R27.1 | Every shadow surface (row dot, expand view, bench/NEXT-UP cites, weekly panel, gate-health stream label, vitals tile, checklist step, DB comments) labels the book a SCREEN ranking probation candidates for live half-size tests — never "evidence accruing"; the weekly shadow panel carries the standing self-impact caveat: "sim assumes a book you're not in — thin books change when you post; screening tool, not evidence" | probe `[R27.1]` |
| R27.2 | The exception lane's realized conversion stays the ONLY path to a waiver; the three-trip adjudication names adverse-markout % as a criterion alongside net P&L (house `markoutX` toxic-flow standard applied to the probation trips' own markouts); profitable-but-markout-toxic REVOKES with the markout stated; untimed trips adjudicate on net alone and the decision log says so | probe `[R27.2]` |
| R27.3 | The regime race keeps running but divergence proposals read "regime X's candidates earned probation slots — a screening rank, not proof the regime is right", never "regime X is right"; exception proposals are a RANKED shortlist (conservative sim net, best first, truncated at free probation slots); the 4-of-7 persistence and two-stream proposal machinery unchanged (gate-proposal copy reframed: realized makes the case, the screen concurs) | probe `[R27.3]` (+ `[R18.4]` `[R19.2]` unchanged) |
| **§28 Scanner shadow cohort (user-directed, Aug 11 2026 — the FILTER question: "are my filters excluding money")** | | |
| R28.1 | Each cycle (scout cadence, self-throttled), per tier scanner: the top 8 gate-passing-or-single-gate-near-miss items NOT on the watchlist shadow-trade with scanner·T1/scanner·T2 provenance, ranked by the allocator's own score (`scanScreenTier` with a loose-ROI screen so ROI near-misses survive); same honest fill sim + participation haircut; blacklist excluded (standing rule) and pump-flagged excluded same as everywhere; chart-dependent gates are unknowable and don't count toward the single-gate test | probe `[R28.1]` |
| R28.2 | Populations separate in every ledger view: the per-gate counterfactual and regime books read watchlist+slice only; the weekly FILTER OPPORTUNITY COST line compares scanner-cohort vs watchlist-cohort net over the same window (since the earliest scanner entry — a young cohort is never compared against 30d of history) and frames outperformance as a PIPELINE finding (needs ≥5 closed scanner trips), screening-labeled | probe `[R28.2]` |
| R28.3 | Bounds: 40 open across ALL cohorts (`SHADOW_MAX_OPEN`); the scanner cohort evicts only its own lowest-score open position, only for a better-scored candidate — watchlist/slice cohorts never evicted; spark fetches ride the scout's existing polite budget (`SCOUT_EVAL_PER_TIER` = 12/cycle, 250ms spacing) — coverage accrues over cycles rather than bursting | probe `[R28.3]` (cap + eviction) · budget is code-fixed (`fetches >= SCOUT_EVAL_PER_TIER`) and network-bound, stated here, not silently dropped |
| R28.4 | One stage per move (honesty carry-over): scanner-cohort trips are excluded from exception-lane evidence (slice and gate counts) — they nominate an item for scout ADMISSION only; ≥3 profitable scanner-cohort trips earn a scout-admission ATTENTION boost (evaluated first, same admission bar, boost stated in the scout log); funnel: scanner shadow → scout admission → watchlist → gates → probation → real size | probe `[R28.4]` (boost set + probation exclusion) · scout evaluation order is code-fixed (boosted-first concat), stated here |
| **§29 Pond prospecting — structural sampling (user-directed, Aug 11 2026)** | | |
| R29.1 | STRATIFIED ROTATION amends R11.2's uniform draw: the 15-item slice draws from ONE named stratum per price cycle, in rotation (equipment by slot, F2P-only, skilling outputs by skill, quest-locked, holiday/rare-adjacent, sub-400gp bulk — the last with its own sub-400 price window), so small strata are sampled on a clock instead of never; the stratum is stamped on every row and on every counterfactual it opens; the rotation is a pure function of the price cycle (stable within it, advancing across it). Every stratum except F2P and the price-band ones is a NAME-PATTERN classification — the mapping API carries no category data — labeled approximate wherever it renders, and it steers sampling only, never a trade | probe `[R29.1]` |
| R29.2 | GAP-BAND SLICE: 5 items per cycle from 250k–1m — the band between the T2 scanner ceiling and the anomaly scan's >1m floor, previously watched by nothing but slice luck — with gate verdicts and shadow eligibility identical to the discovery slice, rendered in the same table under its own stratum stamp | probe `[R29.2]` |
| R29.3 | Per-stratum ledger: distinct items sampled / near-misses shadowed / clean passers / closed trips / simulated net, rendered as the weekly review's §5 Prospecting map ranked by simulated net, each region reading "prospects" / "barren so far" / "too few closed trips to say"; the gap band gets its own verdict line — printing earns it a scanner PROPOSAL, and not printing proves the gap empty rather than assumed | probe `[R29.3]` |
| R29.4 | The ledger is DURABLE: closed slice/gap outcomes roll into it exactly once (a `rolled` stamp, no double counting on later ticks) and survive the shadow book's 30-day pruning, because the quarterly "which pond regions hold edge" map has to outlive the retention window; distinct-item counting is the denominator (a re-draw is not a second test), capped per stratum with the cap stated when reached | probe `[R29.4]` |
| R29.5 | Slice tightening: a slice item clearing ALL gates enters the shadow cohort at FULL eligibility (no bench stamp) — a random draw clearing every gate is the event the slice exists to catch — and surfaces as a WHAT CHANGED line naming the item and its stratum; the weekly discovery section carries the slice hit-rate line (sampled · near-misses shadowed · clean passers · slice-cohort simulated P&L). The ruled 5-per-cycle ledger bound is now SHARED with the gap band (half to the gap, unused allowance flowing back) so neither starves the other and no new budget was minted; blacklist and pump-flagged items are excluded from every cohort | probe `[R29.5]` (+ `[R25.5]` amended) |
| R29.6 | Classifier honesty (user ruling, Aug 11 2026): the per-stratum map carries the standing caveat "N of M strata are name-pattern approximations — directional, not a census" wherever it renders, with N and M DERIVED from the strata table so they cannot drift; and a PROMOTION TRIPWIRE — any per-stratum finding cited in a proposal, a scanner proposal, or a scout priority change carries "Depends on approximate stratum classification — verify before ruling" at the point of decision. The gap band is explicitly exempt and says why: its membership is an exact price range, not a name-pattern guess, so claiming the caveat there would overstate the dependency. Hardening the classifier via wiki-category enrichment is deferred to IMPROVEMENTS.md, conditional on the map ever driving a decision | probe `[R29.6]` |
| **§30 Hours ledger — time-structure prospecting (user-directed, Aug 11 2026)** | | |
| R30.1 | Everything else samples items; this samples HOURS, in two streams whose natures are separated in the copy: MARKET (post-tax spread by hour-of-day, computed from the 168-point hourly series each watched item already fetches — every hour equally represented whether or not the tracker was open, so it is the unbiased stream) and SHADOW (simulated fill rate and net by the hour a counterfactual opened — accrues only while OBSERVING, so it is presence-biased). Hours never observed read "no data", never "bad hour" — the same unobserved-is-unknown doctrine as R18.2. No new requests: the buckets ride the existing timeseries fetch | probe `[R30.1]` |
| R30.2 | The overnight question is answered where both streams can speak: 00:00–06:00 post-tax spread against the all-day average, with shadow corroboration when overnight trips exist and an explicit "unknown, not disproven" when they don't; a wider-overnight reading states the case for sizing the standing overnight book heavier as a PROPOSAL for the user's ruling, never applied from here | probe `[R30.2]` |
| **§31 Crafting-spread monitor (user-directed, Aug 11 2026)** | | |
| R31.1 | Processing basis on 10 high-volume recipes (herb cleaning, unf chains, 3-dose potions, a 4-dose combination potion, a barrows set vs its components), computed every refresh from the /latest bulk fetch (no new requests) on the stated PATIENT convention — output sold at its ask net of GE tax minus inputs bought at their bids. Recipes resolve BY NAME against the live mapping, never hardcoded ids: an unresolvable leg renders "unresolved" rather than a confidently wrong number. Daily ledger, last write of the day wins, 31d, and a recipe's baseline excludes its own current reading | probe `[R31.1]` |
| R31.2 | A basis reaching 2× that recipe's own normal (median absolute basis over ≥5 prior daily readings; below that the row says "baseline accruing" instead of flagging noise) flags a BASIS BREAK with the widest-moving leg named from the same ledger — read two ways in the copy: a flip signal on the mispriced leg and a manipulation tell, since a pump distorts one leg of a recipe before the other. Flags dedupe per recipe per 24h, retain 30d, surface in WHAT CHANGED with the leg that moved, and size/cap nothing | probe `[R31.2]` |
| **§32 Data freshness + timeseries breaker (pre-quiet-period audit, Aug 11 2026)** | | |
| R32.1 | Every background accrual reports when it last accrued, in the weekly review, split by nature: SCHEDULED streams (shadow trips, regime ledger, scanner cohort, stratum samples, gate-health ledger, deploy ledger, recipe basis, hour-of-day, coherence, anomaly scan, walk-up attention) are flagged stale against their own cadence; EVENT-DRIVEN streams (markouts, die-off episodes, basis breaks, accumulation flags) are never marked stale because silence is data there, not a fault. The panel states the load-bearing dependency out loud — accrual requires the app to be open AND visible, since the poll skips while the tab is hidden — so a fortnight of nothing is diagnosable rather than mistaken for a quiet market | probe `[R32.1]` |
| R32.2 | Timeseries circuit breaker: `/timeseries` failures are remembered per item id (no immediate retry) and a run of 8 consecutive failures suspends timeseries fetching for 30 minutes behind a visible banner, serving stale cached series meanwhile; a success resets it. Closes the audit finding that the poll's own backoff counts only bulk-endpoint failures, so a timeseries-specific outage would be re-hit by the scout, the scanner cohort and fillSparks every pass — thousands of rejected requests a day, unattended, with nothing on screen | probe `[R32.2]` |
| **§33 Cold-start integrity (pre-quiet-period audit, Aug 11 2026)** | | |
| R33.1 | A cold, near-empty store (one watched item, no flips, no bank, no history, every ledger empty) renders all four tabs and every review surface without throwing, and no surface renders `NaN`, `undefined`, `[object Object]` or `Infinity` into user-facing copy — the whole tool was built against a populated store, so sparse-data rendering is asserted rather than assumed | probe `[R33.1]` |
| R33.2 | Empty streams say so in words rather than rendering an empty-but-broken panel, and the allocator survives the sparse case (`buildPlan` returns well-formed picks/bench/nextUp with one watch item and no capital) | probe `[R33.2]` |
| **§6 Verification** | | |
| R6.1 | Probe report cross-references this file (`===REQS===` section from `[R#]` assertion tags) | the report itself |
