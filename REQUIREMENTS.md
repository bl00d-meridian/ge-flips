# REQUIREMENTS.md — thesis sleeve + analyst intelligence (consolidated build, Aug 10 2026)

Every requirement from the ruled spec, with a stable ID. The probe suite tags its
assertions with these IDs (`[R1.2]` in the assertion name); the probe report ends with a
`===REQS===` section listing per-ID PASS/FAIL derived from those tags — that section and
this file cross-reference each other. IDs marked **UI** are verified by inspection
(rendering/copy the headless suite exercises only incidentally); everything touching
money math, budgets, or ratification state carries probe tags. No silently dropped
gates: a row in this table without a probe tag says so out loud.

| ID | Requirement | Verified by |
|---|---|---|
| **§0 Recalibration** | | |
| R0.1 | T1 budget 100m→60m, T2 50m→30m; sleeve budget 60m carved from working capital; shadow reserve untouched; decision-log entry "resized to evidence…" | probe `[R0.1]` |
| **§1 Thesis sleeve** | | |
| R1.1 | Sleeve budget disjoint from tier pools and allocator; governs NEW entries only; migrated legacy holds excluded from budget and concurrency count | probe `[R1.1]` `[R1.1a]` |
| R1.1b | Crimson kisten / Elder maul holds migrate into the sleeve as legacy positions with their cost bases | probe `[R1.1a]` |
| R1.1c | Max 3 concurrent new positions (setting) | probe `[R1.1c]` |
| R1.2 | Mandatory fields — no save without item, qty, entry/cost basis, named catalyst + window, one-sentence thesis, exit ladder ≥2 rungs (trigger + fraction each), invalidation | probe `[R1.2]` |
| R1.2b | Ladder edits allowed but decision-logged with required reason | probe `[R1.2b]` |
| R1.3 | Monitoring: price vs entry and vs each rung, days held, catalyst countdown; mark-to-market de-emphasized, "judged at exit, not at the mark"; per-position catalyst-phase buttons, user-flagged only | UI (panel copy) + probe `[R1.4]` (phase→rung path) |
| R1.4 | Rung triggers: price within 5% (setting), date within 7d, or flagged phase → SELL card (qty from fraction, guidance = current instasell); logging the fill records it against the rung; realized P&L at exit vs cost, net of tax | probe `[R1.4]` `[R1.4b]` |
| R1.5 | Weekly SLEEVE section: manual thesis-health light (never inferred), realized exits, sleeve stats fully separate from flipping metrics; rides Export JSON **Amended Aug 11 2026 (R36.1):** the weekly SLEEVE section moved to the Sleeve tab, beneath the positions it describes — the thesis-health lights are only settable there, so it is machinery rather than a report about machinery. The review no longer carries it. | probe `[R1.5]` (separation + export) · UI (weekly section) |
| R1.6 | Guardrails: no sleeve position in current flipping inventory; full budget requires closing a position (no pool/reserve raids); update advisories informational on sleeve items | probe `[R1.6]` (mandate + budget refusal) · UI (Wednesday advisory) |
| **§2 Candidate engine** | | |
| R2.1 | Catalyst calendar (name, window, affected items/clusters); entry watch vs 90-day baseline; accumulation signature; DISCOUNTED vs RAMPING | probe `[R2.1]` |
| R2.2 | Weekly anomaly scan: >1m items, sustained price+volume rise 2–3 weeks, no linked catalyst, "reason unknown — no thesis, no position", max 5 flags/week by strength | probe `[R2.1]` (the scan's verdict math IS `slvSeriesStats`/accum, asserted there); the ≤5 cap and >1m pre-screen are code-fixed (`slice(0,5)`, `px > 1e6`) and network-bound, so not probe-asserted — stated here, not silently dropped · UI (copy) |
| R2.3 | Cluster crossover: catalyst-linked set moves, member lags → laggard flag ("same thesis, unpriced member") | probe `[R2.3]` |
| R2.4 | Ranking = discount depth × catalyst proximity × exit liquidity; warn when intended position > 15% of daily gp volume ("a thesis you can't exit isn't a trade") | probe `[R2.4]` |
| **§3 Analyst briefing (desk side — not probe-testable in the tracker)** | | |
| R3.1 | /briefing skill exists; BRIEFING.md defines sources (official news, wiki upcoming, r/2007scape, dev blogs), politeness, per-claim citation | inspection: BRIEFING.md, .claude/skills/briefing/SKILL.md |
| R3.2 | intelligence.json: typed records with sources, confidence tier, item ids, direction, VALID-UNTIL, contrary evidence, priced-in check; no source no record; dated BRIEF.md with covered range | inspection: BRIEFING.md contract · tracker enforces at import, probe `[R4.1]` |
| R3.3 | Standing rules: never launder rumor into fact; no proposals for flipping-inventory items; nothing requiring reserve/budget raids | inspection: BRIEFING.md · import annotation probe `[R4.1b]` |
| **§4 Intelligence layer** | | |
| R4.1 | Import reads intelligence.json to a ratification queue (thesis, sources, confidence, items); no-source and malformed records rejected; nothing activates unratified | probe `[R4.1]` |
| R4.1b | Flipping-inventory conflicts annotated at import; sleeve refuses those items as candidates | probe `[R4.1b]` |
| R4.1c | Ratify / edit-then-ratify / dismiss, all decision-logged; dismissals absorb same-type variants at Jaccard ≥0.6; resurface only on material change (confidence upgrade / direction flip) | probe `[R4.1c]` |
| R4.2a | Catalyst records create/update calendar entries in place — never duplicates | probe `[R4.2a]` |
| R4.2b | Cluster-membership proposals queue in the cluster ratification queue — membership never recomposes silently **SUPERSEDED Aug 11 2026 (R36.2, user ruling):** coherence membership ADDS now apply automatically at this same evidence bar — membership is mechanical bookkeeping about correlation, not a capital decision, and exposure caps and every gate still bind, so what changed is who presses rather than what qualifies. Coherence DROPS still queue: an add tightens a basket's exposure cap (restraint), a drop loosens it and can free capital the cap was withholding. Whole-basket CANDIDATE ratification (a new basket proposed by the detector) is untouched and still requires a press. | probe `[R4.2b]` |
| R4.2c | Plan lines carry advisory ⚡ tags; never bench or resize by default; optional per-record 0.5× haircut toggle applied after all caps, "sized down: news risk (my ruling)" inline | probe `[R4.2c]` |
| R4.2d | Deflation flags: standing sell-on-drop tag; sleeve refuses those candidates with that reason | probe `[R4.2d]` |
| R4.2e | Wednesday advisory names what's shipping + watchlist/sleeve blast radius; generic line when no intel | UI (day-dependent; formula shared with probe-tested `wedClusterAdvisoryText` pattern) |
| R4.3 | Intel cannot touch: blacklist, proven-loser bench, realized metrics, gate constants, reserve; conflict copy "intel bullish; realized history … — history wins" | probe `[R4.3]` |
| R4.4 | Expiry archives with gradeable outcomes (right/wrong/unclear); BRIEFING SCORECARD hit rate by confidence tier in the weekly review | probe `[R4.4]` |
| **§5 Walk-up integration** | | |
| R5.1 | RULINGS PENDING block after execution items — one line per pending item across all queues with inline ratify/edit/dismiss/snooze 3d | probe `[R5.1]` |
| R5.2 | Cap 5 lines (setting), priority expiring intel → sleeve-actionable → cluster → audits, overflow "+N in queues" | probe `[R5.2]` |
| R5.3 | Staleness ladder: >14d default-action suggestion; >30d auto-dismiss logged "expired unruled — resurface only on material change" | probe `[R5.3]` |
| R5.4 | Header badge with pending count on every tab; no modals, nothing blocks execution | probe `[R5.4]` |
| R5.5 | Briefing reminder: last briefing >7d OR update Wednesday passed since it (Wednesdays assumed — no cheap endpoint); dismissible per occurrence, returns on next trigger; tracker cannot run the briefing itself | probe `[R5.5]` |
| **§7 Manipulation defenses (ruled Aug 10 2026)** | | |
| R7.1 | Source tiers T0–T3 stamped per record (best cited tier); T3 adversarial by default — never the sole basis of a catalyst/thesis; no catalyst record without a T0/T1 primary (enforced at emit AND at import); tiers/communities survive export | probe `[R7.1]` · BRIEFING.md (emit side, inspection) |
| R7.2 | Incentive heuristic: buy rec / price target / urgency framing = PROMOTION, emitted only as a `promotion-warning` record with direction=caution, never buy (tracker coerces regardless) | probe `[R7.2]` · BRIEFING.md (inspection) |
| R7.3 | Pump fingerprint: anomaly flag + promotion record within 14d (setting) → SUSPECTED PUMP; promoted + thin books (gp/day floor, setting) → same regardless of anomaly. Escalation: benched from sleeve candidacy with the reason, seed-style caution cap in the plan (half-size, one slot, wins never graduate it while flagged), posture-is-absence copy on every surface; dismissing the warning lifts it | probe `[R7.3]` |
| R7.4 | Scorecard grades by source tier and named community, not just confidence — "has T3 ever produced a correct, non-priced-in call" is measurable | probe `[R7.4]` |
| R7.5 | Human-layer rule, permanent in BRIEFING.md: never join, monitor, or fetch invite-gated trading discords or paid signal groups; public visibility is a minimum bar, not a guarantee | inspection: BRIEFING.md (a rule about the analyst's own conduct — nothing tracker-side to probe) |
| **§8 Gate-health vocabulary (ruled Aug 10 2026)** | | |
| R8.1 | "die-off detected" ledger tag: a volume-floor bench whose binding side is the confirmed 5m sample (≥5 units, 2 consecutive refreshes) against a 1h book that PASSES the floor writes an extra gate-health ledger row `die-off detected` (item·gate·day dedup, 30d retention, same track-record machinery as any gate); the bench copy names the tag inline | probe `[R8.1]` |
| R8.2 | Each first tag opens a 24h episode ({d,id,t,rec}); recovery = confirmed healthy 5m sample (≥5 units at/above the floor) — a quiet window is NOT recovery; continued fade never stacks episodes; weekly audit reports recovered-within-24h (median hours) vs still-faded-at-24h vs watching, numbers only — the twitchy-vs-trust verdict stays the user's | probe `[R8.2]` |
| R8.3 | Concur-recommended lane: proposals defending a standing rule against softer data batch under their own header and are excluded from S.depProposalCount (rulings-pending); the full ruling flow is reserved for change-proposals | probe `[R8.3]` |
| R8.4 | Proposal-engine discipline (prop-1 rejection): a constant-change proposal emits only with multi-day ledger persistence (item benched by the gate on 4+ of the last 7 days, cited inline); a 3+ near-miss pile without persistence renders as a concur-recommended observation, never a proposal; every change-proposal traces the next allocator stage for each admitted item (an item that relocates to another bench is not created capacity) | probe `[R8.4]` |
| **§9 Quote lines — inventory-mode buffer flow (ruled Aug 10 2026)** | | |
| R9.1 | Sizing transparency: a quote leg smaller than the walk-back (gap + sell-leg replacement) or the holding names the binding cap inline — participation cap / one-third rule / buy limit — in the plan-line reason-string convention; a gap needing multiple cycles says "buffer fills over ~N touches at current flow" | probe `[R9.1]` |
| R9.2 | Placed state: "placed" marks the quoted legs standing on the watch entry — timestamped, persisted (survives export→import validation), elapsed time + fill state shown at every walk-up until the cycle completes or the user presses cleared; stale-buy and sell-aging-ladder rules apply to placed quote legs on the position clocks and surface in the NOW bar | probe `[R9.2]` |
| R9.3 | Partial fills: fill dialog prefills the remaining standing qty (editable) plus price; buy fills add FIFO lots at the fill price, sell fills FIFO-consume at true cost; the line shows "x/y filled · z standing"; a fill completing both legs closes the cycle; the tool never places, cancels, or logs on its own | probe `[R9.3]` |
| R9.4 | Self-cross guard is UNIVERSAL (audit F5, ratified Aug 10 2026): every reprice/undercut/leg proposal checks the user's standing opposite-side orders in EVERY store — positions AND unfilled placed quote legs — both directions, with CANCEL-FIRST copy. Ledger note: the original spec predates this file and was never rowed; the one-directional build was the dropped half of an untracked requirement — this row closes the gap | probe `[R9.4]` (the previously-missing directions asserted explicitly) |
| **§10 Demand-driven briefing cadence (ruled Aug 10 2026)** | | |
| R10.1 | Catalyst-window tightening: any calendar catalyst window live or starting within catWinTightenD (setting, default 14) tightens the staleness threshold from 7d to briefTightStaleD (setting, default 3); reminder copy names the catalyst and the staleness ("catalyst window live (X) — analyst Nd stale"); reverts to weekly automatically when no window qualifies | probe `[R10.1]` |
| R10.2 | Tape-question trigger: a NEW anomaly flag or SUSPECTED PUMP escalation whose first-seen postdates the last briefing import elevates the reminder immediately regardless of staleness, naming the count and lead flag; clears when a briefing imports after the flag's timestamp OR every triggering flag is dismissed/expired (pump: warning-record dismissal/expiry); a flag arriving mid-window re-arms a dismissed occurrence | probe `[R10.2]` |
| R10.3 | One reminder line, never stacked; priority tape-question > catalyst-window > update-Wednesday > weekly staleness; a dismissed occurrence falls through to the next qualifying trigger | probe `[R10.3]` |
| R10.4 | Handshake: flags-pending.json export (every active flag with kind, name, firstSeen ISO, newSinceBrief, detail; exportedAt + lastBriefImportAt stamps); anomaly-flag dismissal silences the reminder only — it NEVER lifts a pump defense (warning-record dismissal remains the only lift path); dismissed state and both cadence settings survive export→import; BRIEFING.md standing rule 9 answers each exported flag by name | probe `[R10.4]` · BRIEFING.md rule 9 (inspection) |
| **§11 Scanner net-widening + discovery slice (ruled Aug 10 2026)** | | |
| R11.1 | T1 scanner defaults: max 8,000 gp, consumables-only OFF (toggle stays); T2 defaults: 5k–250k band, gp-flow floor 1.2m/hr; one-time migration moves stored old-default dials, custom values untouched; ALLOCATOR TIER BANDS UNCHANGED (T1 400–5k, T2 5k–100k) — discovery only; scout admits to the scanner ceiling and stamps the admitting tier | probe `[R11.1]` |
| R11.2 | Rotating discovery slice: 15 random items per scan cycle from the full mapping universe passing basic sanity only (tradeable, price in either tier band, nonzero volume), rendered collapsed ("beyond the net") with full stats; sample rotates with the price cycle (seeded — stable within a cycle); pure exploration — admits, funds, and feeds nothing | probe `[R11.2]` |
| **§12 Closed-loop feedback edges (ruled Aug 10 2026 — attention, never authority)** | | |
| R12.1 | New record types import: `long-catalyst` (requires T0/T1 primary like any catalyst; confidence COERCED to ≤ hinted until officially dated) and `watch-note` (direction always caution) | probe `[R12.1]` |
| R12.2 | Ratified long-catalyst joins the calendar flagged estimate-window; entry-watch screens it like any entry | probe `[R12.2]` |
| R12.3 | Story-resolution feedback: an import naming a flagged item stamps the flag answered (storyId) and archives it with its market signature (story type/confidence, peak-to-flag lag, retrace, nulls when the cache can't say); an import naming it nowhere increments the flag's no-story counter; the flags export carries `noStorySweeps` so the escalation rule (≥1 wider net, ≥2 watch-note) is machine-visible | probe `[R12.3]` |
| R12.4 | flags-pending.json carries `scorecardDigest` (graded outcomes by confidence/source tier/type) and `rulingsDigest` (ratify/dismiss/edit counts by type + recent logged reasons) — the analyst reads its own grades and the user's revealed preferences before sweeping; null until data exists | probe `[R12.4]` |
| R12.5 | Entry-watch DISCOUNTED→RAMPING transition on a calendar item writes a `catalyst-ramping` flag (per cat·item per 30d), exported and tape-question-elevated as a question about the CATALYST ("is the window moving up?") | probe `[R12.5]` |
| R12.6 | Lag profile: explained-flag archive yields median peak-to-flag lag (≥2 data points, else silent); displayed on unexplained flags as attention-only copy ("likely post-peak") — re-ranks nothing, blocks nothing | probe `[R12.6]` |
| **§13 Complexity governance (ruled Aug 10 2026)** | | |
| R13.1 | Walk-up attention budget: distinct-decision count (ruling lines shown + briefing reminder) instrumented per day, trend in the weekly review, and the count stays ≤ 7 under a seeded overload state (cap 5 rulings + 1 reminder = 6 max by construction) | probe `[R13.1]` |
| R13.2 | Feature-touch instrumentation (tabs, buttons, panels — first/last stamps) feeds a 90-day dormancy report in the weekly review; demotions are PROPOSED (collapse behind disclosure, never delete), ratified by the user **Amended Aug 11 2026 (R36.1, user ruling):** DEMOTED from a standing review section to an on-demand button on the Review tab. Not deleted — it is the mechanism that catches surfaces that stopped being used, which is why several cut sections had gone unread; but a report that renders every visit becomes the wallpaper it exists to detect. Asserted both ways: absent from the review's standing steps, produced by its own press, and surviving a re-render. | probe `[R13.2]` (instrumentation + report math) · UI (demotion is a proposal by construction — no demotion code path exists) |
| **§14 Clusters → catalyst baskets merge (ruled Aug 10 2026)** | | |
| R14.1 | Clusters tab removed; basket ledger, review queue, and coherence view live on the Sleeve tab under the calendar; all cluster machinery (caps, exposure, P&L, candidate queue, dismissal memory, Jaccard absorption) carries over unchanged | probe `[R14.1]` + existing cluster-cap probes now running against the merged layout |
| R14.2 | One object, one name: a basket attached to a calendar catalyst renders as that catalyst's basket (⚡ badge + per-catalyst "basket coherence…" button feeding the coherence panel); a catalyst-less basket renders "standing" (kept for caps + blast radius) | probe `[R14.2]` |
| **§15 Integration-audit fixes (F1–F4 ratified Aug 10 2026 — audits/AUDIT-2026-08-10.md)** | | |
| R15.1 | F1: estimate windows look like estimates everywhere they render — calendar card "est. window" badge, entry-watch runway "(estimate)", briefing reminder "(estimated window)" | probe `[R15.1]` |
| R15.2 | F2: lag profile reads the full signature — dominant story class (type [confidence]) and median retrace ride the note alongside the median lag | probe `[R15.2]` |
| R15.3 | F3: a flag with an active watch-note naming its item renders the link ("escalated to watch-note <id> — posture absence") instead of "escalation due" | probe `[R15.3]` |
| R15.4 | F4: one daily-series cache — `dailyFor` is a 31-day slicing read of the shared 90-day cache (no second fetch, no second invalidation story); chart overlays read it via `dailyView` | probe `[R15.4]` |
| **§16 Second-audit fixes (F6–F8 ratified Aug 10 2026 — audits/AUDIT-2026-08-10b.md; F5 is R9.4)** | | |
| R16.1 | F6: one item, one mandate inside flipping — inventory-mode items bench from the plan ("inventory mode owns this item…"); `committed()` counts unfilled standing quote buy legs at cost (gp standing in an offer is deployed gp); one-time "accounting corrected" decision-log entry when standing legs exist at migration | probe `[R16.1]` |
| R16.2 | F7: `S.depProposalCount` carries its reading's timestamp; the walk-up funnel line states "as of the last Plan refresh (N ago)" instead of posing as live | probe `[R16.2]` |
| R16.3 | F8: a negative funnel residual (stage double-count) renders as a red accounting-error row instead of silently disappearing — an error detector must fire, not hide | probe `[R16.3]` |
| **§17 Coherence-driven membership (ruled Aug 10 2026)** | | |
| R17.1 | ADD proposals: non-members with excess corr > 0.5 vs the basket composite (net of market factor, same construction as the detector) in ≥3 of the last 4 weekly readings — persistence, not snapshots; trend attached; class assigned: THESIS when a story source names the item (analyst record / rare wiki category shared with a member), STATISTICAL on numbers alone | probe `[R17.1]` |
| R17.2 | DROP proposals: members < 0.25 for 4 consecutive readings, decay trend attached; thesis members carry "story may still bind — decorrelation can be temporary"; statistical members drop on the numbers alone; a member's reading excludes itself from the composite it is judged against | probe `[R17.2]` |
| R17.3 | Circularity guard: statistical members ≤ 40% of any basket's membership (withheld candidates said out loud, never silently truncated); > 40% flags "thesis diluted — correlation cluster now, consider renaming or splitting"; the guard's reasoning is stated in the panel copy | probe `[R17.3]` |
| R17.4 | Proposals ride the ratification queue (walk-up ruling lines included) with per-basket:item dismissal memory (the singleton-set Jaccard case); confirm applies membership + class and decision-logs; statistical members count toward exposure caps but are EXCLUDED from entry-watch narratives and laggard logic (`catLinkedIds`); member class renders in ledger chips and the coherence view; `mclass`/`cohLog`/`cohProps` survive export→import | probe `[R17.4]` |
| R17.5 | Resurface rule, dismissed ADDS only (ruled Aug 11 2026 — material change quantified for a scalar signal): dismissal stamps the correlation level; the proposal resurfaces only when corr exceeds that level by ≥ 0.15 held in ≥3 of the last 4 readings, copy stating both numbers ("dismissed at 0.52 — now 0.68 for 3 readings, resurfacing on material change"); sub-threshold drift never re-nags; drops have NO resurface path; the shape generalizes to no other dismissal type without a ruling; `dismissedC`/`resurfaced` survive export→import | probe `[R17.5]` |
| **§18 Paper book (ruled Aug 11 2026 — touches no capital, no real metric, no plan)** | | |
| R18.1 | Shadow positions per refresh: every single-gate benched near-miss (stamped with the gate and the gap) plus the funded picks under three regime books; entry at instasell + 1 tick (never the midpoint), allocator-sized (participation × horizon, one-third cap); dedup per family while open + 2×-horizon cooldown; open-count cap | probe `[R18.1]` |
| R18.2 | **The capture-capped term is asserted BEFORE the qty clamp** (Aug 13 2026). `shadowCredit` stores `Math.min(qty, …)`, so once the term exceeds a trip's own size the clamp absorbs it entirely — every prior assertion read the stored value (`buyQ === 10` where qty IS 10), which can see UNDER-crediting and is structurally blind to OVER-crediting, the direction that would flatter every fill rate in the book and the one the model's conservatism claim rests on. Asserted at a volume 1,500× the trip size: the term uncapped, the ambiguous reading haircut from the TERM rather than the clamped size, the clamp itself holding at qty, and credit LINEAR in tape volume — a property assertion rather than a probe-side recomputation. Honest fill simulation: a shadow buy fills only when the tape prints at-or-below its bid while OBSERVED (5m volume-weighted, capture-capped); sell side symmetric; observed-time recorded (unobserved windows are unknown, not unfilled — every verdict carries observed share); buy horizon from open, sell horizon from buy completion, unsold remainder force-exits at the then-current instasell; unfilled expiry recorded as would-not-have-filled | probe `[R18.2]` |
| R18.3 | Weekly per-gate counterfactual: "benched by X alone: paper-traded N, filled M, net simulated P after tax, vs would-not-have-filled K" with the observed-share caveat; all shadow numbers quarantine-labeled simulated everywhere they render **Amended Aug 11 2026 (R35.3):** the per-gate readout moved from the weekly paragraph to the Paper Book sub-view as a TABLE whose every cell opens to its trips; the ruled content (would-have-filled / would-not, both participation readings, the observed-share caveat) is unchanged and still asserted. | probe `[R18.3]` |
| R18.4 | Three regimes on the ROI dial (current / one notch loose / one notch tight, other gates unchanged — stated), same fill sim, all three SIMULATED (never comparable to realized P&L, stated); daily divergence ledger (30d); regime curves side by side in the weekly review; persistent divergence (4 of 7 days) is the proposal-engine evidence — never auto-applied | probe `[R18.4]` |
| R18.5 | Fill-participation haircut (setting `shadowPartPct`, default 50%, ruled Aug 11 2026): a shadow leg claims fills up to capture-cap × participation of the tape volume that crossed its price — the conservative leg drives state and net; the 100%-participation reading accrues on the same record (one setting, no new collection) and the ledger reports both ("filled at N% participation / would also fill at 100%") in the cite, the per-gate line, the gate-health stream, and the expand view; exception proposals cite the conservative number; the knob clamps to [10,100]% | probe `[R18.5]` |
| **§19 Gate audit × paper book unification (ruled Aug 11 2026)** | | |
| R19.1 | Gate-health renders BOTH evidence streams per gate side by side, natures labeled: realized ([real fills] — override stream is the clean evidence, latency stream labeled as latency-only per the definitions protocol) and shadow ([simulated — fill-model caveats]) **Amended Aug 11 2026 (R35.5):** this tree moved from the review's audit Â§4 to the Gate Health sub-view, where each stream opens to its items and outcomes; the simulated stream's label reads "paper" per the Aug 11 rename. The review keeps a one-line read linking through. | probe `[R19.1]` |
| R19.2 | Combined verdict: a too-tight case requires BOTH streams pointing the same direction — realized direction reads ONLY traded-while-benched; agreement → change-proposal citing both streams (4-of-7 persistence and traced consequences still required); disagreement → its own finding ("investigate the fill model or the attribution"), concur-lane, never counted as a change-proposal; incomplete pair → accruing note, no change; the rule applies to regime-divergence proposals identically | probe `[R19.2]` |
| **§20 Surface reorganization (user-directed, Aug 11 2026 — composition change, no new data or mechanisms)** | | |
| R20.1 | Four top-level tabs — Home / Trade / Sleeve / Review; Home is the boot surface (zero clicks) carrying, in order: NOW bar (global, above tabs), RULINGS PENDING digest, WHAT CHANGED delta block, vitals strip, walk-up checklist. Old Watchlist/Scanner/Flip Log fold into Trade sub-views; Routine's walk-up folds into Home, its weekly mode into Review; Shadow Fund panels join Review with their headline in the vitals strip. Nothing deleted — everything is one level down from a surface that earns its position | probe `[R20.1]` |
| R20.2 | Any pending ruling is actionable in 1 click: the digest (same lines, cap, and priority as R5.1–R5.3) renders on Home with its inline ratify/dismiss/snooze controls; the walk-up checklist points at the digest instead of duplicating it | probe `[R20.2]` |
| R20.3 | Vitals strip: funnel headline (pass · funded · % deployed), paper book one-liner (open · closed · leading regime vs baseline, labeled simulated), sleeve one-liner (positions · nearest rung · nearest catalyst countdown), reserve/budget split — each tile a 1-click click-through to its full surface. The plan read behind the funnel tile keeps the deploy ledger and paper book riding Home exactly as they rode the old default tab | probe `[R20.3]` |
| R20.4 | The walk-up attention budget (≤7, R13.1) binds on Home — the count and budget line render on the tier-1 surface | probe `[R20.4]` (+ R13.1 unchanged) |
| R20.5 | WHAT CHANGED delta block, since the last visit (2h-quiet-gap visit clock + end-of-visit snapshot): flags newly explained / escalated, paper trips closed (count + net, simulated), sleeve rungs armed, regime-divergence movement, catalyst window transitions, briefing staleness transitions — one line per nonzero category only, truncated at 6 lines with "+N more" (mobile parity); first tracked visit says so instead of dumping history | probe `[R20.5]` |
| R20.6 | Legacy tab names (watch, scan, log, shadow, routine) alias into the new surfaces in setTab, so every historical data-goto, NOW-bar target, and close→prefill path still lands; the single checklist panel reparents between Home (walk-up) and Review (weekly) with mode following the tab | probe `[R20.6]` |
| R20.7 | Click-depth tier check: featTouch keys used inside 30 days whose surface sits >2 clicks deep render as a named contradiction in the attention report ("usage contradicts the tier assignment — propose a promotion"), clean state says so explicitly; the check ships as machinery because the usage ledger lives client-side | probe `[R20.7]` |
| **§21 Walk-up context awareness (user-directed, Aug 11 2026 — the checklist adapts to operating state)** | | |
| R21.1 | The walk-up checklist is dismissible in one tap for THIS visit (returns automatically next visit, keyed to the visit clock); NOW lines, rulings digest, WHAT CHANGED, and vitals are never part of the collapse — only the guided steps | probe `[R21.1]` |
| R21.2 | Standing minimize preference: one-line summary (title · steps done · reason) with a one-tap expand that is per-view; "pin open" clears the preference | probe `[R21.2]` |
| R21.3 | Auto-minimize without being asked when a visit has zero open positions (standing quote legs count as open book), zero armed rungs, and zero pending rulings — and the NOW bar never points into a walkthrough that isn't rendering | probe `[R21.3]` |
| R21.4 | Review replaces, never stacks: review-due renders as ONE line ("weekly review ready — N days of data", existing 3-day trigger); one tap (or opening Review while due) makes the review the visit's checklist, with the walk-up's execution core absorbed as step zero while a book is live; the NOW bar follows the engaged review via the same effective list; review interaction stamps the visit; no state renders two checklists (single panel by construction, probe-asserted) | probe `[R21.4]` |
| R21.5 | Flat-book posture: WHAT CHANGED and vitals lead the Home surface by construction while flat, and the delta block carries the standing line "book flat since \<date\> — paper book accruing, N trips closed since" | probe `[R21.5]` |
| R21.6 | The review-ready line is a decision point and counts against the same ≤7 walk-up attention budget instead of hiding from it | probe `[R21.6]` |
| **§22 Interface fixes + tag consolidation (user-directed, Aug 11 2026)** | | |
| R22.1 | First-tap fix, cause stated: every render pass rebuilt identical checklist markup via innerHTML (60s poll + the boot data arrival ~1s after open), so a tap whose pointerdown landed on the just-replaced node never became a click. `setHTML` writes only on content change — unchanged renders keep their DOM nodes; probe asserts node identity across a background re-render and a responding first tap | probe `[R22.1]` |
| R22.2 | Scoping: rulings digest, WHAT CHANGED, and the walk-up checklist render on Home ONLY; the NOW bar and the vitals strip are GLOBAL (vitals moved above the tabs — amends the §20 Home-internal placement); the ⚖ badge stays on every tab as the pointer back to Home. **Regression + re-verification (Aug 11 2026):** an ID-specificity CSS rule (`#tab-home{display:flex}`) overrode the tab gate and rendered Home on every tab while existence-based probes passed — the probe now asserts ABSENCE (computed visibility off-Home on every non-Home tab), and the assertion was verified to fail against the original defect | probe `[R22.2]` (negative assertions) |
| R22.3 | Ruling path: every digest line's ratify/edit/dismiss actions are visible inline (no hover gating), and ruling from Home lands visibly — the line leaves the digest and the ⚖ badge updates in the same interaction (the intel handlers' missing re-render was the composition regression); audit deep-links anchor to the Review checklist panel | probe `[R22.3]` |
| R22.4 | No silent unfunded states: every watchlist row leads with an explicit plan-status badge — funded N / NEXT UP #k (exact reason) / qualifying n/3 / fails N gates (reasons) / benched (reason) / blacklisted / live position / mm cycle / gates pending — with the full reason in the title and the expand view | probe `[R22.4]` |
| R22.5 | Auto-promote: an all-gates-passing NEXT UP item funds automatically at the next recompute when slots or budget free — no press, `planPriority` untouched; the promote button only overrides ORDER (copy states it); allocator caps, budgets, and every gate unchanged | probe `[R22.5]` |
| R22.6 | Beyond-the-net gate tags: the full gate chain runs on every slice item each cycle, tagged in the same language as watchlist rows; missing chart data reads "gates pending chart", never nothing; one-tap +watch on every row | probe `[R22.6]` |
| R22.8 | Minimized checklist is ONE line total: the panel header, section title, progress bar, and attention line all fold in — only the summary row (title · steps done · reason · expand) renders, with the attention reading in its tooltip; active cue lines (elevated briefing reminder, review-ready) are cues, not chrome, and still render when genuinely firing | probe `[R22.8]` |
| R22.7 | Tag consolidation: at most four indicators per row — plan status / merged identity (tier+provenance+proof+·M) / aggregated ⚠ N risk chip (hard-bench flags red, advisory amber; full list in title; tap opens the expand view) / test-state dot; basket chips, intel tags, mm state, provenance detail move to the row's expand view; a filter row reaches every collapsed state; scanner rows adopt the same scheme | probe `[R22.7]` |
| **§23 Findability (user-directed, Aug 11 2026)** | | |
| R23.1 | The ⚖ badge ALWAYS renders on every tab: count > 0 → prominent count, click-through to Home's queue; count = 0 → a subtle inert ghost glyph — an empty state that renders proves the mechanism lives, and absence is never ambiguous between "nothing pending" and "badge broken". Diagnosis recorded: the reported missing badge was the hidden-at-zero design (never orphaned from the header path), probe-tested at nonzero only via direct call and never per-tab | probe `[R23.1]` (both states, every tab) |
| R23.2 | Standing "⭳ flags for analyst" export — reminder-independent: a line in the walk-up (expanded state) and an action in the Review scorecard area, exporting the same flags-pending.json regardless of reminder state; a quiet tape exports an honest empty docket (`note`: "zero flags, confirmed — a deliberate export, not an absent one"); the reminder-line placement stays as the escalation path | probe `[R23.2]` |
| R23.3 | Desk-side flags handoff (kills the Downloads hop): /briefing run-procedure step 0 resolves the Downloads folder via the known-folder registry (never hardcoded), moves the newest `flags-pending*.json` to `briefings/flags-pending.json`, deletes older numbered copies, and logs the outcome in the brief header; a >24h-old export proceeds but is labeled stale in the header and Flags Addressed section; a zero-flags `note` file is reported as a deliberate empty docket | inspection: BRIEFING.md step 0 + SKILL.md (desk side — not tracker-probeable) |
| **§24 Review-tab fixes (user-directed, Aug 11 2026)** | | |
| R24.1 | Friction-note lifecycle: only open (unresolved) notes render; copy-all stamps them `exported` (the FRICTION.md carry mark); per-note "✓ handled" and a one-tap "clear exported" sweep move notes into a collapsed "resolved (N)" disclosure; a clean Sunday reads "No open friction notes" | probe `[R24.1]` |
| R24.2 | Every deep link lands ON its target section — viewport-measured to the R22.2 standard (an anchor that "resolves" to a tab isn't tested until the probe confirms where the viewport ends up): all vitals tiles, NOW-bar gotos (now anchored: positions panel, log form, export chip, checklist), audit links (→ the review's audit step), and the ⚖ click-through (→ the digest, flashed) scroll the target's top into view and flash it (`.landed`); checklist steps carry `ckstep-<key>` anchors; wiring asserted separately from anchors because the original defect was a wrong goto value. **Sticky-chrome offset (amended same day):** `scroll-margin-top` set at scroll time from the measured height of pinned top chrome (header now; the vitals strip joins the measurement the moment it is ever sticky/fixed — it is in normal flow today, so its overlay height is honestly zero), +10px breathing room; the probe asserts the target's first VISIBLE title sits fully inside the viewport AND below the chrome's bottom edge — the prior "top in viewport" passed with the title hidden under the header, which is how the offset shipped green. Detector proven against the offset bug (14 landings fail with the pad removed); verified on desktop (1200×900, 58px chrome) and phone (390×844, 93px chrome) via `PROBE_WINDOW` | probe `[R24.2]` (17 destinations + wiring, both viewports) |
| **§25 Paper ↔ row integration (user-directed, Aug 11 2026 — route existing evidence to the point of decision; no new collection)** | | |
| R25.1 | Toxic flow joins the ⚠ pool: realized markout breaching the toxicity threshold renders the advisory-amber chip entry ("avg adverse markout X vs margin Y"), on watch rows and on scanner rows for watched items; requires ≥3 marked-out fills — one bad markout is noise | probe `[R25.1]` |
| R25.2 | Shadow dot (dot-class, the four-BADGE rule holds): ≥3 closed paper trips → solid green (paper-profitable) / solid red (paper-losing) / hollow at fill rate <40% ("the margin exists, the fills don't"); tooltip carries trips, fill rate, net, book, observed-share caveat, quarantine label | probe `[R25.2]` |
| R25.3 | The row expand view answers "what would have happened if I'd traded this" in one tap: trips, entry/exit, would-not-have-filled count, observed-time caveats, all labeled simulated | probe `[R25.3]` |
| R25.4 | NEXT-UP and bench lines cite the per-item shadow slice in the same sentence as the gate's verdict — "bench agrees" (net ≤0; "would not even have filled" at zero fills) vs "screening: probation candidate — a live half-size test decides, not the sim" (net >0; reworded from "evidence accruing" by the R27 reweight); the cite quotes both participation readings; rendered on plan bench rows, NEXT-UP lines, and the row-status badge reasons | probe `[R25.4]` |
| R25.5 | Discovery-slice single-gate near-misses shadow-trade (chart-dependent gates are unknowable for slice items and don't count toward the single-gate test), capped at 5 slice positions per cycle to bound the ledger; blacklist-gate items never shadow-trade — in the slice OR the watch near-miss path — because a ledger arguing against the blacklist is the machine building a case against a non-delegable call | probe `[R25.5]` |
| R25.6 | ANY paper activity renders a dot (user ruling, Aug 11 2026): below the 3-closed-trip verdict threshold the row shows a dim accruing state naming the trip counts — open trips included, since open activity is activity — rather than nothing. A row with paper history that renders nothing is the F18 silent-state defect in a new place. The dot leads the dot slot (primary), and an item with no history renders its reserved slot with no dot in it. Asserted on COMPUTED VISIBILITY, not DOM presence, because the defect class here is a surface that exists and shows nothing | probe `[R25.6]` |
| R25.7 | The two dot families must be unmistakable at a glance and on a phone, since confusing them is a decision error (user ruling, Aug 11 2026): SHAPE carries identity — every paper state is a circle-within-a-circle (◉ verdict, ◎ fills-missing, ◌ accruing), the test dot stays a plain ●/○ — and colour carries state only WITHIN an identity, so green/red belong to the paper family (genuinely good-vs-bad) while test state moves to neutral steel (verified-vs-stale is not). Fixed slots: each dot always emits its slot, so a missing dot leaves its space rather than letting its neighbour slide into it. Tooltips name themselves first ("Paper screen: …" / "Margin test: …"), and a legend in the watchlist header shows both glyph families and all their states side by side. The four-indicator budget counts the dot cluster as ONE slot; reserved empty slots are not indicators | probe `[R25.7]` |
| **§26 Paper-backed exception lane (user-directed, Aug 11 2026 — per-item, evidence-gated, ruled)** | | |
| R26.1 | Evidence bar for a proposal: ≥5 closed paper trips, fill rate ≥60%, net paper-profitable, span ≥7 days, AND no red flags (not pump-suspected, not blacklisted, not toxic-flow); the one-tap proposal renders on the bench line and in the rulings digest; a waived/granted item never re-proposes | probe `[R26.1]` |
| R26.2 | Grant is a ruling (one tap, decision-logged): the item funds despite the named gate at probation terms — half size, a 2× tick-floor safety (its own stated bench reason when breached), one position — for 3 real round trips; probation is an explicit seasoning exemption (the ruling is the ratification) | probe `[R26.2]` |
| R26.3 | Adjudication by realized results on TWO named criteria (amended by R27.2): net P&L and adverse-markout %; 3 profitable probation trips with clean markouts → standing per-item waiver (logged, revocable in the row's expand view); losing → revoked, item returns to the bench, divergence recorded ("sim said yes, reality said no") as fill-model calibration data | probe `[R26.3]` `[R27.2]` |
| R26.4 | Cap 2 active exceptions — probes of the gate's edges, not a parallel permission system; at cap no proposal renders and the bench line states why; a waiver or revocation frees the slot. THE GATE ITSELF NEVER MOVES on paper evidence alone — constants still require the two-stream agreement standard; two lanes, two evidence standards, labeled in every grant | probe `[R26.4]` (+ R19.2 unchanged) |
| **§27 Shadow reweight — screening, not evidence (outside review, ruled Aug 11 2026)** | | |
| R27.1 | Every shadow surface (row dot, expand view, bench/NEXT-UP cites, weekly panel, gate-health stream label, vitals tile, checklist step, DB comments) labels the book a SCREEN ranking probation candidates for live half-size tests — never "evidence accruing"; the weekly shadow panel carries the standing self-impact caveat: "sim assumes a book you're not in — thin books change when you post; screening tool, not evidence" **Amended Aug 11 2026 (R35.3):** the standing self-impact caveat now rides the Paper Book sub-view (the weekly panel it named became a one-line summary linking there). | probe `[R27.1]` |
| R27.2 | The exception lane's realized conversion stays the ONLY path to a waiver; the three-trip adjudication names adverse-markout % as a criterion alongside net P&L (house `markoutX` toxic-flow standard applied to the probation trips' own markouts); profitable-but-markout-toxic REVOKES with the markout stated; untimed trips adjudicate on net alone and the decision log says so | probe `[R27.2]` |
| R27.3 | The regime race keeps running but divergence proposals read "regime X's candidates earned probation slots — a screening rank, not proof the regime is right", never "regime X is right"; exception proposals are a RANKED shortlist (conservative sim net, best first, truncated at free probation slots); the 4-of-7 persistence and two-stream proposal machinery unchanged (gate-proposal copy reframed: realized makes the case, the screen concurs) | probe `[R27.3]` (+ `[R18.4]` `[R19.2]` unchanged) |
| **§28 Scanner paper cohort (user-directed, Aug 11 2026 — the FILTER question: "are my filters excluding money")** | | |
| R28.1 | Each cycle (scout cadence, self-throttled), per tier scanner: the top 8 gate-passing-or-single-gate-near-miss items NOT on the watchlist shadow-trade with scanner·T1/scanner·T2 provenance, ranked by the allocator's own score (`scanScreenTier` with a loose-ROI screen so ROI near-misses survive); same honest fill sim + participation haircut; blacklist excluded (standing rule) and pump-flagged excluded same as everywhere; chart-dependent gates are unknowable and don't count toward the single-gate test | probe `[R28.1]` |
| R28.2 | Populations separate in every ledger view: the per-gate counterfactual and regime books read watchlist+slice only; the weekly FILTER OPPORTUNITY COST line compares scanner-cohort vs watchlist-cohort net over the same window (since the earliest scanner entry — a young cohort is never compared against 30d of history) and frames outperformance as a PIPELINE finding (needs ≥5 closed scanner trips), screening-labeled | probe `[R28.2]` |
| R28.3 | Bounds: 40 open across ALL cohorts (`SHADOW_MAX_OPEN`); the scanner cohort evicts only its own lowest-score open position, only for a better-scored candidate — watchlist/slice cohorts never evicted; spark fetches ride the scout's existing polite budget (`SCOUT_EVAL_PER_TIER` = 12/cycle, 250ms spacing) — coverage accrues over cycles rather than bursting | probe `[R28.3]` (cap + eviction) · budget is code-fixed (`fetches >= SCOUT_EVAL_PER_TIER`) and network-bound, stated here, not silently dropped |
| R28.4 | One stage per move (honesty carry-over): scanner-cohort trips are excluded from exception-lane evidence (slice and gate counts) — they nominate an item for scout ADMISSION only; ≥3 profitable scanner-cohort trips earn a scout-admission ATTENTION boost (evaluated first, same admission bar, boost stated in the scout log); funnel: scanner shadow → scout admission → watchlist → gates → probation → real size | probe `[R28.4]` (boost set + probation exclusion) · scout evaluation order is code-fixed (boosted-first concat), stated here |
| R26.5 | Void-basis re-ruling (user ruling, Aug 11 2026): the paper purge discarded the evidence that grounded every granted exception, and a ruling that survives its own justification must be RE-MADE rather than persist by inertia. Each such record raises its own walk-up ruling line ("exception for X was granted on discarded evidence — revoke, or re-grant on the new epoch's data?") offering both ways out, holds that line until ruled either way, and leaves the digest once ruled. Re-affirming is the user's call, never the sim's, but the decision log distinguishes the two cases explicitly — re-made ON new-epoch evidence that independently clears the same bar (cited), versus re-made on judgment with no evidence yet — because those are different claims and must not read alike. The row's expand view carries a "basis void — re-rule" badge so the state is self-explaining where it renders | probe `[R26.5]` |
| **§29 Pond prospecting — structural sampling (user-directed, Aug 11 2026)** | | |
| R29.1 | STRATIFIED ROTATION amends R11.2's uniform draw: the 15-item slice draws from ONE named stratum per price cycle, in rotation (equipment by slot, F2P-only, skilling outputs by skill, quest-locked, holiday/rare-adjacent, sub-400gp bulk — the last with its own sub-400 price window), so small strata are sampled on a clock instead of never; the stratum is stamped on every row and on every counterfactual it opens; the rotation is a pure function of the price cycle (stable within it, advancing across it). Every stratum except F2P and the price-band ones is a NAME-PATTERN classification — the mapping API carries no category data — labeled approximate wherever it renders, and it steers sampling only, never a trade | probe `[R29.1]` |
| R29.2 | GAP-BAND SLICE: 5 items per cycle from 250k–1m — the band between the T2 scanner ceiling and the anomaly scan's >1m floor, previously watched by nothing but slice luck — with gate verdicts and shadow eligibility identical to the discovery slice, rendered in the same table under its own stratum stamp | probe `[R29.2]` |
| R29.3 | Per-stratum ledger: distinct items sampled / near-misses shadowed / clean passers / closed trips / simulated net, rendered as the weekly review's §5 Prospecting map ranked by simulated net, each region reading "prospects" / "barren so far" / "too few closed trips to say"; the gap band gets its own verdict line — printing earns it a scanner PROPOSAL, and not printing proves the gap empty rather than assumed **Amended Aug 11 2026 (R35.4):** the map now renders in the Prospecting sub-view as a table whose cells open to their sampled items and surviving trips, with the gap band on its own panel; the review keeps a one-line read linking through. | probe `[R29.3]` |
| R29.4 | The ledger is DURABLE: closed slice/gap outcomes roll into it exactly once (a `rolled` stamp, no double counting on later ticks) and survive the paper book's 30-day pruning, because the quarterly "which pond regions hold edge" map has to outlive the retention window; distinct-item counting is the denominator (a re-draw is not a second test), capped per stratum with the cap stated when reached | probe `[R29.4]` |
| R29.5 | Slice tightening: a slice item clearing ALL gates enters the paper cohort at FULL eligibility (no bench stamp) — a random draw clearing every gate is the event the slice exists to catch — and surfaces as a WHAT CHANGED line naming the item and its stratum; the weekly discovery section carries the slice hit-rate line (sampled · near-misses shadowed · clean passers · slice-cohort simulated P&L). The ruled 5-per-cycle ledger bound is now SHARED with the gap band (half to the gap, unused allowance flowing back) so neither starves the other and no new budget was minted; blacklist and pump-flagged items are excluded from every cohort | probe `[R29.5]` (+ `[R25.5]` amended) |
| R29.6 | Classifier honesty (user ruling, Aug 11 2026): the per-stratum map carries the standing caveat "N of M strata are name-pattern approximations — directional, not a census" wherever it renders, with N and M DERIVED from the strata table so they cannot drift; and a PROMOTION TRIPWIRE — any per-stratum finding cited in a proposal, a scanner proposal, or a scout priority change carries "Depends on approximate stratum classification — verify before ruling" at the point of decision. The gap band is explicitly exempt and says why: its membership is an exact price range, not a name-pattern guess, so claiming the caveat there would overstate the dependency. Hardening the classifier via wiki-category enrichment is deferred to IMPROVEMENTS.md, conditional on the map ever driving a decision | probe `[R29.6]` |
| **§30 Hours ledger — time-structure prospecting (user-directed, Aug 11 2026)** | | |
| R30.1 | Everything else samples items; this samples HOURS, in two streams whose natures are separated in the copy: MARKET (post-tax spread by hour-of-day, computed from the 168-point hourly series each watched item already fetches — every hour equally represented whether or not the tracker was open, so it is the unbiased stream) and SHADOW (simulated fill rate and net by the hour a counterfactual opened — accrues only while OBSERVING, so it is presence-biased). Hours never observed read "no data", never "bad hour" — the same unobserved-is-unknown doctrine as R18.2. No new requests: the buckets ride the existing timeseries fetch **Amended Aug 11 2026 (R35.4):** the ledger renders in the Prospecting sub-view, with a per-hour table keeping the two streams in their own columns and each opening to its rows. | probe `[R30.1]` |
| R30.2 | The overnight question is answered where both streams can speak: 00:00–06:00 post-tax spread against the all-day average, with shadow corroboration when overnight trips exist and an explicit "unknown, not disproven" when they don't; a wider-overnight reading states the case for sizing the standing overnight book heavier as a PROPOSAL for the user's ruling, never applied from here | probe `[R30.2]` |
| **§31 Crafting-spread monitor (user-directed, Aug 11 2026)** | | |
| R31.1 | Processing basis on 10 high-volume recipes (herb cleaning, unf chains, 3-dose potions, a 4-dose combination potion, a barrows set vs its components), computed every refresh from the /latest bulk fetch (no new requests) on the stated PATIENT convention — output sold at its ask net of GE tax minus inputs bought at their bids. Recipes resolve BY NAME against the live mapping, never hardcoded ids: an unresolvable leg renders "unresolved" rather than a confidently wrong number. Daily ledger, last write of the day wins, 31d, and a recipe's baseline excludes its own current reading **WITHDRAWN Aug 11 2026 (user ruling).** Removed entirely rather than relocated: the monitor terminated in prose. Its manipulation-tell half duplicated `suspectedPump`, which is already wired to auto-arm restraint; its flip-signal half named a mispriced leg but put it nowhere the funnel could act on, so acting on it meant hand-carrying a name to the watchlist; and ten hardcoded recipes is a fixed, tiny universe beside the stratified slice, which samples the whole item space on a clock. If the idea returns it should return WIRED, which is a new capability with its own price tag and ruling. | WITHDRAWN — no verification. The assertions were removed with the feature; until Aug 13 2026 this column still claimed probe coverage, which is a coverage claim on deleted code |
| R31.2 | A basis reaching 2× that recipe's own normal (median absolute basis over ≥5 prior daily readings; below that the row says "baseline accruing" instead of flagging noise) flags a BASIS BREAK with the widest-moving leg named from the same ledger — read two ways in the copy: a flip signal on the mispriced leg and a manipulation tell, since a pump distorts one leg of a recipe before the other. Flags dedupe per recipe per 24h, retain 30d, surface in WHAT CHANGED with the leg that moved, and size/cap nothing **Amended Aug 11 2026 (R35.4):** the basis table renders in the Prospecting sub-view, each basis opening to its daily readings and the baseline it is judged against. **WITHDRAWN Aug 11 2026 (user ruling)** — see R31.1. Assertions removed with the feature; the rows are kept marked rather than deleted so the record shows the decision. | WITHDRAWN — no verification. The assertions were removed with the feature; until Aug 13 2026 this column still claimed probe coverage, which is a coverage claim on deleted code |
| **§32 Data freshness + timeseries breaker (pre-quiet-period audit, Aug 11 2026)** | | |
| R32.1 | Every background accrual reports when it last accrued, in the weekly review, split by nature: SCHEDULED streams (paper trips, regime ledger, scanner cohort, stratum samples, gate-health ledger, deploy ledger, recipe basis, hour-of-day, coherence, anomaly scan, walk-up attention) are flagged stale against their own cadence; EVENT-DRIVEN streams (markouts, die-off episodes, basis breaks, accumulation flags) are never marked stale because silence is data there, not a fault. The panel states the load-bearing dependency out loud — accrual requires the app to be open AND visible, since the poll skips while the tab is hidden — so a fortnight of nothing is diagnosable rather than mistaken for a quiet market | probe `[R32.1]` |
| R32.2 | Timeseries circuit breaker: `/timeseries` failures are remembered per item id (no immediate retry) and a run of 8 consecutive failures suspends timeseries fetching for 30 minutes behind a visible banner, serving stale cached series meanwhile; a success resets it. Closes the audit finding that the poll's own backoff counts only bulk-endpoint failures, so a timeseries-specific outage would be re-hit by the scout, the scanner cohort and fillSparks every pass — thousands of rejected requests a day, unattended, with nothing on screen | probe `[R32.2]` |
| **§33 Cold-start integrity (pre-quiet-period audit, Aug 11 2026)** | | |
| R33.1 | A cold, near-empty store (one watched item, no flips, no bank, no history, every ledger empty) renders all four tabs and every review surface without throwing, and no surface renders `NaN`, `undefined`, `[object Object]` or `Infinity` into user-facing copy — the whole tool was built against a populated store, so sparse-data rendering is asserted rather than assumed | probe `[R33.1]` |
| R33.2 | Empty streams say so in words rather than rendering an empty-but-broken panel, and the allocator survives the sparse case (`buildPlan` returns well-formed picks/bench/nextUp with one watch item and no capital) | probe `[R33.2]` |
| **§34 Accrual is tab-independent (user ruling, Aug 11 2026)** | | |
| R34.1 | Every plan-driven ledger (gate-health rows, die-off episodes, qualification streaks, paper positions, the per-stratum sampling ledger, the hourly funnel bucket) accrues from the POLL via `accrueBackground()`, never from a render path. Previously these were written inside `renderDeploy`, reached through the vitals renderer — which happened to run on all four tabs, so nothing was broken, but "happens to be called from the current layout" is the coupling that produced the reorg accrual bug and fails silently the next time a surface moves. Asserted with each of the four tabs active AND with no view rendering at all. Streams already poll-driven: `shadowTick`, `runScout`, `scannerShadowScan`, `recipeTick`, `fillSparks`; coherence and the anomaly scan run on their own 6h interval behind a 7-day guard; `walkupLoad` is Home-only by definition since a walk-up reading only exists when you walk up | probe `[R34.1]` |
| **§35 Shared decomposition primitive + three Trade sub-views (user-directed, Aug 11 2026 — "every aggregate decomposes to its rows, in one click")** | | |
| R35.1 | ONE shared primitive, not three bespoke drill-downs: `drill(key, face, spec)` renders any aggregate as an openable face, and one click opens the constituent rows **in place** (a full-width sibling row inside a table, a block sibling anywhere else) with no placement work by the caller. Sort, text filter and cohort selection live in the primitive, so every surface inherits them rather than re-earning them; expansion state is session-scoped (a reading posture, not a persisted setting) and is re-applied by `paintDrills()` after every render pass, so the 60s poll never closes what you opened to read. Asserted on COMPUTED VISIBILITY, not DOM presence | probe `[R35.1]` |
| R35.2 | Honest subset disclosure, built into the primitive: whenever the rows shown are fewer than the population the number counts, the expansion says so and why. Two cases, both asserted — **structural** (`spec.total` exceeds the rows supplied: the per-stratum ledger's counters are durable by R29.4 and outlive the paper book's 30-day pruning, so its expansions can only show surviving rows) and the **row cap** (first 20 of N, with a "show all N" control and a collapse back). A filtered expansion states how many rows its filter is hiding. A counter whose rows are all pruned renders words, never a blank box | probe `[R35.2]` |
| R35.3 | **Paper Book** sub-view (Trade → Paper Book): three ROI-regime equity curves side by side on ONE shared y-scale (three scales would be a picture that lies), labeled "all three SIMULATED … never compare to realized"; the divergence ledger opening to its daily readings; the per-gate table (benched / would-fill / at 100% / would-not / fill rate / net sim / observed median) with EVERY cell opening to its trips — entry and exit price, fill or no-fill with the tape credit at both participation readings, observed share, regime, cohort, benching gate. Forced exits now retain `exitPx` so a forced outcome shows the price it paid; records closed before that render "— not retained", never a guessed number. The "accruing since &lt;date&gt;" epoch banner and the standing self-impact caveat ride this surface. The cohort ledger states where every population stands — watchlist, slice, slice·clean, gap band, scanner·T1/T2 — including the empty ones, and unobserved holes are shown rather than silently excluded | probe `[R35.3]` `[R18.3]` `[R18.4]` `[R18.5]` `[R27.1]` `[R28.2]` |
| R35.4 | **Prospecting** sub-view (Trade → Prospecting): the per-stratum map as a table (sampled / near-miss / clean / closed / filled / net sim / verdict), each cell opening to its sampled items or its surviving trips; the gap band's own panel with its samples and its verdict, exempt from the classifier caveat and saying why; the hours ledger as a table keeping the two streams distinct — market (presence-independent) opening to the watched items that produced each hour's average, paper (presence-biased) opening to the trips opened in that hour, with never-observed hours reading "no data — never observed"; the recipe basis table (**this clause WITHDRAWN Aug 13 2026** with the §31 feature — the panel does not exist and the citation to its assertion was removed with it). The stratum caveat and the promotion tripwire render as already built | probe `[R35.4]` `[R29.3]` `[R29.6]` `[R30.1]` `[R30.2]` |
| R35.5 | **Gate Health** sub-view (Trade → Gate Health): the two-stream tree per gate with each stream opening to its items and outcomes — realized overrides ("traded while still benched") and re-admission latency ("traded after unbenching") kept distinct per the definitions protocol, and the paper stream's would-fill / would-not opening to its trips; die-off episodes opening to their items and 24h results with the unobserved-window caveat intact; the exception lane's standing (on probation / standing waivers / revoked / ranked candidates), including any granted exception whose basis was voided by the epoch reset, which says so where the lane is read. The surface reports standing only — grants, revocations and re-affirmations are still ruled from the bench line, the row's expand view or the rulings digest | probe `[R35.5]` `[R19.1]` `[R26.5]` |
| R35.6 | Scoping tested BY ABSENCE on computed visibility (the standing discipline): each sub-view renders only on its own sub-tab and nowhere else — asserted absent on the other two Trade sub-views, on Plan & Watchlist / Scanner / Flip Log, and on Home and Review. The three are first-class Trade sub-tabs, one press from the watchlist. Detector proven against both failure shapes: `.tsub{display:block}` (sub-view scoping) and an ID rule outranking `section.tab{display:none}` (the historical R22.2 regression) | probe `[R35.6]` |
| R35.7 | The weekly review keeps a ONE-LINE read of each surface with a link through, and stops re-rendering them: the paper-book, hours and recipe checklist steps and the audit's §4 gate health and §5 prospecting are now summaries carrying their own `data-goto` — asserted on the summary functions themselves, not only on the rendered checklist, because the step's own "→ open" arrow would otherwise mask a summary that had stopped linking. The working tables (`paperGateTable`, `strataTable`, `hoursTable`) must NOT appear in the review. Walk-up attention cost unchanged: pull surfaces, no rulings on them, nothing new on Home, walk-up decision count still ≤ 7. **Complexity price tag:** these three absorb the review's paper-book / hours / recipe steps and the audit's §4 and §5 — growth paid for in consolidation, not stacked | probe `[R35.7]` |
| **§36 Weekly-review consolidation + auto-applied bookkeeping (user rulings, Aug 11 2026)** | | |
| R36.1 | Four sections left the weekly review and one was demoted. CUT: the basket ledger and the SLEEVE section (only meaningful when new intelligence lands — they live on the Sleeve tab, visited after an import; the sleeve's thesis-health lights are settable only there, so the block is machinery and moved with it), the recipe basis table (withdrawn entirely, R31.1), and the gp/min-of-attention reminder (a metric never acted on; the checklist foot's claim about it went too, since a closing line must not advertise a step that no longer exists). DEMOTED: the attention/dormancy report to an on-demand button (R13.2). Asserted by ABSENCE in the review AND presence on the tab that now owns each — a cut asserted only in the review would pass equally if the machinery had been deleted | probe `[R36.1]` `[R13.2]` |
| R36.2 | Coherence membership ADDS apply automatically at their existing evidence bar (excess corr > 0.5 vs the basket composite in ≥3 of the last 4 weekly readings), decision-logged with an `auto` stamp that distinguishes them from a press. Coherence DROPS deliberately do NOT auto-apply and their queue line says why, in the copy the user reads: an add only tightens a basket's exposure cap (restraint, which may auto-arm), while a drop removes an item from the cap's scope and can free capital the cap was withholding — the restraint/deployment line, which is the user's to move. Flagged rather than assumed from "membership is bookkeeping" | probe `[R36.2]` |
| R36.3 | The evidence bars still bind at APPLICATION time, not merely at proposal time: blacklisted items are never auto-admitted (the blacklist is the user's alone — no automated path admits, funds, quotes or clears an entry), and the 40% statistical-member cap is re-checked as each add lands, because proposals are made against the basket as it stood and several can land in one pass | probe `[R36.3]` |
| R36.4 | Seed audits auto-execute their own KEEP / RETIRE calls and sibling audits their PRUNE calls — the recommendation that executes is the one the audit produced, never one invented for it. Every auto-execution names the ruling that authorised it in the decision-log reason. PROMOTE is never auto-executed: it names a thesis cluster, and a name is a judgment, not bookkeeping | probe `[R36.4]` |
| R36.5 | The ruling traded a ratification ceremony for a reviewable record, so the record is prominent rather than buried: the review renders an "Applied automatically" block listing every auto-applied change in the last 30 days, decomposable to its rows through the shared primitive, carrying the undo path (an add is removed from the basket's thesis detail, a retired lineage is re-seeded by hand) and the reason drops are excluded | probe `[R36.5]` |
| R36.6 | Auto-application rides the POLL via `accrueBackground()`, never a render path — otherwise which tab the user happened to be on would decide whether a basket recomposed (the R34.1 rule, applied to the first machinery that changes state rather than accruing it) | probe `[R36.6]` |
| **§37 Observed-share floor on paper trips (user ruling, Aug 11 2026)** | | |
| R37.1 | A trip that opens and closes inside a single observation window has not tested patient-fill behaviour — it is tape coincidence — so it must not carry the evidentiary weight of a trip that survived a real horizon. Floor: **≥25% of the fill horizon observed** (`SHADOW_MIN_OBS`, a strategy parameter set by the user when ruling this, one constant to change). Below it a trip is EXCLUDED from every verdict: gate-tuning aggregates, the regime curves and their daily ledger, per-stratum verdicts, the row dot, the hours ledger's fill rate, and exception-lane evidence. Three states are kept distinct in code and copy — counted, insufficient observation, and `unobserved` (horizon elapsed with the tab closed, never a result at all) | probe `[R37.1]` |
| R37.2 | Excluded trips are RECORDED AND VISIBLE, never discarded: every aggregate states its excluded count, that count opens to the trips behind it through the shared primitive (a bare "3 excluded" would be a number the operator cannot audit), and every trip row names its own observed share so an excluded row explains itself. Asserted on the per-gate table, the gate-health tree, the cohort ledger, the hours table and the per-stratum map | probe `[R37.2]` |
| R37.3 | Exception-lane evidence ignores thin trips entirely — six fat-net thin trips ground nothing — and the row cite says the screen has no reading rather than reporting a null result. The floor gates EVIDENCE, it does not ban the item: the same item qualifies once enough observed trips exist | probe `[R37.3]` |
| R37.4 | The durable per-stratum ledger rolls thin trips into their own `insuf` counter rather than the region's verdict numbers, and the regime ledger's daily snapshot excludes them, so a fast fill can never argue for a gate change. Both counters render with their exclusion stated and openable | probe `[R37.4]` |
| **§38 Glossary (user-directed audit, Aug 11 2026)** | | |
| R38.1 | The glossary is a DATA STRUCTURE (`GLOSSARY`), not prose, so coverage is mechanically checkable: 7 groups (gates, paper book, sleeve/catalysts/baskets, prospecting, indicators, metrics, and a seventh for how the tool governs itself — the governance terms fit none of the six subject areas, and burying the record of what the tool did without asking inside "metrics" would hide the thing that most needs finding), ~70 entries, unique ids. Every entry carries three fields: **what** it means, **from** what produces it, and **do** — what decision it should change for the user | probe `[R38.1]` |
| R38.2 | Gate coverage is derived, not maintained by hand: every name `gateName()` can return must resolve to a family entry through the entries' own `names` arrays, so adding a gate the glossary does not cover fails the suite rather than shipping unexplained. Gates are grouped into ~6 FAMILIES (price-and-spread, liquidity, chart/momentum, your-own-history, mandate/ownership, data-availability) with the individual names listed inside their family entry, rather than 19 stubs | probe `[R38.2]` |
| R38.3 | A gate name rendered in a bench reason is tappable straight to its family entry — on the watch row's bench badge, the paper-book per-gate table, the gate-health tree and the deployment funnel's proposal lines. The tap opens the group, scrolls the entry into view below the sticky chrome and flashes it, to the same landing standard as R24.2. **Visibility is measured on the GROUP's painted box**, because Chromium collapses a closed `<details>` via content-visibility on its slot: the hidden children keep a display value, an offsetParent AND a non-zero rect, so every per-element visibility check lies there | probe `[R38.3]` |
| R38.4 | Two stale claims found by the inventory are fixed and asserted against regression: the Item basket entry no longer says its P&L is reported in the weekly review (it is on the Sleeve tab since R36.1), and no longer says candidates "never cap anything until you ratify them" — it states the R36.2 carve-out instead (ADDS self-apply; DROPS and whole-basket candidates queue, because a drop loosens an exposure cap) | probe `[R38.4]` |
| R38.5 | Terms carrying a known limitation carry it at the point of reading rather than in a footnote: paper-book quarantine status and the shadow→paper rename, stratum name-pattern approximation, markout attribution lean, and the observed-share "unknown, not unfilled" doctrine | probe `[R38.5]` |
| R38.6 | Honesty about dead terms: an entry whose decision line is "nothing directly" must NAME the decision it is context for. A term that changes no decision and is context for none is a deletion candidate, and the scan surfaces those while the glossary is being written rather than never | probe `[R38.6]` |
| R38.7 | Navigable at its new size: groups are collapsible, a find box filters entries while keeping the groups it matched, and an expand-all control exists. The glossary panel is global (below the tabs), so it is reachable from every surface | probe `[R38.7]` |
| **§39 Inline glossary popovers (user ruling, Aug 11 2026)** | | |
| R39.1 | TRIGGER PARITY: hover on desktop and tap on touch open the SAME popover with the SAME content, through the same function — the phone is the primary reading surface, so nothing here may be a desktop-only affordance. The pre-existing "tap any badge for its explanation" claim (which the old glossary advertised and nothing implemented) folds into this rather than living beside it | probe `[R39.1]` |
| R39.2 | NO DRIFT: popover content is derived from the same `GLOSSARY` entry the full glossary renders — one source, two presentations. It carries the short definition (the entry's own first sentence, or a `short` override) plus the "what it changes" line and a "full entry →" link, and deliberately NOT the whole entry | probe `[R39.2]` |
| R39.3 | A popover that requires scrolling has failed, and this is MEASURED rather than trusted: every glossed term is opened and its popover asserted not to overflow, at whatever viewport the suite runs — so the phone pass is the binding one | probe `[R39.3]` (all ~70 terms, both viewports) |
| R39.4 | The phone failure mode: a gesture that begins on a term but is a drag or a scroll must NOT open a definition. Movement over 10px, page travel over 4px, or a dwell beyond 700ms all disqualify the tap. The scroll fixture asserts the page actually travelled, so a fixture that cannot move says so rather than passing | probe `[R39.4]` |
| R39.5 | Dismissal: Esc closes; a press outside closes WITHOUT `preventDefault`, so the press still reaches whatever is beneath and the popover never blocks the action under it; a second tap on the same term closes it. No focus trap, no backdrop | probe `[R39.5]` |
| R39.6 | Painted-box visibility per the case law's fourth entry: an open popover is asserted to paint a real box inside the viewport, and a closed one to paint nothing — measured, because presence in the DOM is not visibility | probe `[R39.6]` |
| R39.7 | SCOPE: glossed terms are marked wherever they render — gate names in bench reasons (watch row badge, paper-book per-gate table, gate-health tree, funnel proposals), row status badges, the dot glyphs, cohort labels, paper-book and prospecting column headers, and the gate-health stream labels. RESTRAINT: a dotted underline and nothing else — no icon, no colour of its own, because the indicators carry state and the underlines carry vocabulary; a row must not read as a minefield of help links | probe `[R39.7]` |
| R39.8 | The hover bridge (reported from use, Aug 11 2026): the gap between a term and its popover is dead space, and crossing it fires `pointerout` with a relatedTarget that is neither — which closed the popover before "full entry →" could be reached. Closing is now deferred ~260ms and cancelled if the pointer arrives at the popover or returns to the term; leaving for good still closes, so the delay is a bridge rather than stickiness. The probe settles the scroll position first, because dismiss-on-scroll is correct behaviour and an ambient scroll would otherwise close the popover mid-measurement and blame the bridge | probe `[R39.8]` |
| R39.9 | RESTRAINT, amended from use: **a control that already delivers its own intel is not glossed.** The ⚠ caution chip is a button whose press opens the row's expand view listing every caution and its reason, and a definition popover on top of it competed with the thing being reached for; the expand view's own caution lines already render their `why` inline. Gloss VOCABULARY — a word whose meaning is not otherwise available where it renders — never a control whose press is the explanation | probe `[R39.9]` |
| **§40 Four-touch cadence — per-touch horizons (user ruling, Aug 11 2026)** | | |
| R40.1 | The touch schedule is a SETTING (`DB.touchWindows`, four editable hours-of-day; default 07:00 / 12:00 / 17:00 / 21:30 giving gaps of 5.0 / 5.0 / 4.5 / 9.5h). Every horizon derives from the gap to the NEXT touch: sizing, the fill-horizon gate, fill forecasts, participation sizing, stale-buy clocks and paper-trip lifecycles all read it. **Absence-tolerant by construction** — everything is computed from "now → next window", so a missed touch simply means the next window is further away and the following horizon stretches, with nothing to reset. An **explicitly empty schedule is a real setting** meaning "no cadence kept", which falls back to the single global `fillHorizonH` the tool used before this build | probe `[R40.1]` |
| R40.2 | THE ROUTING LINE, which is the point of the shape: an item the current gap must bench, which the longest scheduled sit would fund, says so on its bench line — "qualifies at the evening touch — needs ~7h, and that sit is 9.5h (this gap is only 5h)". Fast cyclers carry the day; patient, wider-margin items fund at the evening touch. Silent when the current gap already covers the item, and silent when no gap does | probe `[R40.2]` |
| R40.3 | A leg stamps the horizon in force when it was PLACED (`hzH` on positions, quote records and paper trips) and ages against that, never the current gap — a leg placed at the evening touch is mid-sit at 05:00, not long stale. Records written before this build fall back to the old global, which is what they actually ran against. Fixes the audit's finding 6b (`staleBuyInfo`'s hardcoded 15:00 am/pm split, a literal two-touch convention) and 6a (`limitWindows` used a global day horizon, overstating daytime sizing on buy-limit-bound items by up to 2× — a 5h placement rolls one 4h buy-limit window, not two) | probe `[R40.3]` |
| R40.4 | Seasoning RE-EXPRESSED, not rescaled. The rule bought two things — "sustained across sessions" and "not committed on a single day's data" — and at four touches the old "3 passes spanning ≥2h, one per hour" bought only the first. Now: **3 passes, at most one per TOUCH, spanning at least one calendar day.** The span is a date rollover rather than a duration, so four touches cannot buy same-day qualification. With no schedule kept, the old rule stands | probe `[R40.4]` |
| R40.5 | Every paper-book and gate-health aggregate GROUPS by horizon shape (daytime vs overnight) and stamps evidence grade (live / reconstructed / mixed). Grouping, not filtering — a filter defaults to hiding the 5h-vs-9.5h split, which is the distinction the schedule exists to create; a finding that holds at one horizon and not the other is a HORIZON finding, not a gate finding. `shadowByGate()` stays keyed by gate for the verdict machinery and takes a flag for the shape-split view, so the two cannot disagree. A finding resting only on reconstructed evidence says so | probe `[R40.5]` |
| R40.6 | The 25% observation floor stays and is measured per trip against ITS OWN horizon — a 4h trip clears at 1h observed, a 9.5h trip does not. Existing open trips keep the horizon they were opened under; nothing is retro-applied | probe `[R40.6]` |
| R40.7 | RECONSTRUCTION BACKSTOP so the always-on host stops being load-bearing. On the first poll after an observation gap, each open trip's own 5m series is fetched once (capped at 12 items per pass, 250ms spaced, cached) and the fill model is REPLAYED under exactly the live rules — tape at or below bid, volume-weighted, capture-capped, participation-haircut. **Observation credit is SERIES COVERAGE**: a 5m bucket with data is five observed minutes, a bucket without credits nothing, and coverage stops when the trip closes. Spans beyond series retention stay unobservable by construction. Forced exits price at the **series value AT the horizon** for all trips — the proper fix for the defect that caused the epoch reset, correct even on a perfect host, with an approximate-price stamp when the series cannot reach back. Recovery is an EVENT: WHAT CHANGED reports "recovered N trips across a 6h gap" rather than leaving an outage to be inferred from a thin ledger a week later | probe `[R40.7]` |
| R40.8 | Attention is charged per touch (`DB.touchMins`, default 6/6/6/10 — the evening touch places patient capital and does the logging). The equity panel shows **gp per day beside gp per attention-minute**, because the cadence change moves them in opposite directions and either alone reads as a regression; it states the denominator change once, in place: "Denominator changed Aug 11 2026 — cadence" | probe `[R40.8]` |
| R40.9 | Unchanged by the cadence build, and asserted so: gates, floors, reserve and budgets (cadence is not edge), and the walk-up attention count still ≤ 7. The touch-timing heatmap is reframed from "aim the touches there" to scoring the four windows against each other, since the windows are now fixed | probe `[R13.1]` `[R20.4]` (budget) · UI (heatmap copy) |
| **§41 Market volume index, the `measured` tier, and rulings carried in the file (user ruling, Aug 12 2026)** | | |
| R41.1 | The anomaly scan's volume denominator is a MARKET-WIDE CONTROL INDEX (`VOL_INDEX_BASKET`, 8 liquid items deliberately unrelated to each other and to endgame PvM), recomputed at the start of every scan from the same endpoint and cache the scan itself uses. The index is the **median** of the controls' volume ratios, so a single control caught in a story of its own cannot drag it; under `VOL_INDEX_MIN` (4) usable controls there is **no index at all** rather than a guess from two | probe `[R41.1]` |
| R41.2 | The volume term of the accumulation signature is **item-volume-relative-to-index**, not raw: an item flags only when it beats the index by the same 25pp it previously had to beat its own past. An item that merely kept pace with a market-wide move is NOT accumulation. The price leg is unchanged and still binds, so a volume-only mover never flags. The raw figure is retained on the record but never drives the decision | probe `[R41.2]` |
| R41.3 | INTERIM SUPPRESSION for the known discontinuity: when an item's two comparison windows sit on opposite sides of `VOL_BREAK_D` (2026-07-24) the volume term is suppressed outright rather than normalised — the index inherits the same break, so there is nothing trustworthy to normalise against. Suppressed means **not flagged**: the signature is price AND volume, and flagging on price alone would invent the missing half. The test is **date-driven and self-expiring** — once the 2–3-weeks-ago window starts after the break nothing straddles and it stops firing, with no flag day to remember and no code to remove | probe `[R41.3]` |
| R41.4 | The index states where it stands and DECOMPOSES: the scan panel renders its value, its measurement date and how many of the basket returned usable series, and the number opens through the shared primitive to the control rows behind it — an aggregate whose one failure mode (a future update moving the whole basket at once) is visible only in its rows. Missing controls are declared as a subset by the primitive. An unavailable index says so and names the risk rather than silently reverting to the raw term that caused the incident; a scan whose items were suppressed reports the suppression, so an empty result names its own cause instead of reading as a quiet market | probe `[R41.4]` |
| R41.5 | Every flag names the volume term that raised it, in one shared phrase (`anomVolPhrase`) used by the panel, the pump fingerprint, the tape-question reminder and the analyst export, so the four cannot disagree about what the number means. A flag raised BEFORE the index existed says so explicitly and must not read as one the index confirmed; the analyst export carries `volMode` so the desk knows which term it is being asked to answer | probe `[R41.5]` |
| R41.6 | `measured` is a fifth confidence rung: a claim computed directly from the price API and reproducible from the endpoints it cites. It **outranks `confirmed` for claims about the tape**, because for a tape claim a measurement beats a statement about the measurement — while `confirmed` stays the top tier for announcements. That distinction is mechanical, not advisory: a `catalyst` / `catalyst-update` / `long-catalyst` claiming `measured` is a category error and is **rejected on import and counted**, never quietly downgraded, because no honest coercion exists | probe `[R41.6]` |
| R41.7 | A ruling given at the desk can travel back inside `intelligence.json` as `disposition: {action:"dismiss", reason, ruledOn}`. This is not the tool ruling for itself — the file is the handshake the user carries and presses Import on, which IS the constitution's explicit press. It is decision-logged with an `auto` stamp, the reason and the date ruled, so the log records **why** and not merely that, and it renders its reason where the record renders. **Only dismissal is carriable**: dismissing drops an advisory record while ratifying arms one, so the arming direction stays a press in the tool. A disposition with no reason is ignored — a dismissal without a why is not a record | probe `[R41.7]` |
| R41.8 | A RATIFIED record whose copy a later briefing revises is CORRECTED IN PLACE rather than absorbed. Before this, re-importing a ratified id was silently absorbed, so an analyst run that withdrew a number from a record the user had already ratified left the wrong number standing in the tool while the brief claimed it was fixed — found Aug 12 2026 while withdrawing eleven volume figures. The correction keeps the ratification and its teeth (copy changed; nothing re-armed, no gate moved) and stamps `corrected` so the record says the words you ratified are not the words here now. An unchanged re-import is still absorbed, so corrections do not churn | probe `[R41.8]` |
| **§42 Analysis export (user-directed, Aug 12 2026)** | | |
| R42.1 | A curated **read** export, distinct from the state backup: `⭳ export for analysis` on each of the three pull sub-views writing `analysis-<view>-<date>.json`, plus a combined `export all three` in the weekly review writing `analysis-all-<date>.json`. The combined file keeps each section's **own header**, because one merged header would hide that the three truncate independently | probe `[R42.1]` |
| R42.2 | EVIDENCE WITH ITS PROVENANCE, not aggregates — the decomposability rule applied to a file, since an export the reader cannot audit is the same defect as a number they cannot open. Paper book: every closed trip with item, cohort, horizon shape, evidence grade, observed share, entry/exit, the tape credit at BOTH participation readings, net sim, regime and benching gate, plus the per-gate rollups and the three regime curves as series. Gate health: per gate both streams with their constituent items and per-flip outcomes, die-off episodes with their 24h results, and the exception lane with its evidence. Prospecting: per-stratum rows with sampled items and verdicts, the gap-band sample, and the hours ledger with both streams by hour | probe `[R42.2]` |
| R42.3 | Trips **excluded by the observation floor are INCLUDED and marked** (`excludedByObservationFloor`), never dropped — the floor gates verdicts, not the record, and an export that silently omitted them would misrepresent the denominator | probe `[R42.3]` |
| R42.4 | Every export carries a HEADER so nothing is assessed as fresh when it is stale or thin: epoch start, generated-at, counts by cohort / evidence grade / horizon shape / outcome, an observation-coverage summary, the constants in force (gate values and floors, the touch schedule and its gaps, participation haircut, budgets), and the standing caveats that normally render at the surface — simulated-not-realized, stratum name-matching approximation, reconstruction limits, the two gate streams not being interchangeable, and pre-index anomaly flags | probe `[R42.4]` |
| R42.5 | SIZE DISCIPLINE: rows are capped per section with the **newest kept**, and every cap is declared in the header's `truncation` array with kept, total and the ordering — including sections that were NOT truncated, so silence never has to be interpreted. A file too big to paste is useless; one that silently truncates is worse | probe `[R42.5]` |
| R42.6 | HUMAN-READABLE: flat arrays of objects with named fields, and **no internal id that does not resolve inside the file** — every row that references an item carries that item's name beside its id | probe `[R42.6]` |
| **§43 Paper-book defect fixes (external analysis of the 2026-08-12 exports, user-directed)** | | |
| R43.1 | REGIME MEMBERSHIP IS A PROPERTY OF THE CANDIDATE, stamped centrally at open from its own effective ROI against the three floors (`regimesFor`), so no entry path can forget. It used to be attached by two of six paths while the other four hardcoded an empty set; in the epoch that found this those four were 96% of the book and the race had assigned **zero trips in its entire life**. Plus the NEVER-FED DETECTOR: an aggregate whose input population is empty says so in its own words — "the regime race has never been fed" is a different sentence from "the regimes are flat", and a zero that means the first is indistinguishable on screen from a zero that means the second. Applied to the regime curves and the divergence ledger independently, and carried in the analysis export as a flag beside the zeros | probe `[R43.1]` |
| R43.2 | FILL-MODEL CAUSALITY. A leg is credited only by a 5m bucket that STARTED at or after the leg was placed — the buy from its open, the sell from the moment the buy completed — and a bucket credits a leg ONCE however many polls present it. The bucket that completes the buy can never also fill the sell. A trip cannot resolve in the poll cycle that opened it. Before this the tick booked the whole trailing five-minute aggregate on a trip's first tick and re-booked it every poll: 182 of one epoch's 272 trips opened and closed in under a second, median 55ms, all "filled both legs", booking 52% of the headline simulated net from tape that predated them. The reconstruction path obeys the same rule | probe `[R43.2]` |
| R43.3 | Every fill-rate figure renders its NEVER-FILLED count beside it, so a 100% reading is visibly a claim about zero counterexamples rather than a default nobody has tested. Asserted on the hours ledger, the row slice, the exception-lane candidates and the analysis export (per hour and per stratum) | probe `[R43.3]` |
| R43.4 | ONE DEFINITION OF "FAILS EXACTLY ONE GATE" (`failProfile`), read by the deployment funnel, the discovery slice and the scanner cohort. They had three: the funnel counted chart-pending gates as failures while the slice excluded them. **Unknowable is not failing** — "chart still loading" and "no history" mean the gate could not be asked, and never count toward the single-gate test. Separately, the per-stratum SAMPLING DENOMINATOR is counted before the near-miss filter (`strataCount`), not after: it used to exclude every multi-gate failure, so `nearMissRate` was 100% by construction and read 577 of 578 across 22 strata. The three outcomes (near-miss, clean pass, multi-gate wall) now sum to the denominator | probe `[R43.4]` |
| R43.5 | DIE-OFF CONFIRMATION DEPTH is its own constant (`DIEOFF_CONFIRM_REFRESHES`), separate from the volume gate's binding rule, so the episode ledger can be made stricter without touching a gate. An episode that recovers inside ONE TOUCH GAP is AUTO-VOIDED as noise — it never affected a placement, because nothing is placed or repriced between touches — and is excluded from the recovery counts **and the denominator**, because a false positive is not an episode the tag got right or wrong. Evidence: 64 of 64 episodes "recovered within 24h", median 5.4 minutes, across 16 items with ten of them Lumbridge teleport tabs. `DIEOFF_CONFIRM_PROPOSED = 6` (~30 min) is PROPOSED and deliberately not applied | probe `[R43.5]` |
| **§44 Reachability, partial as an outcome, and the headline as a rate (user ruling, Aug 12 2026)** | | |
| R44.1 | REACHABILITY: "the tape printed at or below my bid" does not establish that it reached ME — I may have been behind someone in the queue. With no book depth the one available signal is where the print sat relative to my price. A print **strictly past** my price (below my bid, above my ask) proves the queue at it cleared and takes full capture-capped credit; a print **exactly at** my price is ambiguous and takes the participation haircut only; a print on the wrong side credits nothing. Three readings accrue from the same buckets — strict (proven reachable), operative (drives state and net), lenient (possibly reachable) — and `lenient ≥ operative ≥ strict` always, so the fill claim is bracketed rather than asserted. The price test reads the BUCKET's own average, not `/latest`'s current instabuy, so price and volume come from the same bucket | probe `[R44.1]` `[R18.5]` |
| R44.2 | A leg fills only when credited volume accumulates to the FULL qty inside its horizon. Otherwise the trip resolves **never-filled** (nothing credited) or **partial** (some credited, never completed) — a real outcome for the first time. The old model silently shrank the trip to whatever it had bought and carried on, which is why "would not have filled" was reported zero times in 272 trips. A partial exits its position at the horizon price and counts as a counterexample in every fill rate | probe `[R44.2]` |
| R44.3 | THE HEADLINE IS A RATE, NOT A TOTAL. The bare summed net is retired: net renders **per cohort and never blended** (four cohorts answer four questions and their sum answers none), as **net per gp-day of simulated notional** so it is comparable across populations of different size and duration, with its **distribution** beside it (median trip, top-5 share of net, outcome census) so a total carried by one trip looks carried. Plus a **capital-feasibility** line: peak SIMULTANEOUS notional, swept over open/close events rather than summed, against working capital — flagged when the sim assumed more capital than exists, because a counterfactual that quietly spends money that is not there is not screening evidence | probe `[R44.3]` |
| R44.4 | The raw summed net survives in exactly one place — inside the regime comparison, where the three regimes screen the SAME population so the level cancels and only the difference carries information. It is labelled "comparison only, not a magnitude" where it renders | probe `[R44.3]` |
| R44.5 | Die-off episode confirmation depth ruled to **6** consecutive below-floor 5m samples (~30 min), up from 2, and kept separate from the volume gate's own 2-refresh binding rule, which is unchanged. It must fire before the next placement, so waiting a full touch gap is not available; 30 minutes clears the observed 5.4-minute noise median with room. All 64 episodes of the epoch that produced the evidence would have been suppressed, which is the intended outcome | probe `[R43.5]` |
| **§45 Ambiguous-by-touch, and fill-model calibration (user ruling, Aug 12 2026)** | | |
| R45.1 | The AMBIGUOUS reachability class is widened from "bucket average exactly equals my price" to "bucket average on the wrong side, but a LIVE instabuy observation touched my price inside that bucket". The original rule was correct as specified and inert in practice — a five-minute AVERAGE almost never lands on a specific bid, and it fired on 3.2% of buckets across the epoch, collapsing the proven/possible bracket for reasons unrelated to real reachability. A touch never upgrades a print already strictly past my price. Because a touch is only knowable once its bucket has closed, buckets are OBSERVED while current and CREDITED when the next arrives: fills lag by up to one bucket, which is the honest price of the distinction | probe `[R45.1]` `[R43.2]` |
| R45.2 | RECONSTRUCTION CANNOT PRODUCE THE AMBIGUOUS CLASS. A replay reads buckets after the fact and has no instabuy observation history, so a reconstructed span yields only PROVEN or NEVER. Proven credit still works normally — the limit is the class, not the model. Every trip touched by reconstruction is stamped `reachLimited`, so a reconstructed fill rate is never compared against a live one without the difference being stated | probe `[R45.2]` |
| R45.3 | FILL-MODEL CALIBRATION against realized flips — the test of whether a low paper fill rate is a finding about the market or about the model. Every logged real round trip is replayed with the same bid, ask, qty and horizon; since a logged flip completed both legs by definition, every disagreement is a model error with a known direction: agreement, false partial, false forced-exit, and the damning **false never-filled**, each with the median share of the leg the model did credit so "how wrong" is reported and not just "wrong". Three limits are STATED rather than worked around: the 5m tape retains ~36h so calibration is a ROLLING test; a flip needs a recorded buy duration or its window would have to be invented; and the ambiguous reading cannot be replayed at all, so calibration reports the two BOUNDS and says the operative reading is measurable only going forward. Older flips are explicitly NOT replayed against 1h buckets — an hour bucket credits twelve times the volume per step and would calibrate a different model | probe `[R45.3]` |
| **§46 Calibration window anchor, bucket trace, duration split (user-directed, Aug 12 2026)** | | |
| R46.1 | WINDOW ANCHORING, and the defect the first calibration run exposed. The log moment is the end of the ROUND TRIP, not of the buy: `buy starts → bh → buy done → sh → logged`, so the buy window opens at `logAt − (sh + bh)`. The first version anchored at `logAt − bh`, placing every replay later by the whole sell-leg duration (median 1.25h in this log). A multi-hour buy still overlapped its true window and replayed as filled; a sub-hour buy landed entirely inside or past the SELL period, where the price had moved toward the ask and never returned — so the credit was not small, it was ZERO. That is the 0%-not-partial signature, and it was a harness defect, not a model one. Both leg timings are REQUIRED (`sh = 0` is a real value, a missing `sh` is not); flips outside the 5m retention are skipped as `tooOld`. Extracted as `calibWindow()` so assertions call it rather than re-deriving the arithmetic | probe `[R46.1]` |
| R46.2 | A BUCKET-LEVEL TRACE on every replayed flip: each 5m bucket inside the window with its average low, its low-side volume, what it credited, and WHY — naming both prices when the bucket sat above the fill price. A zero on a leg that really completed in 23 minutes must be explicable down to the bucket, because a summary verdict cannot be interrogated. The row also carries a reach census (buckets reaching the price, of buckets in window) so a zero is visible before the trace is opened. Capped per flip with the truncation stated | probe `[R46.2]` |
| R46.3 | THE DURATION SPLIT renders on EVERY run, never folded into the headline: agreement, false partial, false forced-exit and false never-filled, for buy legs under 1h and 1h-and-over, on both reachability bounds. A model blind only on fast fills is a bounded defect; one blind everywhere is global pessimism; the two call for opposite responses and a blended agreement rate hides which you have. When the disagreement concentrates on fast legs the panel says so and states that paper fill rates must be re-read with duration held constant. Produced by `calibSummarise()` so the split cannot be dropped from a result without an assertion noticing | probe `[R46.3]` |
| **§47 Calibration export — the measurement leaves, the trading record does not (user-directed, Aug 12 2026)** | | |
| R47.1 | `⭳ export calibration` on the calibration panel writes `analysis-calibration-<date>.json` under the same header convention as the other three analysis exports. It carries both bound summaries (agreed, false partial, false forced-exit, false never-filled, median share of the buy credited when wrong), per replayed flip the item, qty, real buy and sell durations, the anchored window as offsets, both verdicts and the reach census, and the per-bucket trace with each bucket's relative price, volume, credit and reason — an above-price bucket states the GAP so reachability is legible. Skips are counted by reason, never silently dropped | probe `[R47.1]` |
| R47.2 | The DURATION SPLIT is present for both bounds even when a bucket is empty, rendered as explicit zero counts rather than omitted, because it is the field that decides bounded-defect vs global pessimism and an absent zero reads as no data. The guarantee lives in `calibSplit()`; the export asserts the contract rather than adding an unreachable fallback, since dead code that reads as a safeguard is worse than none | probe `[R47.2]` |
| R47.3 | THREE EXCLUSIONS, enforced in one place and probe-asserted against a fixture that contains every one of them, searched for by magic value rather than by inspection: **no absolute prices** (bucket lows and the realized spread appear only as percentages against the trader's own fill) and none recoverable — a gp delta beside a percentage inverts to the absolute figure, so the delta the brief asked for is deliberately omitted and the file says so; **no gp** (no realized profit, no bank, and the gp-denominated capital constants stripped from the header, with the withholding stated) while quantities and market volumes survive because they are not money; **no wall clock** (bucket times are minutes from each window's own start, windows are durations, no absolute stamp survives) | probe `[R47.3]` |
| R47.4 | Four honesty lines ride the header: the operative reading lies between the bounds and is measurable only forward; the ambiguous class is unavailable to replay; comparing against the bucket average biases pessimistic when the trader bought at the prevailing instabuy, with the per-bucket trace named as the way to tell that apart from a window error; and the eligible-sample size with the reason for every skip. Size discipline: traces for the 12 most recent flips, newest first, cap declared; a trace over 60 buckets keeps the first 20 and last 20 and says how many it dropped. The stored trace cap is deliberately set ABOVE that threshold so the trim is reachable rather than a dead path | probe `[R47.4]` |
| R47.5 | SELF-COMPARISON, named where the bound is read and MEASURED rather than asserted. Calibration compares each bucket against the trader's OWN realized fill, so a bucket whose average low lands exactly on it very likely CONTAINS the trade being measured — equality is EXPECTED, not ambiguous, and the proven bound's refusal to credit it depresses that bound by construction. The at-price bucket count is carried per flip and in total so the artifact's size is visible; a low proven bound must be read against it before it is read as a market finding. **This does not apply to the paper book**, where the comparison price is a simulated bid that never traded and equality is genuine ambiguity. The two constructs are labelled distinctly in the panel and the export, with an explicit instruction not to carry a conclusion between them | probe `[R47.5]` |
| R47.5b | ⚠ **CORRECTED Aug 12 2026 — this row's sell-side prediction was falsified by measurement, and the correction is recorded here rather than by editing R48.1 silently, because you read the original.** R47.5 and R48.1 both predicted that self-comparison would bite HARDER on the sell leg, on the reasoning that a sell completing in minutes leaves almost no bucket that is not the trade itself. Measured: **zero at-price buckets on the sell side, both bounds identical.** The prediction assumed the trader's own print would DOMINATE its 5m bucket; on a liquid item it is one print among many, so the bucket average never lands on the fill. The consequence is stronger than the retraction: the two sell bounds diverge only where a bucket is at-price or live-touched, neither of which a historical replay can produce, so their identity is a property of the harness rather than a measurement of the market — and a fast-sell failure is a REAL model error, not an artifact. The panel and export both carry the correction naming the old claim | probe `[R48.1]` |
| **§48 Sell-leg calibration (user ruling, Aug 12 2026)** | | |
| R48.1 | The sell leg is graded on ITS OWN ANCHOR, at the moment the buy really finished (`logAt − sh`), against the logged sell duration — never chained off the model's own `buyDone`, because a pessimistic buy would hide the sell entirely and the sell is where 9 of 10 failures land in the 43-trip replay. Same fields as the buy: agreed, false partial, false never-sold, median share credited when wrong, both bounds, split by sell-leg duration. Assembled by `calibRowFor()` so an assertion tests the WIRING — that the sell is graded regardless of the buy's verdict — and not merely the two replay functions in isolation. ~~The self-comparison caveat is stated as HARDER here~~ — **superseded by R47.5b: that prediction was falsified by measurement.** The panel and the export now carry the correction, naming the claim they are correcting rather than swapping the copy | probe `[R48.1]` |
| R48.2 | SELL-LEG PARITY IN THE EXPORT (user-directed, Aug 12 2026). The first export built after the interrogability rule was widened to artefacts shipped the sell **aggregate alone** — a rollup without its rows, which is the same finding in a file as it is on a screen. The sell now carries everything the buy carries, per replayed flip: window offsets against the real buy-completion anchor, both verdicts, credited percentages on both bounds, the reach census, the at-price count, and the bucket-by-bucket trace with each bucket's relative price, high-side volume, credited units and reason. The comparison flips sides — a POSITIVE percentage is proven reach on a sell where it is a miss on a buy — so the reason strings say which in words rather than leaving the sign to be inferred. Same trimming rule as the buy trace, with every truncation declared in the header. A flip with no sell replay states that and why, rather than rendering an empty object | probe `[R48.2]` |
| **§49 Never blend a rate (user ruling, Aug 12 2026)** | | |
| R49.1 | The net-per-cohort rule generalised to EVERY rate — fill rate, credited share, agreement rate, hit rate. A blended figure may render only ALONGSIDE its decomposition, and beside it on the page rather than only inside an expansion the reader must open. `rateBlend()` is the sanctioned renderer and emits both together, so a caller cannot produce the blend without the split. A single-cohort rate says so rather than implying a split it does not have; an empty population renders as "no population", never as 0% | probe `[R49.1]` |
| R49.2 | Applied where rates actually render: the per-gate paper fill rate and the hours ledger's paper fill rate both carry their cohort split inline. The incident that forced it — "median 37% of intended size" was watchlist 100%, scanner 37%, slice 8.3% pooled, and read as a sizing problem across the book when the items closest to fundable were filling completely | probe `[R49.2]` |
| **§50 Fill-forecast input, and the interim soft tag (user ruling, Aug 12 2026)** | | |
| R50.1 | The fill forecast divides by REACHING flow — low-side volume from hours whose average low sat at or below the intended bid, an hour above it contributing zero rather than a little — taken as the MEDIAN of the last six hourly readings rather than a single trailing hour. Graded on 43 replayed trips: spread 518× → 24.6×, wrongly-promised fits 12 → 9. With no hourly series it falls back to the old total-flow input and SAYS which basis it used | probe `[R50.1]` |
| R50.2 | INTERIM: the fill-horizon gate is a SOFT TAG, not a hard bench. It queues as NEXT UP with the forecast and its own uncertainty stated, because on the same 43 the gate would have benched 4 of the 10 trips that actually completed. The downgrade is decision-logged with the evidence (518× range; Varrock tabs predicted 49.8h, filled 1.3h) and NAMES ITS OWN REVERSION, so an interim cannot become permanent by inertia; it logs once however many plan builds run | probe `[R50.2]` |
| R50.3 | HONEST DEGRADATION (user ruling, Aug 12 2026). ~~The old behaviour fell back to total 1h low-side flow and said which input it used~~ — **superseded: saying so is not enough.** The fallback input IS the 518× error, so an estimate built on it claims an accuracy it does not have, which is the claims-vs-computation defect. The estimator now DECLINES in two states and names what it could not compute: **no hourly series at all**, and **fewer than `EST_FLOW_MIN_HOURS` (3) readings**, where "median of the last 6 hours" is not the statistic the copy advertises. `estFillH()` returns null and `fmtEstH()` renders "unavailable" — never a bare dash, never ∞ standing in for "unknown". **Zero reaching flow is explicitly NOT a decline**: nothing printing at or below the bid for six hours is a reading, and the reading is that it does not fill | probe `[R50.3]` |
| R50.4 | REACH SHARE, reported beside the estimate, not buried. Reaching flow ÷ total low-side flow, with the count of hours that reached the bid — the diagnostic for WHY a prediction is long, and per the interrogability rule the estimate ships with what produced it. `reachFlow()` is the single evaluation returning flow, state, reach count and share together, so the figure and the account of the figure cannot come from separate reads; `reachFlowPerH()` is a thin accessor over it. Also fixes a live claims-vs-computation defect: the plan line's tooltip still described the pre-Aug-12 formula ("qty ÷ buy-side 1h flow × 15% capture") for as long as the corrected input had been live | probe `[R50.4]` |
| R50.5 | THE WINDOW COUNT IS A PROPOSED CONSTANT WITH A STATED PRICE, not a silent pick. Six hours: the drift measurement put in-window over pre-open flow at median 1.48× with IQR 0.7–2.58× and tails to 45.6×, so the median needs enough readings that one tail hour cannot carry it (at n=6 it sits between the third and fourth); six is also the shortest window spanning the whole of the shortest touch gap (5.0h), so an estimate rests on readings from the cadence period it will be judged in. **The cost, stated because a constant nobody can argue with is a constant nobody checked:** a 6-hour median needs four bad hours before it moves, so a book dying at 09:00 reads healthy in this input until ~13:00 — while the die-off tag confirms in ~2 minutes. The two disagree for up to four hours BY CONSTRUCTION, and that is the right way round: the die-off tag is a restraint and may act on thin evidence, the estimator funds things and may not. Where they disagree the tag wins. A strategy parameter; it does not move without a ruling | documented (constant + cost) · UI |
| **§51 Observed time — no claim about a period that was not observed (user ruling, Aug 12 2026)** | | |
| R51.1 | Any denominator counting time or occasions counts only OBSERVED ones and states its coverage. The escaping instance: `daysBenchedBy(id, gate, 7)` drove gate-persistence proposals reading "benched on 4 of the last 7 days" against a denominator of 7 even when the app was closed for three of them — and the 4-of-7 bar is the standard that moves gate constants. The numerator was always honest (a ledger row exists only on a day a plan actually built), so the defect was purely the denominator. `DB.obsDays` stamps each day a plan builds; `observedDaysIn()` unions that stamp with the gate ledger's own dates, so the rule works retroactively on an existing store without a migration; `daysBenchedBy()` returns the pair `{n, obs, window}` so a caller cannot render the count without its coverage. The unobserved remainder is named in words, not left to arithmetic. **The 4-day bar itself is unchanged** — that is a strategy parameter and moves only on an explicit ruling | probe `[R51.1]` |
| R51.2 | UNREACHABLE IS NOT ABSENT. Below 4 observed days the persistence bar cannot be reached at all, so reporting "none carry multi-day ledger persistence" would report an absence of observation as evidence about the gate — the never-fed-aggregate defect wearing a different hat. The funnel says which case it is in, names the coverage, and still proposes nothing. Above the bar, the "no persistence" line states the coverage its claim rests on | probe `[R51.2]` |
| **§52 Expiry is not a lift (user ruling, Aug 12 2026)** | | |
| R52.1 | Removing a restraint is deployment, whether by action, by expiry or by a rule change. A ratified CAUTION (watch-note, deflation-flag, or anything carrying teeth) reaching `validUntil` goes **lapsed**: it keeps applying — tags, sleeve refusal and haircut all still in force — and asks for one ruling. A CONTEXT record (catalyst, catalyst-update, long-catalyst, demand-context, cluster-membership) still expires on the calendar exactly as before, because it restrains nothing and so nothing is deployed when it ends; the restraint/deployment line puts it outside this rule. The lapse is decision-logged as still-applying and names the press it waits on | probe `[R52.1]` |
| R52.2 | THE CONTRADICTION CLOSED, in both directions. The standing rule is that a flagged pump caution lifts on ONE path — the user dismissing the warning record, "nothing else" — and three calendar paths contradicted it: `validUntil` deactivation, the pending-expiry sweep, and the 30-day staleness broom. A pump-defense record now does not lapse by calendar at all, pending or ratified, and `promotionWarningsFor()` drops the import-window and expiry clauses that lifted it two more ways. **Once fired it stays fired**: the anomaly leg tested a flag within `pumpWindowD` days of NOW, so a defense that had already fired un-fired itself as its evidence aged — the firing is now stamped on the record, which leaves the DETECTION rule (a strategy parameter) untouched while removing the lift. The copy stops claiming a property the code did not have: "lifts when dismissed or expires" is gone from the panel, the activation line and the record | probe `[R52.2]` |
| R52.3 | ONE walk-up line for lapsed cautions however many there are — the agreed attention price, against a budget of 7. Only the RESTRAINT-PRESERVING action is offered in bulk: "extend all 30d" keeps every caution in force, so a mistaken press costs only absence. There is deliberately **no bulk drop** — dropping lifts a caution per item and a bulk press cannot carry per-item judgment; dropping stays per-record on the desk, requires a stated reason, and is decision-logged as a deployment. Each record states where it stands (lapsed-and-still-applying, or pump-defense-deliberately-unaffected) rather than rendering a passed date with no explanation. The pending auto-dismissal survives with corrected copy: an unruled record was never armed, so it is queue hygiene, not a restraint lift, and the log says which | probe `[R52.1]` `[R52.3]` |
| **§53 Gap band: raised sample, held proposal, standing verdict (user ruling, Aug 12 2026)** | | |
| R53.1 | Per-cycle sample raised 5 → 10 so the band's verdict matures faster. The constant and the copy that describes it are asserted TOGETHER — a constant raised without its copy following is the claims-vs-computation defect, and checking only the constant would not catch it | probe `[R53.1]` |
| R53.2 | A STANDING VERDICT LINE, so the decision arrives as a report rather than as the operator's recollection: clean and contaminated trips counted apart — trips, fills and net for each — never pooled. **The scanner proposal is HELD** and is reconsidered only on clean, post-fix evidence clearing the same bar any funded item clears. While the paper book has open defects on its register, no trip closing now is clean evidence and the line says so rather than counting them; the old total-trip count would have called the band ruleable, which pooled contaminated trips with clean ones — the same defect one level up | probe `[R53.2]` |
| **§54 Contamination labelling (user ruling, Aug 12 2026)** | | |
| R54.1 | The claims-vs-computation rule applied to PROVENANCE rather than definition: a number whose input is under repair is making a claim it cannot support even when its own arithmetic is correct. Any figure computed from the paper book states that dependency where it renders and carries the book's current health state, naming the open defects. **Both directions render** — the realized streams, the market stream and the sampling counts state that they are NOT downstream, so the clean figures are identifiable at a glance rather than from memory of a conversation. A label only on the contaminated figures decays into "unlabelled means nobody checked". `PAPER_DEFECTS` is the register; an entry is removed when the defect is fixed AND the book has re-accrued evidence clear of it, not when the fix lands | probe `[R54.1]` |
| R54.2 | Applied to the four named surfaces: gate health's paper columns, per-stratum sim P&L and fill rates, the hours ledger's paper stream, and the gap-band verdict. Each names its CLEAN counterpart on the same surface rather than in a footnote — the gate's realized stream, the sampling counts, the market stream. `hoursStreamsNote()` is extracted so the labelling is assertable without a fixture that happens to carry market rows | probe `[R54.2]` |
| **§55 Prediction stamped at open (user-directed, Aug 12 2026)** | | |
| R55.1 | The paper export could not grade the estimator: it carried the OBSERVED fill time and no prediction. Every trip now stamps, at OPEN, the predicted fill hours under the reaching-flow input, the reach share and reach count that produced it, the flow figure and its basis state, and the pre-Aug-12 total-flow prediction — the last so before/after is measurable on ONE population rather than across two epochs, which is the only way to compare them without the cohort mix moving underneath. **Retrofit is impossible** — these are open-time facts a rolled 5m series no longer holds — so the regrade's sample starts accruing from the first scan after this ships. Asserted THROUGH `shadowScan`, not by calling the stamper directly, since a probe that calls the stamper passes even if the scan stops calling it | probe `[R55.1]` |
| R55.2 | The export carries prediction beside observation per trip, with the observed buy-leg fill hours and, where the buy never completed, a note that the trip belongs in the false-fit test rather than the ratio distribution. A trip opened before stamping declares itself unavailable and says why, rather than being silently absent from the regrade | probe `[R55.2]` |
| R55.3 | DIAGNOSTIC: the export carries `resolvedInMs` and `openPollSeq`. ISO stamps truncate to the second, so "resolved in under a second" could not be told from "resolved in the same second"; and the minimum-life rule is stated in POLL CYCLES, so a file that cannot show the cycle cannot show whether the rule held. A null `openPollSeq` means the record predates the causality fix, which is itself the answer to why it resolved instantly | probe `[R55.3]` |
| **§56 Epoch 2, the stale host, and the stalled generator (user ruling, Aug 12 2026)** | | |
| R56.1 | THE DETECTOR FOR THE FAILURE THAT FORCED THE RESET. Every trip in epoch 2 must carry an open-cycle stamp; one that does not was written by a build without the causality fix, which is exactly how epoch 1 was corrupted. It raises a FINDING naming the reload, not a caveat, and is silent when every trip carries one — epoch 1 was invisible until a field existed that could show it | probe `[R56.1]` |
| R56.2 | THE RESET. Clears the simulated streams — paper book, divergence ledger, rolled per-stratum outcomes — and voids exception-lane paper evidence while the RULINGS stand. KEEPS the realized streams, the market stream and the per-stratum SAMPLING history, because those record tests performed rather than simulated outcomes and clearing them would destroy real coverage to fix a simulated number. Stamps epoch 2 and decision-logs the user's stated reason verbatim. **Filtering was rejected in favour of discarding** because the divergence ledger, the rolled counters and the exception evidence are CUMULATIVE — a per-trip predicate can exclude rows but cannot un-mix a rolled total. Extracted as `paperEpoch2Reset()` so an assertion can call it; inline in `load()` it was unreachable and a seeded defect on it changed nothing the suite could see | probe `[R56.2]` |
| R56.3 | THE STALLED GENERATOR STATES ITSELF. A book that stops opening trips accrues nothing, and every aggregate goes on rendering as though it were live — opening stopped at 18:10 while trips closed until 20:46 and no surface said so. Nothing was broken: the family cooldown is 2 × FILLH(), and FILLH is the gap to the next touch, so around the evening touch it is ~19h; a family is blocked while its last trip is open OR closed inside that window, so closing does not release it. `shadowScanState()` computes the sentence from the same values the scan gates on, the paper headline renders it whether or not anything is wrong, and a long quiet spell is flagged rather than merely reported. **Not applied, noted for a ruling:** a cooldown scaling with the horizon is longest exactly when the book has the most observation time | probe `[R56.3]` |
| R56.4 | THE BUILD STAMP. A tool whose host never reloads has to notice its own staleness — this one runs as a long-lived foreground tab by design, and the consequence was five hours of trips written by a cached build. The app records `BUILD_ID` at load, renders it in the header always, and re-fetches its own URL with a cache-buster hourly to compare. Three DISTINCT states: current, stale (which also leads WHAT CHANGED with a reload line), and could-not-check — an unreachable check must never render as up-to-date. A stamp that appeared only when stale could not be trusted by its absence | probe `[R56.4]` |
| **§57 The family block, severed from the horizon (user ruling, Aug 12 2026)** | | |
| R57.1 | One constant served two unrelated jobs. CONCURRENCY (no two open trips in the same family) is a property of being OPEN and needs no duration — the open trip is the block, regardless of age. SAMPLE INDEPENDENCE (do not re-sample a family so fast that the second trip is the first trip's conditions measured twice) is a SAMPLING concern with no exposure argument behind it, since the paper book holds no capital and carries no correlation risk. Fused at `2 × FILLH()`, the sampling rule inherited an exposure rule's scale and inverted: ~19h lockout at the evening touch — the touch with the most observation time and the most to learn — against ~9h in the morning. Now an OPEN trip blocks its family and a CLOSED one releases it after a FIXED cooldown that does not move when the horizon does. Applied to the scanner cohort too, so the two cohorts do not sample at different rates | probe `[R57.1]` |
| R57.2 | THE CEILING, computed rather than assumed. A family cycles: blocked while open (about one horizon), then the cooldown, then re-samplable — so the generator can produce `24 ÷ (horizon + cooldown)` trips per family per day, and the book's ceiling is that times the family count unless the concurrency cap binds first. Both are computed, the BINDING one is named, and it RENDERS on the paper surface: a cap nobody can see is a cap nobody accounts for, and if sample size is structurally capped that is worth knowing before waiting weeks for evidence | probe `[R57.2]` |
| R57.3 | RULED Aug 12 2026 at 5h, as proposed, and decision-logged with the reasoning it was approved on. Five hours because: it is the modal touch gap (5.0 / 5.0 / 4.5 / 9.5), so a family becomes re-samplable about once per touch and re-sampling rides the same cadence as the decision it informs; it is five fresh hourly readings, which is what sample independence means for these inputs; and it is horizon-INDEPENDENT, which is the point. Shorter would correlate consecutive samples through the same hourly readings; 9.5h would reproduce the inversion | probe `[R57.3]` |
| **§58 The fill-model version stamp (Aug 12 2026)** | | |
| R58.1 | Every trip records the fill-model version that produced it. A corrected SELL model would change outcomes on trips already closed under the old one — `sellQ` decides filled-vs-forced-exit and therefore `net` — and those trips CANNOT be re-graded: the tick discards each bucket's evidence once credited (`p.pend = null`) and `touchSell`, the live observation the operative reading needs, is unrecoverable from any replay. The naive consequence is that a sell fix forces another epoch; it does not, and believing so is the reasoning error that made epoch 1 unrecoverable. Epoch 1 could not be partitioned only because nothing recorded which build wrote each trip — the moment `openSeq` existed, a corrupt population became identifiable by a field. Generalised: a model change PARTITIONS the book by `fillModelV` instead of invalidating it, and the two versions are never pooled. Bumped on any change to what a bucket credits; it is not a build id, because the version is a claim about the MEASUREMENT rather than about the code | probe `[R58.1]` |
| **§59 The sell discriminator (user ruling, Aug 12 2026)** | | |
| R59.1 | Every failed sell gets a COMPUTED classification — dilution / mislocated-window / neither-fits — from the per-bucket figures carried beside it, never a human reading a trace. DILUTION: small negative gap with real volume, meaning the bucket average sat inside the intra-bucket range and prints above the ask are plausible. MISLOCATED WINDOW: a large gap SUSTAINED across the window — a window containing a dip is not a wrong window, which is what the sustain share separates. **NEITHER-FITS is a real available outcome**, not a residue: a gap between the bands surfaces as an unclassified pile, and prints-with-no-volume surfaces by name as an ILLIQUIDITY finding, so a third mechanism is not forced into one of two buckets | probe `[R59.1]` |
| R59.2 | Counts render always; PERCENTAGES only at n ≥ 8, and the surface says which it is doing. Under the bar a confident split would be the defect the discriminator exists to prevent; an empty population says there is nothing to classify rather than rendering a zero split. Thresholds are PROPOSED not ruled — dilution within 1.0% of the ask (a sell sits one tick inside the spread and the ROI floor is 1.2%, so 1% is inside the item's own intra-bucket range), mislocation at 5.0% sustained over 70% (≈4× the ROI floor, far outside noise, and most of the window rather than a couple of buckets) | probe `[R59.2]` |
| R59.3 | The live book RETAINS a bounded sell-leg trace, including the buckets that credit NOTHING — those are the informative ones. Without it the live population could not be classified at all: the tick nulls `p.pend` once credited, so no per-bucket evidence survived. Instrumentation only — it changes no credit and no verdict, so `FILL_MODEL_V` does not move | probe `[R59.3]` |
| R59.4 | The split renders per population and is NEVER pooled: calibration replays real completed sells where any verdict but sold is a model error; the live book holds simulated sells that never completed. Each carries its own n. The structural asymmetry is stated as a FALSIFICATION TEST rather than a footnote — the live book's sell leg accrues forward from `buyDone` and has no window to place, so mislocation cannot occur there by construction; dilution in both populations confirms the credit rule, while mislocation in calibration with the live book failing anyway means two distinct defects. Renders in panel and export with each case's figures | probe `[R59.4]` |
| R59.5 | THE DERIVATION IS FROZEN (user ruling, Aug 12 2026). 1.0% was derived once from a 1.2% ROI floor and is now a fixed constant that does NOT track it. A threshold defined as a function of a strategy parameter makes its own history incomparable — last week's "dilution" and next month's would mean different things, and a split measured across a floor change would pool two questions with nothing saying so. A classifier has to hold still to be an instrument. `sellThresholdDrift()` REPORTS divergence between the frozen threshold and the floor it came from; it never corrects it, because the point of freezing is that the drift prompts a ruling rather than triggering an adjustment | probe `[R59.5]` |
| R59.7 | THE BUY-LEG TRACE (user ruling, Aug 13 2026) — R59.3's shape on the other leg, for a sharper reason. `shadowCredit` stores `Math.min(qty, …)`, so a per-bucket term LARGER than the trip's own size is clipped and leaves no record: OVER-crediting, the one direction that would flatter every fill rate in the book, was structurally unobservable. Asked whether the book had ever over-credited, the only honest answer was "cannot be reconstructed — the per-bucket inputs are discarded at credit time — and bounded away from catastrophe only by the fact that no trip ever filled inside a single bucket". The trace retains each bucket's low, low-side volume, touch flag and **the credit BEFORE the clamp**, including buckets that credit NOTHING, since those are what say the offer stood and the tape did not come. `buyAbsorption()` reports credit offered against trip size, and the surface reports the median ratio; a trip with no trace reads UNKNOWN rather than as zero absorption, because absence of evidence must not read as evidence of restraint. The cap is DERIVED from the horizon (12 buckets an hour plus the deferred-credit lag) so it can neither truncate the window silently nor become a guard its own upstream limit forbids. **Instrumentation only** — no credit changes, no verdict changes, `FILL_MODEL_V` does not move | probe `[R59.7]` |
| R59.6 | IS A GLOBAL PERCENTAGE THE RIGHT SHAPE? 1% below the ask means something different on a 15%-spread item than on a 1.5%-spread one. The same population is classified a SECOND way — threshold as a fraction of the item's own realized spread — and the two are compared: the spread distribution across the failed-sell population, and how many cases would reclassify. Reported only once n permits, and INSUFFICIENT below the bar rather than estimated. Nothing changes on the strength of it; the absolute threshold stays live until ruled otherwise | probe `[R59.6]` |
| **§60 Surface noise — relocation must not strand (user ruling, Aug 12 2026)** | | |
| R60.1 | 79 persistent blocks, 22,608 characters, ~594 phone lines against a 7-decision walk-up budget. Standing truths and teaching notes move behind `teach()`, whose disclosure content IS the original text — that construction is what makes the claims-vs-computation rule survive the edit, since the tap cannot fail to reach what it opens. A generic scan asserts no `details.teach` on any surface has a summary without a body, so any future relocation inherits the check | probe `[R60.1]` |
| R60.2 | CONDITIONAL WARNINGS ARE NEVER DEMOTED. A warning always on screen is wallpaper, and wallpaper is how a real warning gets missed — so the load-bearing half stays visible while only its explanation moves. Asserted specifically: the correction marker renders OUTSIDE its disclosure (it is a landing path and the corrections rule binds), and the stall line's state renders always while its rationale is gated on the stall | probe `[R60.2]` |
| **§61 Verdict-first surfaces (user ruling, Aug 12 2026)** — *rows written retroactively Aug 13 2026: the assertions shipped tagged and the rows were never added, so six ids reported PASS against nothing. See the pairing check below (R6.2)* | | |
| R61.1 | EACH SURFACE ANSWERS ITS OWN QUESTION FIRST, in one sentence, before any machinery renders — "Does my edge survive honest fills?", "Are my gates too tight or too loose?", "Where in the market is there edge I'm not fishing?". During accrual the answer is a PROGRESS statement carrying the count, the bar and the rate at which it is filling, never an empty table: a surface that cannot answer yet says so and says when it will be able to | probe `[R61.1]` |
| R61.2 | THE COUNT OF ELEMENTS ABOVE THE FIRST DISCLOSURE is small enough to hold in mind (≤3 on the paper surface, asserted structurally by counting children before the first `<details>`). The verdict-first restructure is worthless if the fold line simply moves down the page | probe `[R61.2]` |
| R61.3 | NOTHING RENDERS AN EMPTY SHELL — no headers, no zero-row tables, and no caveats about numbers that do not exist. A caveat attached to an absent number is noise that reads as a warning. Asserted on the prospecting verdict with its ledger emptied: it must read as progress, carry no `<table>` or `<th>`, and not recite the stratum-classifier caveat | probe `[R61.3]` |
| R61.4 | CONTAMINATION AND STALL STATES ARE THE STATED EXCEPTION and stay ABOVE the fold at full weight. Everything else may fold; a provisional-answers warning and a stalled-generator line may not, because a book that is not opening invalidates everything below it and a fold is how that gets missed | probe `[R61.4]` |
| R61.5 | A FOLD'S LABEL SAYS WHAT OPENING IT WOULD TELL YOU — "Regime curves — would a looser or tighter ROI floor have done better?", never "table" or "details". The label is the only thing a reader has to decide whether to open it, so a label naming the element type spends the disclosure's whole budget saying nothing | probe `[R61.5]` |
| R61.6 | ONE TAP OPENS A FOLD AND THE ROWS INSIDE ARE THEN REACHABLE — asserted structurally (starts closed, rows live inside it, one tap opens it) rather than by measuring layout, since whether a closed `<details>` zeroes its children's boxes is engine-dependent in headless and asserting on that would test the renderer instead of the restructure. The interrogability rule binds after the fold exactly as before: a fold that strands its rows is the violation this restructure was most likely to produce | probe `[R61.6]` |
| **§62 The fixed paper horizon, the fill curve and the cadence read (user ruling, Aug 13 2026 — supersedes the gap split ruled the same day, which superseded the duration split ruled that morning)** | | |
| R62.1 | THE PAPER BOOK STOPS PREDICTING THE OPERATOR'S RETURN. Real offers have no deadline; the horizon was an artifact of needing the simulation to end, and tying it to the walk-up made the book answer "does this fill before he wanders back", which is contingent on him and cannot converge — three verdicts came out of the same 75 trips in one day, each from fixing a LABEL rather than from new data. **`PAPER_HORIZON_H = 6`**, from the only uncensored window the book had: of 26 trips ever given more than 6h, the 14 that filled did so in a median 1.73h, 92% inside 4h, none past 5.19h. 4h sits on the steep part of that curve; 6h clears the observed maximum and buys nothing after. It costs sample rate — 24/11 = 2.18 trips/family/day against 2.85 at the old 3.41h median. **The SIZE moves with it, not just the deadline**: `shadowSizeFor`, `paperCapacity` and the paper caller of `scanScreenTier` all read the fixed horizon, since a fixed deadline with schedule-derived sizing is half-fixed and invisible. `scanScreenTier` takes the horizon as a parameter so its LIVE caller is untouched — the live plan's own horizon still moves with the schedule, because this ruling is about the simulated book only. `FILL_MODEL_V` bumps to 2 so the two populations never pool | probe `[R62.1]` |
| R62.2 | THE 1-HOUR OUTPUT FLOOR IS DELETED and input coalescing owns the promise. The floor existed so a placement made minutes before a configured touch was not sized for nothing, and it fired constantly because a NOMINAL touch was imminent while the operator was hours away — 1.00h priced against a 10.63h actual return. Under a fixed horizon the paper book never reads it, and the degenerate input it guarded (two walk-ups moments apart) is handled where it arises: **sessions coalesce at `SESSION_COALESCE_MIN` = 10 minutes**, ruled as the only guard. Keeping both would be a guard whose trigger another layer forbids. Session boundaries — "has he been back since?" — read the walk-up log rather than the schedule, and return **null with no history**, because "no" and "cannot say" are different answers and the schedule version could not express the second | probe `[R62.2]` |
| R62.3 | THE FILL CURVE, WITH ITS CENSORING RENDERED. Horizon becomes a MEASUREMENT rather than a setting: one uncensored 6h trip that filled at 1.9h contributes a point to every hour below it, where three censored populations at 2h/4h/6h would contribute three points — which is why one horizon was ruled rather than several. Each cell's denominator is trips whose OWN window reached that hour; a shorter trip is **silence at that hour, never a failure**. Cells render `fill% / trips at risk`, cohorts are never pooled into one curve, and the surface NAMES the hour past which the population thins below `FILL_CURVE_MIN_AT_RISK` rather than drawing a confident line into a region nothing observed | probe `[R62.3]` |
| R62.4 | THE CADENCE READ — two independent measurements multiplied. The fill curve is a MARKET property and converges; the operator's walk-up gap distribution is BEHAVIOURAL and moves only when the routine moves. Composed, they answer "given how long you actually leave offers standing, what fraction would fill" — the cadence question, computed from a converged market result instead of simulated against a predicted return, which is the property the old design lacked. Uses the **empirical distribution, never a statistic**: the same routine contains 2.15h returns and 10.63h ones and they land on very different parts of the curve. Per cohort. The **time-of-day independence assumption is stated on the surface**, with the split that would falsify it named. With too few gaps it says so rather than substituting a cadence nobody keeps | probe `[R62.4]` |
| R62.5 | GAP LABELLING RETIRED. With no schedule pricing placements, "evening" and "afternoon" as scheduled gaps mean nothing — they were intervals of a cadence the book no longer uses. The honest residue is TIME-OF-DAY OPENED in fixed six-hour buckets, a property of the market clock that does not move when a setting does. The per-gate table's second dimension becomes the FILL MODEL, not the clock: the split exists so two different experiments never pool, and under a fixed horizon that is exactly the v1/v2 partition — keying it on time-of-day would also make every gate row depend on the hour the suite runs | probe `[R62.5]` |
| R62.6 | THE TRADE IS STATED ON THE SURFACE, in one line, rather than left to be forgotten. Buys: results that converge, comparability across trips, and an answer to how long an offer needs to stand. Costs: the book no longer models the operator's cadence at all and cannot say whether a placement made at a given time would fill. The panel says both | probe `[R62.6]` |
| **§63 Exception lane — observed span and the near-miss ranking (user ruling, Aug 13 2026)** | | |
| R63.1 | THE SPAN CLAUSE COUNTS OBSERVED DAYS. `EXC_MIN_DAYS` stays 7 and stays epoch-scoped — it measures the DISPERSION of the evidence, not the age of the item or the book, and that is the right property: five trips from one lucky afternoon must not waive a gate. What was wrong is what it counted. Wall-clock let five trips straddling a five-day closed laptop read as seven days of evidence; days nobody watched are not evidence either way, and this is the standard that puts real capital on a waived gate. `observedDaysSpanned()` intersects the trip span with the observed-day ledger and returns a DURATION — the `−1` is load-bearing, since a date set is inclusive and the raw count would have made the new clause LOOSER than the wall-clock one it replaced. `spanD` is kept and rendered beside it: the difference between the two IS the coverage statement, and `spanDaysTxt()` returns the pair so no caller can render the numerator alone. **Both ledgers prune at 30 days**, so the copy states the ceiling where it renders rather than letting a 60-day record silently report 30 | probe `[R63.1]` |
| R63.2 | THE NEAR-MISS RANKING, and it REPLACES the bar-recitation line rather than sitting beside it. The lane rendered its own rule back at the reader — "no candidates clear the evidence bar right now: ≥5 trips, ≥60% fill, profitable, ≥7 days, no red flags" — naming no item, no clause and no distance, so the operator's only way to learn what was blocking was to have someone read the code. That is the F18 defect on this surface. Every item that COULD qualify is scored against all seven clauses, including the two that were never in the constant list and so were invisible: **a named gate to waive** (an item whose trips cleared every gate has nothing to except) and **a free probation slot**. Sorted closest-first, binding clause named. **Distance is only meaningful where a clause accrues**: trips and observed days close on their own and carry an ETA at a rate the store can evidence; fill rate and net can move either way and a red flag or missing gate does not close by waiting, so no ETA is projected onto them. **Population is every item that could qualify, not every watched item** — discovery-slice and gap-band trips land on UNWATCHED items, so a watchlist-only list would hide real candidates; those render marked. Reports standing only; the grant is still the user's press and the bar is still a strategy parameter | probe `[R63.2]` |
| **§64 Scan 10 / scan 11 first-run findings (user ruling, Aug 13 2026 — `audits/AUDIT-2026-08-13b-scans-10-11.md`)** | | |
| R64.1 | THE CLEARANCE IS STAMPED BY A WRITER, not inferred by a reader. `PAPER_DEFECTS` is a build-time array and `DB.paperDefectsClearedAt` is persisted runtime state; nothing joined them, so `paperCleanFrom()` returned `DB.paperDefectsClearedAt \|\| 0` against a field no code ever wrote. `paperDefectsSweep()` runs once per load (the `paperEpoch2Reset` precedent), compares the running build's register against `DB.paperDefectsSeen`, and **stamps `paperDefectsClearedAt` and increments `paperClrGen` at the moment a defect leaves** — decision-logged with the ids struck. It is EXTRACTED rather than inline in `load()` so an assertion can call it | probe `[R64.1]` |
| R64.2 | **A BOUNDARY NEVER DEFAULTS TO ZERO.** An unset clearance means **no** trip is clean, not every trip. `paperCleanFrom()` returns `null` when the register is non-empty **and** when the stamp is absent; it can never return 0. The defect this closes: the day the last register entry was struck, `from` became 0, `p.t >= 0` matched every trip ever recorded, and the whole book — including every trip that closed while the defect was live — would have been reclassified as clean evidence in one step with nothing said on screen. Absence of evidence about cleanliness must never read as the permissive answer | probe `[R64.2]` |
| R64.3 | **CLEANLINESS IS A POPULATION, NOT A DATE FILTER** — the FILL_MODEL_V precedent applied to contamination. Every trip stamps `clrGen` at open, centrally in `stampPrediction()` so no entry path can forget it (the regime-stamping defect's lesson), and `paperTripClean(p)` requires the trip's generation to equal the current one **as well as** `p.t >= from`. A trip with no stamp predates the scheme and is contaminated — the conservative direction. Trips either side of a clearance are different experiments and are never pooled | probe `[R64.3]` |
| R64.4 | **THE HOLD CANNOT RIDE THE COMMIT THAT SUPPLIES ITS EVIDENCE.** Gap-band readiness counts trips carrying the *current* clearance generation, and at the instant a clearance is written no trip carries it — so the build that strikes the last defect releases nothing, and the bar can be met only by trips opened afterwards. Under the old timestamp filter the same commit both lifted the hold and retro-supplied the evidence the hold was waiting for. The held line states why the trips already present do not count | probe `[R64.4]` |
| R64.5 | **THE CAUSALITY PROMISE MOVES TO THE LAYER THAT CAN ENFORCE IT.** `reconReplay`'s `if (bt < p.t) continue` was unreachable — the caller clamps the window to at least `p.t`, so every bucket reaching the loop already satisfied it — and the Aug 12 instant-trip audit reasoned from it. Per the dead-safeguard ruling the guard is DELETED and the promise stated where it is enforceable: `reconWindowStart(p)` returns an explicit `Math.max(p.t, p.lastObs)` floor rather than relying on `lastObs >= t` happening to hold, and the assertion points at it. A promise that depends on another field's invariant is a coincidence, not a promise | probe `[R64.5]` |
| R64.6 | **THE 40% STATISTICAL CAP STATES ITS REASON WHERE THE PROPOSALS RENDER.** The cap withholds candidates — an automated decision — and its explanation was composed into `S.cohNote` when the cap shipped and read by nothing, so the withheld items were an absence with no explanation anywhere. It renders in `#cohCapNote`, immediately above the queue it explains, because a withheld candidate's entire visible effect is that it is missing from that list | probe `[R64.6]` |
| R64.7 | `S.anomMkt` REMOVED rather than wired: written and never read, and the index reading it duplicated is already persisted as `DB.volIndex` and rendered with its control rows. Wiring it would have created a second source for one number, which is the redundancy finding rather than the fix | verified by inspection |
| **§65 A difference names what it is a difference from (user ruling, Aug 13 2026)** | | |
| R65.1 | **A DELTA-LABELLED FIGURE RENDERS BOTH OPERANDS, IN THE SENTENCE** — not in a tooltip, because the number is read in the line. The incident: the paper vitals tile rendered `tight − current` as *"+2.64m vs current"*, a DIFFERENCE presented as a LEVEL, while the drill underneath showed −2.40m, which is `current` itself. **The headline and its own decomposition disagreed in sign and in magnitude and both were arithmetically correct** — nothing was wrong with either number, the sentence was wrong about which number it was. `deltaVs(val, base, valLab, baseLab)` is the sanctioned renderer, the `rateBlend()` shape applied to differences: it cannot emit the difference without the base, so a caller physically cannot produce the ambiguous form. `paperDivLead()` is extracted from the tile because the tile drives `buildPlan`/`renderDeploy` first and re-stamps the ledger, so an assertion against the rendered tile would test the accrual rather than the sentence | probe `[R65.1]` |
| R65.2 | **WHAT SEPARATES THE REGIMES RENDERS AS A STANDING SECTION**, not on request. Three curves with three sums invite exactly one question — which trips are in one and not the other — and answering it used to mean downloading the export and grouping by hand. For each adjacent pair: both nets with their trip counts, the separating trips, and a drill to item / cohort / **ROI at open** (the post-tax figure membership was decided on) / outcome / notional / net. **Concentration leads the reading**: top-one and top-two share of GROSS movement, never of a signed net, and where one trip carries the callout share the copy says to read it as a fact about those trips rather than about the floor. **Identical regimes are reported too** — no trip in the book sits in the band between them, so the tighter setting is INERT on this evidence rather than vindicated, which the curves alone cannot show | probe `[R65.2]` |
| **§66 Sell discriminator — first-run findings (user ruling, Aug 13 2026)** | | |
| R66.1 | **NO PRINT AT ALL IS THE STRONGEST ILLIQUIDITY CASE, not an ambiguous one.** `classifySellFailure` filed "no high print in ANY bucket" as `unclassified` while the *weaker* condition below it — prints, but no volume — got the `no-liquidity` name. So `ofWhichNoLiquidity` reported **0** while 2 of the 10 "neither fits" cases had no high print across 48 buckets. **An absence filed as an ambiguity**: the pile meaning "we could not tell" must never absorb the cases meaning "there was nothing there" | probe `[R66.1]` |
| R66.2 | **A FALSIFIED PREDICTION READS AS A RESULT.** The recorded claim was that dilution dominates in BOTH populations, making the credit rule the whole story and one fix enough. Measured: it dominates calibration (2 of 3) and not the live book (2 of 17). That case used to fall through to "the split does not match either predicted outcome" — true, and saying nothing. The verdict now names the claim, the measurement that beat it, and what follows: **two distinct defects, each needing its own fix**, and the live book cannot mislocate by construction so a window fix would leave it untouched | probe `[R66.2]` |
| R66.3 | **THE SHAPE REPORT SHIPS ITS ROWS.** `wouldReclassify` shipped as a bare count while the per-case spread it was computed from was exported nowhere — a rollup without its rows, in a file. Every exported case now carries `quotedSpreadPct`, `spreadRelativeBandPct`, `gapAsShareOfSpread`, `classUnderSpreadRelative` and `wouldReclassifyUnderSpreadRelative`; the report carries the moving cases by name and the class counts under the relative form. `sellRelClassOf()` is extracted so the report, the export and any assertion read ONE implementation — inline, the export could only carry the count and a probe could only re-derive it | probe `[R66.3]` |
| R66.4 | **THE SPREAD-RELATIVE DILUTION BAND IS RATIFIED AND LIVE** (user ruling, Aug 13 2026). `SELL_SPREAD_REL_FRACTION = 1/3` of the item's own spread replaces the flat 1.0%, because the phenomenon is proportional and a flat band did different work across a 3.28%–19.17% spread range. Three of 16 cases move, all wide-spread items whose gap is a fifth to a quarter of their own spread. The fraction was chosen so an item whose spread equals the ROI floor lands near the ruled 1.0% — the two agree on the population the absolute figure came from and diverge only where the question is whether they should. Decision-logged. **The retiring absolute band is computed alongside as `absCls` and rendered beside the live one until 2026-08-20**, so the two can be seen disagreeing in the wild before it goes; it is read for no verdict, and the panel opens to the disagreeing cases with each item's spread, gap and band. The retirement is date-driven and self-expiring — no flag day to remember | probe `[R66.4]` |
| R66.5 | **THE LOCALISATION RENDERS IN THE WORDS IT WAS RULED IN**, above the split rather than in a caveat: *every real logged sell completed; 17 simulated sells with a complete buy and a fully observed horizon did not.* It rules OUT window, horizon and observation coverage by construction, which is worth more than any count below it, and localises the defect to the live sell credit path | probe `[R66.5]` |
| R66.6 | **NO THIRD CLASS** (user ruling, Aug 13 2026). The "neither fits" pile was a **threshold artifact, not a mechanism**: its cases sat at 0.13–0.74 of each item's own spread — inside what the spread explains — while the mislocated pile ran 0.40–2.57, beyond it. **Fault A is the mechanism for the whole pile.** The surface records that the discriminator did its job *by refusing* to name a mechanism for cases that turned out to be one already on the register, and that a large residue was evidence about the THRESHOLDS rather than about the market. Asserted in both directions per the eleventh face — the correct claim required and the contradicting one forbidden | probe `[R66.6]` |
| R66.7 | **THE LIVE SELL TRACE SHIPS WITH ITS CLASSIFICATION.** R59.3 built the per-bucket trace precisely so the live book could be classified, and the export carried the classification without it — so per-case medians were all a reader had, and a median cannot recover a per-bucket distribution. **Third occurrence of a rollup shipping without its rows and forcing a derivation by hand**, which is the standing gap the interrogability rule binds exports against. Every failing live sell now exports `sellTrace` in the calibration trace's own idiom — same field names, same reason vocabulary, so the two populations read alike — with each bucket's minute, signed gap against the ask, high-side volume and credited units, **including the buckets that credit nothing**, which are the informative ones. **No trim branch exists**: `SELL_TRACE_CAP` (48) sits below `CALIB_EXPORT_TRACE` (60), so the trace is complete by construction and says so; writing a head/tail trim here would reproduce the dead-guard shape knowingly | probe `[R66.7]` |
| **§6 Verification** | | |
| R6.1 | Probe report cross-references this file (`===REQS===` section from `[R#]` assertion tags) | the report itself |
| R6.2 | THE PAIRING IS CHECKED IN BOTH DIRECTIONS, and a failure either way fails the suite (user ruling, Aug 13 2026). **A tag with no requirement:** an assertion reporting `[Rxx.y]` where this file has no such row and cites it nowhere — the report printed "REQ PASS R61.1" for six ids that did not exist, and a requirement that does not exist cannot fail, so the ledger was lying in the direction it was built to prevent. **A requirement with no assertion:** a row citing `` probe `[Rxx.y]` `` that no assertion reported — §31's withdrawn rows went on claiming coverage after their assertions were deleted with the feature, and R35.4 cited one of them; that is the seasoning-gate shape, a spec claiming an implementation that is not there. Rows verified by inspection, UI or documentation cite no probe tag and are exempt by construction. Runs in `tools/probe/reqpair.sh` rather than in the page, because the check needs to read this file and the in-page suite cannot — which is precisely why the gap survived. It appends a `===PAIRING===` section and **rewrites the report header**, so `head -1` never reads PROBE-PASS while a pairing failure stands | `tools/probe/reqpair.sh`, run by `run.sh` |
