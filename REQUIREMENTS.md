# REQUIREMENTS.md — thesis sleeve + analyst intelligence (consolidated build, Aug 10 2026)

Every requirement from the ruled spec, with a stable ID. The probe suite tags its
assertions with these IDs (`[R1.2]` in the assertion name); the probe report ends with a
`===REQS===` section listing per-ID PASS/FAIL derived from those tags — that section and
this file cross-reference each other. IDs marked **UI** are verified by inspection
(rendering/copy the headless suite exercises only incidentally); everything touching
money math, budgets, or ratification state carries probe tags. No silently dropped
gates: a row in this table without a probe tag says so out loud.

| ID | Requirement | Verified by |
|---|---|---|
| **§0 Recalibration** | | |
| R0.1 | T1 budget 100m→60m, T2 50m→30m; sleeve budget 60m carved from working capital; shadow reserve untouched; decision-log entry "resized to evidence…" | probe `[R0.1]` |
| **§1 Thesis sleeve** | | |
| R1.1 | Sleeve budget disjoint from tier pools and allocator; governs NEW entries only; migrated legacy holds excluded from budget and concurrency count | probe `[R1.1]` `[R1.1a]` |
| R1.1b | Crimson kisten / Elder maul holds migrate into the sleeve as legacy positions with their cost bases | probe `[R1.1a]` |
| R1.1c | Max 3 concurrent new positions (setting) | probe `[R1.1c]` |
| R1.2 | Mandatory fields — no save without item, qty, entry/cost basis, named catalyst + window, one-sentence thesis, exit ladder ≥2 rungs (trigger + fraction each), invalidation | probe `[R1.2]` |
| R1.2b | Ladder edits allowed but decision-logged with required reason | probe `[R1.2b]` |
| R1.3 | Monitoring: price vs entry and vs each rung, days held, catalyst countdown; mark-to-market de-emphasized, "judged at exit, not at the mark"; per-position catalyst-phase buttons, user-flagged only | UI (panel copy) + probe `[R1.4]` (phase→rung path) |
| R1.4 | Rung triggers: price within 5% (setting), date within 7d, or flagged phase → SELL card (qty from fraction, guidance = current instasell); logging the fill records it against the rung; realized P&L at exit vs cost, net of tax | probe `[R1.4]` `[R1.4b]` |
| R1.5 | Weekly SLEEVE section: manual thesis-health light (never inferred), realized exits, sleeve stats fully separate from flipping metrics; rides Export JSON **Amended Aug 11 2026 (R36.1):** the weekly SLEEVE section moved to the Sleeve tab, beneath the positions it describes — the thesis-health lights are only settable there, so it is machinery rather than a report about machinery. The review no longer carries it. | probe `[R1.5]` (separation + export) · UI (weekly section) |
| R1.6 | Guardrails: no sleeve position in current flipping inventory; full budget requires closing a position (no pool/reserve raids); update advisories informational on sleeve items | probe `[R1.6]` (mandate + budget refusal) · UI (Wednesday advisory) |
| **§2 Candidate engine** | | |
| R2.1 | Catalyst calendar (name, window, affected items/clusters); entry watch vs 90-day baseline; accumulation signature; DISCOUNTED vs RAMPING | probe `[R2.1]` |
| R2.2 | Weekly anomaly scan: >1m items, sustained price+volume rise 2–3 weeks, no linked catalyst, "reason unknown — no thesis, no position", max 5 flags/week by strength | probe `[R2.1]` (the scan's verdict math IS `slvSeriesStats`/accum, asserted there); the ≤5 cap and >1m pre-screen are code-fixed (`slice(0,5)`, `px > 1e6`) and network-bound, so not probe-asserted — stated here, not silently dropped · UI (copy) |
| R2.3 | Cluster crossover: catalyst-linked set moves, member lags → laggard flag ("same thesis, unpriced member") | probe `[R2.3]` |
| R2.4 | Ranking = discount depth × catalyst proximity × exit liquidity; warn when intended position > 15% of daily gp volume ("a thesis you can't exit isn't a trade") | probe `[R2.4]` |
| **§3 Analyst briefing (desk side — not probe-testable in the tracker)** | | |
| R3.1 | /briefing skill exists; BRIEFING.md defines sources (official news, wiki upcoming, r/2007scape, dev blogs), politeness, per-claim citation | inspection: BRIEFING.md, .claude/skills/briefing/SKILL.md |
| R3.2 | intelligence.json: typed records with sources, confidence tier, item ids, direction, VALID-UNTIL, contrary evidence, priced-in check; no source no record; dated BRIEF.md with covered range | inspection: BRIEFING.md contract · tracker enforces at import, probe `[R4.1]` |
| R3.3 | Standing rules: never launder rumor into fact; no proposals for flipping-inventory items; nothing requiring reserve/budget raids | inspection: BRIEFING.md · import annotation probe `[R4.1b]` |
| **§4 Intelligence layer** | | |
| R4.1 | Import reads intelligence.json to a ratification queue (thesis, sources, confidence, items); no-source and malformed records rejected; nothing activates unratified | probe `[R4.1]` |
| R4.1b | Flipping-inventory conflicts annotated at import; sleeve refuses those items as candidates | probe `[R4.1b]` |
| R4.1c | Ratify / edit-then-ratify / dismiss, all decision-logged; dismissals absorb same-type variants at Jaccard ≥0.6; resurface only on material change (confidence upgrade / direction flip) | probe `[R4.1c]` |
| R4.2a | Catalyst records create/update calendar entries in place — never duplicates | probe `[R4.2a]` |
| R4.2b | Cluster-membership proposals queue in the cluster ratification queue — membership never recomposes silently **SUPERSEDED Aug 11 2026 (R36.2, user ruling):** coherence membership ADDS now apply automatically at this same evidence bar — membership is mechanical bookkeeping about correlation, not a capital decision, and exposure caps and every gate still bind, so what changed is who presses rather than what qualifies. Coherence DROPS still queue: an add tightens a basket's exposure cap (restraint), a drop loosens it and can free capital the cap was withholding. Whole-basket CANDIDATE ratification (a new basket proposed by the detector) is untouched and still requires a press. | probe `[R4.2b]` |
| R4.2c | Plan lines carry advisory ⚡ tags; never bench or resize by default; optional per-record 0.5× haircut toggle applied after all caps, "sized down: news risk (my ruling)" inline | probe `[R4.2c]` |
| R4.2d | Deflation flags: standing sell-on-drop tag; sleeve refuses those candidates with that reason | probe `[R4.2d]` |
| R4.2e | Wednesday advisory names what's shipping + watchlist/sleeve blast radius; generic line when no intel | UI (day-dependent; formula shared with probe-tested `wedClusterAdvisoryText` pattern) |
| R4.3 | Intel cannot touch: blacklist, proven-loser bench, realized metrics, gate constants, reserve; conflict copy "intel bullish; realized history … — history wins" | probe `[R4.3]` |
| R4.4 | Expiry archives with gradeable outcomes (right/wrong/unclear); BRIEFING SCORECARD hit rate by confidence tier in the weekly review | probe `[R4.4]` |
| **§5 Walk-up integration** | | |
| R5.1 | RULINGS PENDING block after execution items — one line per pending item across all queues with inline ratify/edit/dismiss/snooze 3d | probe `[R5.1]` |
| R5.2 | Cap 5 lines (setting), priority expiring intel → sleeve-actionable → cluster → audits, overflow "+N in queues" | probe `[R5.2]` |
| R5.3 | Staleness ladder: >14d default-action suggestion; >30d auto-dismiss logged "expired unruled — resurface only on material change" | probe `[R5.3]` |
| R5.4 | Header badge with pending count on every tab; no modals, nothing blocks execution | probe `[R5.4]` |
| R5.5 | Briefing reminder: last briefing >7d OR update Wednesday passed since it (Wednesdays assumed — no cheap endpoint); dismissible per occurrence, returns on next trigger; tracker cannot run the briefing itself | probe `[R5.5]` |
| **§7 Manipulation defenses (ruled Aug 10 2026)** | | |
| R7.1 | Source tiers T0–T3 stamped per record (best cited tier); T3 adversarial by default — never the sole basis of a catalyst/thesis; no catalyst record without a T0/T1 primary (enforced at emit AND at import); tiers/communities survive export | probe `[R7.1]` · BRIEFING.md (emit side, inspection) |
| R7.2 | Incentive heuristic: buy rec / price target / urgency framing = PROMOTION, emitted only as a `promotion-warning` record with direction=caution, never buy (tracker coerces regardless) | probe `[R7.2]` · BRIEFING.md (inspection) |
| R7.3 | Pump fingerprint: anomaly flag + promotion record within 14d (setting) → SUSPECTED PUMP; promoted + thin books (gp/day floor, setting) → same regardless of anomaly. Escalation: benched from sleeve candidacy with the reason, seed-style caution cap in the plan (half-size — asserted; **one slot — asserted Aug 19 2026, see below**; wins never graduate it while flagged — asserted), posture-is-absence copy on every surface; dismissing the warning lifts it **COVERAGE CORRECTION, Aug 19 2026 (adversarial pass):** the one-slot limb of this row cited `[R7.3]` and **no assertion exercised it** — `catSlots`, `seedSlots` and `blockedSeed` appear nowhere in the suite. A row citing coverage it does not have is exactly the defect the both-directions pairing check was built for, and the pairing check could not see it because the row DOES carry assertions for its other limbs. The claim was corrected rather than left standing, and **the assertion was written on 2026-08-19**: two items in one caution category and different families fund one and hold the other, with the category and the slot count named in the hold reason, plus a third item outside every category funding alongside — so the assertion cannot be satisfied by a plan that merely funds one thing. `GATE.seedSlots === 1` is pinned in the same condition, because a cap asserted only by its effect leaves the constant free to move | probe `[R7.3]` (half-size, wins-never-graduate, escalation, dismissal, **and the one-slot limb, Aug 19 2026**) |
| R7.4 | Scorecard grades by source tier and named community, not just confidence — "has T3 ever produced a correct, non-priced-in call" is measurable | probe `[R7.4]` |
| R7.5 | Human-layer rule, permanent in BRIEFING.md: never join, monitor, or fetch invite-gated trading discords or paid signal groups; public visibility is a minimum bar, not a guarantee | inspection: BRIEFING.md (a rule about the analyst's own conduct — nothing tracker-side to probe) |
| **§8 Gate-health vocabulary (ruled Aug 10 2026)** | | |
| R8.1 | "die-off detected" ledger tag: a volume-floor bench whose binding side is the confirmed 5m sample (≥5 units, 2 consecutive refreshes) against a 1h book that PASSES the floor writes an extra gate-health ledger row `die-off detected` (item·gate·day dedup, 30d retention, same track-record machinery as any gate); the bench copy names the tag inline | probe `[R8.1]` |
| R8.2 | Each first tag opens a 24h episode ({d,id,t,rec}); recovery = confirmed healthy 5m sample (≥5 units at/above the floor) — a quiet window is NOT recovery; continued fade never stacks episodes; weekly audit reports recovered-within-24h (median hours) vs still-faded-at-24h vs watching, numbers only — the twitchy-vs-trust verdict stays the user's | probe `[R8.2]` |
| R8.3 | Concur-recommended lane: proposals defending a standing rule against softer data batch under their own header and are excluded from S.depProposalCount (rulings-pending); the full ruling flow is reserved for change-proposals | probe `[R8.3]` |
| R8.4 | Proposal-engine discipline (prop-1 rejection): a constant-change proposal emits only with multi-day ledger persistence (item benched by the gate on 4+ of the last 7 days, cited inline); a 3+ near-miss pile without persistence renders as a concur-recommended observation, never a proposal; every change-proposal traces the next allocator stage for each admitted item (an item that relocates to another bench is not created capacity) | probe `[R8.4]` |
| **§9 Quote lines — inventory-mode buffer flow (ruled Aug 10 2026)** | | |
| R9.1 | Sizing transparency: a quote leg smaller than the walk-back (gap + sell-leg replacement) or the holding names the binding cap inline — participation cap / one-third rule / buy limit — in the plan-line reason-string convention; a gap needing multiple cycles says "buffer fills over ~N touches at current flow" | probe `[R9.1]` |
| R9.2 | Placed state: "placed" marks the quoted legs standing on the watch entry — timestamped, persisted (survives export→import validation), elapsed time + fill state shown at every walk-up until the cycle completes or the user presses cleared; stale-buy and sell-aging-ladder rules apply to placed quote legs on the position clocks and surface in the NOW bar | probe `[R9.2]` |
| R9.3 | Partial fills: fill dialog prefills the remaining standing qty (editable) plus price; buy fills add FIFO lots at the fill price, sell fills FIFO-consume at true cost; the line shows "x/y filled · z standing"; a fill completing both legs closes the cycle; the tool never places, cancels, or logs on its own | probe `[R9.3]` |
| R9.4 | Self-cross guard is UNIVERSAL (audit F5, ratified Aug 10 2026): every reprice/undercut/leg proposal checks the user's standing opposite-side orders in EVERY store — positions AND unfilled placed quote legs — both directions, with CANCEL-FIRST copy. Ledger note: the original spec predates this file and was never rowed; the one-directional build was the dropped half of an untracked requirement — this row closes the gap | probe `[R9.4]` (the previously-missing directions asserted explicitly) |
| **§10 Demand-driven briefing cadence (ruled Aug 10 2026)** | | |
| R10.1 | Catalyst-window tightening: any calendar catalyst window live or starting within catWinTightenD (setting, default 14) tightens the staleness threshold from 7d to briefTightStaleD (setting, default 3); reminder copy names the catalyst and the staleness ("catalyst window live (X) — analyst Nd stale"); reverts to weekly automatically when no window qualifies | probe `[R10.1]` |
| R10.2 | Tape-question trigger: a NEW anomaly flag or SUSPECTED PUMP escalation whose first-seen postdates the last briefing import elevates the reminder immediately regardless of staleness, naming the count and lead flag; clears when a briefing imports after the flag's timestamp OR every triggering flag is dismissed/expired (pump: warning-record dismissal/expiry); a flag arriving mid-window re-arms a dismissed occurrence | probe `[R10.2]` |
| R10.3 | One reminder line, never stacked; priority tape-question > catalyst-window > update-Wednesday > weekly staleness; a dismissed occurrence falls through to the next qualifying trigger | probe `[R10.3]` |
| R10.4 | Handshake: flags-pending.json export (every active flag with kind, name, firstSeen ISO, newSinceBrief, detail; exportedAt + lastBriefImportAt stamps); anomaly-flag dismissal silences the reminder only — it NEVER lifts a pump defense (warning-record dismissal remains the only lift path); dismissed state and both cadence settings survive export→import; BRIEFING.md standing rule 9 answers each exported flag by name | probe `[R10.4]` · BRIEFING.md rule 9 (inspection) |
| **§11 Scanner net-widening + discovery slice (ruled Aug 10 2026)** | | |
| R11.1 | T1 scanner defaults: max 8,000 gp, consumables-only OFF (toggle stays); T2 defaults: 5k–250k band, gp-flow floor 1.2m/hr; one-time migration moves stored old-default dials, custom values untouched; ALLOCATOR TIER BANDS UNCHANGED (T1 400–5k, T2 5k–100k) — discovery only; scout admits to the scanner ceiling and stamps the admitting tier | probe `[R11.1]` |
| R11.2 | Rotating discovery slice: 15 random items per scan cycle from the full mapping universe passing basic sanity only (tradeable, price in either tier band, nonzero volume), rendered collapsed ("beyond the net") with full stats; sample rotates with the price cycle (seeded — stable within a cycle); pure exploration — admits, funds, and feeds nothing **Dormant-gated Aug 14 2026 (§81):** the draw is retired; the table renders the gap band's rows only, the header names the retirement, and the dormant forms assert both under this tag | probe `[R11.2]` |
| **§12 Closed-loop feedback edges (ruled Aug 10 2026 — attention, never authority)** | | |
| R12.1 | New record types import: `long-catalyst` (requires T0/T1 primary like any catalyst; confidence COERCED to ≤ hinted until officially dated) and `watch-note` (direction always caution) | probe `[R12.1]` |
| R12.2 | Ratified long-catalyst joins the calendar flagged estimate-window; entry-watch screens it like any entry | probe `[R12.2]` |
| R12.3 | Story-resolution feedback: an import naming a flagged item stamps the flag answered (storyId) and archives it with its market signature (story type/confidence, peak-to-flag lag, retrace, nulls when the cache can't say); an import naming it nowhere increments the flag's no-story counter; the flags export carries `noStorySweeps` so the escalation rule (≥1 wider net, ≥2 watch-note) is machine-visible | probe `[R12.3]` |
| R12.4 | flags-pending.json carries `scorecardDigest` (graded outcomes by confidence/source tier/type) and `rulingsDigest` (ratify/dismiss/edit counts by type + recent logged reasons) — the analyst reads its own grades and the user's revealed preferences before sweeping; null until data exists | probe `[R12.4]` |
| R12.5 | Entry-watch DISCOUNTED→RAMPING transition on a calendar item writes a `catalyst-ramping` flag (per cat·item per 30d), exported and tape-question-elevated as a question about the CATALYST ("is the window moving up?") | probe `[R12.5]` |
| R12.6 | Lag profile: explained-flag archive yields median peak-to-flag lag (≥2 data points, else silent); displayed on unexplained flags as attention-only copy ("likely post-peak") — re-ranks nothing, blocks nothing | probe `[R12.6]` |
| **§13 Complexity governance (ruled Aug 10 2026)** | | |
| R13.1 | Walk-up attention budget: distinct-decision count (ruling lines shown + briefing reminder) instrumented per day, trend in the weekly review, and the count stays ≤ 7 under a seeded overload state (cap 5 rulings + 1 reminder = 6 max by construction) | probe `[R13.1]` |
| R13.2 | Feature-touch instrumentation (tabs, buttons, panels — first/last stamps) feeds a 90-day dormancy report in the weekly review; demotions are PROPOSED (collapse behind disclosure, never delete), ratified by the user **Amended Aug 11 2026 (R36.1, user ruling):** DEMOTED from a standing review section to an on-demand button on the Review tab. Not deleted — it is the mechanism that catches surfaces that stopped being used, which is why several cut sections had gone unread; but a report that renders every visit becomes the wallpaper it exists to detect. Asserted both ways: absent from the review's standing steps, produced by its own press, and surviving a re-render. | probe `[R13.2]` (instrumentation + report math) · UI (demotion is a proposal by construction — no demotion code path exists) |
| **§14 Clusters → catalyst baskets merge (ruled Aug 10 2026)** | | |
| R14.1 | Clusters tab removed; basket ledger, review queue, and coherence view live on the Sleeve tab under the calendar; all cluster machinery (caps, exposure, P&L, candidate queue, dismissal memory, Jaccard absorption) carries over unchanged | probe `[R14.1]` + existing cluster-cap probes now running against the merged layout |
| R14.2 | One object, one name: a basket attached to a calendar catalyst renders as that catalyst's basket (⚡ badge + per-catalyst "basket coherence…" button feeding the coherence panel); a catalyst-less basket renders "standing" (kept for caps + blast radius) | probe `[R14.2]` |
| **§15 Integration-audit fixes (F1–F4 ratified Aug 10 2026 — audits/AUDIT-2026-08-10.md)** | | |
| R15.1 | F1: estimate windows look like estimates everywhere they render — calendar card "est. window" badge, entry-watch runway "(estimate)", briefing reminder "(estimated window)" | probe `[R15.1]` |
| R15.2 | F2: lag profile reads the full signature — dominant story class (type [confidence]) and median retrace ride the note alongside the median lag | probe `[R15.2]` |
| R15.3 | F3: a flag with an active watch-note naming its item renders the link ("escalated to watch-note <id> — posture absence") instead of "escalation due" | probe `[R15.3]` |
| R15.4 | F4: one daily-series cache — `dailyFor` is a 31-day slicing read of the shared 90-day cache (no second fetch, no second invalidation story); chart overlays read it via `dailyView` | probe `[R15.4]` |
| **§16 Second-audit fixes (F6–F8 ratified Aug 10 2026 — audits/AUDIT-2026-08-10b.md; F5 is R9.4)** | | |
| R16.1 | F6: one item, one mandate inside flipping — inventory-mode items bench from the plan; `committed()` counts unfilled standing quote buy legs at cost. **Amended under the mm bench (Aug 14 2026, §79):** the mandate bench holds exactly where a mandate still EXISTS — an item with in-flight legs or held lots benches with the unwind named ("mm benched, unwind in flight…"); one with nothing in flight reverts to normal plan handling (R79.1) (gp standing in an offer is deployed gp); one-time "accounting corrected" decision-log entry when standing legs exist at migration | probe `[R16.1]` |
| R16.2 | F7: `S.depProposalCount` carries its reading's timestamp; the walk-up funnel line states "as of the last Plan refresh (N ago)" instead of posing as live | probe `[R16.2]` |
| R16.3 | F8: a negative funnel residual (stage double-count) renders as a red accounting-error row instead of silently disappearing — an error detector must fire, not hide | probe `[R16.3]` |
| **§17 Coherence-driven membership (ruled Aug 10 2026)** | | |
| R17.1 | ADD proposals: non-members with excess corr > 0.5 vs the basket composite (net of market factor, same construction as the detector) in ≥3 of the last 4 weekly readings — persistence, not snapshots; trend attached; class assigned: THESIS when a story source names the item (analyst record / rare wiki category shared with a member), STATISTICAL on numbers alone | probe `[R17.1]` |
| R17.2 | DROP proposals: members < 0.25 for 4 consecutive readings, decay trend attached; thesis members carry "story may still bind — decorrelation can be temporary"; statistical members drop on the numbers alone; a member's reading excludes itself from the composite it is judged against | probe `[R17.2]` |
| R17.3 | Circularity guard: statistical members ≤ 40% of any basket's membership (withheld candidates said out loud, never silently truncated); > 40% flags "thesis diluted — correlation cluster now, consider renaming or splitting"; the guard's reasoning is stated in the panel copy | probe `[R17.3]` |
| R17.4 | Proposals ride the ratification queue (walk-up ruling lines included) with per-basket:item dismissal memory (the singleton-set Jaccard case); confirm applies membership + class and decision-logs; statistical members count toward exposure caps but are EXCLUDED from entry-watch narratives and laggard logic (`catLinkedIds`); member class renders in ledger chips and the coherence view; `mclass`/`cohLog`/`cohProps` survive export→import | probe `[R17.4]` |
| R17.5 | Resurface rule, dismissed ADDS only (ruled Aug 11 2026 — material change quantified for a scalar signal): dismissal stamps the correlation level; the proposal resurfaces only when corr exceeds that level by ≥ 0.15 held in ≥3 of the last 4 readings, copy stating both numbers ("dismissed at 0.52 — now 0.68 for 3 readings, resurfacing on material change"); sub-threshold drift never re-nags; drops have NO resurface path; the shape generalizes to no other dismissal type without a ruling; `dismissedC`/`resurfaced` survive export→import | probe `[R17.5]` |
| **§18 Paper book (ruled Aug 11 2026 — touches no capital, no real metric, no plan)** | | |
| R18.1 | Shadow positions per refresh: every single-gate benched near-miss (stamped with the gate and the gap) plus the funded picks under three regime books; entry at instasell + 1 tick (never the midpoint), allocator-sized (participation × horizon, one-third cap); dedup per family while open + 2×-horizon cooldown; open-count cap **Dormant-gated Aug 14 2026 (§81):** entry, sizing, dedup and the `regime:`/`bench:` family keys are unchanged, but a dormant-era trip carries NO regime membership — the "joins the regime books" clause is gated with the race, and the dormant form asserts the absent key | probe `[R18.1]` |
| R18.2 | **The capture-capped term is asserted BEFORE the qty clamp** (Aug 13 2026). `shadowCredit` stores `Math.min(qty, …)`, so once the term exceeds a trip's own size the clamp absorbs it entirely — every prior assertion read the stored value (`buyQ === 10` where qty IS 10), which can see UNDER-crediting and is structurally blind to OVER-crediting, the direction that would flatter every fill rate in the book and the one the model's conservatism claim rests on. Asserted at a volume 1,500× the trip size: the term uncapped, the ambiguous reading haircut from the TERM rather than the clamped size, the clamp itself holding at qty, and credit LINEAR in tape volume — a property assertion rather than a probe-side recomputation. Honest fill simulation: a shadow buy fills only when the tape prints at-or-below its bid while OBSERVED (5m volume-weighted, capture-capped); sell side symmetric; observed-time recorded (unobserved windows are unknown, not unfilled — every verdict carries observed share); buy horizon from open, sell horizon from buy completion, unsold remainder force-exits at the then-current instasell; unfilled expiry recorded as would-not-have-filled. **Stage 1d:** the observation-gap cap is the named term `obsAccrual` asserted against the CONSTANT at a gap that reaches it (the old fixture's 5-minute gap could never select a 10-minute cap's arm), and the credit arithmetic is the pure term `fillCredit` with capture and participation as arguments — the bracket and the capture ordering asserted at the term, where no qty clamp exists to absorb them | probe `[R18.2]` |
| R18.3 | Weekly per-gate counterfactual: "benched by X alone: paper-traded N, filled M, net simulated P after tax, vs would-not-have-filled K" with the observed-share caveat; all shadow numbers quarantine-labeled simulated everywhere they render **Amended Aug 11 2026 (R35.3):** the per-gate readout moved from the weekly paragraph to the Paper Book sub-view as a TABLE whose every cell opens to its trips; the ruled content (would-have-filled / would-not, both participation readings, the observed-share caveat) is unchanged and still asserted. | probe `[R18.3]` |
| R18.4 | Three regimes on the ROI dial (current / one notch loose / one notch tight, other gates unchanged — stated), same fill sim, all three SIMULATED (never comparable to realized P&L, stated); daily divergence ledger (30d); regime curves side by side in the weekly review; persistent divergence (4 of 7 days) is the proposal-engine evidence — never auto-applied **Dormant-gated Aug 14 2026 (§81):** the race is retired — curves and ledger render historically under the dormant banner; the evidence source yields null by construction and the dormant form asserts exactly that | probe `[R18.4]` |
| R18.5 | Fill-participation haircut (setting `shadowPartPct`, default 50%, ruled Aug 11 2026): a shadow leg claims fills up to capture-cap × participation of the tape volume that crossed its price — the conservative leg drives state and net; the 100%-participation reading accrues on the same record (one setting, no new collection) and the ledger reports both ("filled at N% participation / would also fill at 100%") in the cite, the per-gate line, the gate-health stream, and the expand view; exception proposals cite the conservative number; the knob clamps to [10,100]% | probe `[R18.5]` |
| **§19 Gate audit × paper book unification (ruled Aug 11 2026)** | | |
| R19.1 | Gate-health renders BOTH evidence streams per gate side by side, natures labeled: realized ([real fills] — override stream is the clean evidence, latency stream labeled as latency-only per the definitions protocol) and shadow ([simulated — fill-model caveats]) **Amended Aug 11 2026 (R35.5):** this tree moved from the review's audit Â§4 to the Gate Health sub-view, where each stream opens to its items and outcomes; the simulated stream's label reads "paper" per the Aug 11 rename. The review keeps a one-line read linking through. | probe `[R19.1]` |
| R19.2 | Combined verdict: a too-tight case requires BOTH streams pointing the same direction — realized direction reads ONLY traded-while-benched; agreement → change-proposal citing both streams (4-of-7 persistence and traced consequences still required); disagreement → its own finding ("investigate the fill model or the attribution"), concur-lane, never counted as a change-proposal; incomplete pair → accruing note, no change; the rule applies to regime-divergence proposals identically | probe `[R19.2]` |
| **§20 Surface reorganization (user-directed, Aug 11 2026 — composition change, no new data or mechanisms)** | | |
| R20.1 | Four top-level tabs — Home / Trade / Sleeve / Review; Home is the boot surface (zero clicks) carrying, in order: NOW bar (global, above tabs), RULINGS PENDING digest, WHAT CHANGED delta block, vitals strip, walk-up checklist. Old Watchlist/Scanner/Flip Log fold into Trade sub-views; Routine's walk-up folds into Home, its weekly mode into Review; Shadow Fund panels join Review with their headline in the vitals strip. Nothing deleted — everything is one level down from a surface that earns its position | probe `[R20.1]` |
| R20.2 | Any pending ruling is actionable in 1 click: the digest (same lines, cap, and priority as R5.1–R5.3) renders on Home with its inline ratify/dismiss/snooze controls; the walk-up checklist points at the digest instead of duplicating it | probe `[R20.2]` |
| R20.3 | Vitals strip in the RULED ORDER (**transitional nav, Aug 14 2026**): FUNNEL · SCORER · SLEEVE · CAPITAL — the Paper Book tile yielded its slot to the Scorer, because the chrome presents the ruled dichotomy (what would my rules find in the market vs what is my actual trading doing); the paper book's open · closed read moved to its own surface header (§65's dormant form asserts it there). Funnel headline (pass · funded · % deployed), scorer one-liner (**rendered from `scorerTileLine()`, its one source term** — rank-readiness in the ruled words, chart-gate observed days of the 7, frontier size at the last scored bucket; states per R82.2), sleeve one-liner (positions · nearest rung · nearest catalyst countdown), reserve/budget split — each tile a 1-click click-through to its full surface. The plan read behind the funnel tile keeps the deploy ledger and paper book riding Home exactly as they rode the old default tab | probe `[R20.3]` |
| R20.4 | The walk-up attention budget (≤7, R13.1) binds on Home — the count and budget line render on the tier-1 surface | probe `[R20.4]` (+ R13.1 unchanged) |
| R20.5 | WHAT CHANGED delta block, since the last visit (2h-quiet-gap visit clock + end-of-visit snapshot): flags newly explained / escalated, paper trips closed (count + net, simulated), sleeve rungs armed, regime-divergence movement (**dormant-gated Aug 14 2026, §81** — the frozen ledger cannot move, so the category is quiet and the dormant form asserts it earned nothing), catalyst window transitions, briefing staleness transitions — one line per nonzero category only, truncated at 6 lines with "+N more" (mobile parity); first tracked visit says so instead of dumping history | probe `[R20.5]` |
| R20.6 | Legacy tab names (watch, scan, log, shadow, routine) alias into the new surfaces in setTab, so every historical data-goto, NOW-bar target, and close→prefill path still lands; the single checklist panel reparents between Home (walk-up) and Review (weekly) with mode following the tab | probe `[R20.6]` |
| R20.7 | Click-depth tier check: featTouch keys used inside 30 days whose surface sits >2 clicks deep render as a named contradiction in the attention report ("usage contradicts the tier assignment — propose a promotion"), clean state says so explicitly; the check ships as machinery because the usage ledger lives client-side | probe `[R20.7]` |
| **§21 Walk-up context awareness (user-directed, Aug 11 2026 — the checklist adapts to operating state)** | | |
| R21.1 | The walk-up checklist is dismissible in one tap for THIS visit (returns automatically next visit, keyed to the visit clock); NOW lines, rulings digest, WHAT CHANGED, and vitals are never part of the collapse — only the guided steps | probe `[R21.1]` |
| R21.2 | Standing minimize preference: one-line summary (title · steps done · reason) with a one-tap expand that is per-view; "pin open" clears the preference | probe `[R21.2]` |
| R21.3 | Auto-minimize without being asked when a visit has zero open positions (standing quote legs count as open book), zero armed rungs, and zero pending rulings — and the NOW bar never points into a walkthrough that isn't rendering | probe `[R21.3]` |
| R21.4 | Review replaces, never stacks: review-due renders as ONE line ("weekly review ready — N days of data", existing 3-day trigger); one tap (or opening Review while due) makes the review the visit's checklist, with the walk-up's execution core absorbed as step zero while a book is live; the NOW bar follows the engaged review via the same effective list; review interaction stamps the visit; no state renders two checklists (single panel by construction, probe-asserted) | probe `[R21.4]` |
| R21.5 | Flat-book posture: WHAT CHANGED and vitals lead the Home surface by construction while flat, and the delta block carries the standing line "book flat since \<date\> — paper book accruing, N trips closed since" | probe `[R21.5]` |
| R21.6 | The review-ready line is a decision point and counts against the same ≤7 walk-up attention budget instead of hiding from it | probe `[R21.6]` |
| **§22 Interface fixes + tag consolidation (user-directed, Aug 11 2026)** | | |
| R22.1 | First-tap fix, cause stated: every render pass rebuilt identical checklist markup via innerHTML (60s poll + the boot data arrival ~1s after open), so a tap whose pointerdown landed on the just-replaced node never became a click. `setHTML` writes only on content change — unchanged renders keep their DOM nodes; probe asserts node identity across a background re-render and a responding first tap | probe `[R22.1]` |
| R22.2 | Scoping: rulings digest, WHAT CHANGED, and the walk-up checklist render on Home ONLY; the NOW bar and the vitals strip are GLOBAL (vitals moved above the tabs — amends the §20 Home-internal placement); the ⚖ badge stays on every tab as the pointer back to Home. **Regression + re-verification (Aug 11 2026):** an ID-specificity CSS rule (`#tab-home{display:flex}`) overrode the tab gate and rendered Home on every tab while existence-based probes passed — the probe now asserts ABSENCE (computed visibility off-Home on every non-Home tab), and the assertion was verified to fail against the original defect | probe `[R22.2]` (negative assertions) |
| R22.3 | Ruling path: every digest line's ratify/edit/dismiss actions are visible inline (no hover gating), and ruling from Home lands visibly — the line leaves the digest and the ⚖ badge updates in the same interaction (the intel handlers' missing re-render was the composition regression); audit deep-links anchor to the Review checklist panel | probe `[R22.3]` |
| R22.4 | No silent unfunded states: every watchlist row leads with an explicit plan-status badge — funded N / NEXT UP #k (exact reason) / qualifying n/3 / fails N gates (reasons) / benched (reason) / blacklisted / live position / mm cycle / gates pending — with the full reason in the title and the expand view | probe `[R22.4]` |
| R22.5 | Auto-promote: an all-gates-passing NEXT UP item funds automatically at the next recompute when slots or budget free — no press, `planPriority` untouched; the promote button only overrides ORDER (copy states it); allocator caps, budgets, and every gate unchanged | probe `[R22.5]` |
| R22.6 | Beyond-the-net gate tags: the full gate chain runs on every slice item each cycle, tagged in the same language as watchlist rows; missing chart data reads "gates pending chart", never nothing; one-tap +watch on every row **Dormant-gated Aug 14 2026 (§81):** the property binds identically on the gap band's rows, which are the table's remaining population | probe `[R22.6]` |
| R22.8 | Minimized checklist is ONE line total: the panel header, section title, progress bar, and attention line all fold in — only the summary row (title · steps done · reason · expand) renders, with the attention reading in its tooltip; active cue lines (elevated briefing reminder, review-ready) are cues, not chrome, and still render when genuinely firing | probe `[R22.8]` |
| R22.7 | Tag consolidation: at most four indicators per row — plan status / merged identity (tier+provenance+proof+·M) / aggregated ⚠ N risk chip (hard-bench flags red, advisory amber; full list in title; tap opens the expand view) / test-state dot; basket chips, intel tags, mm state, provenance detail move to the row's expand view; a filter row reaches every collapsed state; scanner rows adopt the same scheme | probe `[R22.7]` |
| **§23 Findability (user-directed, Aug 11 2026)** | | |
| R23.1 | The ⚖ badge ALWAYS renders on every tab: count > 0 → prominent count, click-through to Home's queue; count = 0 → a subtle inert ghost glyph — an empty state that renders proves the mechanism lives, and absence is never ambiguous between "nothing pending" and "badge broken". Diagnosis recorded: the reported missing badge was the hidden-at-zero design (never orphaned from the header path), probe-tested at nonzero only via direct call and never per-tab | probe `[R23.1]` (both states, every tab) |
| R23.2 | Standing "⭳ flags for analyst" export — reminder-independent: a line in the walk-up (expanded state) and an action in the Review scorecard area, exporting the same flags-pending.json regardless of reminder state; a quiet tape exports an honest empty docket (`note`: "zero flags, confirmed — a deliberate export, not an absent one"); the reminder-line placement stays as the escalation path | probe `[R23.2]` |
| R23.3 | Desk-side flags handoff (kills the Downloads hop): /briefing run-procedure step 0 resolves the Downloads folder via the known-folder registry (never hardcoded), moves the newest `flags-pending*.json` to `briefings/flags-pending.json`, deletes older numbered copies, and logs the outcome in the brief header; a >24h-old export proceeds but is labeled stale in the header and Flags Addressed section; a zero-flags `note` file is reported as a deliberate empty docket | inspection: BRIEFING.md step 0 + SKILL.md (desk side — not tracker-probeable) |
| **§24 Review-tab fixes (user-directed, Aug 11 2026)** | | |
| R24.1 | Friction-note lifecycle: only open (unresolved) notes render; copy-all stamps them `exported` (the FRICTION.md carry mark); per-note "✓ handled" and a one-tap "clear exported" sweep move notes into a collapsed "resolved (N)" disclosure; a clean Sunday reads "No open friction notes" | probe `[R24.1]` |
| R24.3 | **THE LOG-FLIP PRESS LANDS ON THE FORM** (fix, Aug 14 2026 — reported from phone use: the viewport parked on the Results/Sharpe box below the form). The press used to perform NO landing at all: it bare-focused the qty input and let the browser's focus-scroll — geometry- and soft-keyboard-dependent, never offset for sticky chrome — decide where the viewport ended up. It now routes through the ONE landing implementation (`navLand`, extracted from navGoto with behaviour unchanged — R24.2's measured-chrome idiom), and both focuses carry `preventScroll` so neither the immediate focus nor the async prefill's re-focus can fight the landing. Asserted structurally: the form is the landing target and holds focus, the panel below is NOT the target — never by pixel boxes, which would test the renderer. The real-press artifact on the real build measures the actual viewport | probe `[R24.3]` |
| R24.4 | **THE SLEEVE-FORM EDIT PRESS LANDS ON THE FORM** (fix, Aug 14 2026 — the same-class site §7 of the 1e stage report listed and deliberately did not fix): the `[data-slvedit]` press used a bare `scrollIntoView({behavior:"smooth"})` with no measured chrome pad, no landing flash and no rAF deferral — the exact offset class R24.2 closed for deep links, so the form title could park under the sticky header. It now routes through the ONE landing implementation (`navLand`); `slvFormFill` performs no focus call, so nothing fights the landing. Asserted structurally per R24.3's shape, through the real delegated click handler: the form panel is the landing target (carries the flash), the positions panel is NOT, and the form is in edit mode | probe `[R24.4]` |
| R24.2 | Every deep link lands ON its target section — viewport-measured to the R22.2 standard (an anchor that "resolves" to a tab isn't tested until the probe confirms where the viewport ends up): all vitals tiles, NOW-bar gotos (now anchored: positions panel, log form, export chip, checklist), audit links (→ the review's audit step), and the ⚖ click-through (→ the digest, flashed) scroll the target's top into view and flash it (`.landed`); checklist steps carry `ckstep-<key>` anchors; wiring asserted separately from anchors because the original defect was a wrong goto value (transitional nav, Aug 14 2026: the tile slot's wiring assertion now names the Scorer tile → `scorer#scorerVerdictPanel`; the property is unchanged — a tile lands on the surface it names, never on commentary). **Sticky-chrome offset (amended same day):** `scroll-margin-top` set at scroll time from the measured height of pinned top chrome (header now; the vitals strip joins the measurement the moment it is ever sticky/fixed — it is in normal flow today, so its overlay height is honestly zero), +10px breathing room; the probe asserts the target's first VISIBLE title sits fully inside the viewport AND below the chrome's bottom edge — the prior "top in viewport" passed with the title hidden under the header, which is how the offset shipped green. Detector proven against the offset bug (14 landings fail with the pad removed); verified on desktop (1200×900, 58px chrome) and phone (390×844, 93px chrome) via `PROBE_WINDOW` | probe `[R24.2]` (17 destinations + wiring, both viewports) |
| **§25 Paper ↔ row integration (user-directed, Aug 11 2026 — route existing evidence to the point of decision; no new collection)** | | |
| R25.1 | Toxic flow joins the ⚠ pool: realized markout breaching the toxicity threshold renders the advisory-amber chip entry ("avg adverse markout X vs margin Y"), on watch rows and on scanner rows for watched items; requires ≥3 marked-out fills — one bad markout is noise | probe `[R25.1]` |
| R25.2 | Shadow dot (dot-class, the four-BADGE rule holds): ≥3 closed paper trips → solid green (paper-profitable) / solid red (paper-losing) / hollow at fill rate <40% ("the margin exists, the fills don't"); tooltip carries trips, fill rate, net, book, observed-share caveat, quarantine label | probe `[R25.2]` |
| R25.3 | The row expand view answers "what would have happened if I'd traded this" in one tap: trips, entry/exit, would-not-have-filled count, observed-time caveats, all labeled simulated | probe `[R25.3]` |
| R25.4 | NEXT-UP and bench lines cite the per-item shadow slice in the same sentence as the gate's verdict — "bench agrees" (net ≤0; "would not even have filled" at zero fills) vs "screening: probation candidate — a live half-size test decides, not the sim" (net >0; reworded from "evidence accruing" by the R27 reweight); the cite quotes both participation readings; rendered on plan bench rows, NEXT-UP lines, and the row-status badge reasons | probe `[R25.4]` |
| R25.5 | Discovery-slice single-gate near-misses shadow-trade (chart-dependent gates are unknowable for slice items and don't count toward the single-gate test), capped at 5 slice positions per cycle to bound the ledger; blacklist-gate items never shadow-trade — in the slice OR the watch near-miss path — because a ledger arguing against the blacklist is the machine building a case against a non-delegable call **Dormant-gated Aug 14 2026 (§81):** the stratified arm opens nothing (the dormant form asserts it); the gap band keeps its ruled `floor(cap/2)` share; the blacklist exclusion binds on every arm that still draws | probe `[R25.5]` |
| R25.6 | ANY paper activity renders a dot (user ruling, Aug 11 2026): below the 3-closed-trip verdict threshold the row shows a dim accruing state naming the trip counts — open trips included, since open activity is activity — rather than nothing. A row with paper history that renders nothing is the F18 silent-state defect in a new place. The dot leads the dot slot (primary), and an item with no history renders its reserved slot with no dot in it. Asserted on COMPUTED VISIBILITY, not DOM presence, because the defect class here is a surface that exists and shows nothing | probe `[R25.6]` |
| R25.7 | The two dot families must be unmistakable at a glance and on a phone, since confusing them is a decision error (user ruling, Aug 11 2026): SHAPE carries identity — every paper state is a circle-within-a-circle (◉ verdict, ◎ fills-missing, ◌ accruing), the test dot stays a plain ●/○ — and colour carries state only WITHIN an identity, so green/red belong to the paper family (genuinely good-vs-bad) while test state moves to neutral steel (verified-vs-stale is not). Fixed slots: each dot always emits its slot, so a missing dot leaves its space rather than letting its neighbour slide into it. Tooltips name themselves first ("Paper screen: …" / "Margin test: …"), and a legend in the watchlist header shows both glyph families and all their states side by side. The four-indicator budget counts the dot cluster as ONE slot; reserved empty slots are not indicators | probe `[R25.7]` |
| **§26 Paper-backed exception lane (user-directed, Aug 11 2026 — per-item, evidence-gated, ruled)** | | |
| R26.1 | Evidence bar for a proposal: ≥5 closed paper trips, fill rate ≥60%, net paper-profitable, span ≥7 days, AND no red flags (not pump-suspected, not blacklisted, not toxic-flow); the one-tap proposal renders on the bench line and in the rulings digest; a waived/granted item never re-proposes | probe `[R26.1]` |
| R26.2 | Grant is a ruling (one tap, decision-logged): the item funds despite the named gate at probation terms — half size, a 2× tick-floor safety (its own stated bench reason when breached), one position — for 3 real round trips; probation is an explicit seasoning exemption (the ruling is the ratification) | probe `[R26.2]` |
| R26.3 | Adjudication by realized results on TWO named criteria (amended by R27.2): net P&L and adverse-markout %; 3 profitable probation trips with clean markouts → standing per-item waiver (logged, revocable in the row's expand view); losing → revoked, item returns to the bench, divergence recorded ("sim said yes, reality said no") as fill-model calibration data | probe `[R26.3]` `[R27.2]` |
| R26.4 | Cap 2 active exceptions — probes of the gate's edges, not a parallel permission system; at cap no proposal renders and the bench line states why; a waiver or revocation frees the slot. THE GATE ITSELF NEVER MOVES on paper evidence alone — constants still require the two-stream agreement standard; two lanes, two evidence standards, labeled in every grant | probe `[R26.4]` (+ R19.2 unchanged) |
| **§27 Shadow reweight — screening, not evidence (outside review, ruled Aug 11 2026)** | | |
| R27.1 | Every shadow surface (row dot, expand view, bench/NEXT-UP cites, weekly panel, gate-health stream label, vitals tile, checklist step, DB comments) labels the book a SCREEN ranking probation candidates for live half-size tests — never "evidence accruing"; the weekly shadow panel carries the standing self-impact caveat: "sim assumes a book you're not in — thin books change when you post; screening tool, not evidence" **Amended Aug 11 2026 (R35.3):** the standing self-impact caveat now rides the Paper Book sub-view (the weekly panel it named became a one-line summary linking there). | probe `[R27.1]` |
| R27.2 | The exception lane's realized conversion stays the ONLY path to a waiver; the three-trip adjudication names adverse-markout % as a criterion alongside net P&L (house `markoutX` toxic-flow standard applied to the probation trips' own markouts); profitable-but-markout-toxic REVOKES with the markout stated; untimed trips adjudicate on net alone and the decision log says so | probe `[R27.2]` |
| R27.3 | The regime race keeps running but divergence proposals read "regime X's candidates earned probation slots — a screening rank, not proof the regime is right", never "regime X is right"; exception proposals are a RANKED shortlist (conservative sim net, best first, truncated at free probation slots); the 4-of-7 persistence and two-stream proposal machinery unchanged (gate-proposal copy reframed: realized makes the case, the screen concurs) **Dormant-gated Aug 14 2026 (§81):** "keeps running" no longer holds — the race is retired and no divergence copy can render at all (the dormant form asserts the gated source); the exception shortlist and its framing are untouched | probe `[R27.3]` (+ `[R18.4]` `[R19.2]` unchanged) |
| **§28 Scanner paper cohort (user-directed, Aug 11 2026 — the FILTER question: "are my filters excluding money")** | | |
| R28.1 | Each cycle (scout cadence, self-throttled), per tier scanner: the top 8 gate-passing-or-single-gate-near-miss items NOT on the watchlist shadow-trade with scanner·T1/scanner·T2 provenance, ranked by the allocator's own score (`scanScreenTier` with a loose-ROI screen so ROI near-misses survive); same honest fill sim + participation haircut; blacklist excluded (standing rule) and pump-flagged excluded same as everywhere; chart-dependent gates are unknowable and don't count toward the single-gate test | probe `[R28.1]` |
| R28.2 | Populations separate in every ledger view: the per-gate counterfactual and regime books read watchlist+slice only; the weekly FILTER OPPORTUNITY COST line compares scanner-cohort vs watchlist-cohort net over the same window (since the earliest scanner entry — a young cohort is never compared against 30d of history) and frames outperformance as a PIPELINE finding (needs ≥5 closed scanner trips), screening-labeled | probe `[R28.2]` |
| R28.3 | Bounds: 40 open across ALL cohorts (`SHADOW_MAX_OPEN`); the scanner cohort evicts only its own lowest-score open position, only for a better-scored candidate — watchlist/slice cohorts never evicted; spark fetches ride the scout's existing polite budget (`SCOUT_EVAL_PER_TIER` = 12/cycle, 250ms spacing) — coverage accrues over cycles rather than bursting | probe `[R28.3]` (cap + eviction) · budget is code-fixed (`fetches >= SCOUT_EVAL_PER_TIER`) and network-bound, stated here, not silently dropped |
| R28.4 | One stage per move (honesty carry-over): scanner-cohort trips are excluded from exception-lane evidence (slice and gate counts) — they nominate an item for scout ADMISSION only; ≥3 profitable scanner-cohort trips earn a scout-admission ATTENTION boost (evaluated first, same admission bar, boost stated in the scout log); funnel: scanner shadow → scout admission → watchlist → gates → probation → real size | probe `[R28.4]` (boost set + probation exclusion) · scout evaluation order is code-fixed (boosted-first concat), stated here |
| R26.5 | Void-basis re-ruling (user ruling, Aug 11 2026): the paper purge discarded the evidence that grounded every granted exception, and a ruling that survives its own justification must be RE-MADE rather than persist by inertia. Each such record raises its own walk-up ruling line ("exception for X was granted on discarded evidence — revoke, or re-grant on the new epoch's data?") offering both ways out, holds that line until ruled either way, and leaves the digest once ruled. Re-affirming is the user's call, never the sim's, but the decision log distinguishes the two cases explicitly — re-made ON new-epoch evidence that independently clears the same bar (cited), versus re-made on judgment with no evidence yet — because those are different claims and must not read alike. The row's expand view carries a "basis void — re-rule" badge so the state is self-explaining where it renders | probe `[R26.5]` |
| **§29 Pond prospecting — structural sampling (user-directed, Aug 11 2026)** | | |
| R29.1 | STRATIFIED ROTATION amends R11.2's uniform draw: the 15-item slice draws from ONE named stratum per price cycle, in rotation (equipment by slot, F2P-only, skilling outputs by skill, quest-locked, holiday/rare-adjacent, sub-400gp bulk — the last with its own sub-400 price window), so small strata are sampled on a clock instead of never; the stratum is stamped on every row and on every counterfactual it opens; the rotation is a pure function of the price cycle (stable within it, advancing across it). Every stratum except F2P and the price-band ones is a NAME-PATTERN classification — the mapping API carries no category data — labeled approximate wherever it renders, and it steers sampling only, never a trade **Dormant-gated Aug 14 2026 (§81):** the rotation is frozen with no production caller; the dormant forms assert no stratum rows render and no draw opens trips even with a pinned, populated stratum | probe `[R29.1]` |
| R29.2 | GAP-BAND SLICE: 5 items per cycle from 250k–1m — the band between the T2 scanner ceiling and the anomaly scan's >1m floor, previously watched by nothing but slice luck — with gate verdicts and shadow eligibility identical to the discovery slice, rendered in the same table under its own stratum stamp *(the render sample was raised 5 → 10 by R53.1, Aug 12 2026 — annotated here Aug 14 2026 when the sweep found this row still claiming the old constant; the paper-entry budget is the separate `floor(SLICE_SHADOW_CAP/2)` share)*. **The gap band is NOT retired** — it survives the Aug 14 2026 slice retirement intact, serving the held T3 proposal and the routing question | probe `[R29.2]` |
| R29.3 | Per-stratum ledger: distinct items sampled / near-misses shadowed / clean passers / closed trips / simulated net, rendered as the weekly review's §5 Prospecting map ranked by simulated net, each region reading "prospects" / "barren so far" / "too few closed trips to say"; the gap band gets its own verdict line — printing earns it a scanner PROPOSAL, and not printing proves the gap empty rather than assumed **Amended Aug 11 2026 (R35.4):** the map now renders in the Prospecting sub-view as a table whose cells open to their sampled items and surviving trips, with the gap band on its own panel; the review keeps a one-line read linking through. **Dormant-gated Aug 14 2026 (§81):** the map is the frozen historical ledger under a dormant banner — durable, still opening to its rows; the gap row alone accrues, and the verdict answers with the successor | probe `[R29.3]` |
| R29.4 | The ledger is DURABLE: closed slice/gap outcomes roll into it exactly once (a `rolled` stamp, no double counting on later ticks) and survive the paper book's 30-day pruning, because the quarterly "which pond regions hold edge" map has to outlive the retention window; distinct-item counting is the denominator (a re-draw is not a second test), capped per stratum with the cap stated when reached | probe `[R29.4]` |
| R29.5 | Slice tightening: a slice item clearing ALL gates enters the paper cohort at FULL eligibility (no bench stamp) — a random draw clearing every gate is the event the slice exists to catch — and surfaces as a WHAT CHANGED line naming the item and its stratum; the weekly discovery section carries the slice hit-rate line (sampled · near-misses shadowed · clean passers · slice-cohort simulated P&L). The ruled 5-per-cycle ledger bound is now SHARED with the gap band (half to the gap, unused allowance flowing back) so neither starves the other and no new budget was minted; blacklist and pump-flagged items are excluded from every cohort **Dormant-gated Aug 14 2026 (§81):** no clean find can arise from the stratified arm — the dormant form proves it on items that would pass every gate — and the flow-back lapses (the gap keeps its half, unwidened) | probe `[R29.5]` (+ `[R25.5]` amended) |
| R29.6 | Classifier honesty (user ruling, Aug 11 2026): the per-stratum map carries the standing caveat "N of M strata are name-pattern approximations — directional, not a census" wherever it renders, with N and M DERIVED from the strata table so they cannot drift; and a PROMOTION TRIPWIRE — any per-stratum finding cited in a proposal, a scanner proposal, or a scout priority change carries "Depends on approximate stratum classification — verify before ruling" at the point of decision. The gap band is explicitly exempt and says why: its membership is an exact price range, not a name-pattern guess, so claiming the caveat there would overstate the dependency. Hardening the classifier via wiki-category enrichment is deferred to IMPROVEMENTS.md, conditional on the map ever driving a decision (**closed as mooted Aug 14 2026** — the map is dormant and steers nothing; the entry revives only with an un-retire ruling). **Dormant-gated Aug 14 2026 (§81):** the caveat and tripwire still govern every reading of the historical map and render with it | probe `[R29.6]` |
| **§30 Hours ledger — time-structure prospecting (user-directed, Aug 11 2026)** | | |
| R30.1 | Everything else samples items; this samples HOURS, in two streams whose natures are separated in the copy: MARKET (post-tax spread by hour-of-day, computed from the 168-point hourly series each watched item already fetches — every hour equally represented whether or not the tracker was open, so it is the unbiased stream) and SHADOW (simulated fill rate and net by the hour a counterfactual opened — accrues only while OBSERVING, so it is presence-biased). Hours never observed read "no data", never "bad hour" — the same unobserved-is-unknown doctrine as R18.2. No new requests: the buckets ride the existing timeseries fetch **Amended Aug 11 2026 (R35.4):** the ledger renders in the Prospecting sub-view, with a per-hour table keeping the two streams in their own columns and each opening to its rows. | probe `[R30.1]` |
| R30.2 | The overnight question is answered where both streams can speak: 00:00–06:00 post-tax spread against the all-day average, with shadow corroboration when overnight trips exist and an explicit "unknown, not disproven" when they don't; a wider-overnight reading states the case for sizing the standing overnight book heavier as a PROPOSAL for the user's ruling, never applied from here | probe `[R30.2]` |
| **§31 Crafting-spread monitor (user-directed, Aug 11 2026)** | | |
| R31.1 | Processing basis on 10 high-volume recipes (herb cleaning, unf chains, 3-dose potions, a 4-dose combination potion, a barrows set vs its components), computed every refresh from the /latest bulk fetch (no new requests) on the stated PATIENT convention — output sold at its ask net of GE tax minus inputs bought at their bids. Recipes resolve BY NAME against the live mapping, never hardcoded ids: an unresolvable leg renders "unresolved" rather than a confidently wrong number. Daily ledger, last write of the day wins, 31d, and a recipe's baseline excludes its own current reading **WITHDRAWN Aug 11 2026 (user ruling).** Removed entirely rather than relocated: the monitor terminated in prose. Its manipulation-tell half duplicated `suspectedPump`, which is already wired to auto-arm restraint; its flip-signal half named a mispriced leg but put it nowhere the funnel could act on, so acting on it meant hand-carrying a name to the watchlist; and ten hardcoded recipes is a fixed, tiny universe beside the stratified slice, which samples the whole item space on a clock. If the idea returns it should return WIRED, which is a new capability with its own price tag and ruling. | WITHDRAWN — no verification. The assertions were removed with the feature; until Aug 13 2026 this column still claimed probe coverage, which is a coverage claim on deleted code |
| R31.2 | A basis reaching 2× that recipe's own normal (median absolute basis over ≥5 prior daily readings; below that the row says "baseline accruing" instead of flagging noise) flags a BASIS BREAK with the widest-moving leg named from the same ledger — read two ways in the copy: a flip signal on the mispriced leg and a manipulation tell, since a pump distorts one leg of a recipe before the other. Flags dedupe per recipe per 24h, retain 30d, surface in WHAT CHANGED with the leg that moved, and size/cap nothing **Amended Aug 11 2026 (R35.4):** the basis table renders in the Prospecting sub-view, each basis opening to its daily readings and the baseline it is judged against. **WITHDRAWN Aug 11 2026 (user ruling)** — see R31.1. Assertions removed with the feature; the rows are kept marked rather than deleted so the record shows the decision. | WITHDRAWN — no verification. The assertions were removed with the feature; until Aug 13 2026 this column still claimed probe coverage, which is a coverage claim on deleted code |
| **§32 Data freshness + timeseries breaker (pre-quiet-period audit, Aug 11 2026)** | | |
| R32.1 | Every background accrual reports when it last accrued, in the weekly review, split by nature: SCHEDULED streams (paper trips, regime ledger, scanner cohort, stratum samples, gate-health ledger, deploy ledger, recipe basis, hour-of-day, coherence, anomaly scan, walk-up attention) are flagged stale against their own cadence; EVENT-DRIVEN streams (markouts, die-off episodes, basis breaks, accumulation flags) are never marked stale because silence is data there, not a fault. The panel states the load-bearing dependency out loud — accrual requires the app to be open AND visible, since the poll skips while the tab is hidden — so a fortnight of nothing is diagnosable rather than mistaken for a quiet market | probe `[R32.1]` |
| R32.2 | Timeseries circuit breaker: `/timeseries` failures are remembered per item id (no immediate retry) and a run of 8 consecutive failures suspends timeseries fetching for 30 minutes behind a visible banner, serving stale cached series meanwhile; a success resets it. Closes the audit finding that the poll's own backoff counts only bulk-endpoint failures, so a timeseries-specific outage would be re-hit by the scout, the scanner cohort and fillSparks every pass — thousands of rejected requests a day, unattended, with nothing on screen | probe `[R32.2]` |
| **§33 Cold-start integrity (pre-quiet-period audit, Aug 11 2026)** | | |
| R33.1 | A cold, near-empty store (one watched item, no flips, no bank, no history, every ledger empty) renders all four tabs and every review surface without throwing, and no surface renders `NaN`, `undefined`, `[object Object]` or `Infinity` into user-facing copy — the whole tool was built against a populated store, so sparse-data rendering is asserted rather than assumed | probe `[R33.1]` |
| R33.2 | Empty streams say so in words rather than rendering an empty-but-broken panel, and the allocator survives the sparse case (`buildPlan` returns well-formed picks/bench/nextUp with one watch item and no capital) | probe `[R33.2]` |
| **§34 Accrual is tab-independent (user ruling, Aug 11 2026)** | | |
| R34.1 | Every plan-driven ledger (gate-health rows, die-off episodes, qualification streaks, paper positions, the per-stratum sampling ledger, the hourly funnel bucket) accrues from the POLL via `accrueBackground()`, never from a render path. Previously these were written inside `renderDeploy`, reached through the vitals renderer — which happened to run on all four tabs, so nothing was broken, but "happens to be called from the current layout" is the coupling that produced the reorg accrual bug and fails silently the next time a surface moves. Asserted with each of the four tabs active AND with no view rendering at all. Streams already poll-driven: `shadowTick`, `runScout`, `scannerShadowScan`, `recipeTick`, `fillSparks`; coherence and the anomaly scan run on their own 6h interval behind a 7-day guard; `walkupLoad` is Home-only by definition since a walk-up reading only exists when you walk up | probe `[R34.1]` |
| **§35 Shared decomposition primitive + three Trade sub-views (user-directed, Aug 11 2026 — "every aggregate decomposes to its rows, in one click")** | | |
| R35.1 | ONE shared primitive, not three bespoke drill-downs: `drill(key, face, spec)` renders any aggregate as an openable face, and one click opens the constituent rows **in place** (a full-width sibling row inside a table, a block sibling anywhere else) with no placement work by the caller. Sort, text filter and cohort selection live in the primitive, so every surface inherits them rather than re-earning them; expansion state is session-scoped (a reading posture, not a persisted setting) and is re-applied by `paintDrills()` after every render pass, so the 60s poll never closes what you opened to read. Asserted on COMPUTED VISIBILITY, not DOM presence | probe `[R35.1]` |
| R35.2 | Honest subset disclosure, built into the primitive: whenever the rows shown are fewer than the population the number counts, the expansion says so and why. Two cases, both asserted — **structural** (`spec.total` exceeds the rows supplied: the per-stratum ledger's counters are durable by R29.4 and outlive the paper book's 30-day pruning, so its expansions can only show surviving rows) and the **row cap** (first 20 of N, with a "show all N" control and a collapse back). A filtered expansion states how many rows its filter is hiding. A counter whose rows are all pruned renders words, never a blank box | probe `[R35.2]` |
| R35.3 | **Paper Book** sub-view (Trade → Paper Book): three ROI-regime equity curves side by side on ONE shared y-scale (three scales would be a picture that lies), labeled "all three SIMULATED … never compare to realized"; the divergence ledger opening to its daily readings; the per-gate table (benched / would-fill / at 100% / would-not / fill rate / net sim / observed median) with EVERY cell opening to its trips — entry and exit price, fill or no-fill with the tape credit at both participation readings, observed share, regime, cohort, benching gate. Forced exits now retain `exitPx` so a forced outcome shows the price it paid; records closed before that render "— not retained", never a guessed number. The "accruing since &lt;date&gt;" epoch banner and the standing self-impact caveat ride this surface. The cohort ledger states where every population stands — watchlist, slice, slice·clean, gap band, scanner·T1/T2 — including the empty ones, and unobserved holes are shown rather than silently excluded | probe `[R35.3]` `[R18.3]` `[R18.4]` `[R18.5]` `[R27.1]` `[R28.2]` |
| R35.4 | **Prospecting** sub-view (Trade → Prospecting): the per-stratum map as a table (sampled / near-miss / clean / closed / filled / net sim / verdict), each cell opening to its sampled items or its surviving trips; the gap band's own panel with its samples and its verdict, exempt from the classifier caveat and saying why; the hours ledger as a table keeping the two streams distinct — market (presence-independent) opening to the watched items that produced each hour's average, paper (presence-biased) opening to the trips opened in that hour, with never-observed hours reading "no data — never observed"; the recipe basis table (**this clause WITHDRAWN Aug 13 2026** with the §31 feature — the panel does not exist and the citation to its assertion was removed with it). The stratum caveat and the promotion tripwire render as already built | probe `[R35.4]` `[R29.3]` `[R29.6]` `[R30.1]` `[R30.2]` |
| R35.5 | **Gate Health** sub-view (Trade → Gate Health): the two-stream tree per gate with each stream opening to its items and outcomes — realized overrides ("traded while still benched") and re-admission latency ("traded after unbenching") kept distinct per the definitions protocol, and the paper stream's would-fill / would-not opening to its trips; die-off episodes opening to their items and 24h results with the unobserved-window caveat intact; the exception lane's standing (on probation / standing waivers / revoked / ranked candidates), including any granted exception whose basis was voided by the epoch reset, which says so where the lane is read. The surface reports standing only — grants, revocations and re-affirmations are still ruled from the bench line, the row's expand view or the rulings digest | probe `[R35.5]` `[R19.1]` `[R26.5]` |
| R35.6 | Scoping tested BY ABSENCE on computed visibility (the standing discipline): each sub-view renders only on its own sub-tab and nowhere else — asserted absent on the other two Trade sub-views, on Plan & Watchlist / Scanner / Flip Log, and on Home and Review. The three are first-class Trade sub-tabs, one press from the watchlist. Detector proven against both failure shapes: `.tsub{display:block}` (sub-view scoping) and an ID rule outranking `section.tab{display:none}` (the historical R22.2 regression) | probe `[R35.6]` |
| R35.7 | The weekly review keeps a ONE-LINE read of each surface with a link through, and stops re-rendering them: the paper-book, hours and recipe checklist steps and the audit's §4 gate health and §5 prospecting are now summaries carrying their own `data-goto` — asserted on the summary functions themselves, not only on the rendered checklist, because the step's own "→ open" arrow would otherwise mask a summary that had stopped linking. The working tables (`paperGateTable`, `strataTable`, `hoursTable`) must NOT appear in the review. Walk-up attention cost unchanged: pull surfaces, no rulings on them, nothing new on Home, walk-up decision count still ≤ 7. **Complexity price tag:** these three absorb the review's paper-book / hours / recipe steps and the audit's §4 and §5 — growth paid for in consolidation, not stacked | probe `[R35.7]` |
| **§36 Weekly-review consolidation + auto-applied bookkeeping (user rulings, Aug 11 2026)** | | |
| R36.1 | Four sections left the weekly review and one was demoted. CUT: the basket ledger and the SLEEVE section (only meaningful when new intelligence lands — they live on the Sleeve tab, visited after an import; the sleeve's thesis-health lights are settable only there, so the block is machinery and moved with it), the recipe basis table (withdrawn entirely, R31.1), and the gp/min-of-attention reminder (a metric never acted on; the checklist foot's claim about it went too, since a closing line must not advertise a step that no longer exists). DEMOTED: the attention/dormancy report to an on-demand button (R13.2). Asserted by ABSENCE in the review AND presence on the tab that now owns each — a cut asserted only in the review would pass equally if the machinery had been deleted | probe `[R36.1]` `[R13.2]` |
| R36.2 | Coherence membership ADDS apply automatically at their existing evidence bar (excess corr > 0.5 vs the basket composite in ≥3 of the last 4 weekly readings), decision-logged with an `auto` stamp that distinguishes them from a press. Coherence DROPS deliberately do NOT auto-apply and their queue line says why, in the copy the user reads: an add only tightens a basket's exposure cap (restraint, which may auto-arm), while a drop removes an item from the cap's scope and can free capital the cap was withholding — the restraint/deployment line, which is the user's to move. Flagged rather than assumed from "membership is bookkeeping" | probe `[R36.2]` |
| R36.3 | The evidence bars still bind at APPLICATION time, not merely at proposal time: blacklisted items are never auto-admitted (the blacklist is the user's alone — no automated path admits, funds, quotes or clears an entry), and the 40% statistical-member cap is re-checked as each add lands, because proposals are made against the basket as it stood and several can land in one pass | probe `[R36.3]` |
| R36.4 | Seed audits auto-execute their own KEEP / RETIRE calls and sibling audits their PRUNE calls — the recommendation that executes is the one the audit produced, never one invented for it. Every auto-execution names the ruling that authorised it in the decision-log reason. PROMOTE is never auto-executed: it names a thesis cluster, and a name is a judgment, not bookkeeping | probe `[R36.4]` |
| R36.5 | The ruling traded a ratification ceremony for a reviewable record, so the record is prominent rather than buried: the review renders an "Applied automatically" block listing every auto-applied change in the last 30 days, decomposable to its rows through the shared primitive, carrying the undo path (an add is removed from the basket's thesis detail, a retired lineage is re-seeded by hand) and the reason drops are excluded | probe `[R36.5]` |
| R36.6 | Auto-application rides the POLL via `accrueBackground()`, never a render path — otherwise which tab the user happened to be on would decide whether a basket recomposed (the R34.1 rule, applied to the first machinery that changes state rather than accruing it) | probe `[R36.6]` |
| **§37 Observed-share floor on paper trips (user ruling, Aug 11 2026)** | | |
| R37.1 | A trip that opens and closes inside a single observation window has not tested patient-fill behaviour — it is tape coincidence — so it must not carry the evidentiary weight of a trip that survived a real horizon. Floor: **≥25% of the fill horizon observed** (`SHADOW_MIN_OBS`, a strategy parameter set by the user when ruling this, one constant to change). Below it a trip is EXCLUDED from every verdict: gate-tuning aggregates, the regime curves and their daily ledger, per-stratum verdicts, the row dot, the hours ledger's fill rate, and exception-lane evidence. Three states are kept distinct in code and copy — counted, insufficient observation, and `unobserved` (horizon elapsed with the tab closed, never a result at all) | probe `[R37.1]` |
| R37.2 | Excluded trips are RECORDED AND VISIBLE, never discarded: every aggregate states its excluded count, that count opens to the trips behind it through the shared primitive (a bare "3 excluded" would be a number the operator cannot audit), and every trip row names its own observed share so an excluded row explains itself. Asserted on the per-gate table, the gate-health tree, the cohort ledger, the hours table and the per-stratum map | probe `[R37.2]` |
| R37.3 | Exception-lane evidence ignores thin trips entirely — six fat-net thin trips ground nothing — and the row cite says the screen has no reading rather than reporting a null result. The floor gates EVIDENCE, it does not ban the item: the same item qualifies once enough observed trips exist | probe `[R37.3]` |
| R37.4 | The durable per-stratum ledger rolls thin trips into their own `insuf` counter rather than the region's verdict numbers, and the regime ledger's daily snapshot excludes them, so a fast fill can never argue for a gate change. Both counters render with their exclusion stated and openable **Dormant-gated Aug 14 2026 (§81):** the roll and the map's exclusion drill stay live (historical trips still complete their lifecycle); the regime snapshot's dormant form asserts the stronger claim — the frozen ledger takes NO row at all | probe `[R37.4]` |
| **§38 Glossary (user-directed audit, Aug 11 2026)** | | |
| R38.1 | The glossary is a DATA STRUCTURE (`GLOSSARY`), not prose, so coverage is mechanically checkable: 7 groups (gates, paper book, sleeve/catalysts/baskets, prospecting, indicators, metrics, and a seventh for how the tool governs itself — the governance terms fit none of the six subject areas, and burying the record of what the tool did without asking inside "metrics" would hide the thing that most needs finding), ~70 entries, unique ids. Every entry carries three fields: **what** it means, **from** what produces it, and **do** — what decision it should change for the user | probe `[R38.1]` |
| R38.2 | Gate coverage is derived, not maintained by hand: every name `gateName()` can return must resolve to a family entry through the entries' own `names` arrays, so adding a gate the glossary does not cover fails the suite rather than shipping unexplained. Gates are grouped into ~6 FAMILIES (price-and-spread, liquidity, chart/momentum, your-own-history, mandate/ownership, data-availability) with the individual names listed inside their family entry, rather than 19 stubs | probe `[R38.2]` |
| R38.3 | A gate name rendered in a bench reason is tappable straight to its family entry — on the watch row's bench badge, the paper-book per-gate table, the gate-health tree and the deployment funnel's proposal lines. The tap opens the group, scrolls the entry into view below the sticky chrome and flashes it, to the same landing standard as R24.2. **Visibility is measured on the GROUP's painted box**, because Chromium collapses a closed `<details>` via content-visibility on its slot: the hidden children keep a display value, an offsetParent AND a non-zero rect, so every per-element visibility check lies there | probe `[R38.3]` |
| R38.4 | Two stale claims found by the inventory are fixed and asserted against regression: the Item basket entry no longer says its P&L is reported in the weekly review (it is on the Sleeve tab since R36.1), and no longer says candidates "never cap anything until you ratify them" — it states the R36.2 carve-out instead (ADDS self-apply; DROPS and whole-basket candidates queue, because a drop loosens an exposure cap) | probe `[R38.4]` |
| R38.5 | Terms carrying a known limitation carry it at the point of reading rather than in a footnote: paper-book quarantine status and the shadow→paper rename, stratum name-pattern approximation, markout attribution lean, and the observed-share "unknown, not unfilled" doctrine | probe `[R38.5]` |
| R38.6 | Honesty about dead terms: an entry whose decision line is "nothing directly" must NAME the decision it is context for. A term that changes no decision and is context for none is a deletion candidate, and the scan surfaces those while the glossary is being written rather than never | probe `[R38.6]` |
| R38.7 | Navigable at its new size: groups are collapsible, a find box filters entries while keeping the groups it matched, and an expand-all control exists. The glossary panel is global (below the tabs), so it is reachable from every surface | probe `[R38.7]` |
| **§39 Inline glossary popovers (user ruling, Aug 11 2026)** | | |
| R39.1 | TRIGGER PARITY: hover on desktop and tap on touch open the SAME popover with the SAME content, through the same function — the phone is the primary reading surface, so nothing here may be a desktop-only affordance. The pre-existing "tap any badge for its explanation" claim (which the old glossary advertised and nothing implemented) folds into this rather than living beside it | probe `[R39.1]` |
| R39.2 | NO DRIFT: popover content is derived from the same `GLOSSARY` entry the full glossary renders — one source, two presentations. It carries the short definition (the entry's own first sentence, or a `short` override) plus the "what it changes" line and a "full entry →" link, and deliberately NOT the whole entry | probe `[R39.2]` |
| R39.3 | A popover that requires scrolling has failed, and this is MEASURED rather than trusted: every glossed term is opened and its popover asserted not to overflow, at whatever viewport the suite runs — so the phone pass is the binding one | probe `[R39.3]` (all ~70 terms, both viewports) |
| R39.4 | The phone failure mode: a gesture that begins on a term but is a drag or a scroll must NOT open a definition. Movement over 10px, page travel over 4px, or a dwell beyond 700ms all disqualify the tap. The scroll fixture asserts the page actually travelled, so a fixture that cannot move says so rather than passing | probe `[R39.4]` |
| R39.5 | Dismissal: Esc closes; a press outside closes WITHOUT `preventDefault`, so the press still reaches whatever is beneath and the popover never blocks the action under it; a second tap on the same term closes it. No focus trap, no backdrop | probe `[R39.5]` |
| R39.6 | Painted-box visibility per the case law's fourth entry: an open popover is asserted to paint a real box inside the viewport, and a closed one to paint nothing — measured, because presence in the DOM is not visibility | probe `[R39.6]` |
| R39.7 | SCOPE: glossed terms are marked wherever they render — gate names in bench reasons (watch row badge, paper-book per-gate table, gate-health tree, funnel proposals), row status badges, the dot glyphs, cohort labels, paper-book and prospecting column headers, and the gate-health stream labels. RESTRAINT: a dotted underline and nothing else — no icon, no colour of its own, because the indicators carry state and the underlines carry vocabulary; a row must not read as a minefield of help links | probe `[R39.7]` |
| R39.8 | The hover bridge (reported from use, Aug 11 2026): the gap between a term and its popover is dead space, and crossing it fires `pointerout` with a relatedTarget that is neither — which closed the popover before "full entry →" could be reached. Closing is now deferred ~260ms and cancelled if the pointer arrives at the popover or returns to the term; leaving for good still closes, so the delay is a bridge rather than stickiness. The probe settles the scroll position first, because dismiss-on-scroll is correct behaviour and an ambient scroll would otherwise close the popover mid-measurement and blame the bridge | probe `[R39.8]` |
| R39.9 | RESTRAINT, amended from use: **a control that already delivers its own intel is not glossed.** The ⚠ caution chip is a button whose press opens the row's expand view listing every caution and its reason, and a definition popover on top of it competed with the thing being reached for; the expand view's own caution lines already render their `why` inline. Gloss VOCABULARY — a word whose meaning is not otherwise available where it renders — never a control whose press is the explanation | probe `[R39.9]` |
| **§40 Four-touch cadence — per-touch horizons (user ruling, Aug 11 2026)** | | |
| R40.1 | The touch schedule is a SETTING (`DB.touchWindows`, four editable hours-of-day; default 07:00 / 12:00 / 17:00 / 21:30 giving gaps of 5.0 / 5.0 / 4.5 / 9.5h). Every horizon derives from the gap to the NEXT touch: sizing, the fill-horizon gate, fill forecasts, participation sizing, stale-buy clocks and paper-trip lifecycles all read it. **Absence-tolerant by construction** — everything is computed from "now → next window", so a missed touch simply means the next window is further away and the following horizon stretches, with nothing to reset. An **explicitly empty schedule is a real setting** meaning "no cadence kept", which falls back to the single global `fillHorizonH` the tool used before this build | probe `[R40.1]` |
| R40.2 | THE ROUTING LINE, which is the point of the shape: an item the current gap must bench, which the longest scheduled sit would fund, says so on its bench line — "qualifies at the evening touch — needs ~7h, and that sit is 9.5h (this gap is only 5h)". Fast cyclers carry the day; patient, wider-margin items fund at the evening touch. Silent when the current gap already covers the item, and silent when no gap does | probe `[R40.2]` |
| R40.3 | A leg stamps the horizon in force when it was PLACED (`hzH` on positions, quote records and paper trips) and ages against that, never the current gap — a leg placed at the evening touch is mid-sit at 05:00, not long stale. Records written before this build fall back to the old global, which is what they actually ran against. Fixes the audit's finding 6b (`staleBuyInfo`'s hardcoded 15:00 am/pm split, a literal two-touch convention) and 6a (`limitWindows` used a global day horizon, overstating daytime sizing on buy-limit-bound items by up to 2× — a 5h placement rolls one 4h buy-limit window, not two) | probe `[R40.3]` |
| R40.4 | Seasoning RE-EXPRESSED, not rescaled. The rule bought two things — "sustained across sessions" and "not committed on a single day's data" — and at four touches the old "3 passes spanning ≥2h, one per hour" bought only the first. Now: **3 passes, at most one per TOUCH, spanning at least one calendar day.** The span is a date rollover rather than a duration, so four touches cannot buy same-day qualification. With no schedule kept, the old rule stands | probe `[R40.4]` |
| R40.5 | Every paper-book and gate-health aggregate GROUPS by horizon shape (daytime vs overnight) and stamps evidence grade (live / reconstructed / mixed). Grouping, not filtering — a filter defaults to hiding the 5h-vs-9.5h split, which is the distinction the schedule exists to create; a finding that holds at one horizon and not the other is a HORIZON finding, not a gate finding. `shadowByGate()` stays keyed by gate for the verdict machinery and takes a flag for the shape-split view, so the two cannot disagree. A finding resting only on reconstructed evidence says so | probe `[R40.5]` |
| R40.6 | The 25% observation floor stays and is measured per trip against ITS OWN horizon — a 4h trip clears at 1h observed, a 9.5h trip does not. Existing open trips keep the horizon they were opened under; nothing is retro-applied | probe `[R40.6]` |
| R40.7 | RECONSTRUCTION BACKSTOP so the always-on host stops being load-bearing. On the first poll after an observation gap, each open trip's own 5m series is fetched once (capped at 12 items per pass, 250ms spaced, cached) and the fill model is REPLAYED under exactly the live rules — tape at or below bid, volume-weighted, capture-capped, participation-haircut. **Observation credit is SERIES COVERAGE**: a 5m bucket with data is five observed minutes, a bucket without credits nothing, and coverage stops when the trip closes. Spans beyond series retention stay unobservable by construction. Forced exits price at the **series value AT the horizon** for all trips — the proper fix for the defect that caused the epoch reset, correct even on a perfect host, with an approximate-price stamp when the series cannot reach back. Recovery is an EVENT: WHAT CHANGED reports "recovered N trips across a 6h gap" rather than leaving an outage to be inferred from a thin ledger a week later | probe `[R40.7]` |
| R40.8 | Attention is charged per touch (`DB.touchMins`, default 6/6/6/10 — the evening touch places patient capital and does the logging). The equity panel shows **gp per day beside gp per attention-minute**, because the cadence change moves them in opposite directions and either alone reads as a regression; it states the denominator change once, in place: "Denominator changed Aug 11 2026 — cadence" | probe `[R40.8]` |
| R40.9 | Unchanged by the cadence build, and asserted so: gates, floors, reserve and budgets (cadence is not edge), and the walk-up attention count still ≤ 7. The touch-timing heatmap is reframed from "aim the touches there" to scoring the four windows against each other, since the windows are now fixed | probe `[R13.1]` `[R20.4]` (budget) · UI (heatmap copy) |
| **§41 Market volume index, the `measured` tier, and rulings carried in the file (user ruling, Aug 12 2026)** | | |
| R41.1 | The anomaly scan's volume denominator is a MARKET-WIDE CONTROL INDEX (`VOL_INDEX_BASKET`, 8 liquid items deliberately unrelated to each other and to endgame PvM), recomputed at the start of every scan from the same endpoint and cache the scan itself uses. The index is the **median** of the controls' volume ratios, so a single control caught in a story of its own cannot drag it; under `VOL_INDEX_MIN` (4) usable controls there is **no index at all** rather than a guess from two | probe `[R41.1]` |
| R41.2 | The volume term of the accumulation signature is **item-volume-relative-to-index**, not raw: an item flags only when it beats the index by the same 25pp it previously had to beat its own past. An item that merely kept pace with a market-wide move is NOT accumulation. The price leg is unchanged and still binds, so a volume-only mover never flags. The raw figure is retained on the record but never drives the decision | probe `[R41.2]` |
| R41.3 | INTERIM SUPPRESSION for the known discontinuity: when an item's two comparison windows sit on opposite sides of `VOL_BREAK_D` (2026-07-24) the volume term is suppressed outright rather than normalised — the index inherits the same break, so there is nothing trustworthy to normalise against. Suppressed means **not flagged**: the signature is price AND volume, and flagging on price alone would invent the missing half. The test is **date-driven and self-expiring** — once the 2–3-weeks-ago window starts after the break nothing straddles and it stops firing, with no flag day to remember and no code to remove | probe `[R41.3]` |
| R41.4 | The index states where it stands and DECOMPOSES: the scan panel renders its value, its measurement date and how many of the basket returned usable series, and the number opens through the shared primitive to the control rows behind it — an aggregate whose one failure mode (a future update moving the whole basket at once) is visible only in its rows. Missing controls are declared as a subset by the primitive. An unavailable index says so and names the risk rather than silently reverting to the raw term that caused the incident; a scan whose items were suppressed reports the suppression, so an empty result names its own cause instead of reading as a quiet market | probe `[R41.4]` |
| R41.5 | Every flag names the volume term that raised it, in one shared phrase (`anomVolPhrase`) used by the panel, the pump fingerprint, the tape-question reminder and the analyst export, so the four cannot disagree about what the number means. A flag raised BEFORE the index existed says so explicitly and must not read as one the index confirmed; the analyst export carries `volMode` so the desk knows which term it is being asked to answer | probe `[R41.5]` |
| R41.6 | `measured` is a fifth confidence rung: a claim computed directly from the price API and reproducible from the endpoints it cites. It **outranks `confirmed` for claims about the tape**, because for a tape claim a measurement beats a statement about the measurement — while `confirmed` stays the top tier for announcements. That distinction is mechanical, not advisory: a `catalyst` / `catalyst-update` / `long-catalyst` claiming `measured` is a category error and is **rejected on import and counted**, never quietly downgraded, because no honest coercion exists | probe `[R41.6]` |
| R41.7 | A ruling given at the desk can travel back inside `intelligence.json` as `disposition: {action:"dismiss", reason, ruledOn}`. This is not the tool ruling for itself — the file is the handshake the user carries and presses Import on, which IS the constitution's explicit press. It is decision-logged with an `auto` stamp, the reason and the date ruled, so the log records **why** and not merely that, and it renders its reason where the record renders. **Only dismissal is carriable**: dismissing drops an advisory record while ratifying arms one, so the arming direction stays a press in the tool. A disposition with no reason is ignored — a dismissal without a why is not a record | probe `[R41.7]` |
| R41.8 | A RATIFIED record whose copy a later briefing revises is CORRECTED IN PLACE rather than absorbed. Before this, re-importing a ratified id was silently absorbed, so an analyst run that withdrew a number from a record the user had already ratified left the wrong number standing in the tool while the brief claimed it was fixed — found Aug 12 2026 while withdrawing eleven volume figures. The correction keeps the ratification and its teeth (copy changed; nothing re-armed, no gate moved) and stamps `corrected` so the record says the words you ratified are not the words here now. An unchanged re-import is still absorbed, so corrections do not churn | probe `[R41.8]` |
| **§42 Analysis export (user-directed, Aug 12 2026)** | | |
| R42.1 | A curated **read** export, distinct from the state backup: `⭳ export for analysis` on each of the three pull sub-views writing `analysis-<view>-<date>.json`, plus a combined `export all three` in the weekly review writing `analysis-all-<date>.json`. The combined file keeps each section's **own header**, because one merged header would hide that the three truncate independently | probe `[R42.1]` |
| R42.2 | EVIDENCE WITH ITS PROVENANCE, not aggregates — the decomposability rule applied to a file, since an export the reader cannot audit is the same defect as a number they cannot open. Paper book: every closed trip with item, cohort, horizon shape, evidence grade, observed share, entry/exit, the tape credit at BOTH participation readings, net sim, regime and benching gate, plus the per-gate rollups and the three regime curves as series. Gate health: per gate both streams with their constituent items and per-flip outcomes, die-off episodes with their 24h results, and the exception lane with its evidence. Prospecting: per-stratum rows with sampled items and verdicts, the gap-band sample, and the hours ledger with both streams by hour | probe `[R42.2]` |
| R42.3 | Trips **excluded by the observation floor are INCLUDED and marked** (`excludedByObservationFloor`), never dropped — the floor gates verdicts, not the record, and an export that silently omitted them would misrepresent the denominator | probe `[R42.3]` |
| R42.4 | Every export carries a HEADER so nothing is assessed as fresh when it is stale or thin: epoch start, generated-at, counts by cohort / evidence grade / horizon shape / outcome, an observation-coverage summary, the constants in force (gate values and floors, the touch schedule and its gaps, participation haircut, budgets), and the standing caveats that normally render at the surface — simulated-not-realized, stratum name-matching approximation, reconstruction limits, the two gate streams not being interchangeable, and pre-index anomaly flags | probe `[R42.4]` |
| R42.5 | SIZE DISCIPLINE: rows are capped per section with the **newest kept**, and every cap is declared in the header's `truncation` array with kept, total and the ordering — including sections that were NOT truncated, so silence never has to be interpreted. A file too big to paste is useless; one that silently truncates is worse | probe `[R42.5]` |
| R42.6 | HUMAN-READABLE: flat arrays of objects with named fields, and **no internal id that does not resolve inside the file** — every row that references an item carries that item's name beside its id | probe `[R42.6]` |
| **§43 Paper-book defect fixes (external analysis of the 2026-08-12 exports, user-directed)** | | |
| R43.1 | REGIME MEMBERSHIP IS A PROPERTY OF THE CANDIDATE, stamped centrally at open from its own effective ROI against the three floors (`regimesFor`), so no entry path can forget. It used to be attached by two of six paths while the other four hardcoded an empty set; in the epoch that found this those four were 96% of the book and the race had assigned **zero trips in its entire life**. Plus the NEVER-FED DETECTOR: an aggregate whose input population is empty says so in its own words — "the regime race has never been fed" is a different sentence from "the regimes are flat", and a zero that means the first is indistinguishable on screen from a zero that means the second. Applied to the regime curves and the divergence ledger independently, and carried in the analysis export as a flag beside the zeros **Dormant-gated Aug 14 2026 (§81):** stamping is retired — a dormant-era trip carries NO `regimes` key (absent = not evaluated, a third state beside `[]`), the never-fed notes are superseded by the dormant banner whose cause is the true one, and the export names the retirement beside the frozen series so a file reader cannot mistake dormant for never-fed | probe `[R43.1]` |
| R43.2 | FILL-MODEL CAUSALITY. A leg is credited only by a 5m bucket that STARTED at or after the leg was placed — the buy from its open, the sell from the moment the buy completed — and a bucket credits a leg ONCE however many polls present it. The bucket that completes the buy can never also fill the sell. A trip cannot resolve in the poll cycle that opened it. Before this the tick booked the whole trailing five-minute aggregate on a trip's first tick and re-booked it every poll: 182 of one epoch's 272 trips opened and closed in under a second, median 55ms, all "filled both legs", booking 52% of the headline simulated net from tape that predated them. The reconstruction path obeys the same rule. **Stage 1d:** the sell-eligibility conjunction is the named term `sellBucketEligible` (both ticks route through it, asserted at the term — the if/else-if ordering used to deliver the property while the named guard was unreachable), and once-per-bucket is additionally read at the pre-clamp term via the trace's bucket timestamps | probe `[R43.2]` |
| R43.3 | Every fill-rate figure renders its NEVER-FILLED count beside it, so a 100% reading is visibly a claim about zero counterexamples rather than a default nobody has tested. Asserted on the hours ledger, the row slice, the exception-lane candidates and the analysis export (per hour and per stratum) | probe `[R43.3]` |
| R43.4 | ONE DEFINITION OF "FAILS EXACTLY ONE GATE" (`failProfile`), read by the deployment funnel, the discovery slice and the scanner cohort. They had three: the funnel counted chart-pending gates as failures while the slice excluded them. **Unknowable is not failing** — "chart still loading" and "no history" mean the gate could not be asked, and never count toward the single-gate test. Separately, the per-stratum SAMPLING DENOMINATOR is counted before the near-miss filter (`strataCount`), not after: it used to exclude every multi-gate failure, so `nearMissRate` was 100% by construction and read 577 of 578 across 22 strata. The three outcomes (near-miss, clean pass, multi-gate wall) now sum to the denominator | probe `[R43.4]` |
| R43.5 | DIE-OFF CONFIRMATION DEPTH is its own constant (`DIEOFF_CONFIRM_REFRESHES`), separate from the volume gate's binding rule, so the episode ledger can be made stricter without touching a gate. An episode that recovers inside ONE TOUCH GAP is AUTO-VOIDED as noise — it never affected a placement, because nothing is placed or repriced between touches — and is excluded from the recovery counts **and the denominator**, because a false positive is not an episode the tag got right or wrong. Evidence: 64 of 64 episodes "recovered within 24h", median 5.4 minutes, across 16 items with ten of them Lumbridge teleport tabs. `DIEOFF_CONFIRM_PROPOSED = 6` (~30 min) is PROPOSED and deliberately not applied | probe `[R43.5]` |
| **§44 Reachability, partial as an outcome, and the headline as a rate (user ruling, Aug 12 2026)** | | |
| R44.1 | REACHABILITY: "the tape printed at or below my bid" does not establish that it reached ME — I may have been behind someone in the queue. With no book depth the one available signal is where the print sat relative to my price. A print **strictly past** my price (below my bid, above my ask) proves the queue at it cleared and takes full capture-capped credit; a print **exactly at** my price is ambiguous and takes the participation haircut only; a print on the wrong side credits nothing. Three readings accrue from the same buckets — strict (proven reachable), operative (drives state and net), lenient (possibly reachable) — and `lenient ≥ operative ≥ strict` always, so the fill claim is bracketed rather than asserted. The price test reads the BUCKET's own average, not `/latest`'s current instabuy, so price and volume come from the same bucket | probe `[R44.1]` `[R18.5]` |
| R44.2 | A leg fills only when credited volume accumulates to the FULL qty inside its horizon. Otherwise the trip resolves **never-filled** (nothing credited) or **partial** (some credited, never completed) — a real outcome for the first time. The old model silently shrank the trip to whatever it had bought and carried on, which is why "would not have filled" was reported zero times in 272 trips. A partial exits its position at the horizon price and counts as a counterexample in every fill rate | probe `[R44.2]` |
| R44.3 | THE HEADLINE IS A RATE, NOT A TOTAL. The bare summed net is retired: net renders **per cohort and never blended** (four cohorts answer four questions and their sum answers none), as **net per gp-day of simulated notional** so it is comparable across populations of different size and duration, with its **distribution** beside it (median trip, top-5 share of net, outcome census) so a total carried by one trip looks carried. Plus a **capital-feasibility** line: peak SIMULTANEOUS notional, swept over open/close events rather than summed, against working capital — flagged when the sim assumed more capital than exists, because a counterfactual that quietly spends money that is not there is not screening evidence | probe `[R44.3]` |
| R44.4 | The raw summed net survives in exactly one place — inside the regime comparison, where the three regimes screen the SAME population so the level cancels and only the difference carries information. It is labelled "comparison only, not a magnitude" where it renders (historical since Aug 14 2026 — the comparison renders under the race's dormant banner, label intact) | probe `[R44.4]` *(citation corrected Aug 14 2026: this column said `[R44.3]` while the assertion is tagged `[R44.4]` — a documentation mismatch, both ids always ran)* |
| R44.5 | Die-off episode confirmation depth ruled to **6** consecutive below-floor 5m samples (~30 min), up from 2, and kept separate from the volume gate's own 2-refresh binding rule, which is unchanged. It must fire before the next placement, so waiting a full touch gap is not available; 30 minutes clears the observed 5.4-minute noise median with room. All 64 episodes of the epoch that produced the evidence would have been suppressed, which is the intended outcome | probe `[R43.5]` |
| **§45 Ambiguous-by-touch, and fill-model calibration (user ruling, Aug 12 2026)** | | |
| R45.1 | The AMBIGUOUS reachability class is widened from "bucket average exactly equals my price" to "bucket average on the wrong side, but a LIVE instabuy observation touched my price inside that bucket". The original rule was correct as specified and inert in practice — a five-minute AVERAGE almost never lands on a specific bid, and it fired on 3.2% of buckets across the epoch, collapsing the proven/possible bracket for reasons unrelated to real reachability. A touch never upgrades a print already strictly past my price. Because a touch is only knowable once its bucket has closed, buckets are OBSERVED while current and CREDITED when the next arrives: fills lag by up to one bucket, which is the honest price of the distinction | probe `[R45.1]` `[R43.2]` |
| R45.2 | RECONSTRUCTION CANNOT PRODUCE THE AMBIGUOUS CLASS. A replay reads buckets after the fact and has no instabuy observation history, so a reconstructed span yields only PROVEN or NEVER. Proven credit still works normally — the limit is the class, not the model. Every trip touched by reconstruction is stamped `reachLimited`, so a reconstructed fill rate is never compared against a live one without the difference being stated | probe `[R45.2]` |
| R45.3 | FILL-MODEL CALIBRATION against realized flips — the test of whether a low paper fill rate is a finding about the market or about the model. Every logged real round trip is replayed with the same bid, ask, qty and horizon; since a logged flip completed both legs by definition, every disagreement is a model error with a known direction: agreement, false partial, false forced-exit, and the damning **false never-filled**, each with the median share of the leg the model did credit so "how wrong" is reported and not just "wrong". Three limits are STATED rather than worked around: the 5m tape retains ~36h so calibration is a ROLLING test; a flip needs a recorded buy duration or its window would have to be invented; and the ambiguous reading cannot be replayed at all, so calibration reports the two BOUNDS and says the operative reading is measurable only going forward. Older flips are explicitly NOT replayed against 1h buckets — an hour bucket credits twelve times the volume per step and would calibrate a different model | probe `[R45.3]` |
| **§46 Calibration window anchor, bucket trace, duration split (user-directed, Aug 12 2026)** | | |
| R46.1 | WINDOW ANCHORING, and the defect the first calibration run exposed. The log moment is the end of the ROUND TRIP, not of the buy: `buy starts → bh → buy done → sh → logged`, so the buy window opens at `logAt − (sh + bh)`. The first version anchored at `logAt − bh`, placing every replay later by the whole sell-leg duration (median 1.25h in this log). A multi-hour buy still overlapped its true window and replayed as filled; a sub-hour buy landed entirely inside or past the SELL period, where the price had moved toward the ask and never returned — so the credit was not small, it was ZERO. That is the 0%-not-partial signature, and it was a harness defect, not a model one. Both leg timings are REQUIRED (`sh = 0` is a real value, a missing `sh` is not); flips outside the 5m retention are skipped as `tooOld`. Extracted as `calibWindow()` so assertions call it rather than re-deriving the arithmetic | probe `[R46.1]` |
| R46.2 | A BUCKET-LEVEL TRACE on every replayed flip: each 5m bucket inside the window with its average low, its low-side volume, what it credited, and WHY — naming both prices when the bucket sat above the fill price. A zero on a leg that really completed in 23 minutes must be explicable down to the bucket, because a summary verdict cannot be interrogated. The row also carries a reach census (buckets reaching the price, of buckets in window) so a zero is visible before the trace is opened. Capped per flip with the truncation stated | probe `[R46.2]` |
| R46.3 | THE DURATION SPLIT renders on EVERY run, never folded into the headline: agreement, false partial, false forced-exit and false never-filled, for buy legs under 1h and 1h-and-over, on both reachability bounds. A model blind only on fast fills is a bounded defect; one blind everywhere is global pessimism; the two call for opposite responses and a blended agreement rate hides which you have. When the disagreement concentrates on fast legs the panel says so and states that paper fill rates must be re-read with duration held constant. Produced by `calibSummarise()` so the split cannot be dropped from a result without an assertion noticing | probe `[R46.3]` |
| **§47 Calibration export — the measurement leaves, the trading record does not (user-directed, Aug 12 2026)** | | |
| R47.1 | `⭳ export calibration` on the calibration panel writes `analysis-calibration-<date>.json` under the same header convention as the other three analysis exports. It carries both bound summaries (agreed, false partial, false forced-exit, false never-filled, median share of the buy credited when wrong), per replayed flip the item, qty, real buy and sell durations, the anchored window as offsets, both verdicts and the reach census, and the per-bucket trace with each bucket's relative price, volume, credit and reason — an above-price bucket states the GAP so reachability is legible. Skips are counted by reason, never silently dropped | probe `[R47.1]` |
| R47.2 | The DURATION SPLIT is present for both bounds even when a bucket is empty, rendered as explicit zero counts rather than omitted, because it is the field that decides bounded-defect vs global pessimism and an absent zero reads as no data. The guarantee lives in `calibSplit()`; the export asserts the contract rather than adding an unreachable fallback, since dead code that reads as a safeguard is worse than none | probe `[R47.2]` |
| R47.3 | THREE EXCLUSIONS, enforced in one place and probe-asserted against a fixture that contains every one of them, searched for by magic value rather than by inspection: **no absolute prices** (bucket lows and the realized spread appear only as percentages against the trader's own fill) and none recoverable — a gp delta beside a percentage inverts to the absolute figure, so the delta the brief asked for is deliberately omitted and the file says so; **no gp** (no realized profit, no bank, and the gp-denominated capital constants stripped from the header, with the withholding stated) while quantities and market volumes survive because they are not money; **no wall clock** (bucket times are minutes from each window's own start, windows are durations, no absolute stamp survives) | probe `[R47.3]` |
| R47.4 | Four honesty lines ride the header: the operative reading lies between the bounds and is measurable only forward; the ambiguous class is unavailable to replay; comparing against the bucket average biases pessimistic when the trader bought at the prevailing instabuy, with the per-bucket trace named as the way to tell that apart from a window error; and the eligible-sample size with the reason for every skip. Size discipline: traces for the 12 most recent flips, newest first, cap declared; a trace over 60 buckets keeps the first 20 and last 20 and says how many it dropped. The stored trace cap is deliberately set ABOVE that threshold so the trim is reachable rather than a dead path | probe `[R47.4]` |
| R47.5 | SELF-COMPARISON, named where the bound is read and MEASURED rather than asserted. Calibration compares each bucket against the trader's OWN realized fill, so a bucket whose average low lands exactly on it very likely CONTAINS the trade being measured — equality is EXPECTED, not ambiguous, and the proven bound's refusal to credit it depresses that bound by construction. The at-price bucket count is carried per flip and in total so the artifact's size is visible; a low proven bound must be read against it before it is read as a market finding. **This does not apply to the paper book**, where the comparison price is a simulated bid that never traded and equality is genuine ambiguity. The two constructs are labelled distinctly in the panel and the export, with an explicit instruction not to carry a conclusion between them | probe `[R47.5]` |
| R47.5b | ⚠ **CORRECTED Aug 12 2026 — this row's sell-side prediction was falsified by measurement, and the correction is recorded here rather than by editing R48.1 silently, because you read the original.** R47.5 and R48.1 both predicted that self-comparison would bite HARDER on the sell leg, on the reasoning that a sell completing in minutes leaves almost no bucket that is not the trade itself. Measured: **zero at-price buckets on the sell side, both bounds identical.** The prediction assumed the trader's own print would DOMINATE its 5m bucket; on a liquid item it is one print among many, so the bucket average never lands on the fill. The consequence is stronger than the retraction: the two sell bounds diverge only where a bucket is at-price or live-touched, neither of which a historical replay can produce, so their identity is a property of the harness rather than a measurement of the market — and a fast-sell failure is a REAL model error, not an artifact. The panel and export both carry the correction naming the old claim | probe `[R48.1]` |
| **§48 Sell-leg calibration (user ruling, Aug 12 2026)** | | |
| R48.1 | The sell leg is graded on ITS OWN ANCHOR, at the moment the buy really finished (`logAt − sh`), against the logged sell duration — never chained off the model's own `buyDone`, because a pessimistic buy would hide the sell entirely and the sell is where 9 of 10 failures land in the 43-trip replay. Same fields as the buy: agreed, false partial, false never-sold, median share credited when wrong, both bounds, split by sell-leg duration. Assembled by `calibRowFor()` so an assertion tests the WIRING — that the sell is graded regardless of the buy's verdict — and not merely the two replay functions in isolation. ~~The self-comparison caveat is stated as HARDER here~~ — **superseded by R47.5b: that prediction was falsified by measurement.** The panel and the export now carry the correction, naming the claim they are correcting rather than swapping the copy. **Stage 1d:** the anchor is the named term `sellWindowStart()` (the reconWindowStart pattern — the in-loop filter and the minsIn arithmetic shared `start`, making "no tape before the window" an identity for ANY anchor), and the credits-nothing half is driven by a reachable print placed before the anchor | probe `[R48.1]` |
| R48.2 | SELL-LEG PARITY IN THE EXPORT (user-directed, Aug 12 2026). The first export built after the interrogability rule was widened to artefacts shipped the sell **aggregate alone** — a rollup without its rows, which is the same finding in a file as it is on a screen. The sell now carries everything the buy carries, per replayed flip: window offsets against the real buy-completion anchor, both verdicts, credited percentages on both bounds, the reach census, the at-price count, and the bucket-by-bucket trace with each bucket's relative price, high-side volume, credited units and reason. The comparison flips sides — a POSITIVE percentage is proven reach on a sell where it is a miss on a buy — so the reason strings say which in words rather than leaving the sign to be inferred. Same trimming rule as the buy trace, with every truncation declared in the header. A flip with no sell replay states that and why, rather than rendering an empty object | probe `[R48.2]` |
| **§49 Never blend a rate (user ruling, Aug 12 2026)** | | |
| R49.1 | The net-per-cohort rule generalised to EVERY rate — fill rate, credited share, agreement rate, hit rate. A blended figure may render only ALONGSIDE its decomposition, and beside it on the page rather than only inside an expansion the reader must open. `rateBlend()` is the sanctioned renderer and emits both together, so a caller cannot produce the blend without the split. A single-cohort rate says so rather than implying a split it does not have; an empty population renders as "no population", never as 0% | probe `[R49.1]` |
| R49.2 | Applied where rates actually render: the per-gate paper fill rate and the hours ledger's paper fill rate both carry their cohort split inline. The incident that forced it — "median 37% of intended size" was watchlist 100%, scanner 37%, slice 8.3% pooled, and read as a sizing problem across the book when the items closest to fundable were filling completely | probe `[R49.2]` |
| **§50 Fill-forecast input, and the interim soft tag (user ruling, Aug 12 2026)** | | |
| R50.1 | The fill forecast divides by REACHING flow — low-side volume from hours whose average low sat at or below the intended bid, an hour above it contributing zero rather than a little — taken as the MEDIAN of the last six hourly readings rather than a single trailing hour. Graded on 43 replayed trips: spread 518× → 24.6×, wrongly-promised fits 12 → 9. With no hourly series it falls back to the old total-flow input and SAYS which basis it used | probe `[R50.1]` |
| R50.2 | INTERIM: the fill-horizon gate is a SOFT TAG, not a hard bench. It queues as NEXT UP with the forecast and its own uncertainty stated, because on the same 43 the gate would have benched 4 of the 10 trips that actually completed. The downgrade is decision-logged with the evidence (518× range; Varrock tabs predicted 49.8h, filled 1.3h) and NAMES ITS OWN REVERSION, so an interim cannot become permanent by inertia; it logs once however many plan builds run | probe `[R50.2]` |
| R50.3 | HONEST DEGRADATION (user ruling, Aug 12 2026). ~~The old behaviour fell back to total 1h low-side flow and said which input it used~~ — **superseded: saying so is not enough.** The fallback input IS the 518× error, so an estimate built on it claims an accuracy it does not have, which is the claims-vs-computation defect. The estimator now DECLINES in two states and names what it could not compute: **no hourly series at all**, and **fewer than `EST_FLOW_MIN_HOURS` (3) readings**, where "median of the last 6 hours" is not the statistic the copy advertises. `estFillH()` returns null and `fmtEstH()` renders "unavailable" — never a bare dash, never ∞ standing in for "unknown". **Zero reaching flow is explicitly NOT a decline**: nothing printing at or below the bid for six hours is a reading, and the reading is that it does not fill | probe `[R50.3]` |
| R50.4 | REACH SHARE, reported beside the estimate, not buried. Reaching flow ÷ total low-side flow, with the count of hours that reached the bid — the diagnostic for WHY a prediction is long, and per the interrogability rule the estimate ships with what produced it. `reachFlow()` is the single evaluation returning flow, state, reach count and share together, so the figure and the account of the figure cannot come from separate reads; `reachFlowPerH()` is a thin accessor over it. Also fixes a live claims-vs-computation defect: the plan line's tooltip still described the pre-Aug-12 formula ("qty ÷ buy-side 1h flow × 15% capture") for as long as the corrected input had been live | probe `[R50.4]` |
| R50.5 | THE WINDOW COUNT IS A PROPOSED CONSTANT WITH A STATED PRICE, not a silent pick. Six hours: the drift measurement put in-window over pre-open flow at median 1.48× with IQR 0.7–2.58× and tails to 45.6×, so the median needs enough readings that one tail hour cannot carry it (at n=6 it sits between the third and fourth); six is also the shortest window spanning the whole of the shortest touch gap (5.0h), so an estimate rests on readings from the cadence period it will be judged in. **The cost, stated because a constant nobody can argue with is a constant nobody checked:** a 6-hour median needs four bad hours before it moves, so a book dying at 09:00 reads healthy in this input until ~13:00 — while the die-off tag confirms in ~2 minutes. The two disagree for up to four hours BY CONSTRUCTION, and that is the right way round: the die-off tag is a restraint and may act on thin evidence, the estimator funds things and may not. Where they disagree the tag wins. A strategy parameter; it does not move without a ruling | documented (constant + cost) · UI |
| **§51 Observed time — no claim about a period that was not observed (user ruling, Aug 12 2026)** | | |
| R51.1 | Any denominator counting time or occasions counts only OBSERVED ones and states its coverage. The escaping instance: `daysBenchedBy(id, gate, 7)` drove gate-persistence proposals reading "benched on 4 of the last 7 days" against a denominator of 7 even when the app was closed for three of them — and the 4-of-7 bar is the standard that moves gate constants. The numerator was always honest (a ledger row exists only on a day a plan actually built), so the defect was purely the denominator. `DB.obsDays` stamps each day a plan builds; `observedDaysIn()` unions that stamp with the gate ledger's own dates, so the rule works retroactively on an existing store without a migration; `daysBenchedBy()` returns the pair `{n, obs, window}` so a caller cannot render the count without its coverage. The unobserved remainder is named in words, not left to arithmetic. **The 4-day bar itself is unchanged** — that is a strategy parameter and moves only on an explicit ruling | probe `[R51.1]` |
| R51.2 | UNREACHABLE IS NOT ABSENT. Below 4 observed days the persistence bar cannot be reached at all, so reporting "none carry multi-day ledger persistence" would report an absence of observation as evidence about the gate — the never-fed-aggregate defect wearing a different hat. The funnel says which case it is in, names the coverage, and still proposes nothing. Above the bar, the "no persistence" line states the coverage its claim rests on | probe `[R51.2]` |
| **§52 Expiry is not a lift (user ruling, Aug 12 2026)** | | |
| R52.1 | Removing a restraint is deployment, whether by action, by expiry or by a rule change. A ratified CAUTION (watch-note, deflation-flag, or anything carrying teeth) reaching `validUntil` goes **lapsed**: it keeps applying — tags, sleeve refusal and haircut all still in force — and asks for one ruling. A CONTEXT record (catalyst, catalyst-update, long-catalyst, demand-context, cluster-membership) still expires on the calendar exactly as before, because it restrains nothing and so nothing is deployed when it ends; the restraint/deployment line puts it outside this rule. The lapse is decision-logged as still-applying and names the press it waits on | probe `[R52.1]` |
| R52.2 | THE CONTRADICTION CLOSED, in both directions. The standing rule is that a flagged pump caution lifts on ONE path — the user dismissing the warning record, "nothing else" — and three calendar paths contradicted it: `validUntil` deactivation, the pending-expiry sweep, and the 30-day staleness broom. A pump-defense record now does not lapse by calendar at all, pending or ratified, and `promotionWarningsFor()` drops the import-window and expiry clauses that lifted it two more ways. **Once fired it stays fired**: the anomaly leg tested a flag within `pumpWindowD` days of NOW, so a defense that had already fired un-fired itself as its evidence aged — the firing is now stamped on the record, which leaves the DETECTION rule (a strategy parameter) untouched while removing the lift. The copy stops claiming a property the code did not have: "lifts when dismissed or expires" is gone from the panel, the activation line and the record | probe `[R52.2]` |
| R52.3 | ONE walk-up line for lapsed cautions however many there are — the agreed attention price, against a budget of 7. Only the RESTRAINT-PRESERVING action is offered in bulk: "extend all 30d" keeps every caution in force, so a mistaken press costs only absence. There is deliberately **no bulk drop** — dropping lifts a caution per item and a bulk press cannot carry per-item judgment; dropping stays per-record on the desk, requires a stated reason, and is decision-logged as a deployment. Each record states where it stands (lapsed-and-still-applying, or pump-defense-deliberately-unaffected) rather than rendering a passed date with no explanation. The pending auto-dismissal survives with corrected copy: an unruled record was never armed, so it is queue hygiene, not a restraint lift, and the log says which | probe `[R52.1]` `[R52.3]` |
| **§53 Gap band: raised sample, held proposal, standing verdict (user ruling, Aug 12 2026)** | | |
| R53.1 | Per-cycle sample raised 5 → 10 so the band's verdict matures faster. The constant and the copy that describes it are asserted TOGETHER — a constant raised without its copy following is the claims-vs-computation defect, and checking only the constant would not catch it | probe `[R53.1]` |
| R53.2 | A STANDING VERDICT LINE, so the decision arrives as a report rather than as the operator's recollection: clean and contaminated trips counted apart — trips, fills and net for each — never pooled. **The scanner proposal is HELD** and is reconsidered only on clean, post-fix evidence clearing the same bar any funded item clears. While the paper book has open defects on its register, no trip closing now is clean evidence and the line says so rather than counting them; the old total-trip count would have called the band ruleable, which pooled contaminated trips with clean ones — the same defect one level up | probe `[R53.2]` |
| **§54 Contamination labelling (user ruling, Aug 12 2026)** | | |
| R54.1 | The claims-vs-computation rule applied to PROVENANCE rather than definition: a number whose input is under repair is making a claim it cannot support even when its own arithmetic is correct. Any figure computed from the paper book states that dependency where it renders and carries the book's current health state, naming the open defects. **Both directions render** — the realized streams, the market stream and the sampling counts state that they are NOT downstream, so the clean figures are identifiable at a glance rather than from memory of a conversation. A label only on the contaminated figures decays into "unlabelled means nobody checked". `PAPER_DEFECTS` is the register; an entry is removed when the defect is fixed AND the book has re-accrued evidence clear of it, not when the fix lands | probe `[R54.1]` |
| R54.2 | Applied to the four named surfaces: gate health's paper columns, per-stratum sim P&L and fill rates, the hours ledger's paper stream, and the gap-band verdict. Each names its CLEAN counterpart on the same surface rather than in a footnote — the gate's realized stream, the sampling counts, the market stream. `hoursStreamsNote()` is extracted so the labelling is assertable without a fixture that happens to carry market rows | probe `[R54.2]` |
| **§55 Prediction stamped at open (user-directed, Aug 12 2026)** | | |
| R55.1 | The paper export could not grade the estimator: it carried the OBSERVED fill time and no prediction. Every trip now stamps, at OPEN, the predicted fill hours under the reaching-flow input, the reach share and reach count that produced it, the flow figure and its basis state, and the pre-Aug-12 total-flow prediction — the last so before/after is measurable on ONE population rather than across two epochs, which is the only way to compare them without the cohort mix moving underneath. **Retrofit is impossible** — these are open-time facts a rolled 5m series no longer holds — so the regrade's sample starts accruing from the first scan after this ships. Asserted THROUGH `shadowScan`, not by calling the stamper directly, since a probe that calls the stamper passes even if the scan stops calling it | probe `[R55.1]` |
| R55.2 | The export carries prediction beside observation per trip, with the observed buy-leg fill hours and, where the buy never completed, a note that the trip belongs in the false-fit test rather than the ratio distribution. A trip opened before stamping declares itself unavailable and says why, rather than being silently absent from the regrade | probe `[R55.2]` |
| R55.3 | DIAGNOSTIC: the export carries `resolvedInMs` and `openPollSeq`. ISO stamps truncate to the second, so "resolved in under a second" could not be told from "resolved in the same second"; and the minimum-life rule is stated in POLL CYCLES, so a file that cannot show the cycle cannot show whether the rule held. A null `openPollSeq` means the record predates the causality fix, which is itself the answer to why it resolved instantly | probe `[R55.3]` |
| **§56 Epoch 2, the stale host, and the stalled generator (user ruling, Aug 12 2026)** | | |
| R56.1 | THE DETECTOR FOR THE FAILURE THAT FORCED THE RESET. Every trip in epoch 2 must carry an open-cycle stamp; one that does not was written by a build without the causality fix, which is exactly how epoch 1 was corrupted. It raises a FINDING naming the reload, not a caveat, and is silent when every trip carries one — epoch 1 was invisible until a field existed that could show it | probe `[R56.1]` |
| R56.2 | THE RESET. Clears the simulated streams — paper book, divergence ledger, rolled per-stratum outcomes — and voids exception-lane paper evidence while the RULINGS stand. KEEPS the realized streams, the market stream and the per-stratum SAMPLING history, because those record tests performed rather than simulated outcomes and clearing them would destroy real coverage to fix a simulated number. Stamps epoch 2 and decision-logs the user's stated reason verbatim. **Filtering was rejected in favour of discarding** because the divergence ledger, the rolled counters and the exception evidence are CUMULATIVE — a per-trip predicate can exclude rows but cannot un-mix a rolled total. Extracted as `paperEpoch2Reset()` so an assertion can call it; inline in `load()` it was unreachable and a seeded defect on it changed nothing the suite could see | probe `[R56.2]` |
| R56.3 | THE STALLED GENERATOR STATES ITSELF. A book that stops opening trips accrues nothing, and every aggregate goes on rendering as though it were live — opening stopped at 18:10 while trips closed until 20:46 and no surface said so. Nothing was broken: the family cooldown is 2 × FILLH(), and FILLH is the gap to the next touch, so around the evening touch it is ~19h; a family is blocked while its last trip is open OR closed inside that window, so closing does not release it. `shadowScanState()` computes the sentence from the same values the scan gates on, the paper headline renders it whether or not anything is wrong, and a long quiet spell is flagged rather than merely reported. **Not applied, noted for a ruling:** a cooldown scaling with the horizon is longest exactly when the book has the most observation time | probe `[R56.3]` |
| R56.4 | THE BUILD STAMP. A tool whose host never reloads has to notice its own staleness — this one runs as a long-lived foreground tab by design, and the consequence was five hours of trips written by a cached build. The app records `BUILD_ID` at load, renders it in the header always, and re-fetches its own URL with a cache-buster hourly to compare. Three DISTINCT states: current, stale (which also leads WHAT CHANGED with a reload line), and could-not-check — an unreachable check must never render as up-to-date. A stamp that appeared only when stale could not be trusted by its absence | probe `[R56.4]` |
| **§57 The family block, severed from the horizon (user ruling, Aug 12 2026)** | | |
| R57.1 | One constant served two unrelated jobs. CONCURRENCY (no two open trips in the same family) is a property of being OPEN and needs no duration — the open trip is the block, regardless of age. SAMPLE INDEPENDENCE (do not re-sample a family so fast that the second trip is the first trip's conditions measured twice) is a SAMPLING concern with no exposure argument behind it, since the paper book holds no capital and carries no correlation risk. Fused at `2 × FILLH()`, the sampling rule inherited an exposure rule's scale and inverted: ~19h lockout at the evening touch — the touch with the most observation time and the most to learn — against ~9h in the morning. Now an OPEN trip blocks its family and a CLOSED one releases it after a FIXED cooldown that does not move when the horizon does. Applied to the scanner cohort too, so the two cohorts do not sample at different rates | probe `[R57.1]` |
| R57.2 | THE CEILING, computed rather than assumed. A family cycles: blocked while open (about one horizon), then the cooldown, then re-samplable — so the generator can produce `24 ÷ (horizon + cooldown)` trips per family per day, and the book's ceiling is that times the family count unless the concurrency cap binds first. Both are computed, the BINDING one is named, and it RENDERS on the paper surface: a cap nobody can see is a cap nobody accounts for, and if sample size is structurally capped that is worth knowing before waiting weeks for evidence **Amended Aug 14 2026 (§81):** the rotating-cohort term reports what can actually accrue — under the dormant slice that is the gap band's `floor(cap/2)` share alone, and the copy names the dormancy | probe `[R57.2]` |
| R57.3 | RULED Aug 12 2026 at 5h, as proposed, and decision-logged with the reasoning it was approved on. Five hours because: it is the modal touch gap (5.0 / 5.0 / 4.5 / 9.5), so a family becomes re-samplable about once per touch and re-sampling rides the same cadence as the decision it informs; it is five fresh hourly readings, which is what sample independence means for these inputs; and it is horizon-INDEPENDENT, which is the point. Shorter would correlate consecutive samples through the same hourly readings; 9.5h would reproduce the inversion | probe `[R57.3]` |
| **§58 The fill-model version stamp (Aug 12 2026)** | | |
| R58.1 | Every trip records the fill-model version that produced it. A corrected SELL model would change outcomes on trips already closed under the old one — `sellQ` decides filled-vs-forced-exit and therefore `net` — and those trips CANNOT be re-graded: the tick discards each bucket's evidence once credited (`p.pend = null`) and `touchSell`, the live observation the operative reading needs, is unrecoverable from any replay. The naive consequence is that a sell fix forces another epoch; it does not, and believing so is the reasoning error that made epoch 1 unrecoverable. Epoch 1 could not be partitioned only because nothing recorded which build wrote each trip — the moment `openSeq` existed, a corrupt population became identifiable by a field. Generalised: a model change PARTITIONS the book by `fillModelV` instead of invalidating it, and the two versions are never pooled. Bumped on any change to what a bucket credits; it is not a build id, because the version is a claim about the MEASUREMENT rather than about the code | probe `[R58.1]` |
| **§59 The sell discriminator (user ruling, Aug 12 2026)** | | |
| R59.1 | Every failed sell gets a COMPUTED classification — dilution / mislocated-window / neither-fits — from the per-bucket figures carried beside it, never a human reading a trace. DILUTION: small negative gap with real volume, meaning the bucket average sat inside the intra-bucket range and prints above the ask are plausible. MISLOCATED WINDOW: a large gap SUSTAINED across the window — a window containing a dip is not a wrong window, which is what the sustain share separates. **NEITHER-FITS is a real available outcome**, not a residue: a gap between the bands surfaces as an unclassified pile, and prints-with-no-volume surfaces by name as an ILLIQUIDITY finding, so a third mechanism is not forced into one of two buckets. **Stage 1d (census probe:6716/6749/6753):** the assertions drive the classifier WITH spreads as production sends them, assert the deciding figures (`relBandPct`, `spreadPct`) by value, isolate the sustain bar from the dilution band, name the no-spread call as the fallback path, and forbid the why-copy from rendering NaN or a zero-spread claim — the copy names the band that actually decided | probe `[R59.1]` |
| R59.2 | Counts render always; PERCENTAGES only at n ≥ 8, and the surface says which it is doing. Under the bar a confident split would be the defect the discriminator exists to prevent; an empty population says there is nothing to classify rather than rendering a zero split. Thresholds are PROPOSED not ruled — dilution within 1.0% of the ask (a sell sits one tick inside the spread and the ROI floor is 1.2%, so 1% is inside the item's own intra-bucket range), mislocation at 5.0% sustained over 70% (≈4× the ROI floor, far outside noise, and most of the window rather than a couple of buckets) | probe `[R59.2]` |
| R59.3 | The live book RETAINS a bounded sell-leg trace, including the buckets that credit NOTHING — those are the informative ones. Without it the live population could not be classified at all: the tick nulls `p.pend` once credited, so no per-bucket evidence survived. Instrumentation only — it changes no credit and no verdict, so `FILL_MODEL_V` does not move | probe `[R59.3]` |
| R59.4 | The split renders per population and is NEVER pooled: calibration replays real completed sells where any verdict but sold is a model error; the live book holds simulated sells that never completed. Each carries its own n. The structural asymmetry is stated as a FALSIFICATION TEST rather than a footnote — the live book's sell leg accrues forward from `buyDone` and has no window to place, so mislocation cannot occur there by construction; dilution in both populations confirms the credit rule, while mislocation in calibration with the live book failing anyway means two distinct defects. Renders in panel and export with each case's figures | probe `[R59.4]` |
| R59.5 | THE DERIVATION IS FROZEN (user ruling, Aug 12 2026). 1.0% was derived once from a 1.2% ROI floor and is now a fixed constant that does NOT track it. A threshold defined as a function of a strategy parameter makes its own history incomparable — last week's "dilution" and next month's would mean different things, and a split measured across a floor change would pool two questions with nothing saying so. A classifier has to hold still to be an instrument. `sellThresholdDrift()` REPORTS divergence between the frozen threshold and the floor it came from; it never corrects it, because the point of freezing is that the drift prompts a ruling rather than triggering an adjustment. **Stage 1d (census probe:6812):** the freeze is asserted on the band that is LIVE since the ratification — the spread-relative one (`SELL_SPREAD_REL_FRACTION` does not track `GATE.roi`) — with the absolute constant checked as the explicitly-named fallback, not as the headline | probe `[R59.5]` |
| R59.7 | THE BUY-LEG TRACE (user ruling, Aug 13 2026) — R59.3's shape on the other leg, for a sharper reason. `shadowCredit` stores `Math.min(qty, …)`, so a per-bucket term LARGER than the trip's own size is clipped and leaves no record: OVER-crediting, the one direction that would flatter every fill rate in the book, was structurally unobservable. Asked whether the book had ever over-credited, the only honest answer was "cannot be reconstructed — the per-bucket inputs are discarded at credit time — and bounded away from catastrophe only by the fact that no trip ever filled inside a single bucket". The trace retains each bucket's low, low-side volume, touch flag and **the credit BEFORE the clamp**, including buckets that credit NOTHING, since those are what say the offer stood and the tape did not come. `buyAbsorption()` reports credit offered against trip size, and the surface reports the median ratio; a trip with no trace reads UNKNOWN rather than as zero absorption, because absence of evidence must not read as evidence of restraint. The cap is DERIVED from the horizon (12 buckets an hour plus the deferred-credit lag) so it can neither truncate the window silently nor become a guard its own upstream limit forbids. **Instrumentation only** — no credit changes, no verdict changes, `FILL_MODEL_V` does not move | probe `[R59.7]` |
| R59.6 | **THE BAND-TRANSITION REPORT — direction flipped with the ratification (stage 1d, census probe:6827).** This report was built to ask "what would a spread-relative band change?" while the absolute band was live; the Aug 13 ratification made the spread-relative band the LIVE classifier, so that comparison became the live class against a re-derivation of itself — identically zero on every real population, under copy claiming the absolute figure "is not doing any harm". The counterfactual that still carries information is the RETIRED absolute band, which the classifier stamps on every case as `absCls`: the report compares live vs `absCls`, carries the moving cases by name with their spreads, reports class counts under the retired band, and says INSUFFICIENT below the n bar rather than estimating. Cases reach it only through the classifier — hand-built class values are states production cannot produce | probe `[R59.6]` |
| **§60 Surface noise — relocation must not strand (user ruling, Aug 12 2026)** | | |
| R60.1 | 79 persistent blocks, 22,608 characters, ~594 phone lines against a 7-decision walk-up budget. Standing truths and teaching notes move behind `teach()`, whose disclosure content IS the original text — that construction is what makes the claims-vs-computation rule survive the edit, since the tap cannot fail to reach what it opens. A generic scan asserts no `details.teach` on any surface has a summary without a body, so any future relocation inherits the check | probe `[R60.1]` |
| R60.2 | CONDITIONAL WARNINGS ARE NEVER DEMOTED. A warning always on screen is wallpaper, and wallpaper is how a real warning gets missed — so the load-bearing half stays visible while only its explanation moves. Asserted specifically: the correction marker renders OUTSIDE its disclosure (it is a landing path and the corrections rule binds), and the stall line's state renders always while its rationale is gated on the stall | probe `[R60.2]` |
| **§61 Verdict-first surfaces (user ruling, Aug 12 2026)** — *rows written retroactively Aug 13 2026: the assertions shipped tagged and the rows were never added, so six ids reported PASS against nothing. See the pairing check below (R6.2)* | | |
| R61.1 | EACH SURFACE ANSWERS ITS OWN QUESTION FIRST, in one sentence, before any machinery renders — "Does my edge survive honest fills?", "Are my gates too tight or too loose?", "Where in the market is there edge I'm not fishing?". During accrual the answer is a PROGRESS statement carrying the count, the bar and the rate at which it is filling, never an empty table: a surface that cannot answer yet says so and says when it will be able to | probe `[R61.1]` |
| R61.2 | THE COUNT OF ELEMENTS ABOVE THE FIRST DISCLOSURE is small enough to hold in mind (≤3 on the paper surface, asserted structurally by counting children before the first `<details>`). The verdict-first restructure is worthless if the fold line simply moves down the page | probe `[R61.2]` |
| R61.3 | NOTHING RENDERS AN EMPTY SHELL — no headers, no zero-row tables, and no caveats about numbers that do not exist. A caveat attached to an absent number is noise that reads as a warning. Asserted on the prospecting verdict with its ledger emptied: it must read as progress, carry no `<table>` or `<th>`, and not recite the stratum-classifier caveat | probe `[R61.3]` |
| R61.4 | CONTAMINATION AND STALL STATES ARE THE STATED EXCEPTION and stay ABOVE the fold at full weight. Everything else may fold; a provisional-answers warning and a stalled-generator line may not, because a book that is not opening invalidates everything below it and a fold is how that gets missed | probe `[R61.4]` |
| R61.5 | A FOLD'S LABEL SAYS WHAT OPENING IT WOULD TELL YOU — "Regime curves — would a looser or tighter ROI floor have done better?", never "table" or "details". The label is the only thing a reader has to decide whether to open it, so a label naming the element type spends the disclosure's whole budget saying nothing | probe `[R61.5]` |
| R61.6 | ONE TAP OPENS A FOLD AND THE ROWS INSIDE ARE THEN REACHABLE — asserted structurally (starts closed, rows live inside it, one tap opens it) rather than by measuring layout, since whether a closed `<details>` zeroes its children's boxes is engine-dependent in headless and asserting on that would test the renderer instead of the restructure. The interrogability rule binds after the fold exactly as before: a fold that strands its rows is the violation this restructure was most likely to produce | probe `[R61.6]` |
| **§62 The fixed paper horizon, the fill curve and the cadence read (user ruling, Aug 13 2026 — supersedes the gap split ruled the same day, which superseded the duration split ruled that morning)** | | |
| R62.1 | THE PAPER BOOK STOPS PREDICTING THE OPERATOR'S RETURN. Real offers have no deadline; the horizon was an artifact of needing the simulation to end, and tying it to the walk-up made the book answer "does this fill before he wanders back", which is contingent on him and cannot converge — three verdicts came out of the same 75 trips in one day, each from fixing a LABEL rather than from new data. **`PAPER_HORIZON_H = 6`**, from the only uncensored window the book had: of 26 trips ever given more than 6h, the 14 that filled did so in a median 1.73h, 92% inside 4h, none past 5.19h. 4h sits on the steep part of that curve; 6h clears the observed maximum and buys nothing after. It costs sample rate — 24/11 = 2.18 trips/family/day against 2.85 at the old 3.41h median. **The SIZE moves with it, not just the deadline**: `shadowSizeFor`, `paperCapacity` and the paper caller of `scanScreenTier` all read the fixed horizon, since a fixed deadline with schedule-derived sizing is half-fixed and invisible. `scanScreenTier` takes the horizon as a parameter so its LIVE caller is untouched — the live plan's own horizon still moves with the schedule, because this ruling is about the simulated book only. `FILL_MODEL_V` bumps to 2 so the two populations never pool | probe `[R62.1]` |
| R62.2 | THE 1-HOUR OUTPUT FLOOR IS DELETED and input coalescing owns the promise. The floor existed so a placement made minutes before a configured touch was not sized for nothing, and it fired constantly because a NOMINAL touch was imminent while the operator was hours away — 1.00h priced against a 10.63h actual return. Under a fixed horizon the paper book never reads it, and the degenerate input it guarded (two walk-ups moments apart) is handled where it arises: **sessions coalesce at `SESSION_COALESCE_MIN` = 10 minutes**, ruled as the only guard. Keeping both would be a guard whose trigger another layer forbids. Session boundaries — "has he been back since?" — read the walk-up log rather than the schedule, and return **null with no history**, because "no" and "cannot say" are different answers and the schedule version could not express the second | probe `[R62.2]` |
| R62.3 | THE FILL CURVE, WITH ITS CENSORING RENDERED. Horizon becomes a MEASUREMENT rather than a setting: one uncensored 6h trip that filled at 1.9h contributes a point to every hour below it, where three censored populations at 2h/4h/6h would contribute three points — which is why one horizon was ruled rather than several. Each cell's denominator is trips whose OWN window reached that hour; a shorter trip is **silence at that hour, never a failure**. Cells render `fill% / trips at risk`, cohorts are never pooled into one curve, and the surface NAMES the hour past which the population thins below `FILL_CURVE_MIN_AT_RISK` rather than drawing a confident line into a region nothing observed | probe `[R62.3]` |
| R62.4 | THE CADENCE READ — two independent measurements multiplied. The fill curve is a MARKET property and converges; the operator's walk-up gap distribution is BEHAVIOURAL and moves only when the routine moves. Composed, they answer "given how long you actually leave offers standing, what fraction would fill" — the cadence question, computed from a converged market result instead of simulated against a predicted return, which is the property the old design lacked. Uses the **empirical distribution, never a statistic**: the same routine contains 2.15h returns and 10.63h ones and they land on very different parts of the curve. Per cohort. The **time-of-day independence assumption is stated on the surface**, with the split that would falsify it named. With too few gaps it says so rather than substituting a cadence nobody keeps | probe `[R62.4]` |
| R62.5 | GAP LABELLING RETIRED. With no schedule pricing placements, "evening" and "afternoon" as scheduled gaps mean nothing — they were intervals of a cadence the book no longer uses. The honest residue is TIME-OF-DAY OPENED in fixed six-hour buckets, a property of the market clock that does not move when a setting does. The per-gate table's second dimension becomes the FILL MODEL, not the clock: the split exists so two different experiments never pool, and under a fixed horizon that is exactly the v1/v2 partition — keying it on time-of-day would also make every gate row depend on the hour the suite runs | probe `[R62.5]` |
| R62.6 | THE TRADE IS STATED ON THE SURFACE, in one line, rather than left to be forgotten. Buys: results that converge, comparability across trips, and an answer to how long an offer needs to stand. Costs: the book no longer models the operator's cadence at all and cannot say whether a placement made at a given time would fill. The panel says both | probe `[R62.6]` |
| **§63 Exception lane — observed span and the near-miss ranking (user ruling, Aug 13 2026)** | | |
| R63.1 | THE SPAN CLAUSE COUNTS OBSERVED DAYS. `EXC_MIN_DAYS` stays 7 and stays epoch-scoped — it measures the DISPERSION of the evidence, not the age of the item or the book, and that is the right property: five trips from one lucky afternoon must not waive a gate. What was wrong is what it counted. Wall-clock let five trips straddling a five-day closed laptop read as seven days of evidence; days nobody watched are not evidence either way, and this is the standard that puts real capital on a waived gate. `observedDaysSpanned()` intersects the trip span with the observed-day ledger and returns a DURATION — the `−1` is load-bearing, since a date set is inclusive and the raw count would have made the new clause LOOSER than the wall-clock one it replaced. `spanD` is kept and rendered beside it: the difference between the two IS the coverage statement, and `spanDaysTxt()` returns the pair so no caller can render the numerator alone. **Both ledgers prune at 30 days**, so the copy states the ceiling where it renders rather than letting a 60-day record silently report 30 | probe `[R63.1]` |
| R63.2 | THE NEAR-MISS RANKING, and it REPLACES the bar-recitation line rather than sitting beside it. The lane rendered its own rule back at the reader — "no candidates clear the evidence bar right now: ≥5 trips, ≥60% fill, profitable, ≥7 days, no red flags" — naming no item, no clause and no distance, so the operator's only way to learn what was blocking was to have someone read the code. That is the F18 defect on this surface. Every item that COULD qualify is scored against all seven clauses, including the two that were never in the constant list and so were invisible: **a named gate to waive** (an item whose trips cleared every gate has nothing to except) and **a free probation slot**. Sorted closest-first, binding clause named. **Distance is only meaningful where a clause accrues**: trips and observed days close on their own and carry an ETA at a rate the store can evidence; fill rate and net can move either way and a red flag or missing gate does not close by waiting, so no ETA is projected onto them. **Population is every item that could qualify, not every watched item** — discovery-slice and gap-band trips land on UNWATCHED items, so a watchlist-only list would hide real candidates; those render marked. Reports standing only; the grant is still the user's press and the bar is still a strategy parameter | probe `[R63.2]` |
| **§64 Scan 10 / scan 11 first-run findings (user ruling, Aug 13 2026 — `audits/AUDIT-2026-08-13b-scans-10-11.md`)** | | |
| R64.1 | THE CLEARANCE IS STAMPED BY A WRITER, not inferred by a reader. `PAPER_DEFECTS` is a build-time array and `DB.paperDefectsClearedAt` is persisted runtime state; nothing joined them, so `paperCleanFrom()` returned `DB.paperDefectsClearedAt \|\| 0` against a field no code ever wrote. `paperDefectsSweep()` runs once per load (the `paperEpoch2Reset` precedent), compares the running build's register against `DB.paperDefectsSeen`, and **stamps `paperDefectsClearedAt` and increments `paperClrGen` at the moment a defect leaves** — decision-logged with the ids struck. It is EXTRACTED rather than inline in `load()` so an assertion can call it | probe `[R64.1]` |
| R64.2 | **A BOUNDARY NEVER DEFAULTS TO ZERO.** An unset clearance means **no** trip is clean, not every trip. `paperCleanFrom()` returns `null` when the register is non-empty **and** when the stamp is absent; it can never return 0. The defect this closes: the day the last register entry was struck, `from` became 0, `p.t >= 0` matched every trip ever recorded, and the whole book — including every trip that closed while the defect was live — would have been reclassified as clean evidence in one step with nothing said on screen. Absence of evidence about cleanliness must never read as the permissive answer | probe `[R64.2]` |
| R64.3 | **CLEANLINESS IS A POPULATION, NOT A DATE FILTER** — the FILL_MODEL_V precedent applied to contamination. Every trip stamps `clrGen` at open, centrally in `stampPrediction()` so no entry path can forget it (the regime-stamping defect's lesson), and `paperTripClean(p)` requires the trip's generation to equal the current one **as well as** `p.t >= from`. A trip with no stamp predates the scheme and is contaminated — the conservative direction. Trips either side of a clearance are different experiments and are never pooled | probe `[R64.3]` |
| R64.4 | **THE HOLD CANNOT RIDE THE COMMIT THAT SUPPLIES ITS EVIDENCE.** Gap-band readiness counts trips carrying the *current* clearance generation, and at the instant a clearance is written no trip carries it — so the build that strikes the last defect releases nothing, and the bar can be met only by trips opened afterwards. Under the old timestamp filter the same commit both lifted the hold and retro-supplied the evidence the hold was waiting for. The held line states why the trips already present do not count | probe `[R64.4]` |
| R64.5 | **THE CAUSALITY PROMISE MOVES TO THE LAYER THAT CAN ENFORCE IT.** `reconReplay`'s `if (bt < p.t) continue` was unreachable — the caller clamps the window to at least `p.t`, so every bucket reaching the loop already satisfied it — and the Aug 12 instant-trip audit reasoned from it. Per the dead-safeguard ruling the guard is DELETED and the promise stated where it is enforceable: `reconWindowStart(p)` returns an explicit `Math.max(p.t, p.lastObs)` floor rather than relying on `lastObs >= t` happening to hold, and the assertion points at it. A promise that depends on another field's invariant is a coincidence, not a promise | probe `[R64.5]` |
| R64.6 | **THE 40% STATISTICAL CAP STATES ITS REASON WHERE THE PROPOSALS RENDER.** The cap withholds candidates — an automated decision — and its explanation was composed into `S.cohNote` when the cap shipped and read by nothing, so the withheld items were an absence with no explanation anywhere. It renders in `#cohCapNote`, immediately above the queue it explains, because a withheld candidate's entire visible effect is that it is missing from that list | probe `[R64.6]` |
| R64.7 | `S.anomMkt` REMOVED rather than wired: written and never read, and the index reading it duplicated is already persisted as `DB.volIndex` and rendered with its control rows. Wiring it would have created a second source for one number, which is the redundancy finding rather than the fix | verified by inspection |
| **§65 A difference names what it is a difference from (user ruling, Aug 13 2026)** | | |
| R65.1 | **A DELTA-LABELLED FIGURE RENDERS BOTH OPERANDS, IN THE SENTENCE** — not in a tooltip, because the number is read in the line. The incident: the paper vitals tile rendered `tight − current` as *"+2.64m vs current"*, a DIFFERENCE presented as a LEVEL, while the drill underneath showed −2.40m, which is `current` itself. **The headline and its own decomposition disagreed in sign and in magnitude and both were arithmetically correct** — nothing was wrong with either number, the sentence was wrong about which number it was. `deltaVs(val, base, valLab, baseLab)` is the sanctioned renderer, the `rateBlend()` shape applied to differences: it cannot emit the difference without the base, so a caller physically cannot produce the ambiguous form. `paperDivLead()` is extracted from the tile because the tile drives `buildPlan`/`renderDeploy` first and re-stamps the ledger, so an assertion against the rendered tile would test the accrual rather than the sentence **Dormant-gated Aug 14 2026 (§81), amended same day with the transitional nav refresh:** the Paper Book tile — the term's former render home — is gone (its slot is the Scorer's, §82), so `paperDivLead` has no production caller in ANY era; the dormant form asserts the vitals strip carries no divergence clause even over a divergence-rich ledger AND that the open · closed read renders on the paper surface's own header, while the gated live form is an **un-retire tripwire** — a deliberate red stating that an un-retire ruling must name the lead's render home before the race can ship live. `deltaVs` stays live as the sanctioned renderer for any delta | probe `[R65.1]` |
| R65.2 | **WHAT SEPARATES THE REGIMES RENDERS AS A STANDING SECTION**, not on request. **Corrected Aug 13 2026: for each NAMED BAND (`loose  current`, `current  tight`), never for each adjacent pair of `REGIME_DEFS` — see R68.6 for the defect that wording described.** Three curves with three sums invite exactly one question — which trips are in one and not the other — and answering it used to mean downloading the export and grouping by hand. For each adjacent pair: both nets with their trip counts, the separating trips, and a drill to item / cohort / **ROI at open** (the post-tax return on the PLACEMENT pair — close to, and not identical with, the figure membership was decided on, which used the quote pair and the lower of the live and 1h-sustained margin; corrected Aug 13 2026) / outcome / notional / net. **Concentration leads the reading**: top-one and top-two share of GROSS movement, never of a signed net, and where one trip carries the callout share the copy says to read it as a fact about those trips rather than about the floor. **Identical regimes are reported too** — no trip in the book sits in the band between them, so the tighter setting is INERT on this evidence rather than vindicated, which the curves alone cannot show | probe `[R65.2]` |
| **§66 Sell discriminator — first-run findings (user ruling, Aug 13 2026)** | | |
| R66.1 | **NO PRINT AT ALL IS THE STRONGEST ILLIQUIDITY CASE, not an ambiguous one.** `classifySellFailure` filed "no high print in ANY bucket" as `unclassified` while the *weaker* condition below it — prints, but no volume — got the `no-liquidity` name. So `ofWhichNoLiquidity` reported **0** while 2 of the 10 "neither fits" cases had no high print across 48 buckets. **An absence filed as an ambiguity**: the pile meaning "we could not tell" must never absorb the cases meaning "there was nothing there" | probe `[R66.1]` |
| R66.2 | **A FALSIFIED PREDICTION READS AS A RESULT.** The recorded claim was that dilution dominates in BOTH populations, making the credit rule the whole story and one fix enough. Measured: it dominates calibration (2 of 3) and not the live book (2 of 17). That case used to fall through to "the split does not match either predicted outcome" — true, and saying nothing. The verdict now names the claim, the measurement that beat it, and what follows: **two distinct defects, each needing its own fix**, and the live book cannot mislocate by construction so a window fix would leave it untouched. **The verdict is a named term** (`sellVerdictOf`, stage 1d — census probe:7402): the panel routes through it, and the assertion drives the populations' splits through the term and reads the branch, because a whole-panel match was satisfied by an always-emitted span whatever branch the verdict took | probe `[R66.2]` |
| R66.3 | **THE SHAPE REPORT SHIPS ITS ROWS — and both classes come from ONE implementation** (rewritten stage 1d with the R59.6 direction flip). The transition count opens to the moving cases by name with their spreads, and reports class counts under the retired band beside them. Every exported case carries `quotedSpreadPct`, `spreadRelativeBandPct`, `gapAsShareOfSpread`, `classUnderRetiredAbsoluteBand` and `retiredAbsoluteBandWouldDiffer`. The one implementation is the classifier itself: the live class and the `absCls` it stamps beside it — `sellRelClassOf()`, the old second implementation, is deleted, because after the ratification it re-derived the live band and made the comparison a tautology | probe `[R66.3]` |
| R66.4 | **THE SPREAD-RELATIVE DILUTION BAND IS RATIFIED AND LIVE** (user ruling, Aug 13 2026). `SELL_SPREAD_REL_FRACTION = 1/3` of the item's own spread replaces the flat 1.0%, because the phenomenon is proportional and a flat band did different work across a 3.28%–19.17% spread range. Three of 16 cases move, all wide-spread items whose gap is a fifth to a quarter of their own spread. The fraction was chosen so an item whose spread equals the ROI floor lands near the ruled 1.0% — the two agree on the population the absolute figure came from and diverge only where the question is whether they should. Decision-logged. **The retiring absolute band is computed alongside as `absCls` and rendered beside the live one until 2026-08-20**, so the two can be seen disagreeing in the wild before it goes; it is read for no verdict, and the panel opens to the disagreeing cases with each item's spread, gap and band. The retirement is date-driven and self-expiring — no flag day to remember | probe `[R66.4]` |
| R66.5 | **THE LOCALISATION RENDERS IN THE WORDS IT WAS RULED IN**, above the split rather than in a caveat: *every real logged sell completed; 17 simulated sells with a complete buy and a fully observed horizon did not.* It rules OUT window, horizon and observation coverage by construction, which is worth more than any count below it, and localises the defect to the live sell credit path | probe `[R66.5]` |
| R66.6 | **NO THIRD CLASS** (user ruling, Aug 13 2026). The "neither fits" pile was a **threshold artifact, not a mechanism**: its cases sat at 0.13–0.74 of each item's own spread — inside what the spread explains — while the mislocated pile ran 0.40–2.57, beyond it. **Fault A is the mechanism for the whole pile.** The surface records that the discriminator did its job *by refusing* to name a mechanism for cases that turned out to be one already on the register, and that a large residue was evidence about the THRESHOLDS rather than about the market. Asserted in both directions per the eleventh face — the correct claim required and the contradicting one forbidden | probe `[R66.6]` |
| R66.7 | **THE LIVE SELL TRACE SHIPS WITH ITS CLASSIFICATION.** R59.3 built the per-bucket trace precisely so the live book could be classified, and the export carried the classification without it — so per-case medians were all a reader had, and a median cannot recover a per-bucket distribution. **Third occurrence of a rollup shipping without its rows and forcing a derivation by hand**, which is the standing gap the interrogability rule binds exports against. Every failing live sell now exports `sellTrace` in the calibration trace's own idiom — same field names, same reason vocabulary, so the two populations read alike — with each bucket's minute, signed gap against the ask, high-side volume and credited units, **including the buckets that credit nothing**, which are the informative ones. **No trim branch exists**: `SELL_TRACE_CAP` (48) sits below `CALIB_EXPORT_TRACE` (60), so the trace is complete by construction and says so; writing a head/tail trim here would reproduce the dead-guard shape knowingly | probe `[R66.7]` |
| **§67 Sell-leg reach bounds — the pair, not a share (user ruling, Aug 13 2026)** | | |
| R67.1 | **PROVEN / POSSIBLE, AND NO NEW CONSTANT.** The alternative was a share function `clamp(intercept + gap ÷ divisor)`, rejected because both constants stand in for an intra-bucket distribution a 5-minute aggregate cannot show — and measured, the **intercept moved the result about four times as much as the divisor** (85 points across intercepts 0.5→1.0 against 13 across divisors), so the ruling would have landed on the constant nobody was weighing. **PROVEN** = buckets whose average cleared the ask, which is exactly what the model credits today; **POSSIBLE** = buckets whose average sits within ONE SPREAD of it. The two are reported together and **never averaged into a point estimate** — a midpoint is the share function wearing a different hat | probe `[R67.1]` |
| R67.2 | With no spread known the two bounds **collapse to the proven figure** rather than guessing a width. An unknown dispersion is not a wide one | probe `[R67.2]` |
| R67.3 | **THE PAIR DOES NOT FIX FAULT A AND SAYS SO WHERE IT RENDERS.** It makes the uncertainty visible instead of resolving it: the gap between the bounds **is** Fault A, unmeasured — tape that printed inside one spread of the ask and is scored zero because the credit rule reads a five-minute AVERAGE as if it described the interval. Snape grass seed is the worked example and is still credited zero under the proven bound: buckets 0.86% below the ask carrying 625 units, on an item whose own spread is 4.28%. Asserted in both directions — the statement required, and any claim to have fixed or resolved the fault forbidden | probe `[R67.3]` |
| R67.4 | **`FILL_MODEL_V` DOES NOT MOVE.** The credited figure is unchanged and only a reported bound was added, so the book keeps accruing on one model and the epoch is not partitioned. The version is a claim about the MEASUREMENT, and the measurement did not change — same precedent as R59.3 and R59.7, both instrumentation-only. Asserted as a value so a bump cannot pass unnoticed | probe `[R67.4]` |
| **§68 The gate ledger records the value the gate SAW (user ruling, Aug 13 2026)** | | |
| R68.3 | **A RECORDED VALUE OF ZERO SURVIVES THE ROUND TRIP AS ZERO.** `DB.gateLog` rows gained `v`, the figure the gate saw at bench time, because the ledger recorded WHICH gate benched and discarded WHAT it saw — so it could report the ROI floor as the busiest gate in the book (57 distinct items over 4 observed days, sole blocker in 14 of 19 sole-blocked candidate-days) and still not answer whether loosening it from 1.2% to 1.0% would admit anything, since "benched by the ROI floor" means only `eRoi < 1.2` and cannot separate 1.19 from 0.2. **Zero is the value that matters most here**: a 0% ROI item IS benched by the ROI floor with `have: 0`, so every reader uses `!= null` and never truthiness | probe `[R68.3]` |
| R68.4 | **A NULL GATE VALUE RESTORES AS NULL, NEVER AS ZERO.** The import sanitizer's shared helper is `num = v => Number.isFinite(+v) ? +v : null`, and `num(null)` returns **0** — `+null` is 0 and 0 is finite. Sanitizing `v` through it would have converted every *"this gate has no single-number miss"* into *"this gate saw zero"* on every restore, silently, which is the same falsy-zero class the field was added to make measurable. `v` is therefore checked with `typeof === "number" && Number.isFinite`, not through `num()` | probe `[R68.4]` |
| R68.5 | **A ROW THAT PREDATES THE FIELD RESTORES WITH NO `v` KEY — ABSENT IS NOT NULL.** Three states are kept distinct because absence and data-of-absence are different readings: **absent** = the row was written before the field existed, **null** = this gate has no single-number miss, **a number** = the reading. `funded` / `seasoning` / `die-off detected` rows carry an explicit `v: null` at write time for exactly this reason — omitting it would have collapsed three states into two on the first new row. Restored via `hasOwnProperty`, so a backup cannot gain a null it never had | probe `[R68.5]` |
| R68.6 | **THE REGIME BAND IS COMPUTED FROM SET MEMBERSHIP, NEVER FROM ARRAY POSITION** (user ruling, Aug 13 2026). `regimeSeparators` walked ADJACENT PAIRS of `REGIME_DEFS` — ordered `[current, loose, tight]`, which is DISPLAY order, the operator's own setting first — computing `a && !b`. The regimes nest (`tight ⊆ current ⊆ loose`, asserted by `[R43.1]`), so pair 0 was `current && !loose`: **empty for every possible population**. The surface reported *"current and loose are identical … the tighter setting is inert"* from the day it shipped, out of an arithmetic that could not return anything else, while the band the ROI-floor question is about — `loose \ current`, [1.0%, 1.2%) — was computed **nowhere in the file**. Replaced by named `{wider, narrower}` bands: `loose \ current` and `current \ tight`, which also DECOMPOSE the old merged pair rather than replacing one merge with another. **Not a sort of `REGIME_DEFS` by floor** — that would cure the symptom and leave the implicit-ordering dependency one step removed, so the next regime added re-breaks it | probe `[R68.6]` |
| R68.7 | **EMPTY BY POPULATION READS AS INERT.** Where eligible trips exist and none sits in the band, the copy reports the tighter setting inert — not vindicated, untested — against the ELIGIBLE denominator, and must not claim nothing fed it. Asserted in both directions per the eleventh face: the correct claim required, the contradicting claim forbidden. **Re-pointed Aug 13 2026 at `current \ tight`**, the band that can actually be in this state — see R68.8 | probe `[R68.7]` |
| R68.8 | ~~**EMPTY BY CONSTRUCTION READS AS NEVER-FED**~~ **→ SUPERSEDED Aug 13 2026 (user ruling): A BAND THE GATE CONSTANTS FORBID READS AS UNREACHABLE.** The original row asserted that a trip enters `loose \ current` only if its entry path screened below the current floor, *"and only the scanner cohort does"*. **Both halves were false.** The watchlist path applies no ROI pre-screen at all — it admits any single-gate near-miss — and the scanner is not eligible either, because it screens at `SHADOW_LOOSE` and then requires `pass \|\| nearMiss`, while a band item at `eRoi ∈ [1.0%, 1.2%)` fails the ROI floor AND the margin floor. **So `nEligible` counted the scanner's 14 stored trips as chances that were had and missed, when the true number of chances was zero** — a false denominator turning *impossible* into *didn't happen*, inside the classifier written to detect exactly that. Replaced by `bandUnreachable(hiPct)`, DERIVED from `GATE.roi` and `effRoiFloorPct()` so it re-computes if either constant moves, plus the tax-exempt carve-out as a property of the item. The copy states *cannot be fed at all* and is forbidden from both contradicting claims — *nothing has fed this* and *inert*. The old `!nEligible` render branch was **deleted**, not left green: under the corrected predicate a reachable band makes every trip eligible, so its trigger was unreachable from its own upstream limits | probe `[R68.8]` |
| R68.9 | **THE CARRY IS THE RECORD.** The `shadowBook` import sanitizer enumerated fields explicitly and stopped short: **21 were dropped silently on every restore**, and each loss broke a rule stated elsewhere. `fillModelV` gone collapses every trip to `"pre-stamp"` and destroys the never-pool partition; `grade` gone relabels reconstructed evidence as live; `hzH` gone falls back to `DB.fillHorizonH` and **retro-applies a horizon to a leg placed under a different one**, which the cadence ruling forbids in those words; `openSeq` gone makes `paperStaleHostTrips()` fire across the whole book, announcing the exact corruption that forced the epoch-1 discard. Restored with an explicit finite check, **never `num()`** — `num(null)` is 0, so absent fields would restore as legitimate-looking zeros | probe `[R68.9]` |
| R68.10 | **`anchored` IS THE ONLY IRRECOVERABLE FIELD, AND ITS LEGITIMATE VALUE IS FALSY.** Every other carried field records something still knowable. `anchored` is an OPEN-TIME FACT — whether the operator was present when the trip opened — and dropping it leaves no visible gap: it silently moves the trip into *"predates the stamp"*, a different and wrong population that no later pass can undo. Carried on an explicit `typeof === "boolean"` test so **false survives as false**; a truthiness carry would drop precisely the unanchored trips. Flagged at the sanitizer site in code, not only in the commit | probe `[R68.10]` |
| R68.11 | **CAUSALITY AND PRICING STAMPS SURVIVE, INCLUDING THE ZERO-VALUED ONES.** `buyBkt` / `buyDoneBkt` / `sellBkt` are what prove a leg was filled by tape that printed after it was placed — the information-horizon rule's own evidence — and `exitPx` / `exitApprox` / `reachLimited` / `gap` carry the forced-exit and reachability readings. A bucket stamp of 0 and an `exitPx` of 0 are legitimate values | probe `[R68.11]` |
| R68.12 | **TRACES CARRY IN FULL, AND `null` / `0` STAY DISTINCT INSIDE A BUCKET** (user ruling, Aug 13 2026). A trace bucket holds three different readings in adjacent cells: `hi: null` = no high print at all, `hv: 0` = printed but nothing on that side, `credited: 0` = nothing credited. Merging any two is a real misreading, and a per-cell truthiness copy would merge all three. Carried in full rather than truncated — they are already in localStorage, so this is parity rather than new cost, and **a truncated restore that does not say it truncated is the same defect one layer down**. The slice is production's own `BUY_TRACE_CAP` / `SELL_TRACE_CAP`, so it cannot fire on data this app wrote; it exists for a hand-edited file | probe `[R68.12]` |
| R68.13 | **`pend` IS DROPPED, AND THE DROP IS A DECISION.** It is mid-flight per-bucket accrual belonging to a polling session that no longer exists once the file is carried; restoring it would resume a half-finished thought against tape from a different session. Every other field is a RECORD, `pend` is not. Stated at the site so a later enumeration does not "fix" it | probe `[R68.13]` |
| R68.14 | **THE FILL-MODEL PARTITION IS CHECKED WHERE THE POPULATION IS BUILT.** `paperClosed()` applied no partition, so the never-pool rule held **by accident**: every counted trip is v1 while `FILL_MODEL_V` is 2, and the violation fires the moment a v2 trip resolves. This does **not** filter — filtering to the current model would empty the counted book, and discarding evidence is a ruling rather than a defect fix. Instead the split renders ALONGSIDE the figures, above the curves rather than inside a disclosure, naming each model and its count. Silent on a single-model population, which is today | probe `[R68.14]` |
| **§69 A recorded zero is not a missing value (falsy-zero census, Aug 13 2026)** | | |
| R69.1 | **A BUY DURATION OF ZERO SKIPS AS `instantBuy`, A MISSING ONE AS `noWindow`.** `calibWindow` tested `!(bh > 0)`, sending a recorded zero down the missing-timing path. The writer produces zero routinely — `Math.round((now − placedAt)/36e3)/100`, so any buy filling inside ~18s records 0 — and **2 of the 50 logged flips carry `bh: 0` today**, so this was false about real rows rather than hypothetical ones. Skipping is still correct (a zero-width window has no buckets to replay against, and inventing one would fabricate the measurement under test); the REASON had to name the real cause. Three outcomes, not two. `Number(null)` is 0, so the null test runs FIRST or a missing duration arrives disguised as an instant fill | probe `[R69.1]` |
| R69.2 | **THE SKIP COPY NAMES THE REAL CAUSE, AND IS FORBIDDEN FROM CALLING A RECORDED ZERO A MISSING DURATION.** Both the in-app line and the export's `skippedByReason` described the bucket as flips that *"lacked a recorded buy or sell leg duration"*. They have one. `instantBuyZeroWindow` is split out from `missingLegTiming` because the two are opposite readings. Asserted in both directions per the eleventh face | probe `[R69.2]` |
| R69.3 | **A KNOWN ZERO SPREAD IS NOT AN UNKNOWN SPREAD.** `spreadPct > 0` merged them, so a locked book fell through to the frozen `SELL_DILUTION_GAP_PCT` = 1.0% absolute band and classified **DILUTION** for any gap inside it — **inverting the spread-relative band ratified the previous day, on exactly the items where that band is tightest.** A third of a zero spread is zero: the spread explains nothing, so no gap is inside it. The absolute band remains the fallback for an UNKNOWN spread, which is what it was written for | probe `[R69.3]` |
| R69.4 | **A SPREAD OF ZERO IS REPORTED AS 0, NEVER ERASED TO null.** `spreadPct \|\| null` appeared twice, twelve lines apart — in `classifySellFailure`'s case record, where the object is spread LAST and the null therefore **overwrote the record's own value**, and again in `sellReachBounds`. A locked book was made indistinguishable from an unmeasured one in the export and in every reader downstream. The second site was not ranked by the census and was found by grepping the anchor rather than the line number | probe `[R69.4]` |
| **§70 The pump guard has a working test (verification-layer census, Aug 13 2026)** | | |
| R70.1 | **THE PUMP GUARD IS ASSERTED AT THE TERM.** `cautionProven` lived inline as `!pump && !!cat && wins >= 3`, and its own named assertion — *"[R7.3] recent wins never graduate a pump flag — the wins are the bait"* — tested it on probe item 9051 `"Probe sleeve second"`, a name matching **none** of the four `CAUTION_CATS` patterns. `cautionCat()` returned null, `!!cat` was already false, and the conjunction was pinned regardless of the pump limb. **Deleting `!pump &&` from production left the entire suite green, that assertion included** — so the BINDING rule *a manipulation defense never relaxes on the manipulator's chosen evidence* had no working test. Extracted to `cautionProvenFor(pump, cat, wins)` so the limb is assertable without depending on which other conjunct dominates a fixture | probe `[R70.1]` |
| R70.2 | **AND THE END-TO-END PATH RUNS THROUGH AN ITEM THAT ACTUALLY CARRIES A CAUTION CATEGORY.** Extraction alone would leave the integration untested, so a second fixture item (`9070`, *"Probe ranarr seed"* — a seed by name, therefore a real `cautionCat`) is pump-flagged by a promotion warning, given thin books and 3 recent wins, and asserted to refuse graduation. Every premise of the label is present and the pump limb is the only term left that can decide the answer. Both halves were required: the term test alone cannot see a broken call site, and the end-to-end test alone cannot isolate the limb | probe `[R70.2]` |
| **§71 The sacred set is checked on every intel path (adversarial pass, Aug 13 2026)** | | |
| R71.1 | **THE UNIVERSAL IS EXERCISED UNIVERSALLY.** `[R4.3]` snapshotted the sacred set around exactly ONE `activateIntel` call, and that call carried the `demand-context` record — **the single branch of `activateIntel` with no write path at all**, which matches nothing and falls to a bare `return "advisory …"`. The branches that DO write (catalyst, catalyst-update, cluster-membership, long-catalyst) were activated later with no snapshot around them. A universal claim was being exercised against the one case that structurally cannot break it. Unprotected: a write to `DB.blacklist` from any branch, breaching *"the blacklist is the user's alone"*; or to `GATE.roi` / `DB.tickFloor` / `DB.reserve` / `DB.shadowReserve`, breaching the strategy-parameter rule and the deployment half of the restraint/deployment line — **both BINDING**. Every activation now routes through `activateChecked`, so a record type added to `activateIntel` inherits the check instead of needing someone to remember a new assertion | probe `[R4.3]` |
| R71.1b | **COVERAGE NOTE — THREE BRANCHES ARE COVERED BY ARGUMENT, NOT BY SEED** (logged so a future reader is not misled by the green). Three discriminating seeds were run and each bit its own target: a `DB.blacklist` write in the **catalyst** branch (the EVERY-activation assertion red, the old one-branch assertion still PASS, import green); **cluster-membership bypassing the wrapper** (coverage assertion red, import green); a `DB.reserve` write inside **importIntelligence** (import red, activations green). The remaining branches — `catalyst-update`, `long-catalyst`, `watch-note` — were **not individually seeded**. They route through the same `activateChecked` wrapper, and R71.2 asserts they reach it, so per-branch protection follows from those two facts. **That is an inference and is recorded as one**: if the wrapper is ever bypassed for one branch specifically, only R71.2 catches it, and R71.2 is itself proven by exactly one seed | probe `[R4.3]` · argument |
| R71.2 | **AND THE TYPE COVERAGE IS ITSELF ASSERTED**, so this cannot quietly degenerate back to what it was. The suite fails if the activations do not span `demand-context`, `catalyst`, `catalyst-update`, `cluster-membership` and `long-catalyst` — a new writing branch with no fixture exercising it goes red and names the gap, rather than passing because the one inert branch still passes | probe `[R4.3]` |
| R71.3 | **THE IMPORT PATH IS COVERED SEPARATELY**, because the activation wrapper does not reach it. `importIntelligence` is the first half of the journey and is also the **file-as-press** channel — a `disposition` block arriving inside the file is the user's own press and may DROP advisories with no button — which makes it the surface where a sacred-set write would be least visible. Asserted by pushing every record type through in one batch, valid and invalid alike (a rejected record still executes the path), plus a disposition block | probe `[R4.3]` |
| **§72 The allocator horizon term — by scaling, and under a real schedule (adversarial pass, Aug 13 2026)** | | |
| R72.1 | **THE TERM SCALES; IT IS NEVER RESTATED.** The prior assertion copied the production body character for character, so both sides moved together and the only defect it could catch was an edit the author forgot to mirror. Asserted now by proportion — doubling the volume gate doubles the term, and doubling the HORIZON doubles it — which is the dependence a copied expression cannot see | probe `[R72.1]` |
| R72.2 | **THE CLAMP IS WIRING, ASSERTED APART FROM THE TERM.** `planHorizonUnits` was split so the `Math.min` sits at the call site rather than inside the extracted function. The earlier extraction stopped one layer short, leaving the term reachable only by passing `qty = 1e9` — a value production cannot produce, since the real call at `index.html:4190` passes `planQty`, bounded by `planCap` | probe `[R72.2]` |
| R72.3 | **AND THE HORIZON DEPENDENCE IS ASSERTED UNDER A REAL SCHEDULE.** Under `DB.touchWindows = []`, which the whole first block of the suite runs on, `FILLH()` and `DB.fillHorizonH` are the same 4 — so substituting one for the other is invisible. That is the M118 recurrence: sizing reverting from the touch schedule to the no-cadence fallback, which on a real cadence (gaps 5.0 / 5.0 / 4.5 / 9.5h against a fallback of 4) **misprices the overnight buy leg by up to 2.4× with the suite green**. The fallback is DERIVED from the live gap rather than pinned at 4: the gap sweeps 0→9.5h with the wall clock and passes through 4 several times a day, which made a fixed value an intermittent assertion — it failed its own first run at gap = 3.97 | probe `[R72.3]` |
| **§73 The gate interaction surface — headline is not binding (user rulings, Aug 13 2026)** | | |
| R73.1 | **THE EFFECTIVE ROI FLOOR IS DERIVED, NOT CARRIED AS A LITERAL.** `GATE.roi` is 1.2% and **selects nothing**: the margin gate's tax limb (`eMargin ≥ taxMult × tax`, tax being 2% of the SALE price) is a sustained-ROI floor in disguise at `taxMult·τ/(1 − τ − taxMult·τ)` = `0.06/0.92` = **6.5217%**. Every taxed item below that is benched by the margin floor whatever the ROI floor says — including the whole 1.2%–6.52% range the ROI floor passes. Measured over 4,497 live items: **2,358 fail the ROI floor and 2,358 of 2,358 also fail the margin floor.** Extracted as `effRoiFloorPct()` because the same quantity inside `candidateFor()` is buried behind `Math.max(taxMult·tax, tickFloor)` and cannot be read there. Asserted by DEPENDENCE (moving `taxMult` moves the floor), never by restating the expression | probe `[R73.1]` |
| R73.2 | **AND THE DERIVED FLOOR IS WHERE THE PRODUCTION GATE ACTUALLY FLIPS.** The algebra is checked against the gate's own `Math.max(taxMult × tax, tickFloor)` at a price high enough that the TICK limb cannot be what decides — an item 0.2 points under the derived floor fails, one 0.2 points over passes, and the assertion states which input pins it in both cases, per the clamp rule's Aug 13 qualification | probe `[R73.2]` |
| R73.3 | **BAND REACHABILITY IS COMPUTED FROM BOTH CONSTANTS.** `bandUnreachable(hiPct)` tests `hiPct ≤ GATE.roi` (every item inside fails the ROI floor) AND `hiPct ≤ effRoiFloorPct()` (every item inside also fails the margin floor). Both limbs are tested rather than the second inferred from the first, because the implication holds only while `GATE.roi ≤ effRoiFloorPct()`. Raising `GATE.roi` above the effective floor makes the same band reachable, which a hardcoded `false` could not express | probe `[R73.3]` |
| R73.4 | **THE TAX-EXEMPT CARVE-OUT IS THE ONE REGION WHERE THE ROI FLOOR ACTS ALONE**, so it is the one population that can feed an otherwise unreachable band. `tax = 0` collapses `marginNeed` to the halved tick floor, opening the region at roughly `buy > 667`. Eligibility is a property of the ITEM, read from `exemptIds` centrally in the separator — never a label an entry path remembers to attach — and the copy names the carve-out where it renders | probe `[R73.4]` |
| R73.5 | **THE DEPLOYMENT FUNNEL DECLARES ITSELF HEADLINE-ATTRIBUTED, AND THE WORD *KILLERS* IS STRUCK — ALONG WITH THE ⚑ MARK ITSELF.** `firstGate` is `fails[0].g` — position in `GATE_CHAIN_ORDER`, not causal weight. Measured: the ROI floor is the headline on 2,358 of 4,497 live items and the sole binding gate on **zero**; the volume floor, second to last in the chain, is the sole binding gate on **every one** of the 280 it heads. The ⚑ line read *"the two biggest killers this refresh"*, which claims causation the ranking cannot support. **The flag is gone entirely (user ruling, Aug 13 2026):** its only job was "look here", and R73.10's binds ranking does that honestly, so keeping it would re-create the conclusion the strike removed. Asserted by absence — the character must not appear in the panel | probe `[R73.5]` |
| R73.6 | **AND THE MARK IS ABSENT FROM THE SURFACES THAT READ THE FULL FAIL SET.** The marginal-gate attribution, the near-miss piles, the paper book's `benchedBy` and the strata sampler all run through `failProfile()` and report the BINDING gate; they are sound and carry no mark. Asserted by absence, per the scoping ruling — a mark sprayed over the whole panel would stop distinguishing the two kinds of surface, which is its entire job | probe `[R73.6]` |
| R73.7 | **THE TERM SHIPS WITH ITS GLOSSARY ENTRY IN THE SAME COMMIT.** `gate-headline` carries all three fields, names `fails[0]` in its `from`, and its `do` names the decision it should change (do not read a big headline count as capacity that relaxing the gate would release) rather than hedging to "nothing directly" | probe `[R73.7]` |
| R73.8 | **BOTH CLOSURES LAND ONCE, CARRY THE ARITHMETIC, AND ARE STAMPED `auto: 1, by: "user"`.** The ROI-floor loosening experiment and the paper-book population fix are CLOSED, not benched: loosening `GATE.roi` to 1.0% admits 0 of the 41 live band items, and the loose regime cannot be fed while the entry condition is `pass \|\| nearMiss`. The ROI entry also records the STRIKE of the gate choice that rested on *"sole blocker in 14 of 19, 74%"* — 0% by construction, since `DB.gateLog` stores only `fails[0]`. Keyed once-per-store like `FILL_GATE_SOFT_KEY`. **Shipped first WITHOUT `auto`**, on the reasoning that a user ruling is not machine-applied bookkeeping — overruled the same day: see R73.11 | probe `[R73.8]` |
| R73.10 | **THE CAUSAL NUMBER LEADS THE PANEL; FIRST-FAIL IS DEMOTED AND LABELLED AS ORDERING** (user ruling, Aug 13 2026). Leave-one-out — *relax this gate alone and this many candidates clear the gate chain* — is exactly the set `failProfile()` already computes as single-gate near-misses, and has been computed on every refresh since Aug 12 while the panel rendered the positional number three lines above it. **No new machinery**: the Aug 10 ENABLER refactor made every gate evaluate independently, so the counterfactual needs no re-run. The block renders `binds` first with `heads` beside it as a secondary reading, and a gate that heads candidates while binding none **renders its zero** rather than dropping out — that gate is the finding. Ordering is asserted on POSITION IN THE DOCUMENT, since both blocks render and "leads" is the whole ruling. **Claims exactly what it computes:** *clears the gate chain*, never *gets funded* — the family rule, tiering, caps, budgets and seasoning all run afterwards. **And it states that single-gate relaxations do not add**, with the two-gate and multi-gate counts no single relaxation reaches. Paid for under the zero-based complexity budget by absorbing the per-item near-miss list, which is the same population and now lives inside the `binds` drill | probe `[R73.10]` |
| R73.11 | **`auto` MEANS WRITTEN BY THE TOOL, UNIFORMLY; PROVENANCE IS ITS OWN FIELD** (user ruling, Aug 13 2026). The flag was stamped on machine-applied bookkeeping (Aug 11 2026) so the review could show those as a reviewable block; two closure entries written in code to record the USER's rulings then made both readings apply at once. **Ruled: consistency wins — a flag that means one thing in some rows and another in others is unusable to a reader six weeks out, who cannot tell which sense a given row is in, and the review's own auto block filters on it.** So `auto` answers *did this appear without a press* and the new `by` answers *whose judgment was it* (`"user"` \| `"tool"`, absent on hand-pressed entries where the press is the provenance). Three states asserted at the writer, including the third — `auto` with no `by`, a row predating the field — which reads as **unrecorded**, never as a default to either side. The five pre-existing tool-written user rulings were back-filled with `by: "user"`; the auto-applied bookkeeping paths take `"tool"` by default. The review's block renders a **whose call** column and says in its own copy that it is everything the tool WROTE, not everything the tool DECIDED | probe `[R73.11]` |
| R73.9 | **THE GATES EXPORT CARRIES THE SAME MARK AS THE SCREENS.** A file read away from the tool has no tooltip to fall back on, and per-gate counts are what an analyst sums first. `attributionCaveat` states the basis, names `fails[0]`, gives the worked measurement, and points at the paper export's `benchedByGate` as the field that IS the binding gate — with the warning that the two are different populations and must not be joined on gate name | probe `[R73.9]` |
| **§74 The instrument gate core — stage 1a of the universe scorer (build ruled Aug 14 2026; design `audits/DESIGN-2026-08-14-universe-scoring.md`, staging `audits/STAGE0-2026-08-14-frontier.md`)** | | |
| R74.1 | **THE CORE IS A PURE FUNCTION OF (STATS, CONFIG).** `marketGateFails(st, cfg)` evaluates the market gates — ROI floor, margin floor, book skew, flow imbalance, trend (both limbs), volume trend, volume floor, momentum — with every constant arriving in `cfg`, never read from `GATE`. The same stats object must fail one config and pass another; a hardcoded constant anywhere in the arithmetic collapses the two | probe `[R74.1]` |
| R74.2 | **PURITY IS MECHANICALLY ASSERTED, BOTH HALVES.** Functional: mutating `GATE`, `DB.tickFloor` and the other ambient sources the old inline chain read changes nothing about the core's output for fixed arguments. Source: `String(fn)` over `marketGateFails` + `marginNeedCfg` + `volFloorCfg` contains none of the forbidden identifiers — `GATE.`, `DB.`, `S.`, `exemptIds`, `itemWins`, `provenLoser`, `slowHistory`, `isBlk`, `workingStack`, `lastWalkupAt`, `tickFloorFor`, `TICKF`, `volFloorFor`, `marginNeedFor`, `geTax`, `calc(`. The source half exists because a leak can be inert on one fixture and real on another — the functional half alone is exactly the kind of green the twelfth face warns about | probe `[R74.2]` |
| R74.3 | **THE LIVE CHAIN ROUTES THROUGH THE CORE — ONE IMPLEMENTATION, NOT TWO.** `candidateFor`'s market-gate conditions are membership tests against the core's output at `liveMarketConfig()`; the chain keeps its copy, its `fails[0]` headline order and its operator overlays. Asserted as set-equivalence between the chain's market-gate fails and the core's, plus a concrete verdict (the fixture passes the ROI floor) that a seeded change **inside the core** flips — proving the chain's verdict moves when code this assertion never calls directly is edited | probe `[R74.3]` |
| R74.4 | **THE CORE'S VOCABULARY IS EXACTLY THE CONFIG-FREE MARKET KEYS** (rewritten for stage 1c, user ruling Aug 14 2026). The core emits SCHEMA KEYS (`MARKET_GATE_KEYS`: `roi`, `margin`, `skew`, `imbalance`, `trend`, `volTrend`, `volFloor`, `momentum`) — no display names, no digits, no baked constants. Display names live in ONE map (`MARKET_GATE_LABEL`), whose values are **persisted ledger keys and do not move** (the paper-book rename precedent): the `"3× tax"` inside the live chain's label is the one place the constant may stand, because the live chain's `taxMult` IS 3 and the label names the live gate; instrument surfaces render from keys plus their own config's constants. Swept across stats that trip every limb, every emitted key is in the list, every key has a label, and no operator gate — sizing, blacklist, proven-loser bench, fill history, drift bench, seasoning — can appear, because the core takes no input that could express one | probe `[R74.4]` |
| R74.5 | **THE OPERATOR-STATE LEAKS ARE EXCLUDED ONE BY ONE, EACH WITH ITS OWN ASSERTION.** (a) Tested-price overrides: a margin test on the watch row moves `calc()` (which is the operator's plan reading the operator's ruled input) and does NOT move `marketStatsFor` — the instrument never sees the experiments. (b) The wins waiver: the core reports the volume floor while the live chain, given 2 realized round trips, forgives it — the overlay lives in the chain only, so the instrument's population (which has no flip log) cannot inherit forgiveness tuned on mine. Blacklist and proven-loser are covered by R74.4's inexpressibility. Chart inputs (`tr`, `vt`, `moState`) are null in `marketStatsFor` until the T0 archive exists (stage 1b) — the core skips them, and unknown is not failing | probe `[R74.5a]` `[R74.5b]` |
| **§75 The T0 market archive — stage 1b of the universe scorer (build ruled Aug 14 2026; the stage with the clock)** | | |
| R75.1 | **A BUCKET ROUND-TRIPS EXACTLY, THREE STATES INTACT.** Every `/5m` bulk response and one `/1h` reading per wall-clock hour are written to IndexedDB (`geflips-t0`, the constitution's one named localStorage exception) as column-packed whole-universe records, riding `loadHour` — a data arrival, never a render (R34.1). In the pack, **NaN price = no print on that side, 0 volume = printed nothing** — different readings, never conflated (the falsy-zero class), and NaN survives the structured clone. An absent bucket reads `undefined`, not empty | probe `[R75.1]` |
| R75.2 | **RETENTION PRUNES STRICTLY-OLDER KEYS AND REPORTS ITS COUNT.** 72h for `m5` (longest horizon + reconstruction slack), 8d for `h1` (7d trend window + slack). The cutoff is exclusive — the boundary bucket survives — and the prune returns how many it deleted, so a caller can see a prune that did work versus one that found nothing | probe `[R75.2]` |
| R75.3 | **COVERAGE IS KEYS-PRESENT OVER WINDOW ARITHMETIC.** The bucket keys ARE the observation record — a separate ledger could drift from the data it describes. `t0Coverage` returns `{observed, expected}` as a pair (the `daysBenchedBy` construction): observed counts keys actually in the store, expected is arithmetic on the window, and no reader can render one without the other. An accrual gap is an unobserved period from the moment it starts — scoring and replay over it report *unobserved*, never *quiet* | probe `[R75.3]` |
| R75.6 | **EVERY BUCKET CARRIES ITS PACK VERSION FROM BIRTH.** `pv = T0_PACK_V` on every m5/h1 record — the partition answer at the store's creation (conformance ruling item 3): the regime that writes a bucket is `t0Pack`'s shape, `pv` records it, and a shape change bumps the constant so readers partition instead of guessing from fields present. Stamped before any real bucket accrued, so no unversioned population exists | probe `[R75.5]` |
| R75.5 | **THE WIRING IS CROSSED, NOT ASSUMED — AND THE FIXTURE KEY LIVES IN THE FUTURE.** Every other §75 assertion calls the archive directly; this one stubs the fetch layer, calls the real `loadHour`, and requires the bucket to appear **without the test touching accrual** — the seam under test is `loadHour → t0Accrue`, and a one-line wiring nobody tests is the state-coupled-to-a-fetch-path shape (scan 10). The accrual is fire-and-forget, so the read retries on a bound rather than assuming a completion time (fifth face). **The fixture timestamp is ~2103, and the first form of this test is why**: a past-range key (safe from production *writes*) was deleted by `t0Accrue`'s own retention prune within the same accrual call — a working accrual and a working prune composing into a vanishing fixture, green on accrue and empty on read. **A fixture range must clear both the write path and the delete path**; only the future clears retention by construction, and since `t0Prune` cannot reach the future, the fixture's cleanup is an explicit keyed delete whose result is itself asserted | probe `[R75.5]` `[R75.5b]` |
| R75.4 | **ACCRUAL FAILURE IS SURFACED; PROBE FIXTURES CLEAN UP AND THE CLEANUP IS ASSERTED.** A run of `T0_FAIL_WARN` consecutive accrual failures raises the standing `warn()` channel naming the gap's start — an archive that silently stops is a stalled generator whose gaps would read as market silence. Probe fixtures use keys in a range production cannot reach (< 1e12 ms) because the probe profile persists between runs; the cleanup itself is asserted so a broken cleanup cannot silently grow the store | probe `[R75.4]` |
| **§76 The universe scorer — stage 1c: scoring + T2 rollups (build ruled Aug 14 2026)** | | |
| R76.1 | **THE GRID IS 16 CELLS WITH EXACTLY ONE CONTROL, DEDUPED BY HASH.** 5 `taxMult` × 3 `volBase` at `roi` 1.2, plus the one cell where roi binds (`taxMult 0.5, roi 1.0, b 250` — measured 54 → 61); the b=100 corner stays deferred until capture calibration exists (ruled). The CONTROL is the live config, marked; when it equals a grid cell the two are one population under one hash, and if the user moves a live constant the control's hash moves with it and its old population closes — which is correct, not a bug | probe `[R76.1]` |
| R76.2 | **POPULATION IDENTITY IS THE CONFIG HASH, AND EVERY CONSTANT MOVES IT.** `cfgHash` runs over the full 10-constant vector in fixed key order; perturbing any single constant produces a different hash, so an edited config is a NEW population, never a continuation — `FILL_MODEL_V` partitioning applied at the instrument's birth. First-fail never enters the schema (standing sub-ruling): the scorer stores verdicts computed from full fail sets and no ordering-derived figure exists to mislabel | probe `[R76.2]` |
| R76.3 | **ONE PASS PER 5m BUCKET.** The scorer rides `loadHour` behind a bucket guard: a re-run of the same bucket returns null and accrues nothing, so a render storm or a double poll cannot double-count a cycle. Cadence is the bucket cadence because volumes decide the gates and volumes arrive per bucket — scoring faster would re-score price-only changes against stale volume | probe `[R76.3]` |
| R76.4 | **FLOW AND STOCK ARE FIRST-CLASS AND ALLOWED TO DIVERGE** (the stage-0 churn finding as schema: frontier intersection 27 of union 171 across two times of day, so a snapshot understates the population ~6×). `cycles`/`fundedItemCycles` are the FLOW; `ids`/`distinct` are the STOCK (distinct items EVER funded, capped at `SCORER_ID_CAP` sized from the union flow, with a declared `capped` flag — the `strataSeen` pattern). A cell where they agree is a stable list; a cell where they diverge is a churning frontier; both readings come from these figures and neither is derivable from the other | probe `[R76.4]` |
| R76.5 | **HOUR BANDS KEY TO THE BUCKET'S UTC HOUR, NEVER THE WALL CLOCK'S.** Today's config funds 13 at 22:00Z and 5 at 10:00Z — pooling across hours pools populations that answer different questions, so the 24-band split is in the schema from birth. A delayed cycle scores old data and files under the data's hour: the information-horizon rule applied to attribution | probe `[R76.5]` |
| R76.6 | **THE IMPORT CARRY RE-DERIVES WHAT IS DERIVABLE.** `scorerT2` is enumerated explicitly in `validateImport` (an unlisted key silently dies on restore — R68.9 applied before the fact). The stored hash key is **never trusted**: it is recomputed from the sanitized config, so a hand-edited file cannot file data under a hash its config does not produce — a pooled population wearing a partition's clothes. `distinct` re-derives from the carried ids; ids dedupe and cap on the way in | probe `[R76.6]` |
| R76.8 | **THE SCORER'S WIRING IS CROSSED, NOT ASSUMED.** §76's own assertions call `scorerCycle` directly, which proves the scorer and not the wiring — the same gap R75.5 closed for the archive, on the second consumer of the same arrival. The stubbed `loadHour` must advance `S.scoredBucket` to the stub's bucket without the test touching the scorer | probe `[R76.8]` |
| R76.9 | **EVERY FUNDED ITEM-CYCLE CARRIES ITS CHART-COVERAGE STAMP** (conformance ruling flag 1, fixed before anything accrues). Until the h1 archive matures, every verdict is a SIX-GATE verdict; when stage 1d wires `tr`/`vt`, full-chain cycles must not pool with six-gate cycles under one configHash. `fundedNoChart` decomposes `fundedItemCycles` as charted + uncharted from birth. The decision term (`scorerNoChart`) is **extracted and asserted in both directions directly**, because its with-chart limb is unreachable through `scorerCycle` until 1d — faking a fixture for an unreachable limb is the eighth face, and the extraction is how the limb stays honest until the wiring exists. The era fact itself (`marketStatsFor` returns `tr: null`) is pinned, so 1d's wiring cannot arrive without flipping something red. The `ids` stock is deliberately NOT era-split: era-pure stock re-derives from T0 replay, and that answer is in the partition register | probe `[R76.9]` `[R76.9b]` |
| R76.7 | **A SCORER THAT STOPS SAYS SO, IN THE RIGHT WORDS.** The failure note (extracted term, `t0FailNote` pattern) is null below `SCORER_FAIL_WARN` and at the bar names the gap's meaning: **unobserved cycles, not a quiet market** — the observed-time rule pre-installed in the copy. No surface exists until stage 1e; the warn channel is what keeps a dead scorer from dying silently in the meantime | probe `[R76.7]` |
| **§77 Row 23 and the reconciliation diff (consolidated rulings, Aug 14 2026)** | | |
| R76.10 | **THE BLACKLIST CANARY — row 23 as amended.** The blacklist-never-accrues precedent narrowed to its actual property: it forbids the machine accruing **counterfactual profit evidence against the veto**, not the instrument *seeing* vetoed items — total exclusion would silence a canary. Blacklisted items are scored at the gate level **at the cycle layer** (`isBlk` never enters the pure core or the stats builder), EXCLUDED from funded counts, the ids stock, the coverage split, and every future net and ranking, and COUNTED per cell as the caution pair `blFunded`/`blIds` — *this cell would fund N blacklisted items*, a red-flag figure at 1e, because a loose config admitting known-bad items is evidence against the config. **Trips and fill sim never run on them** (1d inherits as a hard rule). Membership stamps at the cycle, so un-blacklisting moves an item into the main figures from that cycle forward. Honest caveat on record: the blacklist's n is tiny; the pump-flag population is the real defense-training corpus, the blacklist supplies permanent anchors. **The assertion pair discriminates by construction**: R76.10 fails if the item leaks into funding, R76.10b fails if it vanishes — the seeds prove each direction with the other green | probe `[R76.10]` `[R76.10b]` `[R76.10c]` |
| R77.1 | **THE RECONCILIATION DIFF RECORDS BOTH DIRECTIONS WITH FULL FAIL SETS, EVERY SCORED BUCKET** (flag 3 — the cutover's plumb line, started immediately because every unrecorded cycle is reconciliation history that does not exist). Per cycle: `extra` = items the control config passes that the watchlist does not hold; `plan.missing` = plan-funded items the control fails, each with its config-free fail keys. Blacklisted items appear in neither. One row per bucket in IDB `rdiff` (the third store, DB v2), 90-day retention — localStorage could not hold a ledger whose value is its length. The cycle's own return carries the summary so a caller reads the diff from the cycle that made it | probe `[R77.1]` `[R77.1b]` |
| R77.2 | **AN UNOBSERVED PLAN SIDE IS NULL WITH ITS REASON, NEVER AN EMPTY COMPARISON.** Three states: a plan never built this session and one stale past 10 minutes both record `plan: null` with `planWhy`; an empty watchlist records a real comparison against zero holdings. The diff must not claim a comparison it could not make — the observed-time rule at the row level. The plan side reads `S.lastPlanPicks`, stamped by `buildPlan` itself (a computation the poll drives; the diff's accrual rides `loadHour`) | probe `[R77.2]` |
| R76.11 | **A GUARDED-OUT CYCLE DOES NOT CONSUME ITS BUCKET** (the boot race — deployment-check finding, M154). `doRefresh` runs `loadLatest` and `loadHour` concurrently, so the scorer's inline call can fire before `S.latestAt` exists; the input guard returns **before** the bucket is marked scored, and a catch-up `scorerCycleSafe()` after the `Promise.all` scores the same bucket once both inputs are settled — without which a walk-up shorter than ~5 minutes would score nothing while the archive accrued normally. The bucket guard makes the two call sites unable to double-count. Found by the first run of the real-network deployment check, which is the layer that can see boot races the stubbed-fetch assertions cannot | probe `[R76.11]` |
| R77.3 | **RDIFF FIXTURE ROWS ARE PRODUCTION-SHAPED KEYS AND ARE CLEANED BY KEY, ABSENCE ASSERTED.** The diff fixtures use current-day buckets (they must — the row keys ARE the cycle's bucket), so the probe deletes every row its cycles wrote rather than leaving them to retention, and asserts the deletion — the M152 discipline applied where the future-range trick is unavailable | probe `[R77.3]` |
| **§78 The fill sim on the frontier — stage 1d of the universe scorer (build ruled Aug 14 2026)** | | |
| R78.1 | **TRIPS OPEN AT THE FUNDED EDGE, SHARED ACROSS CELLS, FULLY STAMPED AT BIRTH.** One trip per (item × participation level {25, 50, 100%} × **horizon in the RULED set {6h, 9.5h}** — ratified Aug 14 2026, the 9.5h variant answering the overnight question as a stamped population) for every item at least one cell funds this cycle; the funding cells are stamped on the trip (`cells`) and its outcome rolls into each at close — a cell that starts funding mid-trip joins at the next reopen, because its own simulated placement could only happen at the cycle it first funded. Birth stamps: `hzH`, `fv` (`SCORER_FILL_V` — the trip layer's own fill-model version, never pooled with the paper book's), `v`, `sz` (the sizing convention: one GE buy limit, stamped so a changed convention partitions), `openBkt`, `openSeq`, the bucket's UTC `hour`, `pump`, `noChart`, `grade`. Items failing every cell get no trip; one rolling cycle per (item, participation, horizon) — close, then reopen if still funded | probe `[R78.1]` |
| R78.2 | **NO TRIPS ON BLACKLISTED ITEMS, EVER — row 23c's surviving core at the trip layer, discriminating with the canary.** The frontier assembly in `scorerCycle` is the single owner of the promise: a bl item never reaches the trip layer, while the SAME item still counts into the caution pair — a seed that leaks it into trips leaves R76.10/10b green, and one that silences the canary leaves this green. **Blacklisting an item MID-TRIP voids its open trips at that cycle** (`voided-bl`, no nets, counted per cell as `vbl` beside the figures, never inside them): accruing further P&L on a vetoed item would be the counterfactual-evidence shape the precedent forbids. Un-blacklisting starts fresh trips at the next funded edge; the eras never mix | probe `[R78.2]` `[R78.2b]` |
| R78.3 | **THE CAUSALITY RULES TRANSFER WHOLE.** The opening cycle cannot observe, credit or resolve a trip (minimum life, `openSeq`); a bucket that began before the trip was placed is not even observed into its pend; a rolled bucket credits each capture lifecycle's leg ONCE (`buyBkt`/`sellBkt` guards), asserted at a scale where the qty clamp cannot pin the reading and with a re-presented bucket adding nothing | probe `[R78.3]` |
| R78.4 | **THE SELL WINDOW OPENS WHERE THE BUY FINISHED, per capture lifecycle.** Sell credit only from buckets passing `sellBucketEligible` — after that lifecycle's own `buyDone`, never the bucket that completed its buy, once each. A filled lifecycle nets tax on every bound from the same formula (sold at the ask, unsold remainder at the exit value, cost at the bid) | probe `[R78.4]` |
| R78.6 | **THE CAPTURE POINTS {0.05, 0.15, 0.30} ARE SEPARATE LIFECYCLES, not three readings of one.** Capture moves buy completion, buy completion anchors the sell window, so each point runs its own legs against the same tape (`vars`); the same bucket can complete the buy at 0.30 while 0.05 is a quarter done. Economics land under keys carrying horizon, participation, capture and fill-model version (`h6\|p50\|c15\|fv1`) — no aggregate can pool across any stamp | probe `[R78.6]` |
| R78.7 | **HORIZON OUTCOMES CARRY THEIR NAMES, and the exclusions are never inside n.** A watched partial or forced exit prices at the last observed bucket value — read only within the observation cap of the horizon, i.e. the series value AT the horizon within the model's stated resolution, with a spot-price fallback stamped `exitApprox`. A horizon that expired beyond the cap unwatched is an UNOBSERVED HOLE (net 0, counted as `unobs`); a trip observed under the `SHADOW_MIN_OBS` floor rolls as `insuf`; an unfilled watched horizon is data (`unfilled`, counted). Three exclusion states, three names — counted, insufficient observation, unobserved hole — the observed-share floor's discipline transferred (census probe:4411's property half) | probe `[R78.7]` |
| R78.9 | **PUMP EXPOSURE IS STAMPED AT BIRTH AND NEVER EXCLUDED** (row 15 pre-ruling — the opposite treatment from the blacklist, because the properties differ). A pump-flagged item's trips exist, carry `pump: 1`, are IN the counted figures, and every econ bucket decomposes by the stamp (`pumpN`/`pumpNet`), so a proposal citing a materially pump-exposed cell can carry its caveat inline at 1e | probe `[R78.9]` |
| R78.10 | **A CLOSED TRIP ROLLS ONCE INTO EVERY BIRTH CELL**, nets at all three reach bounds (`net` operative, `netS` proven, `netL` possible — never averaged, no midpoint field exists and the econ schema is asserted as an exact key set so one cannot appear silently), **gross movement (`gross` = Σ\|net\| terms) with the top single mover (`top` = max \|net\|)** for the ruled concentration measurement, and the pump/coverage decompositions. A second tick re-rolls nothing. Trips whose cells were replaced by an import roll nowhere — their rows persist in the t1 ledger, where the closed population's evidence lives on | probe `[R78.10]` |
| R78.11 | **THE CAPTURE PROBE'S VERDICT (BINDING, ratified): if cell rankings flip across {0.05, 0.15, 0.30}, the instrument's verdict IS "insufficient calibration to rank"** — rendered as the verdict, never a caveat. The rank gate's disqualifiers, all named, full fail sets: **capture-readiness first** — until real frontier-class flips grade capture, EVERY ranking renders **"cannot rank yet"** in those words (ratified Aug 14 2026 as the rendered verdict; the dependency stays named — the operator's calibration flips inside the tape window); beats the control at the PROVEN bound at every capture point (one reason per failing point, never only the first); **concentration against the RULED 0.5 ceiling, measured as the top mover's share of the cell's GROSS movement** (\|net\| terms — a share of a negative net is not a proportion, and the gross denominator keeps the reading on losing cells; a rank-disqualifier only, never a data exclusion); capture-stability. Rankings order by `netS` with the hash as deterministic tiebreak, over cells with counted evidence at every point. A cell's econ state has three names — counted, only-excluded with its split, never-fed. The terms' consumers are STAGED for 1e (the cell verdict line and ranking panel, named here per the scan-2 staging rule) | probe `[R78.11]` |
| R78.12 | **THE BOOT CARRY: held, never dropped; persisted, resumed.** Open trips persist to the `t1open` carry each tick and resume after a reload — their observations are not silently discarded, and a resumed trip whose horizon expired unwatched closes as a hole. A frontier arriving before the carry loads is HELD and run by `t1RunPending` on load — a held pass does not consume its bucket (the R76.11 property on this layer's own `S.t1Bucket` guard, separate from the scorer's). A carry that cannot be read warns IMMEDIATELY, naming the loss as unrecorded holes | probe `[R78.12]` `[R78.12b]` |
| R78.13 | **ECON BUCKETS SURVIVE THE IMPORT CARRY WITH ALL FOUR STAMPS IN THE KEY** — both ruled horizons carry; a key missing any of horizon/participation/capture/fill-model is dropped (an unstamped population cannot be carried without pooling it), unknown fields inside a bucket are dropped, numbers coerce (`gross` included). R76.6's discipline extended to the 1d schema | probe `[R78.13]` |
| R78.14 | **A FILL SIM THAT STOPS SAYS SO, IN THE RIGHT WORDS.** The failure note (extracted term, `t0FailNote` pattern) is null below `T1_FAIL_WARN` and at the bar names the gap's meaning: unobserved trip time, never evidence of no fill. The freshness panel carries the layer's own scheduled stream (`S.t1LastBucket`, the bucket last opened for — the thing the claim names) | probe `[R78.14]` |
| R78.15 | **PROBE FIXTURES: the t1/t1open stores are probe-profile-only and cleared at block start and end; this block's rdiff rows are deleted by their cycle buckets; the cleanup is asserted** (M152/R77.3 discipline — fixture rows must not accumulate across runs or read as production data) | probe `[R78.15]` |
| R78.16 | **THE RULED HORIZON SET {6h, 9.5h} MEASURES THE SAME TAPE TWICE, AS TWO STAMPED POPULATIONS** (user ruling, Aug 14 2026). Six hours past the same open moment, the 6h lifecycle has closed while the 9.5h one is still accruing — the overnight comparison the retired duration-threshold labelling inverted twice, now answered by construction: horizon is in the trip key and the econ key, so the two measurements cannot pool and neither needs a label to be told apart | probe `[R78.16]` |
| R78.17 | **THE RULED CONSTANTS ARE PINNED, AND THE UNGRADED-CAPTURE ERA FACT IS THE TRIPWIRE** (staging practice, fourth rule). `SCORER_HORIZONS` is exactly {6, 9.5}; `SCORER_CONC_CEIL` is exactly 0.5; `SCORER_CAPTURE_GRADED` is FALSE and `scorerRankReadiness()` carries the ruled words ("cannot rank yet", the frontier-class dependency, the calibration-flips-inside-the-tape-window condition) — so the grading era cannot arrive without turning this red and forcing the accounting, and a drifted constant is a ruling, never an edit. The ruling itself is decision-logged once (`auto: 1, by: "user"` — written by the tool, decided by the user) | probe `[R78.17]` |
| **§79 MM mode benched (user ruling, Aug 14 2026) — disabled until the universe scorer stands, not deleted** | | |
| R79.1 | **THE BENCH IS PINNED AND NO NEW MM CYCLE CAN START.** `MM_BENCHED` is true and flips only by ruling — the assertion pins it so the un-bench cannot land silently (the staging practice's fourth rule, third use). `placeQuote` refuses; enrollment and per-item toggles are frozen; MM-READY candidate lines stop. **The plan reverts per item as its unwind completes:** an enrolled item with nothing in flight competes in the plan without the ownership bench; one with held lots or a standing quote stays benched with the unwind NAMED inline, because funding it in parallel would double-commit against its own in-flight legs | probe `[R79.1]` |
| R79.2 | **NOTHING STRANDS, NOTHING VANISHES** (the M152 lesson: the disable path must clear the delete path). Standing quote legs remain rendered, ageing, fillable, repriceable and clearable until resolved — they are real offers in-game; held lots remain tracked and sellable through the FIFO true-cost flow (the unwind is the operator's to execute; the tool proposes sell-down); the mm panel states the bench where the machinery renders; and NO placement control renders anywhere under the bench. Per-item `invTarget` persists frozen and re-keys as pinned-item state at cutover, or retires by ruling | probe `[R79.2]` |
| R79.3 | **THE BENCH IS DECISION-LOGGED ONCE WITH ITS TRUE BASIS AND THE IN-FLIGHT SNAPSHOT.** Basis recorded honestly: walk-up attention cost — a realized-P&L case could NOT be measured either way, because flip records carry no mm stamp and `invTarget` keeps no history, so mm flips CANNOT be attributed; the log says so instead of claiming an unverifiable P&L. The snapshot (enrolled items, targets, held units at cost, standing legs, lots without mode) is computed in the operator's own browser at activation, giving the cutover un-bench its evidence base. Written by the tool, decided by the user (`auto: 1, by: "user"`); once per store | probe `[R79.3]` |
| **§80 The scorer surface — stage 1e of the universe scorer (ruled Aug 14 2026)** | | |
| R80.1 | **A PULL SURFACE IN THE RULED CLASS, SCOPED TO ITS OWN SUB-VIEW.** Trade → Scorer renders only on its sub (computed visibility asserted where it must be ABSENT, per the scoping standard); it presents no rulings, so the walk-up attention budget is untouched | probe `[R80.1]` |
| R80.2 | **THE FIRST SCREEN IS THE VERDICT (the walk-up test, ruled with 1e).** The first rendered element answers *"Can this instrument tell me anything yet?"* in one sentence, then four waiting-on lines with real numbers: **accruing** (cycles + archive coverage, with the observes-only-while-open honesty), **chart gates** (N of 7 OBSERVED days — observed, not wall), **ranking** (the ruled cannot-rank-yet words with the named dependency), **reconciliation diff** (day N, row count, its reader named). IDB-backed figures load asynchronously: a pending read says *reading*, a failed read says *could not check — treat as unread, never as current*, and stale numbers never render as current | probe `[R80.2]` `[R80.2b]` |
| R80.3 | **WHEREVER A RANKING WOULD RENDER, THE READINESS VERDICT RENDERS INSTEAD, IN THE RULED WORDS** — "cannot rank yet" leads the economics section with the frontier-class dependency named, and NO ordering of cells renders anywhere on the surface while capture is ungraded (the absence half asserted) | probe `[R80.3]` |
| R80.4 | **HORIZONS ARE SEPARATE STAMPED GROUPS ON THE SURFACE** — each (horizon × participation) renders in its own `[data-hz][data-part]` container; the same cell's h6 and h9.5 figures never share a container and no combined-across-horizons figure exists (design §9's per-lifecycle render obligation, the never-pool rule made structural in the DOM) | probe `[R80.4]` |
| R80.5 | **THE GRID'S POOLED PER-CYCLE AVERAGE RENDERS WITH ITS DECOMPOSITION** — the hour extremes inline beside it and all 24 bands in its opening (an unfed band reads UNOBSERVED, never quiet); flow and stock both render (the churn finding as columns); the blacklist canary opens to its rows and renders a real zero as "none" with the counted-from-birth claim, never as absence | probe `[R80.5]` |
| R80.6 | **A CELL'S ECON STATE RENDERS UNDER ITS OWN NAME** — counted figures with nets at stated bounds and concentration as the top mover's share OF GROSS; only-exclusions with their named split (observation floor / holes / voided); an unfed key says no cell carries econ yet — three states, never a zero standing in for any of them | probe `[R80.6]` |
| R80.7 | **EVERY ECON BUCKET OPENS TO ITS TRIPS WITH THE TRUNCATION DECLARED** — the loaded window (7 days of the 30-day ledger), the cap, the carried-of-total count, and the beyond-window re-derivation path (T0 replay) stated in the drill itself; pump-stamped rows visible as such | probe `[R80.7]` |
| R80.8 | **THE EXPORT (`analysis-scorer-*.json`) STATES WHAT WAS IN FORCE AND WHAT WAS TRUNCATED** — the ruled constants (horizons, participation, captures, the 0.5 gross ceiling), the cannot-rank words, coverage figures, and rows riding with every rollup (cells with bands, econ, canary; open and closed trips). Figures the session has not loaded export as NOT LOADED, never as a stale guess. Registered in the collector (`analysis-scorer` class, sweep.sh + CLAUDE.md table) | probe `[R80.8]` `[R80.8b]` |
| R80.9 | **THE GENERATOR RENDERS ITS OWN STATE, always** — bucket ages when stamped, "not yet this session" after a reload (session-scoped stamps are could-not-check, never up-to-date), failure streaks named when present, and the line says why it renders unconditionally | probe `[R80.9]` |
| R80.10 | **EVERY SCORER TERM SHIPS ITS GLOSSARY ENTRY IN THE SAME COMMIT** (what / produced-by / what-it-changes), and the surface marks the vocabulary with the standard popover underline — controls that deliver their own intel stay unglossed per the standing restraint | probe `[R80.10]` |
| R80.11 | **THE WEEKLY REVIEW KEEPS A ONE-LINE SCORER SUMMARY WITH A LINK THROUGH** — the pull-surface consolidation rule; the line reads from sync state only and says the coverage figures load on the surface itself | probe `[R80.11]` |
| **§81 The two retirements — dormancy lane (user ruling, Aug 14 2026)** | | |
| R81.1 | **THE REGIME RACE IS DORMANT, PINNED.** `REGIME_RACE_RETIRED = true` is the era fact — the un-retire flips it only by ruling, and the pin plus every dormant-gated assertion goes red if it flips silently (the staging practice's fourth rule, fourth use after R76.9/R78.17/R79.1). Superseded by the universe scorer's config grid on two measured grounds: the comparator was broken by construction (R68.6) and the `loose \ current` band is unfeedable while the margin gate's effective ROI floor stands above the loose floor (`bandUnreachable` — arithmetic, not a sampling shortfall). **What stops:** regime stamping on new trips — the era fact is NO `regimes` key on a dormant-era trip (absent = not evaluated; `[]` keeps meaning evaluated-nothing per R43.1; the import carry preserves the distinction via `hasOwnProperty` per R68.5, and the export writes `null`, never `[]`); the daily divergence snapshot (the ledger is frozen at its last pre-retirement reading); divergence evidence and proposals (gated at `shadowRegimeEvidence`, the one owner every reader tests). **What stays:** the historical curves, separator bands and ledger render under a dormant banner naming the ruling, the successor and the grounds; every drill still opens; the freshness row reads DORMANT, never stale (a retired stream flagged red forever is wallpaper); the paper one-liner, WHAT CHANGED and the analysis export state the dormancy wherever the race used to speak (the vitals tile did too until the transitional nav refresh of Aug 14 2026 removed it — §82; its open · closed read lives on the paper surface header). The never-fed detectors are superseded by the banner while dormant — on a post-retirement store their own predicates are true and the banner is the true cause | probe `[R81.1]` (pin + surfaces) · `[R18.1]` `[R18.4]` `[R20.5]` `[R27.3]` `[R37.4]` `[R43.1]` `[R65.1]` (dormant forms under the original tags) |
| R81.2 | **THE SLICE'S SAMPLING ROLE IS DORMANT, PINNED.** `SLICE_SAMPLING_RETIRED = true`, same pin discipline. Superseded by universe coverage: the scorer scores every /mapping item each 5m bucket, so the rotating 15-item stratified sample is a strict subset of standing coverage. **What stops:** the stratified draw and rotation (`beyondNetSample`/`currentStratum` have no production caller while dormant), new slice paper trips and clean-find lines, new stratum-ledger accrual. **What stays:** the GAP BAND's draw, register, verdict line and paper entries — it keeps **exactly its ruled share** of the shared per-cycle budget, `floor(SLICE_SHADOW_CAP / 2)`, because widening it is a strategy change needing its own ruling; the durable per-stratum map renders as the frozen historical ledger under a dormant banner, every count still opening to its rows; historical slice trips and cohort rows keep rendering, and the cohort ledger's slice rows name the dormancy instead of promising accrual that cannot come. The capacity report's rotating-cohort term reports the gap's share alone (claims-vs-computation); the prospect verdict answers with the successor. Watchlist and scanner admission stand until cutover | probe `[R81.2]` (pin + surfaces) · `[R11.2]` `[R22.6]` `[R25.5]` `[R29.1]` `[R29.3]` `[R29.5]` `[R33.2]` `[R37.4]` `[R57.2]` `[R61.3]` (dormant forms under the original tags) |
| R81.3 | **BOTH RETIREMENTS ARE DECISION-LOGGED ONCE PER STORE**, `auto: 1, by: "user"` — written by the tool, decided by the user — each carrying RATIFIED, the basis (the race: the two measured grounds with the derived floor cited; the slice: the coverage supersession), an **at-retirement snapshot computed in the operator's own browser** (divergence readings and fed-trip counts; strata totals) so the un-retire ruling has its evidence base, what stays, and the pin sentence. Double-guarded (`S.*Logged` + the store scan — the store guard is what makes it once) | probe `[R81.3]` |
| R81.4 | **LEGACY ASSERTIONS ARE DORMANT-GATED, NEVER DELETED** — a retired feature whose tests vanish is how it returns broken (the R31.x lesson read forward). Every live-form assertion the retirement touches sits behind the same flag production reads, with a dormant-form assertion under the same tag beside it, so (a) every affected requirement id still reports every run and the pairing stays clean in both directions, and (b) the un-retire ruling re-arms the original assertions by flipping one constant, with no archaeology. Direct-call tests of machinery with no production caller while dormant (`regimesFor`, `paperDivLead`, the rotation) are gated per scan 13 rather than left holding dead code alive | structural — enforced by R6.2's pairing check every run, plus the gated assertions themselves |
| **§82 Transitional nav + header refresh (user ruling, Aug 14 2026)** | | |
| R82.1 | **THE TRADE SUB-TAB ROW IS IN THE RULED ORDER** — Plan & Watchlist · Flip Log · Scorer · Paper Book · Gate Health · Scanner · Prospecting: my-trading surfaces first, then the measurement instruments with the Scorer promoted ahead of the legacy ones, then the two cutover-era retirement candidates last. **Scanner and Prospecting carry the subdued treatment (`.sunset`) and NOTHING else does** — signaled, never hidden: both stay fully live until the cutover ruling. The treatment is asserted as computed opacity on the painted button (a dead CSS rule cannot pass), subdued at rest and **full strength while active**, because the subduing is for scanning the row, not for reading the open surface. Display-only: no admission, gating or accrual logic moved with the order | probe `[R82.1]` |
| R82.2 | **THE SCORER TILE'S COPY COMES FROM `scorerTileLine()`, ITS ONE SOURCE TERM** (the paperDivLead extraction pattern — assert the term, and assert the tile renders it, so the two cannot drift). The term compresses the ruled phrasing and never contradicts it: while capture is ungraded it says **"cannot rank yet"** in the ruled words and never claims grading; a chart-gate figure it does not have renders as *reading…* or *could not check* — never as a number, and could-not-check is never up-to-date; with the archive read it reports observed days toward the 7 (observed, not wall) and the frontier's size **at its bucket**; a session with no scored cycle says so instead of showing a stale frontier. The frontier readout (`S.scorerFrontierN`/`S.scorerFrontierBkt`) is a display readout stamped by the scoring cycle — nothing gates on it. The tile's async chart-gate fill-in is a **targeted text refresh of the tile alone**, never a full vitals render from the callback (renderHomeVitals runs the plan; re-running it from async callbacks stamps ledgers at unpredictable moments — the R65.1 lesson) | probe `[R82.2]` (states) · `[R20.3]` (wiring + order) |
| R82.3 | **THE PROSPECTING DISPOSITION IS RULED — LEAVE UNTIL CUTOVER — AND DECISION-LOGGED ONCE PER STORE** (`auto: 1, by: "user"`, RATIFIED, the cutover named in the reason), so the cutover consolidation inherits a DECIDED item, not an open question. No fold, no rename; the subdued Trade-row treatment is the era signal. Double-guarded (`S.prospectHoldLogged` + the store scan — the store guard is what makes it once, the R81.3 shape) | probe `[R82.3]` |
| R82.4 | **THE PAPER BOOK ERA QUALIFIER IS RATIFIED: HEADER, NOT TAB.** `#paperRegimeSub` carries "· the cutover's plumb line", glossed inline from the `plumb-line` entry (shipped same commit); the tab label stays "Paper Book" and persisted keys do not move. **One term, one meaning:** the entry covers BOTH referents the phrase already had — the paper book (fill-model reference) and the reconciliation diff (control-vs-plan history) — as two members of one concept (a standing reference the cutover gate reads), and the scorer verdict's occurrence of the phrase marks from the same entry, so the definition cannot fork | probe `[R82.4]` |
| **§83 The friction export — the missing leg of an existing loop (user ruling, Aug 14 2026)** | | |
| R83.1 | **THE EXPORT CARRIES EXACTLY THE NEVER-CARRIED ENTRIES** — open and resolved alike (a note handled in-app still belongs in the desk file's record), each with its capture timestamp (`t`/`at`), date, context stamp and `resolvedAt`; the header states counts (total / carriedBefore / resolvedInApp / inFile) and that nothing is truncated; a re-export after the carry is an empty file with honest counts, never a silent duplicate carry. Filenames are time-suffixed (`analysis-friction-<date>-<hhmmss>.json`) because successive exports carry DISJOINT entries — an **accumulating class**: the collector moves every member and deletes nothing but byte-identical duplicates (keep-newest would destroy notes the app has stamped carried), and the desk folds collected files into FRICTION.md dated from the capture timestamps (standing instruction in CLAUDE.md) | probe `[R83.1]` |
| R83.2 | **CARRIED IS A MARK, NEVER A REMOVAL** (M152's shape): the payload builder is READ-ONLY — building it stamps nothing; the `exp` stamp rides the export press (`frictionMarkCarried`, after the payload is built, since the builder filters on the same field), marks every included entry, and deletes none. `exp` keeps its ONE meaning — carried out of the app (file export or markdown copy alike) — and "clear exported" in the weekly review remains the one explicit sweep to resolved | probe `[R83.2]` |
| R83.3 | **THE EXPORT LIVES WHERE THE CAPTURE LIVES** — inside the global ✎ friction disclosure (zero standing elements added above it; the footer renders on every tab, asserted per tab) and in the weekly review's friction step beside the markdown copy (scoped match — the footer's copy cannot satisfy it). It rides the existing `data-analysis` bus; no new infrastructure | probe `[R83.3]` |
| R83.4 | **THE STATE-BACKUP CARRY PRESERVES THE FRICTION LIFECYCLE** (fix, Aug 14 2026 — found building the export): the import sanitizer used to rebuild rows without `exp`/`res`, so a restore silently revived carried and handled notes as open. `exp` and `res` survive the carry; absent stays absent | probe `[R83.4]` |
| **§84 Scorer item visibility (user ruling, Aug 14 2026)** | | |
| R84.1 | **THE CONTROL CELL'S FUNDED SET RENDERS NAMED, THIS CYCLE** — a closed disclosure directly under the verdict block whose label carries the count, and **the label count equals the rows inside it** (the headline-reconciles-to-rows property, seeded both directions). Rows carry the market figures that pass the item (post-tax margin, ROI, gate-side 1h volume — labelled as the min-of-sides the gate actually reads) plus UTC hour bands seen this session, and a second drill renders the FULL gate detail — every gate's value against the control config's own constants (config-free keys, never ledger labels), with unknown chart gates reading *skipped, not failing*. The display sort is by a STATED figure (post-tax margin, descending), named in the copy beside the distinction that the cannot-rank verdict gates cell rankings, never membership; "would fund" stays conditional throughout. A session with no scored cycle says so in words — never an empty table | probe `[R84.1]` |
| R84.2 | **THE FRONTIER BROWSER RENDERS UNION MEMBERSHIP WITH BREADTH** — funded-by M-of-grid per item (the ruled structure metric), the cells as compact keys, session hour bands, session flow (cycles funded) beside distinct-ever stock (the churn-aware pair, each labelled with its scope), truncation declared inside the disclosure when any cell's ids stock is capped. **A blacklisted item is never a member**: it renders only in the ruled canary count line (opening to names — the veto forbids counterfactual profit evidence, not sight), and pump-stamped items carry their mark inline, stamped never excluded | probe `[R84.2]` |
| R84.3 | **DELTA MEMBERSHIPS RENDER NAMES ONLY** — per adjacent-cell pair laddered through the control's grid coordinates (taxMult steps, volume-base steps, the one ruled roi notch): the items the looser cell admits, with market figures. **The economics half stays behind the readiness verdict**: where a net would render, the ruled cannot-rank words render instead, and no gp net appears anywhere in the section while capture is ungraded (asserted as an absence) | probe `[R84.3]` |
| R84.4 | **THE RDIFF LINE OPENS TO THE CURRENT CYCLE'S NAMED DISAGREEMENTS** — control passes · watchlist lacks, and plan-funded · control fails, the latter with FULL fail sets (config-free keys), cohort-split in one drill. Labelled INFORMATIONAL: the ledger's reader is still the cutover gate, this window is not a new consumer, and the store's staged status (scan 2's re-report list) does not change. The plan side's unobserved state renders its reason | probe `[R84.4]` |
| R84.5 | **NO ITEM-LEVEL ACTIONS EXIST IN ANY OF THE FOUR SECTIONS** — no watch-add, no pin, no size, no buy, no navigation-as-action: every button in the sections is a drill face, asserted structurally (button census equals drill-face census) plus the named action attributes forbidden. The scorer measures; admission changes are the cutover's; viewing is not proposing | probe `[R84.5]` |
| R84.6 | **ONE EVALUATOR OWNS THE GATE COMPARISONS** — `marketGateEval` returns all gates in chain order with three states (pass / fail / unknown) and `marketGateFails` DERIVES from it, so a pass-value display and the fail chain cannot disagree. Preserved exactly: unknown is not failing; the ROI null limb FAILS (the live chain's "sustained ROI unknown" bench); a momentum fail carries `have: null` in the fails shape as it always has. The §74 assertions continue to pin the derived chain | probe `[R84.6]` |
| **§85 The grid readings — three closed questions (user ruling, Aug 18 2026)** | | |
| R85.1 | **THE THREE CLOSURES LAND ONCE PER STORE, GUARDED PER SUBJECT, STAMPED `auto: 1, by: "user"`** — written by the tool, decided by the user, the pairing the `auto`/`by` ruling exists to distinguish. Guarding per subject rather than by one sentinel is the requirement, not an implementation detail: a store that took some entries and not others must COMPLETE rather than stay half-written, and a closure appended to `SCORER_CLOSURES` later must reach a store that already carries the earlier ones. A second call adds nothing | probe `[R85.1]` |
| R85.2 | **THE BASIS IS FROZEN, AND SAYS THAT IT IS** — unlike the retirement snapshots, whose at-retirement figures ARE a live quantity at the moment they are taken, these entries record a reading the user ruled on (the 16-cell grid over 1,024 observed cycles, exported 2026-08-18). Recomputing at first load would file DIFFERENT numbers under a ruling made on these ones. Each entry names what it closes, the evidence it closes on, and — for the volume-base denial — that this reading supersedes nothing about the original proposal, 50 being outside the tested axis {1000, 500, 250} | probe `[R85.2]` |
| R85.3 | **EVERY ENTRY THIS WRITER PRODUCES SHIPS THE THREE THINGS A CITED AGGREGATE MUST SHIP** (standing ruling, Aug 18 2026), asserted over the WHOLE list and not over one sampled string: a **distinct-item count beside every trip count** (`scorerCountPair` — the stage-0 churn finding as a term, so there is no way to write the one without the other), a **concentration figure** (`scorerConcNote`, in the ruled top-mover-share-of-gross measurement against the 0.5 ceiling — and its **not-derivable** state is a disclosure that renders, never an omission, because `top` is a maximum and does not subtract), and the **capture-grading status** from the one extracted term (`scorerCitedCaveat`), so an entry and any later proposal surface cannot word it differently. **The general machinery — a proposal surface that REFUSES to render without the three — is a separate ruling and is NOT built here**; this writer complying is not that, and the requirement says so rather than letting the narrow detector read as the wide one | probe `[R85.3]` |
| **§86 The reconciliation-diff READER — the staged store's stage arrived (user ruling, Aug 18 2026)** | | |
| R86.1 | **THE CAP TAKES THE NEWEST ROWS, AND THE READ IS A PURE TERM OVER THEM.** `t0All(store, lo, hi, cap)` reads keys first and derives its bound from them, because IndexedDB's `getAll(range, count)` returns the FIRST `count` in key order — a bare cap silently hands back the OLDEST rows, which is the defect measured on the t1 window (3.67h of a 168h span, 97.6% unobserved holes). It returns the carried rows with `total`, `dropped`, `from` and `to`, so the truncation can state the window it actually covers rather than only its size. Everything downstream (`rdiffCoverage`, `rdiffPlanStates`, `rdiffRuns`, `rdiffSummarise`) is a **pure function of (rows, cell)** so assertions call the terms instead of reaching through IndexedDB. **The store-level fixture cleans at block START as well as end** (the R78.15 discipline): an end-only clean survives a run that dies and survives a SEEDED run proving the cleanup assertion, and the surviving rows then read as production history on the next run — found exactly that way here, two orphaned rows turning the capped read red with a total of 6 for a fixture of 4 | probe `[R86.1]` |
| R86.2 | **THE GAP IS EXPLAINED BEFORE ANYTHING IS CLASSIFIED, IN FIVE STATES.** `rdiffCoverage` reconciles the ledger's length against the control cell's `cycles` and returns **complete** / **explained** (the pre-diff window — cell birth to the diff's first bucket — holds at least as many cycles as the gap) / **partly explained** (it does not, and the remainder is NAMED: accrual is fire-and-forget while `r.cycles++` is synchronous, so a session ending between them counts the cycle and loses the row) / **not comparable** (the cell is YOUNGER than the diff — a live constant moved and closed the earlier population, so differencing the two counts is the pooling error, not a finding) / **cannot check** (no cell, or no creation time). A ledger whose length nobody has reconciled cannot support a classification of its contents, so the order is the requirement | probe `[R86.2]` |
| R86.3 | **THE PLAN SIDE'S STATES LEAD, AND THE MISSING DENOMINATOR COUNTS OBSERVED ROWS ONLY.** A `plan: null` row is UNOBSERVED, never agreement; the reasons are tallied **by the writer's own words** (`byWhy`) rather than parsed into named buckets, because the prose IS the store's schema here and a pattern-matching reader would break silently the day the wording changed. `rdiffSummarise` computes the missing side over `rows.filter(r => r.plan)` and renders that denominator inline | probe `[R86.3]` |
| R86.4 | **PERSISTENCE IS MEASURED IN ADJACENT *OBSERVED* BUCKETS, AND A GAPPED RUN IS `cannot say`.** `rdiffRuns` returns `longest` and `longestClean` per item; a run whose adjacency spans a bucket the ledger has no row for is excluded from `longestClean`, because the ledger has no row there and it is evidence neither way. An item whose only multi-bucket runs are gapped classifies as **cannot say** — not as persistent and not as timing. This is the observed-time rule living inside a run detector | probe `[R86.4]` |
| R86.5 | **THE `timing` CLASS EXISTS ON THE MISSING SIDE ONLY, AND SAYS WHAT IT DOES NOT ESTABLISH.** The poll runs `scorerCycleSafe()` and rebuilds the plan twenty lines later in `accrueBackground()`, so every row compares the control at bucket T against a plan stamped at T−1 and a single-bucket `missing` entry can be one poll of price movement. The mechanical test is survival across a poll boundary. **The class is ABSENT from the `extra` side** — that side compares against watchlist MEMBERSHIP, a persisted set no plan staleness can move — and its absence is asserted, not merely omitted, per the scoping standard. The copy states that `timing` is CONSISTENT with the skew and is not proof of it, and that extraction-defect-versus-understood-difference is the reader's call which nothing here decides | probe `[R86.5]` |
| R86.6 | **THE EXPORT CARRIES THE ROLLUP'S ROWS AND THE READER'S THREE STATES.** `analysis-rdiff-*.json` renders in the ruled ORDER (coverage → plan side → disagreements), declares its truncation with the real window, ships every classified row with its buckets, clean-run length and full fail set, and carries the class meanings inline so a file read away from the tool needs no tooltip. Not-read-this-session and could-not-read are **distinct stated states** and neither renders as an empty ledger; the collector carries the class and the sweep table lists it | probe `[R86.6]` |
| **§87 The qualification store's scoped retention (user ruling, Aug 18 2026 — deployment-class, ruled before it was built)** | | |
| R87.1 | **THE PRUNE IS SCOPED, NOT REPLACED — AND THE TWO HALVES ARE A DISCRIMINATING PAIR.** A `"pool"` row SURVIVES a cycle in which its id is not in `DB.watch` (the defect: the old rule created and deleted every pool row inside one call, so the seasoning gate could never be satisfied and the plan would have funded nothing, permanently, behind a bench line reading "qualifying" with an ETA that could never arrive). A `"watch"` row still DIES on watchlist departure — unchanged behaviour, and the half that a fix which merely deleted the prune would break. Seeded both directions: restoring the unscoped prune reddens the first and leaves the second green; deleting the prune entirely does the reverse | probe `[R87.1]` |
| R87.2 | **THE RETENTION PREDICATE IS EXTRACTED AND ITS WINDOW IS INJECTED.** `qualRetain(q, now, watchHas, retMs)` is a pure predicate; the assertion passes its own `retMs` rather than reaching through `QUAL_RET_MS`, because an assertion pinned by a 30-day constant passes on any fixture shorter than a month and would report coverage it does not have. **A row whose `src` is ABSENT follows the membership rule regardless of age** — absent means *predates the field*, the third state, and the branch tests `src === "pool"` precisely so that absent is read as NOT-POOL rather than silently converted to watch. **`QUAL_RET_MS` is a STORAGE bound and its comment says so: seasoning has no expiry and giving it one is a separate ruling** | probe `[R87.2]` |
| R87.3 | **PROVENANCE IS STAMPED CENTRALLY, ON THE CANDIDATE, AND AN UNMARKED CANDIDATE SAYS SO.** `planCandidates` is the one place that decides `src`; `markSrc` is the only writer. A candidate arriving without a stamp leaves its row's `src` **absent** and raises the standing warn — never filed as `"watch"`, because a wrong stamp is worse than no stamp when the retention rule branches on it. This is the regime-race lesson applied before the second entry path exists: four of six paths hardcoded an empty set in an epoch where those four were 96% of the book, and nothing said so for a whole epoch | probe `[R87.3]` |
| R87.4 | **THE CAP IS A BELT, IT EVICTS POOL ROWS ONLY, AND IT IS NEVER SILENT.** `QUAL_ROW_CAP` bounds the pool population least-recently-seen first; a hand-curated row is never evicted because the machine-fed one filled the store. Eviction costs an item its streak, so it is a **plan-behaviour event, not housekeeping**: it stamps `DB.qualEvict` (running total, last count, when) and raises `qualEvictNote` on the standing warn channel — the declared-truncation pattern, with the note extracted as a term so it is assertable without driving the store to 3,000 rows | probe `[R87.4]` |
| R87.5 | **`src` SURVIVES THE IMPORT CARRY IN ALL THREE STATES, AND AN UNRECOGNISED VALUE STAYS ABSENT.** `validateImport` enumerates qual row fields explicitly and silently drops anything else (the R68.9 shape), so a partition field that dies on restore would make *absent* lie — worse than having no field, because this one decides retention. Only the three ruled values carry; absent stays absent; a hand-edited file cannot smuggle a fourth regime | probe `[R87.5]` |
| R87.6 | **THE GRANDFATHERED REGIME IS IDENTIFIED BY FINGERPRINT AND CLOSES ITSELF.** Rows written before `src` existed cannot be told apart by a field, so `qualGrandfatheredShape` names the one the Aug 10 migration left: `n === 3` with a span of **exactly** two hours, which no real pass can preserve because the first counted pass moves `lastAt` to now. The census renders **only while that population is non-empty** — a regime that closes on its own does not earn a permanent zero on the surface — and states, beside it, how many rows predate the field but have been through a real evaluation | probe `[R87.6]` |
| **§89 The pool switch — built, flagged OFF (user ruling, Aug 18 2026)** | | |
| R89.1 | **`CUTOVER_POOL` IS FALSE AND PINNED.** The one deployment-class change in the migration, written so the cutover ruling is a flag flip plus verification rather than a build. The pin goes red the moment the flag flips, forcing the stanza to account for it — the staging practice's fourth rule, sixth use. **Both paths assert:** the watchlist path is bit-identical to today's while the flag is off, and the pool path is exercised by driving the flag under a fixture, so the branch that will carry the money is not first executed on the day it carries it | probe `[R89.1]` |
| R89.2 | **THE POOL SYNTHESISES THE MINIMUM WATCH ROW, AND ITS ABSENCES ARE THE CORRECT READING.** `cutoverPoolRows` returns `{ id }` and nothing else, so every operator overlay `candidateFor` reads off `w` — manual qty, `invTarget`, tier override, the tested pair, the admitting scanner's tier — is absent. That is the truth about a pool item, not a gap: inventing defaults would be the instrument's verdict wearing the operator's clothes. **A pinned item keeps its real watch row** and is never synthesised, so the watchlist does not stop existing at cutover — it becomes the pin list | probe `[R89.2]` |
| R89.3 | **THE BLOCK RESTORES `S.scorerCtlPass`.** It drove the control cell to a synthetic three-item list in order to exercise the armed branch and never put it back, so **every block after it ran against a fabricated pool** — the M152 leak this project cites, on the one session field the cutover switch reads. The leak had no consequence only because §92 happens not to read that field, which is an ordering dependency rather than a design | probe `[R89.3]` |
| **§90 The ledger regime stamps — the cutover's one true prerequisite (user ruling, Aug 18 2026)** | | |
| R90.1 | **EVERY `gateLog` ROW CARRIES THE CANDIDATE'S OWN PROVENANCE.** `gateLogRow` is the single writer for all four push sites (bench, funded, seasoning, die-off), so the stamp cannot be attached by three paths and forgotten by the fourth. The grain is the ITEM because the row is about one item — the day then decomposes inside itself rather than only between days. **A candidate with no provenance leaves `src` ABSENT**, pre-stamp, never defaulted to a population, exactly as `v`'s three states work beside it | probe `[R90.1]` |
| R90.2 | **EVERY `deployLog` ROW CARRIES THE POOL REGIME IN FORCE AND THE CANDIDATE COUNT.** `poolRegime` is `watch` before the flip and `mixed` after — **`mixed` is the honest value, because a post-flip hour aggregates pinned watch rows and pool items and a per-candidate stamp would be a category error at this grain**. `n` is a SECOND field for a SECOND concern (one flag, one meaning): `poolRegime` says which regime, `n` says how many candidates it scored. `pass` without `n` is the readiness sweep's §2b finding — a count whose denominator was never stored, so a bigger pool and a better day are indistinguishable in the series | probe `[R90.2]` |
| R90.3 | **BOTH STAMPS SURVIVE THE IMPORT CARRY IN ALL THREE STATES.** Recognised values carry; absent stays absent; an unrecognised value is dropped rather than smuggled, so a hand-edited file cannot introduce a fourth population. **This is the whole point of stamping before the flip rather than with it:** a row written unstamped can never be classified afterwards, and a stamp that died on restore would put pre-stamp rows and restored rows into the same indistinguishable state | probe `[R90.3]` |
| **§91 The three vacuous restraints, made honest (user ruling, Aug 18 2026 — behaviour on the money path, ruled before it was built)** | | |
| R91.1 | **MOMENTUM RETURNS `null` ON TOO FEW POINTS, AND THE HONEST READER IS THE ONLY READER.** It returned **`"flat"`** — a definite, PASSING verdict manufactured from an empty input — so the chain's `moState == null ? unknown : (=== "knife" ? fail : pass)` received a pass, and the falling-knife defense silently stopped defending on every item without a spark series. The instrument's stats builder read the identical absence and reported null: **two readers of the same missing input, one honest.** `momentumState(pts, buy)` is now the single term and both callers route through it — **asserted BEHAVIOURALLY at every state boundary, not as wiring** (corrected Aug 19 2026: the source-substring form was deleted as "a grep, not an assertion", and this row went on citing it). The pair agrees at the knife line from both sides, the chase line from both sides, the direction sign at a fixed q, the flat-range guard at exactly its threshold, and the five-point minimum — a second interpreter carrying a shifted constant agrees on one fixture and cannot survive all eight; `chartPts(id)` is the one line the chart wiring changes. **This does not newly bench anything** — unknown is not failing — and says so: the restraint returns when the series does, and what changes today is that the absence stops wearing a passing verdict | probe `[R91.1]` |
| R91.2 | **DRIFT CARRIES THREE STATES ON `drifty`.** `sitRisk` needs 24 hourly points; with none, `stabilityWeight` returned `drifty: false`, so an item nothing had looked at was indistinguishable from one measured and found steady — and the drift bench, which exists **precisely for the case where you are away**, could never fire on an item without sparks. Now: `true` measured-and-drifty, `false` measured-and-steady, **`null` unknown**. `chk(stw.drifty && …)` is unchanged and still falsy on null, so nothing is newly benched; the value becomes readable so a surface can render the restraint as INERT rather than as satisfied | probe `[R91.2]` |
| R91.3 | **THE 5m STREAK REPORTS NOT-COUNTED RATHER THAN ZERO.** `S.vol5Low` is written by a loop over `DB.watch`, so for a non-member the streak is not zero, it is **not running** — and `\|\| 0` collapsed the two, after which the label stated *"0/N consecutive refreshes — not binding yet"*, claiming a counter that does not exist. That is never-fed rendered as a reading of zero, **inside a bench reason**. `streak` now carries a number (counted) or `null` (not counted) so a reader can test rather than parse copy, and the label says which. **Proposed and NOT built: feeding the streak universe-wide from the archive** — cheap in compute, but it pulls `DB.dieOffLog` into the pool population, which is a persisted store needing its own bound and partition answer, so it belongs with the chart wiring where the archive read already exists | probe `[R91.3]` |
| **§92 The post-cutover plan surface — display-only, built ahead of the flip (user ruling, Aug 18 2026 — step A)** | | |
| R92.1 | **FOUR NO-HISTORY STATES, AND AN OBSERVED DENOMINATOR.** `poolPersistence(id)` returns **not-scored** (empty slot WITH a reason — *not applicable* is a different claim from zero), **first-cycle**, **accruing**, **settled**, plus **era-closed** when the row's `ch`/`v` stamp no longer matches. The denominator is **observed cycles since the item entered the pool**, computed against the control cell's cycle count at first pass (`c0`), never the cell's whole life: an item that entered an hour ago has about twelve cycles available, and rendering `1 of 1024` would be false in exactly the way the observed-time rule forbids | probe `[R92.1]` |
| R92.2 | **THE BADGE RENDERS A PAIR, NEVER A BARE PERCENTAGE**, and a percentage may appear only beside it — `93 of 108` and `93%` are different claims and only the first is rulable. Slot A is persistence (a property of the ITEM, the trader's question); slot B is pump exposure, which is the caution badge already on the line — a second pump mark would be the duplicate the gloss-nothing-that-delivers-its-own-intel restraint forbids. **Breadth stays behind the drill** because it is a property of the CONFIG GRID, the instrument-tuner's question, and its denominator is about to change. The six-gate era rides the tooltip and decomposes rather than pooling | probe `[R92.2]` |
| R92.3 | **TWO GROUPS, NEVER INTERLEAVED.** Tenured sorts by `score` as today; Pool sorts by the **unweighted core** `eMargin × horizonUnits`, and the header says the history weights are **NOT APPLIED, and the DATA half is computed separately** (corrected Aug 19 2026, adversarial pass): the header separates its two claims (**corrected Aug 19 2026, adversarial pass**): *not applied* is a fact about the SORT and stays in the constant; *not fed* is a fact about the DATA, is computed per weight by `planPoolFedLine`, and the constant now FORBIDS the string. Pinning both halves in one constant was an anti-tripwire — it holds the data half green through the transition that makes it false. The data half is asserted on a MIXED pool, because a single-row fixture cannot separate a union from a universal Four of the score's six terms are operator-history terms defaulting to a neutral 1.0, so one ranked list places every pool item mid-field **not because the evidence says middling but because there is no evidence** — the never-fed-aggregate rule inside a sort comparator. **This is a RENDER grouping only**: `buildPlan` decides what is funded and at what size and neither moves, because re-ordering the allocator would be deployment-class | probe `[R92.3]` |
| R92.4 | **`#planSub` READS "N PASS THE GATES, OF M SCORED" AND NAMES ITS POOL.** It read `pass.length + " of " + DB.watch.length`, which after the flip is a pool numerator over a pin-list denominator and renders an arithmetically impossible fraction — *"9 of 3 pass the gates"*, the kind of defect that makes a reader distrust everything else on the page. Fixed at the SOURCE: `M` is the candidate count `deployLog.n` now stores. The funnel tile's `pass` names its pool for the same reason; `funded` (slot-capped) and `% deployed` (capital-denominated) do not need to and do not get one | probe `[R92.4]` |
| R92.5 | **INERT RESTRAINTS ARE STATED ONCE, OVER THE POPULATION, AS THE ARCHIVE'S STATE.** **Rewritten Aug 19 2026 (adversarial pass).** Each chart-derived reading is COUNTED over the rows that carry it (`momentum on 1 of 3`), never unioned into a claim about the population; a candidate with no live price is reported as a dead FEED in its own sentence rather than credited with absent readings it never had; and the 5m die-off streak has its own sentence naming its own trigger, because it returns on a RULING and not when the archive matures — the old frame said *"restraints the archive cannot feed yet … they return when the archive does"* beside *"at 7 of 7 observed days"*, which is the self-contradiction the first repair was written to end, surviving one clause deeper. Historically: a pool item had momentum unknown, drift unknown, its 5m streak not counted and its chart gates still loading — **four unknowns stamped on every row would read as four problems with the item, which is the opposite of true.** So the bench block carries one line naming the archive's coverage, and it renders **nothing** when no pool item is present, because a standing line about an empty population is the never-fed defect in copy. **An unknown restraint must never read as a passed one** and the line says so in those words | probe `[R92.5]` |
| R92.6 | **THE MUST-LAND-WITH COPY SET, ASSERTED BY PRESENCE AND ABSENCE.** `gov-propose` states the **property** — *no capital is committed without an explicit press* — with the watchlist demoted to a dated example (M156: the surface-not-property defect found in the tool's own statement of its constitutional rule); `+ watch` becomes `+ pin`; the sub-tab reads **Plan & Pins** and the old string is asserted ABSENT, because a rename checked only by presence leaves the old form standing somewhere; the family and caution bench copy each gain their one word of context for a pool item | probe `[R92.6]` |
| **§93 Operator state re-keyed from rows to items — built, flagged OFF (user ruling, Aug 18 2026 — step B, ruled before it was built)** | | |
| R93.1 | **`ITEM_OPS` IS FALSE AND PINNED, AND OFF MEANS THE STORE IS NOT READ.** `opsOf` consults `DB.itemOps` only when the flag is armed, so today's behaviour is bit-identical and the store can be migrated and carried without changing anything the plan proposes. Two flags rather than one, deliberately: `CUTOVER_POOL` and `ITEM_OPS` change different things, and the re-key can be proven on today's watchlist — where every value has a known correct answer — **before** the pool exists to test it against | probe `[R93.1]` |
| R93.2 | **ONE STORE, ONE READER, STORE-FIRST-ROW-SECOND.** Four fields migrate (tested pair, manual qty, tier override, t2 graduation); **scout provenance dies with the scanner** because it records how an item reached the watchlist and after cutover that describes nothing; **`invTarget` stays out**, already reserved by its own ruling. `opsOf` is the single reader, which is what makes the copy safe: **a value can never be applied from both places**, and an interrupted migration still answers from the row | probe `[R93.2]` |
| R93.3 | **THE `bands` PARTITION — THE QUESTION NOBODY WOULD HAVE THOUGHT TO ASK.** A tier override made under one band pair may be a no-op, or may mean something different, under another — and the T2 ceiling is a live question. So the row records **which world it was made in**, in three states: **absent** = predates the field → APPLIED (which is exactly today's behaviour, and is what makes the migration behaviour-neutral by construction); **same pair** → applied; **different pair** → **NOT APPLIED, and the surface says why**. Withholding is the conservative direction: an override WIDENS what the allocator may fund, so declining to apply one narrows, and narrowing may auto-arm while widening may not | probe `[R93.3]` |
| R93.4 | **THE MIGRATION COPIES, IS IDEMPOTENT, AND LEAVES THE ORIGINALS.** A one-way move would be unrollbackable and would put the whole book behind one boot. **What happens to the watch-row originals: they stay, untouched, until the admission machinery retires with the cutover** — the duplication is visible, one-era, and retires with the rows that hold it rather than needing its own deletion pass. A second run overwrites nothing, so an operator change made after the copy survives a re-run. Migrated rows carry **`src: "migrated"` and NO `bands`**: a migrated `tAt` is genuinely old and must be distinguishable from a fresh one, and inventing a band pair we do not know would be worse than the absence | probe `[R93.4]` |
| R93.5 | **`opsSet` IS THE ONE WRITER AND STAMPS AT THE PRESS.** Live bands are recorded at the moment of the press, never inferred later; the migrated mark is cleared, because an operator-set value is not a copied one. **The press IS the provenance** — there is no automated writer and there must never be one; if one ever appears, rows gain `by` before it writes its first row. Retention is staleness on `setAt` against a **storage** bound plus a declared cap, warned when it drops anything: **an override that expired silently would be a restraint lifting with no press**, which is the constitutional line | probe `[R93.5]` |
| R93.6 | **THE CARRY PRESERVES EVERY FIELD AND ITS STATES, AND `bands` CARRIES ONLY AS A WELL-FORMED PAIR.** A malformed pair reads as absent, and absent MEANS applied — so a hand-edited file must not be able to turn a withheld override into an applied one by mangling its stamp. Unrecognised `src` values are dropped; a row with no recognised field is not created | probe `[R93.6]` |
| R93.7 | **THE CONTROLS RENDER ON THE ITEM — the point of the re-key.** A pool item has no watch row, so the tier override and the tested pair live on its **plan line**, which every pool item has. They exist for **pool-provenance items only**: a tenured item still has its watch row, and a second control writing the same field from a second place is the two-owners defect. **While `ITEM_OPS` is OFF they render nothing**, asserted by absence — a press that writes a store nothing reads is worse than an absent control, because it looks like it worked. **The flag is a PARAMETER of `poolControlsHTML`, not a const it reads**: the third application of the extraction pattern in this build, after two seeds failed to bite on flag-gated properties the probe had re-derived. The tier control states the bands contract where it is pressed | probe `[R93.7]` |
| **§94 Chart wiring — corrected scope, behind its coverage check (user ruling, Aug 18 2026 — step C)** | | |
| R94.1 | **THE COVERAGE GATE IS A TERM WITH THREE STATES.** `chartWireState(h1)` returns **ready** at 7 OBSERVED days, **accruing** below it, and **could-not-check** when the archive has not been read — never "absent", which would read as a clean zero. The days are observed coverage, never wall days. Taking the coverage pair as an argument is what lets the assertion inject a seven-day archive instead of waiting three days for one | probe `[R94.1]` |
| R94.2 | **ONE SERIES FEEDS FOUR CONSUMERS — `tr`, `vt`, MOMENTUM AND DRIFT — AND THAT IS THE CORRECTION.** Wiring only the two gates `marketGateEval` names would have made things worse: the *"chart still loading"* bench currently **masks** two vacuous restraints (momentum and drift both read the per-item spark object and both pass on an empty one), so removing the mask without feeding them turns two restraints live-and-off. `chartPts(id)` is the single line the wiring changed and every consumer inherits it at once. **While the gate is not ready every reader returns exactly what it returned before** — asserted with a populated cache sitting behind a not-ready gate, so inertness is proven rather than assumed | probe `[R94.2]` |
| R94.3 | **THE UNIVERSE-WIDE 5m STREAK IS BEHIND ITS OWN FLAG, `VOL5_UNIVERSE = false`, PINNED.** `S.vol5Low` was written by a loop over `DB.watch`, so a pool item's streak was not zero but **not running**. This makes the counter count — and it is deployment-class, because a die-off bench that can newly fire on a pool item changes what the allocator may fund. **An id that leaves the population loses its streak**, or a shrinking population would leave counters nothing updates, which is the same not-running defect one level up. It ships with the chart wiring by ruling — not for a data dependency (5m data is already universe-wide) but because `DB.dieOffLog`'s bound and partition belong in the same commit as the population change that makes them necessary | probe `[R94.3]` |
| R94.4 | **THE DIE-OFF LEDGER STAMPS ITS POPULATION AND IS BOUNDED.** `pop` records whether the streak that opened an episode was counted over the watchlist or the universe: an episode opened while the counter ran over 43 items and one opened while it ran over ~4,500 are **not the same evidence**, and summing them across the flip pools populations that answer different questions. Absent reads as **pre-stamp**, never as either; an unrecognised value is dropped on carry. `DIEOFF_LOG_CAP` bounds a ledger whose population is about to stop being a 43-item watchlist | probe `[R94.4]` |
| **§95 The money-path guards that had no assertion at all (user ruling, Aug 19 2026 — from the adversarial pass)** | | |
| R95.1 | **THE BLACKLIST BENCH IS ASSERTED.** `chk(isBlk(w.id), "blacklist", …)` in `candidateFor` is the only thing between a blacklisted pin and the allocator, and **it had no assertion of any kind** — deleting it left the suite green while a vetoed item funded. Asserted at the CHAIN, because the chain is what decides funding, and by the discriminating pair: the same item benches on `blacklist` with the item added to `DB.blacklist` and does not without it | probe `[R95.1]` |
| R95.2 | **THE PROVEN-LOSER BENCH IS ASSERTED.** Three net-negative completed flips bench the item — measurement outranking the live spread — and nothing exercised it | probe `[R95.2]` |
| R95.3 | **AND ITS RE-TEST RELEASE, BOTH HALVES.** A FRESH one-unit margin test recorded after the last loss releases the bench; a STALE one does not. The discriminating pair, and the only assertion anywhere that exercises `TESTED_TTL_MS` in the bench path | probe `[R95.3]` |
| R95.4 | **THE TESTED-PRICE TTL IS ASSERTED AT `calc()`.** A fresh tested pair overrides live prices and an expired one reverts to them. The adversarial pass found that **deleting the 16h expiry entirely left the suite green** — every fixture that set a tested pair set a fresh one, so the expiry branch was never reached | probe `[R95.4]` |
| R95.5 | **§95's FIXTURE CLEANS BOTH ENDS AND ITS TEARDOWN HAS A DETECTOR**, like its two neighbours. The block captured `S.latest[9501]` and `S.hour[9501]` and then tore down with a key-delete two lines later, so the capture was **dead code and the author's stated intent was contradicted by the next statement** — and `S.spark` was not captured at all. All four item caches are now restored SET-OR-DELETE, so a pre-existing row cannot be silently removed, and the restore is asserted rather than assumed | probe `[R95.5]` |
| R95.6 | **`TESTED_TTL_MS` IS 16 HOURS AND THE VALUE IS PINNED, not only its branch.** R95.3 and R95.4 exercise the EXPIRY and cannot see the magnitude: both build their stale timestamp as `Date.now() - TESTED_TTL_MS - 60e3`, so the comparison is true by construction at any positive value — setting the TTL to 90 days left both green. That is deployment-class in the constitution own sense, because a longer TTL widens what the allocator may fund on TWO independent paths at once: a stale tested pair keeps overriding live prices inside `calc()`, so sizing and every downstream gate read a snapshot instead of the market, and a proven-loser bench stays released longer. Pinned in the `[R89.1]` / `[R94.3]` shape | probe `[R95.6]` |
| **§96 One series, one resolver, every consumer — the acceptance criterion (user ruling, Aug 19 2026)** | | |
| R96.1 | **THE CHART-READY TRANSITION CANNOT UNMASK AN INERT RESTRAINT.** Given an ARCHIVE series and NO spark — exactly the post-transition state of a pool item — **every** chart-derived reading on the live chain is FED: trend, volume trend, momentum and drift. The Aug 18 wiring fed three fields on the INSTRUMENT surface while the live chain (where the *"chart still loading"* mask actually sits) kept reading the per-item spark, and drift was never wired at all — so lifting the mask would have left two restraints live-and-off. Nothing could see that, because every assertion named `chartReady`/`chartPts`/`chartVols` and none named the chain's consumers. **The discriminating half:** with NEITHER source every one of them reads UNKNOWN, so a consumer manufacturing a passing value from no data cannot satisfy the first half | probe `[R96.1]` |
| R96.2 | **`itemSeries` IS THE ONE RESOLVER AND NAMES ITS SOURCE.** The item's own `/timeseries` spark wins when it exists; the universe archive answers otherwise; neither is silently mixed with the other, and `src` says which answered. This is what makes R96.1 structural rather than a matter of remembering to wire the fourth call site — a new consumer inherits the resolved series by construction | probe `[R96.2]` |
| **§97 Sibling admission's only volatility restraint (user ruling, Aug 19 2026 — a fixed defect with no guard)** | | |
| R97.1 | **`calm` IS ASSERTED, THREE WAYS.** It is the one volatility restraint at the ONE admission path that does not require a full-gate pass, and the adversarial pass found it both structurally inert AND unasserted — the production defect was fixed the same day and seed S89 confirmed the guard still had nothing watching it, which is the worst state on the list. The fixture isolates it: the candidate's name shares no family key and no 4+ character token with the seed, so `famHit` is false and `microHit` is the only route in, with every other conjunct satisfied by construction. **A STEADY candidate is admitted, a DRIFTY one refused, and an UNKNOWN series does NOT satisfy it** — admitting on unknown widens what reaches the watchlist, so the conservative reading is the ruled one. The steady case is load-bearing: without it the other two could both be refusals for an unrelated reason and read as a working restraint | probe `[R97.1]` |
| R97.2 | **THE FIXTURE CLEANS BOTH ENDS, AND ITS PARTITION QUESTION IS ANSWERED.** It writes `DB.watch`, `DB.sibBorn` (**persisted** — "one sibling life per item, for ever"), `DB.scoutLog`, `DB.sibStats` and the `S.*` item caches. Every one is snapshotted and restored by value; `S.items` is spliced back to its original LENGTH rather than filtered, because a filter would silently drop a legitimately-added item if two fixtures ever collided. The probe ids sit in a range no other block uses. A fixture that leaks reads as production data on the next block — the M152 discipline | probe `[R97.2]` |
| **§98 One allocator fixture, five unasserted caps (user ruling, Aug 19 2026 — from the adversarial pass)** | | |
| R98.1 | **THE FAMILY RULE IS ASSERTED.** One funded position per item family, the loser benched by name. **Every prior allocator fixture named its items "Probe alpha / beta / gamma", which `familyKey` maps to distinct strings — so `bestByFam` never held two members and the rule was a pass-through in every plan the suite ever built.** The fixture uses a real collision (`Adamant arrow` / `Adamant arrowtips`, which `familyKey` normalises alike) rather than two names that merely look related | probe `[R98.1]` |
| R98.2 | **THE WORTH-A-SLOT FLOOR IS EXERCISED IN THE DIRECTION THAT MATTERS.** Every allocator fixture set `DB.minExpectGp = 0` in the belief that this disabled the floor; it does, which is precisely why nothing ever tested that the floor REMOVES a line. Asserted with the floor above what the lines earn, and the same plan re-run without it | probe `[R98.2]` |
| R98.3 | **`committed()` COUNTS AN OPEN POSITION'S CAPITAL.** Every cluster and budget fixture ran with `DB.positions` empty, so the positions limb contributed 0 and deleting it entirely left the assertions green — the exposure figure that gates cluster caps was tested only on its quote-leg half | probe `[R98.3]` |
| R98.4 | **`clusterExposure` COUNTS INVENTORY LOTS.** Same shape, same cause: every cluster fixture ran with `DB.invLots` empty. **A held lot is committed capital, and a cap that ignores it is a cap on half the book** | probe `[R98.4]` |
| R98.5 | **THE SHARED FIXTURE CLEANS BOTH ENDS AND ANSWERS ITS PARTITION QUESTION.** One fixture serves all four assertions rather than four fixtures — four would be four chances to differ from the plan the allocator actually runs. It writes persisted state (`DB.qual` rows, `DB.clusters`, budgets, capital), every store is restored **by value** rather than by key-delete so a pre-existing row cannot be half-removed, `S.items` is spliced back to its original LENGTH, and the ids sit in a 98xx range no other block uses | probe `[R98.5]` |
| R98.6 | **THE TIER-2 CONCURRENCY CAP IS ASSERTED.** Three live T2 positions hold a fourth T2 candidate, with the cap named in the hold reason. Nothing exercised it: **every allocator fixture in the suite ran with `DB.positions` empty**, so `t2Live` was always 0 and `t2Live >= T2_MAX_CONCURRENT` could never bind — the same shape as R98.3 and R98.4, on the ramp rule's other half. The discriminating arm is the same plan with no positions, which funds | probe `[R98.6]` |
| **§99 The blacklist exception guards and per-consumer readiness (re-pass findings, Aug 19 2026)** | | |
| R99.1 | **AN EXCEPTION CAN NEVER DELETE A BLACKLIST BENCH — and this shipped UNASSERTED.** The ruling ("a confirm()-gated full-state restore IS an automated path for blacklist purposes") was implemented in `buildPlan`'s exception routing in the same sitting that wrote four other assertion sections, and **the constitutional boundary itself got none**. Two guards, both asserted: an exception naming `"blacklist"` is refused, **and** any exception on a blacklisted item is refused whatever gate it names — routing on the gate name alone would let an edited file re-point a legitimately-granted exception at the veto | probe `[R99.1]` |
| R99.2 | **THE IMPORTED GATE NAME IS ENUMERATED.** It was free-form while the `status` beside it was checked against a known set, so a file could carry any gate name — including `"blacklist"` — into a store whose own comment calls its rows RULINGS. Asserted three ways: the blacklist name dropped, a nonexistent gate dropped, a real gate kept | probe `[R99.2]` |
| R99.3 | **READINESS IS PER CONSUMER, AND THE MASK KEYS ON THE WEAKEST-FED ONE.** The four chart consumers have four different minima — trend 3, momentum 5, drift 24, volume-trend 48 — while the *"chart still loading"* mask fired on `tr == null` alone, the shallowest. A 20-point archive series therefore fed trend and momentum, **lifted the mask, and left drift unknown with nothing benching it** — the exact failure the source repair claimed to close, surviving one level up, because changing the SOURCE does not close a hole that was never about the source. The acceptance fixture could not see it: 30 points and 60 volumes clear all four thresholds at once. **NaN price points are not counted as fed** — the archive pushes NaN for an hour with no print on either side, so an unfiltered length would call a starved series ready. Benching while any consumer is unfed is restraint-TIGHTENING, the direction that may auto-arm | probe `[R99.3]` |
| **§100 The hour weight: routed through the resolver, third-stated, and held out of the ranking (user ruling, Aug 19 2026 — the re-pass's third money-path finding)** | | |
| R100.1 | **AN UNFED WEIGHT IS A THIRD STATE, NOT AN AVERAGE.** `hourWeight` returned `{ w: 1 }` both when the hour-of-day volume profile was MISSING and when it was measured and flat, and that 1.0 multiplied the plan score inside a score-sorted ranking. Measured on the live book the day this shipped — 43 watch items, every one fed, `w` running 0.765 to 1.247 with a median of 1.049 — **a 1.0 from absence lands at the p25 of the fed distribution**: not an edge case that sorts harmlessly to one end but squarely mid-field, on no evidence. The reading now carries `fed`, and the discriminating pair is two candidates whose `w` is the identical number 1.0 — one from a UNIFORM 24-hour profile, one from no profile at all. **Asserted on `candidateFor`, not on `hourWeight`**: a correct term with no assertion on the branch that reads it is what the two preceding passes each shipped (MISTAKES M157/M158/M159) | probe `[R100.1]` |
| R100.2 | **THE HOLD-OUT: A SERIES-UNFED CANDIDATE IS NOT PLACED IN THE SCORE-RANKED ORDER.** It is held below the ranked block and sorted on the same unweighted core the pool group uses. **Nothing about funding or sizing moves** — `buildPlan` has already decided what is funded and at what size; this is the render grouping, exactly as the two-group split is, and only the item's PLACE in a list changes. Asserted at `planGroups`, on two candidates differing in nothing but a 24-slot volume profile. **Ordering change measured on live data: 0 of 43 watch items are held today**, because both series-derived weights are fed for the whole book — the restraint is entirely for the post-transition population and for a `/timeseries` breaker outage | probe `[R100.2]` |
| R100.3 | **AND ITS SCOPE, WHICH IS THE DISCRIMINATING NEGATIVE: a LOG-derived absence never holds an item out.** The two families of history weight fail for different reasons and the difference is the whole rule. SERIES-derived (hour, stability) are absent only when the DATA PIPELINE has not fed them — a failed fetch, a tripped breaker, an accruing archive — which is a TOOL STATE, temporary and nothing to do with the item, and ranking on it is ranking on nothing. LOG-derived (reliability, wins) are absent when YOU HAVE NOT TRADED THE ITEM, which is a FACT ABOUT THE BOOK and the normal state of every new candidate. The ranked item in the fixture has zero flips and an unfed reliability weight and ranks anyway. **Inverting the scope would have held 40 of the 43 live watch items out of their own ranking**, and a hold-out that empties the ranking is a deletion of the ranking. `wins` is in neither set: it ranges [1.0, 1.5] and absence gives its FLOOR rather than its middle, which is the conservative reading `wins` is in neither set: it ranges [1.0, 1.5] and absence gives its FLOOR rather than its middle, which is the conservative reading rather than a fabricated average. **`logFed` was removed Aug 19 2026** as a write-only field with no production reader — the distinction it named is carried by `unfed`, which every reader already uses | probe `[R100.3]` |
| R100.4 | **ERA FACT, PINNED: the T0 archive does not feed an hour-of-day profile.** With the archive READY and no spark, the resolver reads `byHourSrc: "none"` and the chain's hour weight is UNFED. The archive's bucket keys are hour timestamps, so building a profile out of them is buildable and is a separate deployment-class ruling — it would feed a weight for an entire population whose ordering was ruled on the assumption that it was not fed. **That wiring must turn this assertion red before it lands**, which is the same shape as the `marketStatsFor().tr === null` tripwire inside `[R76.9]` | probe `[R100.4]` |
| R100.5 | **THE RESOLVER RESOLVES TWO READINGS AND NAMES BOTH SOURCES.** `src` answers for the price/volume series; the hour profile is a different reading with a different availability, and the archive branch was **silently DISCARDING a spark's profile** while returning `src: "archive"` — one field answering for two sources, which is the one-flag-one-meaning defect. `byHourSrc` is its own answer, and the resolver's `byHour` field — which no caller read, a standing scan-2(a) orphan sitting exactly where the missing wiring went — acquires its reader rather than being deleted. **STATED PLAINLY (Aug 19 2026, adversarial pass): routing `hourWeight` through the resolver is a behavioural IDENTITY today.** `itemSeries` derives `byHour` from `sp` on every branch before choosing one and never returns null, so the routed and unrouted forms are the same function of the same array and **no assertion can distinguish them** until something other than the spark feeds a profile. The routing's value is structural and prospective; this row pins the resolver's SHAPE, and the discard it fixed was real only inside a return value nothing read | probe `[R100.5]` |
| R100.6 | **THE HELD BLOCK STATES ITS REASON ONCE OVER THE POPULATION, NAMES THE WEIGHT ACTUALLY UNFED, AND IS ABSENT WHEN NOTHING IS HELD.** Said once rather than per row, because four unknowns stamped on a line read as four problems with the item — the same ruling that produced `planInertLine`. The header names only the weights unfed across the block rather than reciting all of them, since a line naming a restraint the block does not carry is the same defect as one hiding a restraint it does. **Asserted by absence as well as presence:** a plan holding nothing renders no such line, because a standing claim that something is unfed when nothing is would be the never-fed defect written the other way round | probe `[R100.6]` |
| R100.7 | **RELIABILITY DISCLOSES TOTAL ABSENCE, NOT ONLY PARTIAL.** It rendered *"thin history (2/4 trips in 30d) — reliability weight off"* for one to three trips and **said nothing at all for zero** — so the row with SOME evidence explained its neutral weight while the row with NONE rendered the same 1.0 silently, which is the inverse of what the operator needs. **Measured on the live book: 30 of 43 watch items have never been traded**, so the silent case was the common one. Said ONCE where the group already covers it: the pool header states it over that population, so a pool row does not repeat it, while a tenured row's absence is item-specific and earns its line — the inert-restraint ruling applied to a per-item weight. **This is the DISPLAY half of the series/log ruling; the ranking half (normalising over fed weights instead of injecting a neutral) is deployment-class, measured in `audits/RELWEIGHT-2026-08-19-normalisation.md`, and not built** | probe `[R100.7]` |
| **§101 The correctness queue from the second adversarial pass, asserted at its consumers (Aug 19 2026)** | | |
| R101.1 | **THE `no history` BENCH FIRES ONLY WHEN NEITHER SOURCE HAS ANYTHING.** It read the SPARK's `noData` flag, which records only that this item's own `/timeseries` returned zero rows — while `itemSeries` correctly falls through such a spark to the archive. So `tr`, `vt`, momentum and drift could all be real readings off 168 archive buckets while this line benched the row with *"no price history published for this item"*, false of the data the same chain had just consumed, and capable of being the HEADLINE reason through `fails[0]`. **Discriminating pair:** empty spark + populated archive does NOT bench; the same spark with the archive cold DOES. Direction is a loosening — it removes a bench that fired on a false premise | probe `[R101.1]` |
| R101.2 | **THE WATCH ROW RAISES ITS RISK CHIPS OFF THE SAME RESOLVED SERIES THAT BENCHED THE ROW.** Five display readers still read `sp.pts` directly, so an archive-fed item showed no FALLING chip, a dimmed … that reads as still-loading, and a trend column sorting to `-Infinity`, while `candidateFor` benched that very row with *"falling chart"* off the archive. A component reporting nothing where it HAS something, on the surface the operator rules from, about the reading that caused the bench. `hourVerdict`'s readers are deliberately NOT routed: `byHour` has no archive equivalent in this era, so their — is honest. Matched inside the one item's flag list rather than against the page, per the ninth face | probe `[R101.2]` |
| R101.3 | **THE SCOUT'S EVICTION GUARD AND THE GATE CHAIN AGREE ABOUT WHETHER AN ITEM WAS JUDGED.** `chartedNow()` defined *charted* as SPARK PRESENCE while the chain had moved to the resolver, so an item fully judged off the archive was absent from the set and the guard declined to act on the stated grounds that it *"could not be judged this pass"* — a precondition that had become false for exactly the population it protected. **The predicate was EXTRACTED to be asserted at all** (`scoutEvictable(w, ctx)`): it lived inside an async, timer-driven filter lambda, so the only reachable subject was the term rather than the branch — M157's remedy rather than its repeat. Ownership and clock clauses asserted alongside, so the repair cannot pass by evicting everything | probe `[R101.3]` |
| R101.4 | **A CLEARED ITEM-STORE OVERRIDE DOES NOT FALL BACK TO THE WATCH ROW — the third state, and the partition answer.** `opsPick`'s `!= null` could not express CLEARED: writing `null` to remove an override fell through to the row and resurrected the value the operator had just deleted, which is the opposite of clearing and **WIDENS what the allocator may fund**. Three states: key absent asks the row, key present and null answers null, key present with a value answers itself. No stored row changes meaning — `opsSet` never wrote a null. **Asserted through `opsOf`'s ARMED branch**, which is why `opsOf` gained a defaulted `armed` parameter: the const made the branch that will carry this unreachable, and asserting `opsPick` instead is the shape M157 records | probe `[R101.4]` |
| R101.5 | **THE GRANT WRITER ENUMERATES THE GATE NAME EXACTLY AS THE IMPORT WRITER DOES.** `benchedBy` is a free-form string on a paper trip, and `exceptionEvidence` took whichever name appeared most often — so a stale or hand-edited book could nominate any gate at all, including `"blacklist"`, into a lane whose grants DELETE a bench. Two writers, two definitions of a valid gate, one of them enforced. Same test and same list at both, which is the dead-safeguard rule's *pick one owner* applied to a promise that had two | probe `[R101.5]` |
| R101.6 | **EVERY GATE NAME THE CHAIN CAN PUSH IS LISTED IN `GATE_CHAIN_ORDER`, AND AN UNLISTED ONE IS SAID OUT LOUD.** The list began as a DISPLAY ordering, where an unlisted name merely sorted last — the funnel still has that tolerant fallback. It has since taken a second job: the import validator's whitelist for an exception's gate, and now the grant writer's, where an unlisted name means the exception is **dropped**. A gate added to the chain without being listed would be silently unexceptable and nothing said so. `chk` records the miss on `S.gateNameOff`; `gateNameOffWarn` renders it as a warning bar on every plan RENDER, in both modes (**corrected Aug 19 2026**: it said "every plan build", and it sat below two early returns, so an operator on the sell plan would never have seen it — the state is written by `chk` on every headless `buildPlan()` and stamped on a render path, which is the state-coupled-to-a-render sub-class, and the row now says which); the suite asserts the set is empty for a real candidate and that the warning appears and clears. Recorded rather than thrown — a chain name is a developer fact, and the right response is a visible warning and a red assertion, not a broken plan for the operator | probe `[R101.6]` |
| **§102 What the fourth adversarial pass found (Aug 19 2026 — zero money-path findings that bite today)** | | |
| R102.1 | **THE LOADING COUNTER IS KEYED ON THE GATE, NOT ON THE SENTENCE — live defect.** `#planSub`'s *"N charts still loading — verdicts will improve"* counted bench rows by matching `/still loading/` against the bench DETAIL. The readiness repair rewrote that detail to name the point counts and the thresholds, and **the substring left the file entirely**, so the count was permanently zero and the plan stopped explaining why nothing passes — on every cold boot, which is exactly when that explanation is the whole message. A counter keyed on rendered copy breaks the next time the copy is improved; the gate's IDENTITY is stable, and `failed` was derived from `fails[0]` anyway | probe `[R102.1]` |
| R102.2 | **THE TWO HISTORY GATES AND THE SCOUT'S EVICTION GUARD ARE ONE TERM.** `historyVerdict` returns `noHist` / `loading` / `judged`, and `judged` is the exact complement of the two benches. `chartedNow` had been answering *"was this judged?"* with its own arithmetic and the two drifted: it called an item judged at **3** finite points while the chain benched it unreadable until **24**, so an item in that window was evictable AND benched on an unknowable verdict — it never refreshed `lastPass` and was removed after 48h for *"no full-gate pass"*, a verdict with no trial behind it, which is what `GATE_UNKNOWABLE` exists to forbid. Asserted at the term AND at the consumer: the scout does not evict a 20-point item | probe `[R102.2]` |
| R102.3 | **A CLEARED TEST DATE DOES NOT RELEASE THE PROVEN-LOSER BENCH.** `provenLoser` read `opsOf(id).tAt || (w && w.tAt) || 0` — but `opsOf` already falls back to the row, through `opsPick`, which was rewritten this session so a CLEARED override returns null instead of resurrecting the row's value. The second `||` read that null as falsy and went to the row anyway, **reinstating the resurrect path one level up, in the one reader that added its own fallback — and on a RESTRAINT**, since a resurrected test date unbenches a proven loser. The limb was redundant for every production caller, so removing it is behaviour-identical today and leaves `opsPick` the single owner of the three states | probe `[R102.3]` |
| R102.4 | **THE ITEM-STORE IMPORT KEEPS THREE STATES AND CHECKS THE RANGE.** `num()` is `Number.isFinite(+v) ? +v : null` and `+null` is 0, so a **CLEARED** override — a present key holding null — restored from an import as the VALUE zero. Zero is not nothing: `itemTier` reads `tierOv: 0` as a live manual override to UNTIERED, and the allocator benches the item with copy claiming a decision the operator had deliberately removed. **The guard for this exact hazard is written out for `gateLog.v` a few lines below**, in as many words, and was not applied to the store that grew a null-bearing third state in the same session. Out-of-range now stays ABSENT rather than becoming a value: `tierOv: 7` reached `pools[7]`, which is undefined, and produced a funded line with a **NaN quantity** that `qty <= 0` cannot catch, because NaN fails every comparison. The watch-row branch has always had a range check; this is the branch that becomes authoritative once the store is armed | probe `[R102.4]` |
| R102.5 | **THE CHART-COVERAGE CONSTANTS ARE PINNED BY VALUE.** `CHART_MIN_DAYS === 7`, `CHART_COV_WINDOW_MS === 8d`, and all four `SERIES_MIN` thresholds. The readiness assertions bounded the day count only loosely, so a silent change to 5 would have **lifted the chart mask two days early and stayed green** — and that mask is the constitution's own fourth cutover prerequisite. The coverage window is read by the gate and by every surface that reports it, so widening it would arm `ready` on staler coverage than the point window can serve. Same argument as R95.6, same shape as `[R89.1]`/`[R94.3]`, applied where it was missing | probe `[R102.5]` |
| R102.6 | **EACH `SERIES_MIN` THRESHOLD IS THE CONSUMER'S OWN MINIMUM, CHECKED AT THE BOUNDARY AND ONE BELOW.** The four numbers live twice — in the mask's constant and inside `trendPct`, `momentumState`, `sitRisk` and `volTrendPct` — and the mask is what keeps all four restraints honest. If a consumer's own minimum rises without the constant, the mask over-reports readiness and that restraint reads unknown **with nothing benching it**, which is the defect the mask was built to close, arriving from the other side. `sitRisk` and `SERIES_MIN` had no assertion anywhere in the suite before this | probe `[R102.6]` |
| **§103 The `num()` null sweep — one helper, three occurrences, and the field with the widest blast radius (Aug 19 2026)** | | |
| R103.1 | **A WATCH ROW'S `qty: null` RESTORES AS NULL — live, money-path, and it predates the cutover work.** `num()` is `Number.isFinite(+v) ? +v : null`, and `+null` is 0, which is finite. Every watch row is created with `qty: null` — scout add, sibling add and manual add all write it explicitly — and null there means SIZE ME AUTOMATICALLY. So a state-backup restore rewrote the entire watchlist as a **manual override of zero**. Asserted with a real 0 and a real 250 beside the null, because a repair that dropped nulls entirely would pass a null-only test and lose the hand-typed zero | probe `[R103.1]` |
| R103.2 | **AND AT THE CONSUMER, which is where it becomes gp.** `opsPick`'s `w[k] != null` is the branch that separates the two readings, and `planQty` is what reads it: restored as zero, `wanted` becomes 0, `qty` becomes 0, and `chk(!(qty > 0), "sizing", …)` benches the item — **with a reason naming working capital or a missing buy limit, neither of which happened**. Which input pins the output is stated at the assertion: `Math.min(wanted, cap)` selects `wanted` in the zero case, so the clamp is present and not binding, and selects `cap` in the null case, which is the reading being asserted different | probe `[R103.2]` |
| R103.3 | **THE ANOMALY SCAN'S OWN LAG SIGNATURE KEEPS ITS NULLS.** `peakToFlagD` and `retracePct` are written null by the scan whenever the peak or the mid is unmeasurable, and `flagLagProfile` filters on `x != null` — so a restored zero enters the median lag as a flag that fired **on** the peak day and the median retrace as a flag that did not retrace. A measurement never made, rendered as a measurement of zero, inside the copy the scan uses to state its own latency | probe `[R103.3]` |
| R103.4 | **`runwayD` AND `rung` KEEP THEIRS, EACH WITH A REAL ZERO BESIDE IT.** `catRunwayD` returns null for a catalyst whose window date will not parse, and the ramp-flag line reads `!= null && > 0`, so a restored zero renders *"inside/past the window"* — a definite claim about timing from a measurement never taken. `rung: null` marks a HAND exit while rung 0 is the FIRST ladder rung, and the exit history renders `rung + 1`, so a restore relabelled every manual sleeve exit as a ladder exit | probe `[R103.4]` |
| R103.5 | **THE SANITIZER IS ITS OWN FIXED POINT.** Running `validateImport` twice equals running it once, on every field that carries a null. This is the class property rather than a field list: the import **writes explicit nulls of its own** — `sourceTier`, `peakToFlagD`, `retracePct`, `runwayD`, `rung`, the coherence reading `c`, the trend array — and the state backup is `JSON.stringify(DB)`, so backup → restore → backup → restore fed each one straight back through the guard that could not see it. A sanitizer that is not a fixed point corrupts a little further on every hop, and **no single-pass assertion can see that** | probe `[R103.5]` |
| R103.6 | **THE THREE KNOWN `num`-SPELLED IDIOMS ARE ENUMERATED AND PINNED — and this row no longer claims the class is closed (corrected Aug 19 2026 by the fifth adversarial pass).** Read over `validateImport`'s own source: no `num(x) >= 0`, no `[0,…].includes(num(x))`, and exactly THREE `num(x) != null`, each deliberate and named — the two `bands` edges, where rejecting a malformed pair would move an override from WITHHELD to APPLIED, and the econ nets, whose else-branch is 0 so there is no distinction to lose. **What it cannot see is a spelling nobody enumerated.** It was built from a LIST of ways to write the coercion, so five guards respelled `nz(x) >= 0` and two bare `+s.tier` coercions passed it while still mapping null to zero — and it reported the class closed the whole time, which is a detector occupying the slot where a real one would have gone. `[R104.8]` is the detector for the PROPERTY; this row is the enumeration of what is known, and keeping both is deliberate | probe `[R103.6]` |
| **§104 What the fifth adversarial pass found, and the sweep rewritten against the property (Aug 19 2026)** | | |
| R104.1 | **A RESTORED POSITION KEEPS THE HORIZON IT WAS PLACED UNDER, AND AN UNSTAMPED LEG STAYS UNSTAMPED — live, money-path, and older than every cutover component.** `hzH` is stamped at placement and `legHorizonH` falls back to `Math.max(1, DB.fillHorizonH || 4)` without it, but the positions carry never listed it. So a state-backup restore aged every open leg against 4h: a leg placed at the 21:30 touch under a 9.5h horizon read at 05:00 as past **2×** its horizon, `sellAgeInfo` returned rung 2, and the sell card said **UNDERCUT & EXIT** on a leg behaving exactly as placed. **The same function already enforced this for the simulation and not for the money** — the paper book's carry does `put("hzH", fin(p.hzH))` under a comment calling the loss forbidden in the cadence ruling's own words. Absent stays absent deliberately: a leg placed before the field existed was aged against the old global at the time, and inventing a stamp would be a different defect | probe `[R104.1]` |
| R104.2 | **AND THE CONSUMER READS THE RESTORED STAMP RATHER THAN THE FALLBACK.** Asserted at `legHorizonH`, which is the branch that turns the stamp into a sell instruction — a correct carry with a reader that never sees it is the shape the two preceding passes each found (M157–M159). No clamp is involved and the assertion says so: the ternary selects the stamp when finite and positive and the fallback otherwise, and the fixture drives both arms | probe `[R104.2]` |
| R104.3 | **THE STANDING QUOTE CARRIES ITS OWN STAMP TOO.** `w.quotePlaced` is written with `hzH` at placement and `legHorizonH` is called on the quote record as well as on the position, so the omission had the same consequence on the leg the operator is being told to reprice. Same absent-stays-absent discipline | probe `[R104.3]` |
| R104.4 | **AN IMPORT MAY NEVER LOOSEN: caps resolve to their TIGHT END, budgets to their DEFAULT — and identically for NULL and ABSENT, because both mean the file carries no value** (user ruling, Aug 19 2026). A cap **is** the restraint, so resolving it to a looser default lets a malformed or hand-edited file lift one through a channel that may only ever DROP restraints on the user's press. The defect that earned the ruling was mine: a null clamped `partCapPct` to its floor of 1% before the sweep and resolved it to the default 10% after — **a tenfold widening of the participation cap arriving as a bug fix** — and the sweep's audit had filed that whole clamp group under "lands somewhere harmless". Seven keys are cap-like (`partCapPct`, `clusterCapPct` → 1; `scoutT1Cap`, `scoutT2Cap`, `sibPerSeed`, `sibTotal` → 0; `sleeveMaxPos` → 1); the budgets and both reserves keep their defaults, because a budget of 0 does not restrain, it **disables** | probe `[R104.4]` |
| R104.5 | **AND THE FOUR COUNTEREXAMPLES KEEP THEIR DEFAULTS, which is what proves the classification was read rather than pattern-matched.** `pumpThinGp`'s floor of 0 means nothing is ever thin enough to flag; `seedTrips`' floor of 1 seeds a lineage on a third of the evidence; `pumpWindowD`'s floor of 1 looks back a single day; `clusterMinDays`' floor of 5 confirms a basket on a sixth of the history. **Every one of those floors is the LOOSE end**, so a rule that mechanically floored every clamp would have loosened four settings in the name of a rule against loosening. `impTight` therefore takes the tight end as an explicit argument rather than deriving it from the clamp | probe `[R104.5]` |
| R104.6 | **A NULL SOURCE TIER DOES NOT RESTORE AS TIER 0, AND A REAL TIER 0 STILL DOES.** The guard was a bare unary plus — `[0,1,2,3].includes(+s.tier)` — and `+null` is 0, so the null-to-zero trap survived the first sweep entirely, **because that sweep grepped for `num(`**. Tier 0 is the most authoritative source class, so this is the spelling that most flatters its own input: an unrated source restored as an official one. The same line existed on the `intelligence.json` import path and is fixed there too, which is why `nz` is now module-scope rather than local to the sanitizer | probe `[R104.6]` |
| R104.7 | **THE FIVE GUARDS THAT WERE RENAMED, NOT REPAIRED.** `null >= 0` is **true**, so `nz(x) >= 0` admitted a null exactly as `num(x) >= 0` had, and `Math.max(0, null)` is 0 — five of the first sweep's 106 conversions changed nothing. **ABSENT is the case that bites on ordinary data:** `buyQStrict` and `sellQStrict` are absent on every paper trip until its first credit, so every open trip restored as a trip that had been measured and found nothing reachable — a never-fed aggregate manufactured inside the fix for a different defect. A real measured zero still carries, which is the discriminating half | probe `[R104.7]` |
| R104.8 | **THE PROPERTY, NOT THE LIST — the detector that replaces `[R103.6]`.** It feeds the sanitizer one row per store with every optional field NULL, then again with every optional field ABSENT, collects every output path whose value is the number 0, and requires that set to equal a stated allow-list. **It reads no source text, so no new spelling can defeat it**: a bare `+x`, a `Number(x)`, an `x | 0`, a `Math.round(x)` or a `>= 0` guard each put a new path in the set. `[R103.6]` was built from a list of known spellings and was blind to the two that survived the first sweep; it is kept as an enumeration of *known* idioms and its label no longer claims the class is closed. **The allow-list is the deliverable**: 54 paths in seven named groups — migration flags, counters, never-timestamps, sentinel ids, legitimately-zero settings, the filter defaults, and the four admission caps whose zero is this session's ruling rather than a pre-existing default. The null and absent fixtures produce the identical set, which is itself the healthy reading | probe `[R104.8]` |
| R104.9 | **THE CAP SET IS NINE KEYS AND ITS MEMBERSHIP IS PINNED BY NAME.** The defect pass 6 found was a classification error, not a coding one: the first version drew the cap set from the 23 keys that happen to share one object literal and missed `slots` (how many lines the plan funds) and `watchCap` (how large the watchlist may grow), **both of which bound funding as hard as the seven it did catch**. The test for membership is what the key DOES — does it bound what may be funded, sized, or admitted — not where it sits. A key added to the settings block without being classified does not appear here, and a key removed from the set turns this red. The membership was the thing that was wrong, so the membership is the thing asserted | probe `[R104.9]` |
| **§105 The three unambiguous pass-6 fixes (Aug 19 2026)** | | |
| R105.1 | **A WHOLE-STATE RESTORE IS PERSISTED, and the persist step is reached from the restore itself.** `flush()` alone was a no-op by the time it ran: it opens `if (!dirty) return true;` and `dirty` is set in exactly one place, inside `save()`, which `Object.assign(DB, v.db)` never calls. On a tab with nothing pending the restore lived **in memory only** and survived just if some later unrelated save happened to fire — the same import persisting or vanishing depending on what else ran that minute, **a race rather than a clean failure**. Asserted at `applyImport`, the branch, which was **extracted for this**: the restore lived inside a `FileReader.onload` no test can drive, so the only reachable subject was a term beside it, and that is the shape M157–M159 record. The extraction is the remedy the rule prescribes — make the branch reachable, then assert the branch | probe `[R105.1]` |
| R105.2 | **AND THE DEFAULT PERSIST STEP IS `save()` BEFORE `flush()`.** Production passes no second argument, so the default is what ships, and a default parameter is not observable from outside the call — this reads the function's own text and says so. If it ever reads `flush()` alone again, the restore returns to depending on whatever the app did next | probe `[R105.2]` |
| R105.3 | **A RECORD-LEVEL NULL SOURCE TIER DOES NOT RESTORE AS TIER 0, and a real tier 0 still does.** The third bare unary plus in the same family, **three lines below one of the two the previous repair fixed** — missed because that repair searched for the exact expression `includes(+s.tier)` rather than for the property, which is precisely the error the repair existed to correct. Tier 0 is the most authoritative source class, so an unrated record restored as an official one and passed a catalyst admission gate the code says it must fail | probe `[R105.3]` |
| R105.4 | **CLEARING THE QUANTITY BOX CLEARS THE OVERRIDE rather than setting a size of zero — driven through production's own delegated `change` handler**, not a copy of its arithmetic. `clampNum("", 0)` is 0, so an emptied box wrote a manual size of zero: `opsPick` returned 0 because 0 is not null, `planQty`'s `wanted` became 0, and the item benched on `sizing` **with a reason blaming working capital or a missing buy limit, neither of which had happened**. Live, with no import involved. A typed `0` clears too — indistinguishable from an empty box through `clampNum`, and there is no reading in which funding exactly zero units is a thing to ask for. **The correct pattern sat twelve lines above it in the same handler, on `h.paid`** — the third time that has been true of this defect | probe `[R105.4]` |
| R105.5 | **AND THE CONSUMER SEPARATES THEM:** a cleared row sizes automatically off the cap with `auto: true`, a typed 50 stays 50. Which input pins the output is stated, and **the first form of this assertion got that wrong**: it used a manual 250 against a cap of 100, so `Math.min(wanted, cap)` selected the cap in both arms and the two readings were the same number. The fixture now uses a value below the cap, so the clamp is present and not binding in either arm | probe `[R105.5]` |
| **§108 The pre-flip fix list (user ruling, Aug 20 2026)** | | |
| R108.1 | **AN UNEVALUATED CYCLE NEITHER BREAKS A SEASONING STREAK NOR CREDITS ONE.** A candidate carrying `failed` has been told no by one of two very different things: a RULE that looked at it and declined it, or the ABSENCE of anything to look at. `candidateFor`'s no-price return has carried a `failed` string since the beginning, so every reader counted it as a rule saying no. **The money:** a price feed that answers 200 with no rows leaves every watch item `failed` for lack of anything to read, and the loop spent that as a gate verdict — the whole book's tenure deleted and returned to the qualifying queue for a calendar day. `noEval` is stamped once, centrally, at the point the candidate is built; the `failed` string stays because the bench still has to render a reason. **Fixture discriminates:** both rows carry `failed` and differ only on the stamp | probe `[R108.1]` |
| R108.2 | **AND `candidateUnevaluated` IS THE ONE TERM, WITH THREE READERS.** The seasoning loop, the gate ledger (which was logging a day the gate never ran, as a bench by "plan gate", because `gateName("no live price in /latest")` is null) and the deployment funnel's kill attribution (whose own copy already promised "unknown is not failing" for the chart case). Asserted as MEMBERSHIP rather than behaviour, because a fourth reader added later must inherit the answer rather than re-derive it from the string — which is exactly how this defect came to exist | probe `[R108.2]` |
| R108.3 | **AND THE LOOP DOES NOT RUN AT ALL WITHOUT LIVE DATA** — the live-data precondition `scorerCycle` and `stampDeployLog` both have and this did not. At boot `S.latestAt` is 0 and `S.qualStamp` is unset, so the stamp `"0\|0"` differed from `undefined` and the guard PASSED: a first refresh that failed deleted every streak and saved. The stamp guard could never have caught it — it exists to stop a SECOND pass on the same data, not a first pass on none. Companion, not asserted here because the harness cannot produce a network response: `loadLatest` now stamps `S.latestAt` only when the payload is usable, and warns when it is not, so the field means *when we last had usable prices* for all twenty of its readers rather than *when a fetch returned* | probe `[R108.3]` |
| R108.4 | **THE SEASONING WAIVER READS THE TRADE'S DATE, NOT THE LOG INSTANT.** `qualExemption` tested `f.id >= cut` and `f.id` is `Date.now()` stamped when the row was logged; the record carries an honest `date` field and two other readers in the file already use it. One question, two implementations, and the one that lifts a gate read the wrong clock. **The ordinary route needs no import:** `#lDate` is a plain editable date input, and everyday log-lag does it in weaker form. The import edge is the loud version — `id > 0 ? id : uid()` stamps every id-less row with the import instant, waiving seasoning for a whole foreign history at once. Direction is one-way: the old test only ever over-granted. An unreadable date grants nothing, because absence may not lift a restraint. **Fixture discriminates:** both rows are logged now and differ only in `date` | probe `[R108.4]` |
| R108.5 | **THE PLACEMENT STAMP HAS A ONE-HOUR FLOOR, ASSERTED AT THE SELL LADDER.** `gapHoursAt`'s output floor was removed on Aug 13 2026 because a nominal imminent touch was pricing SIZING against an hour while the operator was hours away; that ruling's stated replacement guard is session coalescing, which lives entirely inside `walkUpSessions()` and has no path to `gapHoursAt`. **A stamp is a different consumer from a sizing read** — it is frozen onto a record and never re-derived. Unfloored, a repair logged at 11:56 against a 12:00 touch stamps `0.07`, lands at `stage: "selling"` with no `stageAt`, and `sellAgeInfo` reaches rung 2 in eight minutes: UNDERCUT & EXIT, which relists one tick under the instabuy and gives up the whole quoted spread. The floor also restores what the repair path had before it was stamped at all, since `legHorizonH`'s own fallback is `Math.max(1, …)` | probe `[R108.5]` |
| R108.6 | **`ITEM_OPS` IS A READ-SIDE SWITCH, NOT A WRITE-SIDE ONE.** A press writes wherever the value lives — the row when there is one, the store always — and the flag decides only which of them `opsOf` believes. The first form of this repair branched, which closed the snapshot-versus-row gap at the reconciliation boot and **re-opened it on the very next press**: from then until the flip every press landed on the row alone while the snapshot aged, and `opsPick` prefers the store for any key the store owns. Writing both closes a second finding with it — `itemOpsPrune` deletes a store row at 90 days and `opsPick` then falls through to the row, which with a stale row silently REVERTS an override the operator set, a restraint lapsing on a clock. **Fixture uses `tierOv: 0` on purpose:** untiered is a bench the operator applied, and the failure mode was that bench lifting with no press | probe `[R108.6]` |
| R108.7 | **THE THREE CUTOVER FLAGS ARE ONE QUESTION, AND THE TERM THAT ANSWERS IT READS THE SAME FLAG THE GUARD READS.** `CUTOVER_POOL` armed while `VOL5_UNIVERSE` is off is the one combination of the six that WIDENS: a pool item is not a watch row, so its 5-minute die-off streak is not zero but NOT COUNTED, `volGateFor` falls through to the 1h branch, and the 5m binding is a `Math.min` that can only ever shrink a size — pool items sized LARGER than an identically-placed pin in a collapsing book, and they are the population with no operator history. The guard REFUSES rather than repairing, because deriving one flag from another would arm deployment-class machinery on one press. **The second half is the repair's own defect:** `cutoverFault` decided the streak question off the injected set while `vol5Population` decided it off the const, so every exercise of the armed pool path ran in exactly the state the guard refuses — the refused state wearing the legal state's clothes, and a coverage claim with nothing behind it | probe `[R108.7]` |
| **§6 Verification** | | |
| R6.1 | Probe report cross-references this file (`===REQS===` section from `[R#]` assertion tags) | the report itself |
| R6.2 | THE PAIRING IS CHECKED IN BOTH DIRECTIONS, and a failure either way fails the suite (user ruling, Aug 13 2026). **A tag with no requirement:** an assertion reporting `[Rxx.y]` where this file has no such row and cites it nowhere — the report printed "REQ PASS R61.1" for six ids that did not exist, and a requirement that does not exist cannot fail, so the ledger was lying in the direction it was built to prevent. **A requirement with no assertion:** a row citing `` probe `[Rxx.y]` `` that no assertion reported — §31's withdrawn rows went on claiming coverage after their assertions were deleted with the feature, and R35.4 cited one of them; that is the seasoning-gate shape, a spec claiming an implementation that is not there. Rows verified by inspection, UI or documentation cite no probe tag and are exempt by construction. Runs in `tools/probe/reqpair.sh` rather than in the page, because the check needs to read this file and the in-page suite cannot — which is precisely why the gap survived. It appends a `===PAIRING===` section and **rewrites the report header**, so `head -1` never reads PROBE-PASS while a pairing failure stands | `tools/probe/reqpair.sh`, run by `run.sh` |
